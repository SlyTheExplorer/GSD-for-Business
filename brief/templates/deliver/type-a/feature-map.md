---
audience:
  type: internal
  confidentiality: internal
voice:
  tone: {{voice.tone}}
  perspective: {{voice.perspective}}
  languages: {{languages}}
business_context:
  model: {{business_model}}
  region: {{region}}
deliverable: feature-map
generated_by: brief-deliver-type-a
generated_at: {{ISO-timestamp}}
---

# FEATURE-MAP — {{project_title}}

> Type A deliverable: FEATURE-MAP. Visual feature tree. Mermaid `mindmap`
> is the preferred rendering — most modern markdown viewers (GitHub, GitLab,
> Obsidian, Notion-with-plugin, VS Code with Mermaid extension) render it
> natively. ASCII tree is the fallback comment block below for renderers
> without Mermaid support.

## Feature Tree (Mermaid — preferred)

```mermaid
mindmap
  root(({{project_title}}))
    Components
      <!-- INSERT: ## Component Map -->
    Value
      <!-- INSERT: ## Value Proposition -->
```

## Feature Tree (ASCII tree — fallback)

> ASCII tree fallback. The synthesis agent fills the same Component Map +
> Value Proposition sections into both blocks. If your renderer does not
> support Mermaid, use this ASCII view; the content is byte-identical to
> the Mermaid block.

```
{{project_title}}
├── Components/
│   <!-- INSERT-ASCII: ## Component Map -->
└── Value/
    <!-- INSERT-ASCII: ## Value Proposition -->
```

---

> Generated by BRIEF Type A synthesis agent (brief-deliver-type-a) from
> .planning/workstreams/tech-arch/tech-arch.md (Component Map) +
> .planning/workstreams/business-model-canvas/canvas.md (Value Proposition).
> Edit the Mermaid block to refine hierarchy or grouping; the ASCII tree
> is regenerated by `/brief-deliver --type-a` from the same source sections.
