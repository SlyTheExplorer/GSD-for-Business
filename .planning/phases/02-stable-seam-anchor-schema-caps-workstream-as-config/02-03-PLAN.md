---
phase: 02-stable-seam-anchor-schema-caps-workstream-as-config
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - brief/bin/lib/state.cjs
  - .planning/STATE.md
  - tests/state.test.cjs
autonomous: true
requirements:
  - FND-05
threat_model:
  boundary: "The state.cjs write path is first-party code; STATE.md frontmatter is written by this process only. No external input surface."
  register:
    - id: T-02-03-01
      category: Tampering
      component: "D-04 rename atomicity (state.cjs line 814 + STATE.md line 2 + 5 test-assertion lines)"
      disposition: mitigate
      mitigation: "All 7 edits land in a single atomic commit per Phase 1 D-09 (inherited). Split would leave the repo non-buildable between commits and breach the 63-baseline delta-cap per Plan 10 empirical policy"
    - id: T-02-03-02
      category: Repudiation
      component: "gsd_state_version → brief_state_version one-shot migration"
      disposition: accept
      mitigation: "Per D-07 no-aliases policy, no backwards-compat reader is shipped. STATE.md is a first-party artifact under git; rollback = git revert"
must_haves:
  truths:
    - ".planning/STATE.md frontmatter line 2 reads 'brief_state_version: 1.0', not 'gsd_state_version'"
    - "brief/bin/lib/state.cjs line ~814 initializes 'brief_state_version: '1.0'' (no gsd_state_version literal remains in the source)"
    - "All 5 test assertion lines in tests/state.test.cjs (350, 365, 382, 442, 1725) reference brief_state_version, not gsd_state_version"
    - "node --test tests/state.test.cjs passes with zero failures (all assertions updated atomically)"
    - "npm test failure count is ≤ 63 after commit (no delta-cap breach)"
    - "No `gsd_state_version` literal remains in `brief/`, `tests/`, or `.planning/STATE.md` (verified by the grep below). Residue elsewhere in `.planning/` audit records (10-PARTIAL-AUDIT.md, CONTEXT/RESEARCH/PATTERNS) is intentional and preserved per Phase 1 HALT-ACCEPTED policy."
  artifacts:
    - path: "brief/bin/lib/state.cjs"
      provides: "buildStateFrontmatter writes brief_state_version"
      contains: "brief_state_version: '1.0'"
    - path: ".planning/STATE.md"
      provides: "migrated frontmatter anchor version field"
      contains: "brief_state_version: 1.0"
    - path: "tests/state.test.cjs"
      provides: "5 assertion lines updated"
      contains: "brief_state_version"
  key_links:
    - from: "buildStateFrontmatter (state.cjs:~814)"
      to: "STATE.md write path via syncStateFrontmatter → reconstructFrontmatter"
      via: "direct function call"
      pattern: "brief_state_version"
    - from: "tests/state.test.cjs (5 lines)"
      to: "state.cjs:814 write"
      via: "assertion on STATE.md content / extracted frontmatter"
      pattern: "brief_state_version"
---

<objective>
Rename the state anchor frontmatter field from `gsd_state_version` to `brief_state_version` in a single atomic commit across ALL 7 touchpoints: `brief/bin/lib/state.cjs:814`, `.planning/STATE.md:2`, and the 5 test-assertion lines in `tests/state.test.cjs` (350, 365, 382, 442, 1725 — note: 02-RESEARCH.md R-3 undercounted as 4; 02-PATTERNS.md line 908 empirically verified 5).

Purpose: D-04 closes one item in the Phase 1 rename residue and locks in the hard-rename policy (D-07 no aliases) before Plan 04 introduces the new `brief:` nested map. Splitting the rename across multiple commits would leave the repo non-buildable between commits and breach the Phase 1 Plan 10 empirical delta-cap (baseline 63 failures + 4 test failures = 67 > cap-effective 16 → HALT). The 7 edits MUST land together.

Output: one atomic commit (or one sequence of edits if CI is not driving the commit) in which every `gsd_state_version` literal in tracked source is replaced with `brief_state_version`; tests still pass; STATE.md still renders correctly.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-stable-seam-anchor-schema-caps-workstream-as-config/02-CONTEXT.md
@.planning/phases/02-stable-seam-anchor-schema-caps-workstream-as-config/02-RESEARCH.md
@.planning/phases/02-stable-seam-anchor-schema-caps-workstream-as-config/02-PATTERNS.md
@.planning/phases/01-foundation-fork-hygiene-removal-rename/10-PARTIAL-AUDIT.md
@brief/bin/lib/state.cjs
@tests/state.test.cjs

<interfaces>
state.cjs line 814 current code (verified via grep 2026-04-19):
```javascript
const fm = { gsd_state_version: '1.0' };
```

.planning/STATE.md line 2 current content (verified via grep 2026-04-19):
```yaml
gsd_state_version: 1.0
```

tests/state.test.cjs 5 lines with gsd_state_version literal (verified via grep 2026-04-19):
- Line 350: `assert.strictEqual(output.gsd_state_version, '1.0', 'should have version 1.0');`
- Line 365: `gsd_state_version: 1.0` (inside template literal — the fixture YAML)
- Line 382: `assert.strictEqual(output.gsd_state_version, '1.0', 'version from frontmatter');`
- Line 442: `assert.ok(content.includes('gsd_state_version: 1.0'), 'should have version field');`
- Line 1725: `\`---\ngsd_state_version: '1.0'\nstatus: executing\n...` (inside template literal — fixture YAML)

After-state expected at each site: literal `gsd_state_version` → `brief_state_version` (dashes-vs-underscores: snake_case is preserved; only the prefix changes). The value `1.0` / `'1.0'` stays.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Atomic D-04 rename across state.cjs, STATE.md, and tests/state.test.cjs (5 lines)</name>
  <read_first>
    - brief/bin/lib/state.cjs (lines 810-820 — the write site; also grep for any OTHER `gsd_state_version` literal that may have been added since 2026-04-19 research)
    - .planning/STATE.md (lines 1-5 — the migration target)
    - tests/state.test.cjs (lines 345-390, 440-445, 1720-1730 — the 5 assertion contexts; reading surrounding context protects against off-by-one line drift since the 2026-04-19 grep)
    - .planning/phases/02-stable-seam-anchor-schema-caps-workstream-as-config/02-CONTEXT.md (D-04 — rationale and atomic-commit mandate)
    - .planning/phases/02-stable-seam-anchor-schema-caps-workstream-as-config/02-PATTERNS.md (section "MODIFIED: tests/state.test.cjs" lines 640-666 — verified 5-line count)
    - .planning/phases/01-foundation-fork-hygiene-removal-rename/10-PARTIAL-AUDIT.md §4 (Phase 1 HRD-05 residue categorization; confirms the 4 state-test assertions are NOT currently in the 63-failure catalog — meaning they PASS today because state.cjs still writes `gsd_state_version`; if this rename is NOT bundled atomically, those 4 tests flip to failing)
  </read_first>
  <action>
    Before making ANY edit, run a pre-edit grep to catch any references that may have been added since the 2026-04-19 research snapshot:
    ```
    grep -rn "gsd_state_version" brief/ tests/ .planning/STATE.md 2>/dev/null
    ```
    Expected result (as of 2026-04-19): exactly 7 lines matched — 1 in state.cjs, 1 in STATE.md, 5 in tests/state.test.cjs. If the count differs, enumerate the new references and update EVERY tracked-source occurrence in this same task (documentation-only or .planning/phases/ audit-record occurrences may be left alone — those are historical records, not live source).

    Then make the following 7 coordinated edits in the SAME atomic commit:

    **Edit 1 — brief/bin/lib/state.cjs line 814** (or wherever `const fm = { gsd_state_version: '1.0' };` appears; confirm via fresh grep):
    ```diff
    - const fm = { gsd_state_version: '1.0' };
    + const fm = { brief_state_version: '1.0' };
    ```

    **Edit 2 — .planning/STATE.md line 2**:
    ```diff
    - gsd_state_version: 1.0
    + brief_state_version: 1.0
    ```
    IMPORTANT: do NOT add the nested `brief:` map in this plan — that is Plan 04's job. Only rename the version field.

    **Edit 3 — tests/state.test.cjs line 350**:
    ```diff
    - assert.strictEqual(output.gsd_state_version, '1.0', 'should have version 1.0');
    + assert.strictEqual(output.brief_state_version, '1.0', 'should have version 1.0');
    ```

    **Edit 4 — tests/state.test.cjs line 365** (inside a template literal fixture YAML):
    ```diff
    - gsd_state_version: 1.0
    + brief_state_version: 1.0
    ```

    **Edit 5 — tests/state.test.cjs line 382**:
    ```diff
    - assert.strictEqual(output.gsd_state_version, '1.0', 'version from frontmatter');
    + assert.strictEqual(output.brief_state_version, '1.0', 'version from frontmatter');
    ```

    **Edit 6 — tests/state.test.cjs line 442**:
    ```diff
    - assert.ok(content.includes('gsd_state_version: 1.0'), 'should have version field');
    + assert.ok(content.includes('brief_state_version: 1.0'), 'should have version field');
    ```

    **Edit 7 — tests/state.test.cjs line 1725** (inside template literal fixture YAML — watch the `'1.0'` quoting):
    ```diff
    - `---\ngsd_state_version: '1.0'\nstatus: executing\n...
    + `---\nbrief_state_version: '1.0'\nstatus: executing\n...
    ```

    After all 7 edits, run the verification grep:
    ```
    grep -rn "gsd_state_version" brief/ tests/ .planning/STATE.md 2>/dev/null
    ```
    Expected result: ZERO matches. If any match remains, it's either (a) a reference the pre-edit grep missed (handle it now before committing), or (b) a path the grep didn't search (verify with the acceptance criteria below).

    Do NOT:
    - Edit `.planning/phases/` audit records (e.g., 10-PARTIAL-AUDIT.md, CONTEXT.md, RESEARCH.md, PATTERNS.md — these are historical records that reference the old name by design).
    - Add a backwards-compat reader in state.cjs that also accepts `gsd_state_version` (D-07 no-aliases — explicitly rejected).
    - Split the 7 edits across multiple commits — each intermediate state is broken (source says `brief_state_version` but tests still assert `gsd_state_version` OR vice versa).
    - Add the `brief:` nested map to STATE.md or state.cjs (Plan 04's job).

    Implements D-04 per 02-CONTEXT.md; inherits D-07 (no aliases) and D-09 (atomic buildable commits) from Phase 1.
  </action>
  <verify>
    <automated>node --test tests/state.test.cjs &amp;&amp; ! grep -rn "gsd_state_version" brief/ tests/ .planning/STATE.md 2>/dev/null &amp;&amp; grep -q "^brief_state_version: 1.0$" .planning/STATE.md &amp;&amp; grep -q "brief_state_version: '1.0'" brief/bin/lib/state.cjs</automated>
  </verify>
  <acceptance_criteria>
    - ZERO `gsd_state_version` literals remain in live source: `grep -rn "gsd_state_version" brief/ tests/ .planning/STATE.md 2>/dev/null` produces NO output (exit 1)
    - `.planning/STATE.md` line 2 is exactly `brief_state_version: 1.0`: `awk 'NR==2' .planning/STATE.md | grep -qE "^brief_state_version: 1\.0$"`
    - `brief/bin/lib/state.cjs` writes brief_state_version: `grep -q "brief_state_version: '1.0'" brief/bin/lib/state.cjs`
    - All 5 test-assertion lines updated: `grep -c "brief_state_version" tests/state.test.cjs` ≥ 5 (exact count depends on other Phase 2 changes; must be ≥ 5)
    - No `gsd_state_version` in tests/state.test.cjs: `! grep -q "gsd_state_version" tests/state.test.cjs`
    - tests/state.test.cjs passes: `node --test tests/state.test.cjs` exits 0
    - No regressions elsewhere: `node --test tests/frontmatter.test.cjs` exits 0 (Plan 01 changes preserved)
    - npm-test baseline preserved (delta-cap guard): `npm test 2>&1 | grep -cE '^✖'` returns ≤ 63
    - Phase 1 audit records left alone: `grep -q "gsd_state_version" .planning/phases/01-foundation-fork-hygiene-removal-rename/10-PARTIAL-AUDIT.md` may or may not match (historical record — no assertion)
    - STATE.md still parses correctly: `node -e "const {extractFrontmatter} = require('./brief/bin/lib/frontmatter.cjs'); const fs = require('fs'); const fm = extractFrontmatter(fs.readFileSync('.planning/STATE.md', 'utf-8')); console.log(fm.brief_state_version === '1.0' || fm.brief_state_version === 1.0 ? 'OK' : 'FAIL: ' + fm.brief_state_version)"` prints `OK`
    - STATE.md has no orphan `gsd_state_version`: `! grep -q "gsd_state_version" .planning/STATE.md`
  </acceptance_criteria>
  <done>
    All 7 tracked-source occurrences of `gsd_state_version` have been renamed to `brief_state_version` in the same atomic commit; state-test suite passes; npm-test baseline unchanged (≤ 63 failures); STATE.md round-trips via extractFrontmatter.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| state.cjs write path | First-party code writing first-party STATE.md; no external input surface in this plan |
| tests/state.test.cjs fixtures | First-party fixture strings, version-controlled |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03-01 | Tampering | D-04 rename atomicity across 7 sites | mitigate | All 7 edits in one atomic commit per D-09 inherited; pre-edit grep catches drift since 2026-04-19 snapshot; post-edit grep confirms zero remaining literal (Task 1) |
| T-02-03-02 | Repudiation | gsd_state_version → brief_state_version one-shot | accept | Per D-07 no-aliases; STATE.md is git-tracked; rollback = git revert |
</threat_model>

<verification>
1. Pre-edit grep baseline: 7 matches expected. If different, enumerate and include in the atomic change.
2. Post-edit grep: 0 matches in live source (`brief/`, `tests/`, `.planning/STATE.md`).
3. `node --test tests/state.test.cjs` passes (5 updated assertions all green).
4. `node --test tests/frontmatter.test.cjs` still passes (Plan 01 preserved).
5. `npm test 2>&1 | grep -cE '^✖'` ≤ 63 (delta-cap held).
6. STATE.md parses correctly via extractFrontmatter (sanity check before Plan 04 builds on this).
</verification>

<success_criteria>
- `gsd_state_version` literal eliminated from all live source (brief/, tests/, STATE.md) — grep returns empty.
- 5 tests/state.test.cjs assertions now reference `brief_state_version` and pass.
- state.cjs:814 writes `brief_state_version: '1.0'`.
- .planning/STATE.md line 2 reads `brief_state_version: 1.0`.
- npm test failure count ≤ 63 (Phase 1 baseline preserved).
- One atomic commit (or one coherent change set) — no intermediate broken state.
- Phase 9 HRD-05 drift catalog is decremented by the `gsd_state_version` category (tracked in summary).
</success_criteria>

<output>
After completion, create `.planning/phases/02-stable-seam-anchor-schema-caps-workstream-as-config/02-03-SUMMARY.md` recording: final grep count (should be 0), the 7 edit locations with old/new snippets, pre-commit and post-commit `npm test` failure counts (must both be ≤ 63), and a note that this closes one Phase 9 HRD-05 residue item.
</output>
