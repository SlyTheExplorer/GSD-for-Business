---
phase: 02-stable-seam-anchor-schema-caps-workstream-as-config
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - CLAUDE.md
autonomous: true
requirements:
  - FND-09
threat_model:
  boundary: "CLAUDE.md is a documentation artifact — no input surface; future CLAUDE.md regeneration is the only operational risk"
  register:
    - id: T-02-02-01
      category: Tampering
      component: "## Surface Caps section in CLAUDE.md"
      disposition: mitigate
      mitigation: "Prefix section with HTML comment guard `<!-- BRIEF PHASE 2 / FND-09 — DO NOT REGENERATE. Preserve across CLAUDE.md template rebuilds. -->` so future CLAUDE.md regenerators (Phase 3+ /brief-profile-user, template rebuilds) recognize the section as hand-authored and skip it (per 02-RESEARCH.md Pitfall 5)"
    - id: T-02-02-02
      category: INFO
      component: "Surface-count claims (61 commands, 18 agents)"
      disposition: accept
      mitigation: "Numbers are a point-in-time snapshot from Phase 2 entry (per D-08); section text includes forward-looking pointer to Phase 9 HRD-02 for the actual audit, so the numbers are contextual not authoritative"
must_haves:
  truths:
    - "CLAUDE.md contains an H2 section titled exactly '## Surface Caps'"
    - "The section states the cap as ≤12 user-facing slash commands and ≤8 skills"
    - "The section defines 'user-facing' as what bin/install.js registers under commands/<runtime>/brief/ for end-user invocation"
    - "The section points forward to Phase 9 HRD-02 as the enforcement/audit gate"
    - "The section is preceded by a regeneration-guard HTML comment so future CLAUDE.md rebuilds preserve it"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Surface Caps policy text (new H2 section)"
      contains: "## Surface Caps"
  key_links:
    - from: "## Surface Caps heading"
      to: "definition of user-facing"
      via: "inline text"
      pattern: "user-facing"
    - from: "## Surface Caps section"
      to: "Phase 9 HRD-02 forward-pointer"
      via: "inline text"
      pattern: "Phase 9 HRD-02"
---

<objective>
Add a new `## Surface Caps` H2 section to the project-root `CLAUDE.md`, stating the ≤12 user-facing slash commands and ≤8 skills caps with rationale, a precise definition of "user-facing", an acknowledgement of the current 61-command / 18-agent count inherited from GSD, and a forward pointer to Phase 9 HRD-02 for enforcement. Guard the section with an HTML comment so future CLAUDE.md regenerations preserve it.

Purpose: FND-09 requires the cap to be documented in CLAUDE.md BEFORE any feature work in Phase 3-8 adds new commands. This plan is the policy text only (per D-07 no pre-commit enforcement in Phase 2; audit is Phase 9 HRD-02). It is fully independent of all other Phase 2 work and runs in Wave 1 parallel to the frontmatter extension.

Output: CLAUDE.md with one new H2 section inserted between existing top-level sections, preceded by a preservation HTML comment. No other file touched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-stable-seam-anchor-schema-caps-workstream-as-config/02-CONTEXT.md
@.planning/phases/02-stable-seam-anchor-schema-caps-workstream-as-config/02-RESEARCH.md
@.planning/phases/02-stable-seam-anchor-schema-caps-workstream-as-config/02-PATTERNS.md
@CLAUDE.md

<interfaces>
CLAUDE.md section structure (existing top-level sections as of Phase 2 entry):
- `## Project`
- `## Technology Stack` (long — includes Architectural Responsibility Map, Stack tables, etc.)
- `## Conventions` (short — "not yet established")
- `## Architecture` (short — "not yet mapped")
- `## Project Skills` (short — placeholder)
- `## BRIEF Workflow Enforcement` (policy)
- `## Developer Profile` (managed section with guard: `> This section is managed by generate-claude-profile -- do not edit manually.`)

Insertion target: between `## Architecture` and `## Project Skills`. Place the `## Surface Caps` section there to keep project-policy sections grouped together, consistent with 02-PATTERNS.md recommendation (lines 789-790).

Regeneration-guard pattern to adopt — HTML comment (invisible in rendered markdown but load-bearing for regenerators):
```
<!-- BRIEF PHASE 2 / FND-09 — DO NOT REGENERATE. Preserve across CLAUDE.md template rebuilds. -->
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert `## Surface Caps` section into CLAUDE.md with regeneration guard</name>
  <read_first>
    - CLAUDE.md (ENTIRE file — you need to locate the exact insertion point between `## Architecture` and `## Project Skills`; also read the `## Developer Profile` regeneration-guard style at the end for reference)
    - .planning/phases/02-stable-seam-anchor-schema-caps-workstream-as-config/02-CONTEXT.md (decisions D-06 through D-09 — verbatim phrasing requirements)
    - .planning/phases/02-stable-seam-anchor-schema-caps-workstream-as-config/02-PATTERNS.md (section "MODIFIED: CLAUDE.md (add ## Surface Caps section)" lines 759-798 — provides the exact draft to ship)
    - .planning/phases/02-stable-seam-anchor-schema-caps-workstream-as-config/02-RESEARCH.md (§Pitfall 5 at lines 479-487 — explains why the HTML-comment guard matters)
    - .planning/REQUIREMENTS.md (FND-09 line)
  </read_first>
  <action>
    Edit CLAUDE.md to insert the following content BETWEEN the existing `## Architecture` section and the existing `## Project Skills` section (both currently present in CLAUDE.md). Preserve all other content unchanged.

    Exact text to insert (copy verbatim — every keyword in this block is grep-asserted by the acceptance criteria and VALIDATION.md):

    ```markdown
    <!-- BRIEF PHASE 2 / FND-09 — DO NOT REGENERATE. Preserve across CLAUDE.md template rebuilds. -->

    ## Surface Caps

    BRIEF enforces a minimal command/skill surface for memorability and to prevent bloat (Pitfalls #1 skill bloat and #12 slash-command memorability failure from the inherited GSD pitfall catalog):

    - **≤12 user-facing slash commands**
    - **≤8 skills**

    **Definition of "user-facing":** what `bin/install.js` registers under `commands/<runtime>/brief/` for end-user invocation. Internal helpers, sub-commands routed through a parent command, and template files do NOT count against the cap.

    **Rationale:** Command surface memorability degrades rapidly past ~12 items (Miller's Law, applied to CLI surfaces); skill bloat degrades agent selection accuracy as the skill catalog grows. Fixing the cap BEFORE domain features ship in Phase 3-8 prevents the inherited GSD surface from re-expanding.

    **Enforcement:** Documentation-only in Phase 2 (per Phase 2 decision D-07). No pre-commit hook, no automated gate in Phase 2 — that would block planner work mid-stream and is out of scope for the stable-seam phase. The audit + pruning runs in **Phase 9 HRD-02** (v1 launch gate).

    **Current state:** As of v1 design (Phase 2 entry, 2026-04), BRIEF inherits 61 renamed `brief-*` commands and 18 renamed agents from GSD. Both counts exceed the cap. The reduction to ≤12 user-facing commands and ≤8 skills is the Phase 9 HRD-02 audit. Subsequent Phases (3-8) MUST NOT add new commands beyond their requirement-mapped set (e.g., Phase 2 adds `+1` with `/brief-status`; Phase 3 adds `/brief-define`; etc.).

    **Scope clarification:** This cap applies to the *user-facing* command surface defined above. It does NOT constrain:
    - Internal `brief/bin/brief-tools.cjs` subcommands (e.g., `brief-tools.cjs state json`, `brief-tools.cjs status`) — these are implementation dispatchers, not user slash commands.
    - Workflow markdown files under `brief/workflows/` — these are agent-internal and not user-invocable.
    - Reference files under `brief/references/` — these are documentation, not skills.
    ```

    Implementation approach — use the Edit tool. Anchor: the line immediately preceding `## Project Skills` heading. Do NOT use find-and-replace on short literal strings (e.g., `## Project Skills`) if that literal appears elsewhere — use the full line `## Project Skills` as the anchor (unique in CLAUDE.md per read_first verification).

    Implements FND-09 per decisions D-06, D-07, D-08, D-09 of 02-CONTEXT.md. Specifically:
    - D-06 locks the ≤12/≤8 numbers and the "user-facing" definition.
    - D-07 locks enforcement-by-doc-only in Phase 2.
    - D-08 mandates acknowledging the current 61-command / 18-agent count.
    - D-09 mandates the forward-looking pointer to Phase 9 HRD-02 and the "MUST NOT add new commands" clause for Phases 3-8.

    Do NOT:
    - Enumerate which 49 inherited commands to prune (Phase 9 HRD-02 owns this per D-09).
    - Introduce any enforcement mechanism (pre-commit hook, CI check) — D-07 rejects.
    - Use emojis — CLAUDE.md constraint "Only use emojis if the user explicitly requests it".
    - Change any other section of CLAUDE.md.
  </action>
  <verify>
    <automated>grep -q "^## Surface Caps$" CLAUDE.md &amp;&amp; grep -q "≤12" CLAUDE.md &amp;&amp; grep -q "≤8" CLAUDE.md &amp;&amp; grep -q "Phase 9 HRD-02" CLAUDE.md &amp;&amp; grep -q "user-facing" CLAUDE.md &amp;&amp; grep -q "BRIEF PHASE 2 / FND-09 — DO NOT REGENERATE" CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - Section heading present: `grep -qE "^## Surface Caps$" CLAUDE.md`
    - Both caps quoted: `grep -q "≤12 user-facing slash commands" CLAUDE.md &amp;&amp; grep -q "≤8 skills" CLAUDE.md`
    - User-facing definition present: `grep -qE "bin/install\.js.*commands/<runtime>/brief/" CLAUDE.md`
    - Phase 9 HRD-02 forward-pointer present: `grep -q "Phase 9 HRD-02" CLAUDE.md`
    - Regeneration-guard HTML comment present: `grep -q "BRIEF PHASE 2 / FND-09 — DO NOT REGENERATE" CLAUDE.md`
    - Forward policy clause "MUST NOT add new commands": `grep -qE "MUST NOT add new commands" CLAUDE.md`
    - Current-state count mentioned: `grep -qE "61 renamed.*brief-\*" CLAUDE.md &amp;&amp; grep -q "18 renamed agents" CLAUDE.md`
    - Section is placed between `## Architecture` and `## Project Skills` (verify with `awk '/^## Architecture$/,/^## Project Skills$/' CLAUDE.md | grep -q "## Surface Caps"`)
    - Zero emojis added: `grep -qE "[🚀🎯✅❌⭐️📦🔧]" CLAUDE.md &amp;&amp; exit 1 || exit 0` (trick shell — must succeed meaning no match)
    - No enforcement-mechanism language: `! grep -qE "pre-commit.*hook|CI check.*cap|automated gate.*enforce" CLAUDE.md` (should exit 0 meaning not found)
    - No other CLAUDE.md section was modified (quick sanity): file length should have grown by roughly the size of the inserted block (~25-30 lines); no existing H2 sections disappeared — check `grep -c "^## " CLAUDE.md` increased by exactly 1 vs. pre-edit count (pre-edit count can be computed by `git show HEAD:CLAUDE.md | grep -c "^## "`)
  </acceptance_criteria>
  <done>
    CLAUDE.md has a new `## Surface Caps` section between Architecture and Project Skills, satisfying all 5 must_have truths and prefixed with an HTML regeneration-guard comment. No other content changed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLAUDE.md content | Documentation artifact — read by agents on every invocation; only manipulation risk is future regeneration dropping this section (mitigated via HTML comment guard) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02-01 | Tampering | ## Surface Caps section in CLAUDE.md | mitigate | Prefix section with HTML regeneration-guard comment so future CLAUDE.md rebuilds recognize it as hand-authored (02-RESEARCH Pitfall 5) |
| T-02-02-02 | Information Disclosure | Surface-count claims (61 commands, 18 agents) | accept | Point-in-time snapshot; forward-pointer to Phase 9 HRD-02 makes the numbers contextual not authoritative |
</threat_model>

<verification>
1. All 6 grep assertions above return 0 (section present with all required content).
2. `awk '/^## Architecture$/,/^## Project Skills$/' CLAUDE.md | grep -q "## Surface Caps"` — placement correct.
3. Zero incidental regressions: `git diff CLAUDE.md` shows ONLY the insertion block (no deletions, no other modifications).
</verification>

<success_criteria>
- `## Surface Caps` section exists in CLAUDE.md with correct content (all 5 must_have truths satisfied).
- Regeneration-guard HTML comment is present immediately before the section.
- Placement is between `## Architecture` and `## Project Skills`.
- No other CLAUDE.md content changed.
- FND-09 requirement status can be marked satisfied.
</success_criteria>

<output>
After completion, create `.planning/phases/02-stable-seam-anchor-schema-caps-workstream-as-config/02-02-SUMMARY.md` recording: section text shipped, placement, regeneration-guard adopted, forward pointer to Phase 9 HRD-02 noted.
</output>
