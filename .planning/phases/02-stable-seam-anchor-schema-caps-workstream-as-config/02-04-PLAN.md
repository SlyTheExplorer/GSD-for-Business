---
phase: 02-stable-seam-anchor-schema-caps-workstream-as-config
plan: 04
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-03
files_modified:
  - brief/bin/lib/state.cjs
  - .planning/STATE.md
  - tests/state-brief-roundtrip.test.cjs
  - .planning/ASSUMPTIONS.md
autonomous: true
requirements:
  - FND-05
threat_model:
  boundary: "state.cjs read/write paths — first-party; STATE.md frontmatter is written only by this process; the new `brief:` map is populated by future Phase 4/5/6/7 orchestrator steps (first-party too)"
  register:
    - id: T-02-04-01
      category: Tampering
      component: "cmdStateJson + syncStateFrontmatter preservation allowlist"
      disposition: mitigate
      mitigation: "Extend the existing 3-branch allowlist with a 4th branch preserving `existingFm.brief` — closes R-5 silent-drop defect identified in 02-RESEARCH.md"
    - id: T-02-04-02
      category: Tampering
      component: "A4 smoke test fixture coverage"
      disposition: mitigate
      mitigation: "Fixture MUST include array-of-objects, nested-object-at-leaf, and null leaf (per Pitfall 1); scalar-only fixtures falsely pass. Test also cycles through `brief-tools.cjs state json` to catch R-5 allowlist drift (per stronger-test note in R-5)"
    - id: T-02-04-03
      category: Repudiation
      component: "A4 verification entry in ASSUMPTIONS.md"
      disposition: mitigate
      mitigation: "Append `### A4 — VERIFIED` only AFTER the smoke test passes green; timestamp + source file cited for traceability"
must_haves:
  truths:
    - ".planning/STATE.md frontmatter contains a `brief:` nested YAML map with the full D-03 schema initialized to empty/null placeholders (return_stack: [], gap_queue: [], last_gate_results.{align,audience,compliance}: null, current_workstream: null)"
    - "`tests/state-brief-roundtrip.test.cjs` smoke test PASSES — injecting a representative D-03 payload (arrays of objects, nested objects, null) through writeStateMd → extractFrontmatter deep-strict-equals the input across 2 write cycles"
    - "The same smoke test includes a cycle that invokes `brief-tools.cjs state json` (via runGsdTools) between writes and verifies the `brief:` map survives the cmdStateJson rebuild-from-body path (closes R-5)"
    - "`cmdStateJson` preserves `existingFm.brief` via a new allowlist branch"
    - "`syncStateFrontmatter` preserves `existingFm.brief` on write via the same allowlist-mirror"
    - "`.planning/ASSUMPTIONS.md` has a new `### A4 — STATE.md round-trips state.brief.* without loss — VERIFIED` entry with timestamp, test file reference, and scope statement"
    - "npm test failure count is ≤ 63 (delta-cap preserved)"
  artifacts:
    - path: "tests/state-brief-roundtrip.test.cjs"
      provides: "A4 smoke test — 2 write cycles + state-json cycle covering full D-03 schema"
      exports: "node:test describe/test blocks"
    - path: "brief/bin/lib/state.cjs"
      provides: "cmdStateJson and syncStateFrontmatter preserve existingFm.brief"
      contains: "existingFm.brief"
    - path: ".planning/STATE.md"
      provides: "brief: nested map initialized with D-03 shape"
      contains: "brief:"
    - path: ".planning/ASSUMPTIONS.md"
      provides: "A4 verification entry"
      contains: "A4"
  key_links:
    - from: "writeStateMd"
      to: "syncStateFrontmatter preservation of brief: map"
      via: "delegated call"
      pattern: "existingFm\\.brief"
    - from: "cmdStateJson rebuild path"
      to: "preserved brief: map in built frontmatter"
      via: "allowlist branch"
      pattern: "existingFm\\.brief"
    - from: "A4 smoke test"
      to: "writeStateMd + cmdStateJson production paths"
      via: "direct require + runGsdTools"
      pattern: "writeStateMd\\(|runGsdTools"
---

<objective>
Verify Assumption A4 (the `state.cjs` round-trip preserves namespaced `state.brief.*` fields) by:
1. Initializing the `brief:` nested map in `.planning/STATE.md` with the full D-03 schema (all placeholders).
2. Extending `cmdStateJson` and `syncStateFrontmatter` in `brief/bin/lib/state.cjs` with a 4th preservation branch that passes `existingFm.brief` through the rebuild-from-body allowlist (per 02-RESEARCH.md R-5 / A-P2-4 — without this, the `brief:` map is silently dropped on the next `brief-tools.cjs state json` invocation).
3. Creating `tests/state-brief-roundtrip.test.cjs` that exercises the full D-03 schema (arrays-of-objects, nested object leaves, null scalars) across 2 write cycles AND 1 `brief-tools.cjs state json` cycle — the stronger A4 test per 02-RESEARCH.md R-5.
4. Appending a VERIFIED `### A4 —` entry to `.planning/ASSUMPTIONS.md` after the smoke test passes.

Purpose: FND-05 final deliverable. Depends on Plan 01 (frontmatter.cjs extension for D-20 shapes) and Plan 03 (brief_state_version rename — avoids mixing two deltas in one STATE.md edit). After this plan, Phase 4/5/6/7 writers can populate `brief.*` without re-litigating shape or worrying about silent drops.

Output: 4 artifacts modified (state.cjs, STATE.md, ASSUMPTIONS.md) + 1 created (state-brief-roundtrip.test.cjs); A4 goes VERIFIED in ASSUMPTIONS.md; npm test baseline preserved (≤ 63 failures); Phase 4+ unblocked.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/ASSUMPTIONS.md
@.planning/phases/02-stable-seam-anchor-schema-caps-workstream-as-config/02-CONTEXT.md
@.planning/phases/02-stable-seam-anchor-schema-caps-workstream-as-config/02-RESEARCH.md
@.planning/phases/02-stable-seam-anchor-schema-caps-workstream-as-config/02-PATTERNS.md
@.planning/phases/02-stable-seam-anchor-schema-caps-workstream-as-config/02-VALIDATION.md
@.planning/phases/02-stable-seam-anchor-schema-caps-workstream-as-config/02-01-PLAN.md
@.planning/phases/02-stable-seam-anchor-schema-caps-workstream-as-config/02-03-PLAN.md
@brief/bin/lib/state.cjs
@brief/bin/lib/frontmatter.cjs
@tests/state.test.cjs
@tests/helpers.cjs

<interfaces>
D-03 schema (locked in 02-CONTEXT.md lines 56-68):

| Field                                 | Shape                                           | Used by    |
|---------------------------------------|-------------------------------------------------|------------|
| `brief.return_stack`                  | array of frame objects (LIFO, max depth 3)      | Phase 6    |
| `brief.gap_queue`                     | array of {topic, criticality, raised_at}        | Phase 6    |
| `brief.last_gate_results.align`       | {decision, severity, findings_count, at} or null | Phase 4    |
| `brief.last_gate_results.audience`    | same shape or null                              | Phase 5    |
| `brief.last_gate_results.compliance`  | same shape or null                              | Phase 7    |
| `brief.current_workstream`            | string slug or null                             | Phase 7    |

state.cjs cmdStateJson current allowlist (verified 2026-04-19, lines 955-984):
```javascript
// Preserve frontmatter-only fields that cannot be recovered from the body.
if (existingFm && existingFm.stopped_at && !built.stopped_at) {
  built.stopped_at = existingFm.stopped_at;
}
if (existingFm && existingFm.paused_at && !built.paused_at) {
  built.paused_at = existingFm.paused_at;
}
if (built.status === 'unknown' && existingFm && existingFm.status && existingFm.status !== 'unknown') {
  built.status = existingFm.status;
}
```

state.cjs syncStateFrontmatter current mirror (lines 852-868):
```javascript
if (derivedFm.status === 'unknown' && existingFm.status && existingFm.status !== 'unknown') {
  derivedFm.status = existingFm.status;
}
```

tests/helpers.cjs API (verified 2026-04-19): `createTempProject(prefix?)`, `cleanup(tmpDir)`, `runGsdTools(args, cwd, env?)` — args may be string or string[].

brief/bin/brief-tools.cjs: dispatches `state json` subcommand to `cmdStateJson(cwd, raw)`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — Create tests/state-brief-roundtrip.test.cjs with full D-03 coverage (A4 smoke)</name>
  <read_first>
    - tests/state.test.cjs (lines 416-483 — canonical STATE.md frontmatter round-trip pattern; reuse this shape)
    - tests/helpers.cjs (createTempProject, runGsdTools signatures)
    - brief/bin/lib/state.cjs (writeStateMd at ~923, cmdStateJson at ~955, syncStateFrontmatter at ~852)
    - brief/bin/lib/frontmatter.cjs (post Plan-01 state — has the recursive serializer + null preservation)
    - .planning/phases/02-stable-seam-anchor-schema-caps-workstream-as-config/02-PATTERNS.md (section tests/state-brief-roundtrip.test.cjs, lines 82-145)
    - .planning/phases/02-stable-seam-anchor-schema-caps-workstream-as-config/02-CONTEXT.md (D-01 smoke test contract + D-03 schema)
    - .planning/phases/02-stable-seam-anchor-schema-caps-workstream-as-config/02-RESEARCH.md (R-5 stronger-test note; Code Examples lines 530-583; Pitfall 1 lines 439-448)
    - .planning/phases/02-stable-seam-anchor-schema-caps-workstream-as-config/02-VALIDATION.md (test map lines 44-49)
  </read_first>
  <behavior>
    Create NEW file tests/state-brief-roundtrip.test.cjs implementing the A4 smoke test (FND-05). The file MUST exercise FOUR distinct test cases:

    1. Cycle 1 (writeStateMd round-trip with populated D-03 payload) — fixture includes array-of-objects (return_stack, gap_queue), nested object leaf (last_gate_results.align with decision/severity/findings_count/at), null leaves (audience, compliance), and scalar string (current_workstream: 'bmc'). Assert deep-strict-equal between each brief.* field and the injected value. Verify null leaves come back as JS null, not string "null".
    2. Cycle 2 (writeStateMd again — no-drift check) — take the content from Cycle 1 read-back, re-write, read again, assert deep-strict-equal between Cycle 2 and Cycle 1 brief maps.
    3. Cycle 3 (brief-tools.cjs state json — R-5 stronger test) — after Cycle 2, invoke runGsdTools(['state', 'json'], tmpDir). Parse the JSON stdout. Assert the parsed output contains brief with all D-03 fields intact. This catches R-5 — without the allowlist extension (Task 2), this cycle drops the brief map silently.
    4. Placeholder test (all-empty fixture, Phase 2 initial state) — seeds STATE.md with return_stack: [], gap_queue: [], all gate results null, current_workstream: null. Round-trips once via writeStateMd. Asserts the placeholder shape survives — empty arrays stay arrays; null leaves are JS null.
  </behavior>
  <action>
    Create NEW file tests/state-brief-roundtrip.test.cjs. Imports per 02-PATTERNS.md lines 86-93:

    ```javascript
    const { test, describe, beforeEach, afterEach } = require('node:test');
    const assert = require('node:assert/strict');
    const fs = require('fs');
    const path = require('path');
    const { runGsdTools, createTempProject, cleanup } = require('./helpers.cjs');
    const { writeStateMd } = require('../brief/bin/lib/state.cjs');
    const { extractFrontmatter } = require('../brief/bin/lib/frontmatter.cjs');
    ```

    Structure as a single describe('state.brief.* round-trip (A4 verification, FND-05)', () => { ... }) block with beforeEach/afterEach for tmpDir.

    Seed fixture pattern (adapted from 02-RESEARCH.md Code Examples + 02-PATTERNS.md lines 112-124). Build `initialContent` as a multi-line string that includes: `brief_state_version: 1.0`, a minimal `progress:` block with `total_phases: 9, completed_phases: 1`, and the populated `brief:` map with:
    - return_stack: one frame object [{from_phase: 'DESIGN', to_phase: 'DISCOVER', reason: 'gap_detected', pushed_at: '2026-04-18T10:00:00Z'}]
    - gap_queue: one entry [{topic: 'market_sizing', criticality: 'MATERIAL', raised_at: '2026-04-18T10:00:00Z'}]
    - last_gate_results.align: {decision: 'ALIGNED', severity: 'info', findings_count: 0, at: '2026-04-18T10:00:00Z'}
    - last_gate_results.audience: null
    - last_gate_results.compliance: null
    - current_workstream: 'bmc'

    Test 1 — Cycle 1 round-trip. After writeStateMd + extractFrontmatter, use:
    - `assert.deepStrictEqual(fm.brief.return_stack, [{from_phase: 'DESIGN', to_phase: 'DISCOVER', reason: 'gap_detected', pushed_at: '2026-04-18T10:00:00Z'}], 'array-of-objects preserved')`
    - `assert.deepStrictEqual(fm.brief.last_gate_results.align, {decision: 'ALIGNED', severity: 'info', findings_count: 0, at: '2026-04-18T10:00:00Z'}, 'nested object leaf preserved')`
    - `assert.strictEqual(fm.brief.last_gate_results.audience, null, 'null leaf stays JS null')`
    - `assert.strictEqual(fm.brief.current_workstream, 'bmc', 'scalar preserved')`

    Test 2 — Cycle 2 no-drift. Read content1 from disk after cycle 1 (not from memory), then write it back using the literal one-line pattern (matches 02-RESEARCH.md §Code Examples lines 568-580):

    ```javascript
    const content1 = fs.readFileSync(statePath, 'utf-8'); writeStateMd(statePath, content1, tmpDir);
    ```

    Read fm2 via extractFrontmatter. Assert `assert.deepStrictEqual(fm2.brief, fm1.brief, 'brief: map unchanged across two write cycles')`.

    Test 3 — Cycle 3 cmdStateJson (R-5 stronger test). After cycle 2, call `const result = runGsdTools(['state', 'json'], tmpDir)`. Check `result.success` (if helpers returns that shape) OR just JSON.parse(result.stdout) inside a try/catch. Assert:
    - `assert.ok(parsed.brief, 'brief namespace survives cmdStateJson rebuild (allowlist extension live)')`
    - `assert.strictEqual(parsed.brief.current_workstream, 'bmc')`
    - `assert.strictEqual(parsed.brief.last_gate_results.align.decision, 'ALIGNED')`
    - `assert.strictEqual(parsed.brief.last_gate_results.audience, null)`

    Test 4 — Placeholder (all-empty fixture). Build a second `placeholderContent` string with `brief:` map containing ONLY placeholders: `return_stack: []`, `gap_queue: []`, `last_gate_results: {align: null, audience: null, compliance: null}`, `current_workstream: null`. Seed, writeStateMd, extractFrontmatter. Assert:
    - `assert.deepStrictEqual(fm.brief.return_stack, [])`
    - `assert.deepStrictEqual(fm.brief.gap_queue, [])`
    - `assert.strictEqual(fm.brief.last_gate_results.align, null)`
    - `assert.strictEqual(fm.brief.current_workstream, null)`

    Do NOT:
    - Bypass writeStateMd via fs.writeFileSync for production-path assertions (only acceptable for initial fixture seeding — per 02-RESEARCH.md Anti-Patterns).
    - Use scalar-only fixtures (falsely PASS per Pitfall 1).
    - Use JSON.stringify comparison (flattens null/"null" drift).
    - Use `assert.ok(fm.brief)` as the primary assertion (weak; passes even when leaves are corrupted).

    At this point (after Task 1, before Task 2), Test 3 (R-5 stronger test) MUST FAIL because cmdStateJson's allowlist doesn't yet preserve brief:. Tests 1/2/4 MAY pass already (depending on whether Plan 01 extended reconstructFrontmatter correctly — they SHOULD pass since Plan 01 is a dependency). This is RED-green for the R-5 fix.

    Implements D-01 per 02-CONTEXT.md; covers D-02/D-03 schema; tests D-21 preservation.
  </action>
  <verify>
    <automated>node --test tests/state-brief-roundtrip.test.cjs 2>&amp;1 | tee /tmp/t1-state-red.log; grep -qE "fail|not ok" /tmp/t1-state-red.log &amp;&amp; echo "RED confirmed" || (echo "ERROR: tests unexpectedly green — Cycle 3 must fail before Task 2" &amp;&amp; exit 1)</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f tests/state-brief-roundtrip.test.cjs`
    - Imports canonical set: `grep -q "require('node:test')" tests/state-brief-roundtrip.test.cjs &amp;&amp; grep -q "createTempProject" tests/state-brief-roundtrip.test.cjs &amp;&amp; grep -q "writeStateMd" tests/state-brief-roundtrip.test.cjs`
    - 4 test cases present: `grep -cE "^\s*test\(" tests/state-brief-roundtrip.test.cjs` returns at least 4
    - Fixture includes array-of-objects `from_phase`: `grep -q "from_phase" tests/state-brief-roundtrip.test.cjs`
    - Fixture includes nested-object-at-leaf: `grep -q "decision:" tests/state-brief-roundtrip.test.cjs &amp;&amp; grep -q "findings_count" tests/state-brief-roundtrip.test.cjs`
    - Fixture includes null leaf: `grep -qE "audience:\s*null" tests/state-brief-roundtrip.test.cjs`
    - Cycle-3 uses runGsdTools + state json: `grep -q "runGsdTools" tests/state-brief-roundtrip.test.cjs &amp;&amp; grep -qE "state.*json" tests/state-brief-roundtrip.test.cjs`
    - Assertion style uses deepStrictEqual: `grep -c "assert.deepStrictEqual" tests/state-brief-roundtrip.test.cjs` is at least 3
    - writeStateMd called at least 3 times: `grep -c "writeStateMd" tests/state-brief-roundtrip.test.cjs` is at least 3
    - Cycle 3 (state-json preservation) FAILS before Task 2: `node --test tests/state-brief-roundtrip.test.cjs 2>&amp;1 | grep -qE "fail|not ok"` (exit 0 means failure signal present — which is the expected RED state)
    - Existing tests unaffected: `node --test tests/state.test.cjs` exits 0 AND `node --test tests/frontmatter.test.cjs` exits 0 AND `node --test tests/frontmatter-roundtrip.test.cjs` exits 0
  </acceptance_criteria>
  <done>
    New file tests/state-brief-roundtrip.test.cjs present with 4 test cases covering all D-03 shapes + 2-cycle integrity + R-5 state-json preservation; Cycle 3 FAILS as expected (confirms the test exercises the R-5 defect that Task 2 will fix); other test suites unaffected.

    **Plan-level completion note for orchestrator/checker tooling:** this plan follows a TDD red-before-green shape. Task 1 legitimately ends RED (Cycle 3 must fail before Task 2 extends the allowlist); plan-level completion is judged at Task 3 SUMMARY write, NOT at Task 1 exit. Tooling interpreting Task 1's failing test as plan failure would be a false positive — the intermediate RED is the designed contract per 02-RESEARCH.md R-5.
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend cmdStateJson + syncStateFrontmatter allowlist (D-21) AND initialize brief: map in STATE.md (D-02/D-03)</name>
  <read_first>
    - brief/bin/lib/state.cjs (ENTIRE file — 1618 lines; specifically lines 714-836 buildStateFrontmatter, 852-868 syncStateFrontmatter, 923-935 writeStateMd, 955-984 cmdStateJson — do NOT modify buildStateFrontmatter structure, only the preservation paths)
    - .planning/STATE.md (current content after Plan 03 — frontmatter has `brief_state_version: 1.0` but NO `brief:` map yet)
    - .planning/phases/02-stable-seam-anchor-schema-caps-workstream-as-config/02-PATTERNS.md (section MODIFIED: brief/bin/lib/state.cjs, lines 575-636 — the 3-edit pattern)
    - .planning/phases/02-stable-seam-anchor-schema-caps-workstream-as-config/02-CONTEXT.md (D-02 nested map; D-03 schema; D-21 allowlist extension mandate)
    - .planning/phases/02-stable-seam-anchor-schema-caps-workstream-as-config/02-RESEARCH.md (R-5 + A-P2-4 — why this edit is load-bearing)
    - tests/state.test.cjs (50+ existing assertions — must stay green; the regression canary)
    - tests/state-brief-roundtrip.test.cjs (the contract you must satisfy — specifically Cycle 3 must turn green)
  </read_first>
  <action>
    Three coordinated edits:

    Edit 1 — brief/bin/lib/state.cjs cmdStateJson allowlist extension (around lines 971-981, after the `status === 'unknown'` preservation branch). Add a 4th preservation branch:

    ```javascript
    // Preserve the brief:* namespaced map (cannot be recovered from body).
    // Per Phase 2 D-21 — without this, the brief: map is silently dropped on `state json` rebuild.
    if (existingFm && existingFm.brief) {
      built.brief = existingFm.brief;
    }
    ```

    Place it grouped with the other preservation branches (between the `stopped_at` / `paused_at` / `status` branches and the `output(...)` call at the end of cmdStateJson).

    Edit 2 — brief/bin/lib/state.cjs syncStateFrontmatter mirror (around lines 852-868, after the `status === 'unknown'` preservation branch, BEFORE the `reconstructFrontmatter(derivedFm)` call). Add:

    ```javascript
    // Preserve brief:* namespaced map across write cycles (buildStateFrontmatter doesn't regenerate it).
    // Per Phase 2 D-21 — paired with the cmdStateJson allowlist extension above.
    if (existingFm && existingFm.brief) {
      derivedFm.brief = existingFm.brief;
    }
    ```

    Edit 3 — .planning/STATE.md frontmatter initialization (D-02 / D-03 placeholder shape). Hand-edit is acceptable for this one-shot migration per PATTERNS.md §860 'initial fixture seeding' carve-out; post-commit `state json` validation at acceptance line ~316 confirms the production path preserves the map. Append the following nested map to the existing STATE.md frontmatter block — insert BEFORE the closing `---` line, AFTER the existing `progress:` block:

    ```yaml
    brief:
      return_stack: []
      gap_queue: []
      last_gate_results:
        align: null
        audience: null
        compliance: null
      current_workstream: null
    ```

    After the edit, validate by running:
    ```
    node -e "const {extractFrontmatter} = require('./brief/bin/lib/frontmatter.cjs'); const fs = require('fs'); const fm = extractFrontmatter(fs.readFileSync('.planning/STATE.md', 'utf-8')); console.log(JSON.stringify(fm.brief, null, 2))"
    ```
    Expected output — the full D-03 placeholder map with null leaves (NOT string "null") and empty arrays.

    Do NOT:
    - Change buildStateFrontmatter structure — that would ripple into 50+ tests. Only extend the two preservation sites.
    - Widen the allowlist beyond `brief:` (e.g., to Object.keys(existingFm)) — that would leak stale frontmatter.
    - Initialize brief.* with Phase 4/5/6/7 content — D-03 says the Phase 2 commit declares shape only.
    - Edit the STATE.md body (# Project State, etc.) — only the frontmatter block changes.

    Implements D-02, D-03, D-21 per 02-CONTEXT.md.
  </action>
  <verify>
    <automated>node --test tests/state-brief-roundtrip.test.cjs &amp;&amp; node --test tests/state.test.cjs &amp;&amp; node --test tests/frontmatter.test.cjs &amp;&amp; node --test tests/frontmatter-roundtrip.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    - tests/state-brief-roundtrip.test.cjs ALL pass: `node --test tests/state-brief-roundtrip.test.cjs` exits 0 with 0 failures (Cycle 3 now green)
    - No regressions in tests/state.test.cjs: `node --test tests/state.test.cjs` exits 0
    - No regressions in tests/frontmatter.test.cjs / tests/frontmatter-roundtrip.test.cjs: both exit 0
    - cmdStateJson preservation branch added: `grep -qE "existingFm\.brief" brief/bin/lib/state.cjs`
    - syncStateFrontmatter mirror added: `grep -c "existingFm\.brief" brief/bin/lib/state.cjs` is at least 2 (cmdStateJson + syncStateFrontmatter)
    - STATE.md contains brief: map: `grep -qE "^brief:$" .planning/STATE.md`
    - STATE.md has 4 required D-03 subkeys: `grep -q "  return_stack:" .planning/STATE.md &amp;&amp; grep -q "  gap_queue:" .planning/STATE.md &amp;&amp; grep -q "  last_gate_results:" .planning/STATE.md &amp;&amp; grep -q "  current_workstream:" .planning/STATE.md`
    - STATE.md 3 gate result nulls: `grep -qE "^\s*align: null$" .planning/STATE.md &amp;&amp; grep -qE "^\s*audience: null$" .planning/STATE.md &amp;&amp; grep -qE "^\s*compliance: null$" .planning/STATE.md`
    - STATE.md parses correctly: `node -e "const {extractFrontmatter} = require('./brief/bin/lib/frontmatter.cjs'); const fs = require('fs'); const fm = extractFrontmatter(fs.readFileSync('.planning/STATE.md', 'utf-8')); if (fm.brief.current_workstream !== null || !Array.isArray(fm.brief.return_stack) || fm.brief.last_gate_results.align !== null) process.exit(1)"` exits 0
    - state json preserves map: `node brief/bin/brief-tools.cjs state json 2>/dev/null | node -e "const j = JSON.parse(require('fs').readFileSync(0, 'utf-8')); if (!j.brief || j.brief.current_workstream !== null) process.exit(1)"` exits 0
    - npm test baseline preserved: `npm test 2>&amp;1 | grep -cE '^✖'` is at most 63
  </acceptance_criteria>
  <done>
    cmdStateJson + syncStateFrontmatter preserve `existingFm.brief` via a 4th allowlist branch; STATE.md has the D-03 `brief:` placeholder map initialized; all tests in tests/state-brief-roundtrip.test.cjs pass; no regressions in state/frontmatter/frontmatter-roundtrip test suites; npm-test baseline unchanged (≤ 63).
  </done>
</task>

<task type="auto">
  <name>Task 3: Append `### A4 — VERIFIED` entry to .planning/ASSUMPTIONS.md</name>
  <read_first>
    - .planning/ASSUMPTIONS.md (ENTIRE file — match the Phase 1 verification entry format at lines 13-56 for A1, FND-06, A-REPO, FND-07)
    - .planning/phases/02-stable-seam-anchor-schema-caps-workstream-as-config/02-PATTERNS.md (section MODIFIED: .planning/ASSUMPTIONS.md, lines 802-819)
    - .planning/phases/02-stable-seam-anchor-schema-caps-workstream-as-config/02-CONTEXT.md (D-01 verification approach; D-20 + D-21 mitigations that make A4 pass)
    - tests/state-brief-roundtrip.test.cjs (the source-of-truth test referenced in the entry)
    - .planning/phases/02-stable-seam-anchor-schema-caps-workstream-as-config/02-VALIDATION.md (line 47 — grep contract for the entry)
  </read_first>
  <action>
    Append a new `### A4 —` section to .planning/ASSUMPTIONS.md. Placement: after the last existing Phase 1 entry but in a NEW top-level subsection `## Phase 2 Verifications`.

    Content to append (match the existing entry format, filling placeholders from actual test run):

    A markdown block with heading `## Phase 2 Verifications` followed by `### A4 — STATE.md round-trips state.brief.* without loss — VERIFIED`. The section MUST include:
    - `**Status:** VERIFIED`
    - `**Timestamp:** <UTC ISO-8601>` (executor fills with `date -u +"%Y-%m-%dT%H:%M:%SZ"`)
    - `**Phase:** 02-stable-seam-anchor-schema-caps-workstream-as-config`
    - `**Requirement:** FND-05 (ROADMAP.md Success Criterion #1 for Phase 2)`
    - `**Verification source:** tests/state-brief-roundtrip.test.cjs`
    - A Coverage bullet list that mentions: array-of-objects (return_stack, gap_queue), nested object leaves (last_gate_results.align with decision/severity/findings_count/at), null leaves (audience/compliance), scalar strings (current_workstream: 'bmc')
    - `**Verification command:**` code block with `node --test tests/state-brief-roundtrip.test.cjs`
    - `**Expected output:** 0 failures across 4 test cases` (Cycle 1, Cycle 2, Cycle 3, Placeholder)
    - `**Actual output:**` — executor attaches the literal `# tests`/`# pass`/`# fail` summary lines from `node --test tests/state-brief-roundtrip.test.cjs 2>&1 | grep -E "^# (tests|pass|fail)"`
    - `**Implication:**` paragraph noting that D-05 sidecar is NOT needed — D-20 + D-21 closed R-1 and R-5
    - `**Downstream unblock:**` Phase 4 / Phase 5 / Phase 6 / Phase 7 can write state.brief.* with round-trip fidelity
    - `**Risk if future code re-introduces drop:**` noting the regression guard (state-brief-roundtrip.test.cjs Cycle 3)

    The executor MUST:
    - Fill in the `**Timestamp:**` placeholder with actual UTC ISO-8601 at verification moment.
    - Fill in the `**Actual output:**` placeholder with real test summary output.
    - NOT append this section UNTIL Task 2 tests pass green — the entry asserts VERIFIED, so must be truthful.

    Do NOT edit existing Phase 1 entries in ASSUMPTIONS.md.

    Implements FND-05 verification documentation per 02-CONTEXT.md D-01 and 02-VALIDATION.md line 47 grep contract.
  </action>
  <verify>
    <automated>grep -q "^### A4 —" .planning/ASSUMPTIONS.md &amp;&amp; grep -q "VERIFIED" .planning/ASSUMPTIONS.md &amp;&amp; grep -q "tests/state-brief-roundtrip.test.cjs" .planning/ASSUMPTIONS.md &amp;&amp; grep -q "FND-05" .planning/ASSUMPTIONS.md &amp;&amp; grep -q "## Phase 2 Verifications" .planning/ASSUMPTIONS.md</automated>
  </verify>
  <acceptance_criteria>
    - New H3 section present: `grep -q "^### A4 —" .planning/ASSUMPTIONS.md`
    - New H2 Phase-2 section present: `grep -q "^## Phase 2 Verifications" .planning/ASSUMPTIONS.md`
    - Status is VERIFIED: `grep -qE "Status:\*\*\s*VERIFIED" .planning/ASSUMPTIONS.md`
    - Source test file cited: `grep -q "tests/state-brief-roundtrip.test.cjs" .planning/ASSUMPTIONS.md`
    - FND-05 referenced: `grep -q "FND-05" .planning/ASSUMPTIONS.md`
    - Timestamp placeholder filled (ISO-8601 format): `grep -qE "Timestamp:\*\*\s*[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}Z" .planning/ASSUMPTIONS.md`
    - Coverage bullet mentions array-of-objects, nested object, null leaf: `grep -q "array-of-objects" .planning/ASSUMPTIONS.md &amp;&amp; grep -q "null lea" .planning/ASSUMPTIONS.md`
    - Phase 1 entries left unchanged: `grep -c "^### " .planning/ASSUMPTIONS.md` should have increased by exactly 1 vs. pre-edit (pre-edit count: `git show HEAD:.planning/ASSUMPTIONS.md | grep -c "^### "`)
    - A4 verification entry appended AFTER all Phase 1 entries (sanity check ordering): `awk '/^### FND-07/,/### A4/' .planning/ASSUMPTIONS.md | wc -l` is greater than 1 (both headings present in that order)
  </acceptance_criteria>
  <done>
    .planning/ASSUMPTIONS.md has a new `## Phase 2 Verifications` subsection containing the `### A4 — VERIFIED` entry with timestamp, source test file, 4-cycle coverage description, and actual `# tests`/`# pass`/`# fail` output attached. FND-05 is now officially verified.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| state.cjs preservation allowlist | First-party allowlist controlling which frontmatter fields survive the rebuild-from-body path. Widening it inappropriately could leak stale data (mitigated: only brief: added, not Object.keys) |
| STATE.md frontmatter | First-party artifact under git; the new brief: map is populated by this process in Phase 2 and by Phase 4+ orchestrator steps later |
| A4 verification claim in ASSUMPTIONS.md | Written only AFTER the smoke test passes (Task 3 gates on Task 2 completion) — prevents premature VERIFIED claim |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04-01 | Tampering | cmdStateJson + syncStateFrontmatter preservation allowlist | mitigate | Task 2 Edit 1+2: add a 4th preservation branch scoped ONLY to `existingFm.brief`; do NOT widen to Object.keys; closes R-5 silent-drop |
| T-02-04-02 | Tampering | A4 smoke test fixture coverage | mitigate | Task 1 fixture includes array-of-objects, nested object leaf, null leaf per Pitfall 1; Cycle 3 specifically exercises the cmdStateJson path (not just writeStateMd+extractFrontmatter) per R-5 stronger-test note |
| T-02-04-03 | Repudiation | A4 VERIFIED claim in ASSUMPTIONS.md | mitigate | Task 3 gates on Task 2 test green; executor fills timestamp + actual output from real test run |
</threat_model>

<verification>
1. All 4 new test cases in tests/state-brief-roundtrip.test.cjs pass green.
2. No regressions in tests/state.test.cjs / tests/frontmatter.test.cjs / tests/frontmatter-roundtrip.test.cjs.
3. STATE.md has the `brief:` nested map parseable via extractFrontmatter with the expected D-03 placeholder shape.
4. `brief-tools.cjs state json` output includes `brief` key with full D-03 shape (R-5 closed).
5. npm test failure count ≤ 63 (Phase 1 baseline preserved — Phase 2 delta-cap discipline).
6. .planning/ASSUMPTIONS.md has the `### A4 — VERIFIED` entry with timestamp filled, source cited, and FND-05 referenced.
7. No widening of the state.cjs preservation allowlist beyond the `brief:` branch.
</verification>

<success_criteria>
- FND-05 is satisfied: the A4 smoke test passes, Assumption A4 is VERIFIED in writing, and the preservation paths (D-21) are live.
- STATE.md has the forward-declared D-03 schema initialized (Phase 4+ can begin writing into it).
- Zero test-suite regressions across state, frontmatter, frontmatter-roundtrip, and state-brief-roundtrip.
- npm test failure count ≤ 63.
- ASSUMPTIONS.md has an auditable A4 record.
</success_criteria>

<output>
After completion, create `.planning/phases/02-stable-seam-anchor-schema-caps-workstream-as-config/02-04-SUMMARY.md` recording: 4 test cases passing, state.cjs diff summary (2 preservation branches added), STATE.md frontmatter diff, ASSUMPTIONS.md `### A4 — VERIFIED` entry with actual test output attached, npm-test count pre/post (both ≤ 63).
</output>
