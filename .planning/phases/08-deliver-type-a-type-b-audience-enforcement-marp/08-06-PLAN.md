---
phase: 08-deliver-type-a-type-b-audience-enforcement-marp
plan: 06
type: execute
wave: 3
depends_on: [08-04]
files_modified:
  - agents/brief-deliver-type-b.md
  - brief/templates/deliver/type-b/internal-deck.md
  - brief/templates/deliver/type-b/proposal-deck.md
  - brief/templates/deliver/type-b/exec-summary.md
  - brief/templates/deliver/type-b/decision-memo.md
  - brief/templates/deliver/marp-themes/default.css
  - brief/templates/deliver/marp-themes/partner.css
  - brief/templates/deliver/marp-themes/confidential.css
  - tests/brief-deliver-type-b-templates.test.cjs
  - tests/brief-deliver-ko-en-branching.test.cjs
autonomous: true
requirements: [DLV-05, DLV-06, DLV-07, DLV-09]

must_haves:
  truths:
    - "agents/brief-deliver-type-b.md is parameterized via {{ARTIFACT}} (∈ internal-deck / proposal-deck / exec-summary / decision-memo) — single agent file"
    - "Type B agent embeds banned-words list inline (matching voice-fit.cjs BANNED_EN/BANNED_KO from Plan 02 byte-identity) for self-check before final output"
    - "Type B agent embeds concreteness rule inline ('compared to what / by how much / when') + 4 hand-written exemplars from voice-fit-vocabulary.md (Plan 02)"
    - "Type B agent embeds Korean honorific rule inline (D-D04) — fires only when language=ko AND audience.confidentiality in {partner, public, external}"
    - "internal-deck.md template: Marp frontmatter directives (marp: true, theme: default, paginate: true, footer: 'CONFIDENTIAL — Internal use only — Do not share') + 7-9 slide sequence (Cover / Problem / Solution / Market / Strategy / Roadmap / Ask / [Team] / [Appendix])"
    - "proposal-deck.md template: Marp frontmatter (theme: partner, footer: 'Partner-only — Do not redistribute') + 7-9 slide sequence with 'Traction' replacing 'Strategy', 'Team' redacted for partner audience"
    - "Cover slide of internal-deck.md and proposal-deck.md contains literal first-slide watermark text (NOT only Marp footer directive — survives copy-paste)"
    - "exec-summary.md template: 5-section narrative (Context / Problem / Recommendation / Risks / Ask) — NO Marp directives (markdown only, 1-2 pages)"
    - "decision-memo.md template: ADR 4-section (Context / Decision / Alternatives Considered / Consequences) — NO Marp directives"
    - "ko/en branching: region: kr → ko 단일 emit (no .en.md sibling); --en flag opt-in → adds {name}.en.md pair; region: us → en 단일 emit"
    - "3 Marp CSS theme files exist (default.css for INTERNAL, partner.css for PROPOSAL, confidential.css for CONFIDENTIAL banner styling)"
  artifacts:
    - path: "agents/brief-deliver-type-b.md"
      provides: "Parameterized Type B deck/document agent with inline banned-words + concreteness + ko-honorific rules"
      min_lines: 250
      contains: "{{ARTIFACT}}"
    - path: "brief/templates/deliver/type-b/internal-deck.md"
      provides: "Marp INTERNAL deck source with footer + literal Cover watermark + 7-9 slide sequence"
      min_lines: 80
      contains: "marp: true"
    - path: "brief/templates/deliver/type-b/proposal-deck.md"
      provides: "Marp PROPOSAL deck source with partner-safe content (Traction replacing Strategy)"
      min_lines: 80
      contains: "marp: true"
    - path: "brief/templates/deliver/type-b/exec-summary.md"
      provides: "EXEC-SUMMARY 5-section narrative (NO Marp)"
      min_lines: 50
      contains: "## Context"
    - path: "brief/templates/deliver/type-b/decision-memo.md"
      provides: "DECISION-MEMO ADR 4-section (NO Marp)"
      min_lines: 50
      contains: "## Decision"
    - path: "brief/templates/deliver/marp-themes/default.css"
      provides: "Marp default theme — INTERNAL deck CSS (~100 LOC)"
      min_lines: 60
      contains: "section"
    - path: "brief/templates/deliver/marp-themes/partner.css"
      provides: "Marp partner theme — PROPOSAL deck CSS"
      min_lines: 60
    - path: "brief/templates/deliver/marp-themes/confidential.css"
      provides: "CONFIDENTIAL banner styling overlay"
      min_lines: 40
    - path: "tests/brief-deliver-type-b-templates.test.cjs"
      provides: "Wave 0 fixture for Type B template structure + Marp directives + slide sequence + watermark"
      contains: "marp: true"
    - path: "tests/brief-deliver-ko-en-branching.test.cjs"
      provides: "Wave 0 fixture for D-D03 region: kr → ko 단일; --en → ko/en pair"
      contains: "voice.languages"
  key_links:
    - from: "agents/brief-deliver-type-b.md"
      to: "brief/bin/lib/voice-fit.cjs (Plan 02)"
      via: "Agent prompt embeds BANNED_EN, BANNED_KO, HONORIFIC_VIOLATION_KO regex constants verbatim for self-check"
      pattern: "leverage|synergize|transform"
    - from: "agents/brief-deliver-type-b.md"
      to: "brief/references/voice-fit-vocabulary.md (Plan 02)"
      via: "Agent prompt embeds 4 hand-written exemplars (INVESTOR-IR / EXEC-SUMMARY / DECISION-MEMO / Korean) verbatim"
      pattern: "exemplar"
    - from: "brief/templates/deliver/type-b/{internal,proposal}-deck.md"
      to: "brief/bin/lib/export.cjs watermarkFor (Plan 04)"
      via: "{{watermark_text}} placeholder in template fills from watermarkFor(confidentiality, language) at template-fill time (Plan 08 workflow)"
      pattern: "{{watermark_text}}"
---

<objective>
Ship `agents/brief-deliver-type-b.md` (parameterized Type B agent) + 4 Type B templates + 3 Marp CSS themes + 2 Wave 0 test files. Implements DLV-05 (INTERNAL-DECK), DLV-06 (PROPOSAL-DECK), DLV-07 (EXEC-SUMMARY + DECISION-MEMO), DLV-09 (filename + first-slide watermark) — Layer 2 of the 4-layer audience defense.

Purpose: With Plan 04's export.cjs Marp invocation + watermark mapping in place, Plan 06 ships the actual deck/document templates that get rendered. Type B agent is parameterized (single file, 4 artifacts) — same Phase 5 D-01 pattern as Plan 05. Inline ban-list + concreteness + Korean honorific rules at agent prompt (NOT new gates per CONTEXT.md C-D03/C-D04/D-D04 reject). Marp directive `footer:` + literal Cover slide watermark = Layer 2 watermark defense.

Output: 1 agent + 4 templates + 3 CSS themes + 2 Wave 0 tests. Plan 08 wires this into `commands/brief/deliver.md` + `workflows/deliver.md` for Type B path; Plan 08 canary E2E exercises the full Marp render flow.
</objective>

<execution_context>
@~/.claude/brief/workflows/execute-plan.md
@~/.claude/brief/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-deliver-type-a-type-b-audience-enforcement-marp/08-CONTEXT.md
@.planning/phases/08-deliver-type-a-type-b-audience-enforcement-marp/08-RESEARCH.md
@.planning/phases/08-deliver-type-a-type-b-audience-enforcement-marp/08-PATTERNS.md
@.planning/phases/08-deliver-type-a-type-b-audience-enforcement-marp/08-02-PLAN.md
@.planning/phases/08-deliver-type-a-type-b-audience-enforcement-marp/08-04-PLAN.md
@.planning/phases/08-deliver-type-a-type-b-audience-enforcement-marp/08-05-PLAN.md
@agents/brief-domain-researcher.md
@agents/brief-audience-guard.md

<interfaces>
<!-- From Plan 02 voice-fit.cjs — Type B agent prompt embeds these regex constants verbatim. -->

```javascript
// EMBED VERBATIM in agent prompt's <banned_vocabulary> block:
const BANNED_EN_LIST = ['leverage', 'synergize', 'transform', 'holistic', 'delve', 'groundbreaking',
                        'best-in-class', 'seamless', 'cutting-edge', 'revolutionary', 'game-changing',
                        'landscape', 'unlock', 'empower', 'robust', 'innovative'];

const BANNED_KO_LIST = ['혁신적인', '차별화된', '게임체인저', '패러다임 시프트', '시너지',
                        '활용', '최적화', '글로벌 스탠더드'];

const HONORIFIC_VIOLATION_KO_LIST = ['-야', '-지', '-라구요', '-거든요', '-는데요'];
```

From Plan 02 brief/references/voice-fit-vocabulary.md — agent prompt embeds the 4 hand-written exemplars (INVESTOR-IR / EXEC-SUMMARY / DECISION-MEMO / Korean) verbatim for in-context style anchor.

From Plan 04 brief/bin/lib/export.cjs watermarkFor + WATERMARKS_EN/WATERMARKS_KO:
```javascript
WATERMARKS_EN[confidentiality] / WATERMARKS_KO[confidentiality]
// public → 'Public' / '공개'
// partner → 'Partner-only — Do not redistribute' / '파트너 전용 — 재배포 금지'
// internal → 'Internal — Do not distribute outside {organization}' / '내부용 — {조직명} 외 배포 금지'
// confidential → 'CONFIDENTIAL — Internal use only — Do not share' / '기밀 — 내부 사용만 — 공유 금지'
```

Type B template uses `{{watermark_text}}` placeholder; Plan 08 workflow fills via `watermarkFor(fm['audience.confidentiality'], language)` at template-fill time.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — Write 2 RED test fixtures (templates + ko/en branching)</name>
  <files>tests/brief-deliver-type-b-templates.test.cjs, tests/brief-deliver-ko-en-branching.test.cjs</files>
  <read_first>
    - .planning/phases/08-deliver-type-a-type-b-audience-enforcement-marp/08-PATTERNS.md (lines 873-916 — Type B template patterns + Marp frontmatter + Cover slide watermark + 7-9 slide sequence)
    - .planning/phases/08-deliver-type-a-type-b-audience-enforcement-marp/08-RESEARCH.md (Code Example 3 lines 1383-1465 — Marp deck source template; Pattern 6 lines 743-786 — banned-words density check that agent self-checks against; Wave 0 Gaps lines 1596-1597 — ko-en-branching test required)
    - .planning/phases/08-deliver-type-a-type-b-audience-enforcement-marp/08-02-PLAN.md (voice-fit-vocabulary.md exemplar contract)
    - .planning/phases/08-deliver-type-a-type-b-audience-enforcement-marp/08-04-PLAN.md (watermarkFor signature + WATERMARKS_EN/WATERMARKS_KO mapping)
    - tests/brief-audience-roundtrip.test.cjs (existing node:test pattern)
  </read_first>
  <behavior>
    - templates test 1 (RED): All 4 Type B template files exist + 3 Marp CSS theme files exist.
    - templates test 2 (RED): internal-deck.md frontmatter contains `marp: true`, `theme: default`, `paginate: true`, `footer: 'CONFIDENTIAL — Internal use only — Do not share'` (or `{{watermark_text}}` placeholder which Plan 08 workflow fills).
    - templates test 3 (RED): internal-deck.md body contains a Cover slide with literal watermark string (NOT only the footer directive). Cover slide section MUST contain `> **{{watermark_text}}**` or equivalent literal-content watermark per RESEARCH.md Code Example 3 lines 1402-1407.
    - templates test 4 (RED): internal-deck.md has 7-9 slide sections (count `^---$` separators; 7-9 separators mean 8-10 slides counting cover). Slide sequence per D-D01: Cover / Problem / Solution / Market / Strategy / Roadmap / Ask + optional Team + optional Appendix.
    - templates test 5 (RED): proposal-deck.md frontmatter has `theme: partner` + `footer: 'Partner-only — Do not redistribute'` (or `{{watermark_text}}` placeholder).
    - templates test 6 (RED): proposal-deck.md body has slide labeled `Traction` (NOT `Strategy`); does NOT include `Risks` slide content (RISK workstream excluded for partner safety).
    - templates test 7 (RED): exec-summary.md does NOT contain `marp: true` directive (markdown only). Body has 5 sections: `## Context`, `## Problem`, `## Recommendation`, `## Risks`, `## Ask`.
    - templates test 8 (RED): decision-memo.md does NOT contain `marp: true`. Body has 4 ADR sections: `## Context`, `## Decision`, `## Alternatives Considered`, `## Consequences`.
    - templates test 9 (RED): All 4 templates have 5 mandatory frontmatter fields (audience.type, audience.confidentiality, voice.tone, voice.perspective, business_context.model) + voice.languages + deliverable + generated_by + generated_at placeholders.
    - templates test 10 (RED): 3 Marp CSS theme files exist with valid CSS (contains `section {` declarations); each has differentiating brand color comment (default = neutral / partner = brand outward / confidential = red banner).
    - templates test 11 (RED): agents/brief-deliver-type-b.md exists with `name: brief-deliver-type-b` + `tools: Read, Grep, Glob, Write` (NO Bash — agent does not invoke Marp directly).
    - templates test 12 (RED): agent body embeds `<banned_vocabulary>` block with all 16 EN words + all 8 KO words listed.
    - templates test 13 (RED): agent body embeds `<concreteness_rule>` block with the 'compared to what / by how much / when' rule.
    - templates test 14 (RED): agent body embeds `<korean_honorific_rule>` block listing the 5 반말 endings + the 'audience external + region:kr' conditional firing.
    - templates test 15 (RED): agent body embeds 4 hand-written exemplar blocks (INVESTOR-IR / EXEC-SUMMARY / DECISION-MEMO / Korean) — sourced from voice-fit-vocabulary.md (Plan 02). Verifiable: count `^### (INVESTOR-IR|EXEC-SUMMARY|DECISION-MEMO|Korean)` headings in agent file = 4.
    - ko/en test 1 (RED): When deliver.cjs (Plan 01) synthesizeTypeA invoked with `options: {}` against Korea-first fixture (region: kr), output frontmatter contains `voice.languages: [ko]` (NO en sibling file emitted).
    - ko/en test 2 (RED): With `options: { en: true }` AND region: kr, output frontmatter contains `voice.languages: [ko, en]` (Plan 08 workflow MAY emit en sibling file separately; for THIS test, just verify the languages array).
    - ko/en test 3 (RED): With region: us (modify fixture config.json brief.region), output frontmatter contains `voice.languages: [en]`.
    - ko/en test 4 (RED): Type B template (e.g., proposal-deck.md) supports `voice.languages: {{languages}}` placeholder.
    - All ~19 tests fail (RED).
  </behavior>
  <action>
Create 2 test files using node:test:

`tests/brief-deliver-type-b-templates.test.cjs` — 15 tests covering templates + Marp CSS + agent file structure. Use `fs.readFileSync` directly (markdown/CSS files, not modules).

`tests/brief-deliver-ko-en-branching.test.cjs` — 4+ tests covering D-D03. Imports `require('../brief/bin/lib/deliver.cjs')` (Plan 01) — the deliver.cjs frontmatter composition logic determines voice.languages. Tests run synthesizeTypeA against the Korea fixture from Plan 01, then mutate fixture config.json brief.region to 'us' to test the en-default branch.

Both test files use try/catch wrapping for predictable RED failure when require fails or files missing.

Commit message: `test(08-06): add failing Wave 0 tests for Type B templates + agent + ko/en branching (DLV-05..07,09)`. Run `node --test tests/brief-deliver-type-b-templates.test.cjs tests/brief-deliver-ko-en-branching.test.cjs` and assert non-zero exit (RED).
  </action>
  <verify>
    <automated>node --test tests/brief-deliver-type-b-templates.test.cjs tests/brief-deliver-ko-en-branching.test.cjs 2>&1 | grep -E "fail|ENOENT" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - 2 test files exist with combined ≥ 19 `test(` invocations
    - `node --test tests/brief-deliver-type-b-templates.test.cjs tests/brief-deliver-ko-en-branching.test.cjs` exits non-zero (Wave 0 RED — files missing)
    - `git log -1 --format=%s` matches `^test\\(08-06\\): add failing Wave 0 tests for Type B`
  </acceptance_criteria>
  <done>2 RED test files committed; node --test exits non-zero with predictable failure</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — Implement Type B agent + 4 templates + 3 Marp CSS themes</name>
  <files>agents/brief-deliver-type-b.md, brief/templates/deliver/type-b/internal-deck.md, brief/templates/deliver/type-b/proposal-deck.md, brief/templates/deliver/type-b/exec-summary.md, brief/templates/deliver/type-b/decision-memo.md, brief/templates/deliver/marp-themes/default.css, brief/templates/deliver/marp-themes/partner.css, brief/templates/deliver/marp-themes/confidential.css</files>
  <read_first>
    - .planning/phases/08-deliver-type-a-type-b-audience-enforcement-marp/08-PATTERNS.md (lines 397-462 — Type B agent analog with 5 distinctive additions: Marp frontmatter / banned-words inline / concreteness rule / honorific guard / process step ordering; lines 873-916 — Type B template patterns + 7-9 slide sequence + Marp CSS no-analog notes)
    - .planning/phases/08-deliver-type-a-type-b-audience-enforcement-marp/08-RESEARCH.md (Code Example 3 lines 1383-1465 — internal-deck.md template VERBATIM; Pattern 4 — Marp invocation; Pattern 6 — banned-words list)
    - agents/brief-domain-researcher.md (FULL file — base template to mirror)
    - agents/brief-audience-guard.md (frontmatter discipline reference for Type B agent's frontmatter rules)
    - brief/references/voice-fit-vocabulary.md (Plan 02 — copy 4 hand-written exemplars VERBATIM into Type B agent prompt)
    - tests/brief-deliver-type-b-templates.test.cjs (Plan 06 Task 1 — implementation must satisfy)
    - tests/brief-deliver-ko-en-branching.test.cjs
  </read_first>
  <action>
**Sub-task A — agents/brief-deliver-type-b.md:**

Create the agent file mirroring brief-domain-researcher.md byte-identity with Type B distinctive additions per PATTERNS.md lines 397-462.

Frontmatter:
```yaml
---
name: brief-deliver-type-b
description: Type B deck/document agent for BRIEF DELIVER phase. Generates 4 communication artifacts (INTERNAL-DECK / PROPOSAL-DECK / EXEC-SUMMARY / DECISION-MEMO) as markdown source. Type B INTERNAL/PROPOSAL DECKs are Marp-compatible markdown (rendered by /brief-export via npx --yes @marp-team/marp-cli@4.3.1). Type B EXEC-SUMMARY/DECISION-MEMO are pure markdown (1-2 pages). Parameterized at Task-spawn time by {{ARTIFACT}} + <business_context>. Embeds banned-words inline ban-list + concreteness rule + Korean honorific rule (when applicable) for SELF-CHECK before final output. Spawned by brief/workflows/deliver.md. Read-only on workstream artifacts; Write tool ONLY for {{OUT_PATH}}.
tools: Read, Grep, Glob, Write
color: green
---
```

Body sections:

1. `<role>` block — adapted from PATTERNS.md lines 378-389 with Type B specifics: "produce a single markdown file at {{OUT_PATH}} matching the {{ARTIFACT}} schema. For INTERNAL-DECK / PROPOSAL-DECK: includes Marp frontmatter (marp: true, footer: '...'), Cover slide literal watermark, 7-9 slide sequence (Sequoia/YC variant). For EXEC-SUMMARY: 5-section narrative. For DECISION-MEMO: ADR 4-section."

2. `<required_reading>` — list relevant workstream artifacts per {{ARTIFACT}} (Type B reads more workstreams than Type A — INTERNAL-DECK reads BMC + GTM + ROADMAP + RISK + TECH-ARCH + market-sizing.md from DISCOVER + OPERATIONS for Team slide).

3. `<business_context_contract>` — per Phase 5 D-13/D-14.

4. **`<banned_vocabulary>` block** — per PATTERNS.md lines 414-426 verbatim:
```markdown
<banned_vocabulary>
The following words/phrases are FORBIDDEN in your output (Pitfall #10 + C-D03):
English: leverage / synergize / transform / holistic / delve / groundbreaking /
best-in-class / seamless / cutting-edge / revolutionary / game-changing / landscape /
unlock / empower / robust / innovative.
Korean (when language=ko): 혁신적인 / 차별화된 / 게임체인저 / 패러다임 시프트 / 시너지 /
활용 / 최적화 / 글로벌 스탠더드.

If you write any banned word, REWRITE the sentence with concrete language.
The voice-fit.cjs post-check will fire if density > 2/page; the workflow then
issues a 1-shot regenerate request with banned-word feedback.
</banned_vocabulary>
```

5. **`<concreteness_rule>` block** — per PATTERNS.md lines 429-439 verbatim:
```markdown
<concreteness_rule>
EVERY claim must answer ONE of: "compared to what?" / "by how much?" / "when?".

GOOD: "We reduce 15-person legal review cycles from 3 weeks to 4 days [VERIFIED:internal-pilot-2026-04|2026-04-25]."
BAD: "We deliver innovative solutions" (no comparison, no number, no time).

The 4 hand-written exemplars below ship inline as your style anchor (per C-D04 — must NOT be LLM-generated).

### INVESTOR-IR style (1)
[페이앱 v2 reduces user onboarding from 12 minutes (industry baseline per Stripe Atlas 2025 benchmark) to 90 seconds — 87% reduction. Cohort 2026-Q2 (n=4,300 users) showed 91-day retention of 47% vs control 23% (p < 0.01). [VERIFIED:internal-pilot-cohort-2026-Q2-analysis-2026-04-25|2026-04-25]]

### EXEC-SUMMARY style (1)
[We are recommending the Vendor X migration based on 3 data points: (a) Q1-2026 pilot showed 23% reduction in MTTR vs incumbent (n=14 incidents); (b) annual cost differential is $147K savings at current scale [SOURCE: 2026 vendor quote 2026-03-15]; (c) 9 of 11 engineering team members preferred Vendor X interface in blind 2-week trial [VERIFIED:internal-trial-2026-Q1].]

### DECISION-MEMO style (1)
[We will adopt Vendor X over Vendor Y because (1) total cost of ownership over 3 years is $441K vs $580K (24% lower); (2) Vendor X SLA covers Korea region without latency surcharge per 2026-Q1 RFP response; (3) Korean PIPA compliance attested via ISO/IEC 27018 + Korea ISMS-P certification (Vendor Y has PIPA pending until Q4-2026).]

### Korean exemplar (격식체 + 구체 수치 + 출처)
[페이앱은 2026년 2분기에 91일 유지율을 47%까지 개선했습니다 (대조군 23%, p < 0.01, n=4,300명). 한국 핀테크 시장 평균 91일 유지율은 27% 수준이며 [출처: 한국핀테크산업협회 2025 연간보고서, p. 47], 페이앱은 1.7배의 차이를 보였습니다.]
</concreteness_rule>
```

6. **`<korean_honorific_rule>` block** — per PATTERNS.md lines 443-452 verbatim:
```markdown
<korean_honorific_rule>
When language=ko AND audience.confidentiality in {partner, public, external}:
- Use 격식체 (formal endings: -습니다 / -ㅂ니다) ONLY.
- 반말 / 구어체 BANNED: -야 / -지 / -라구요 / -거든요 / -는데요.
- Korean 투자자/임원 culture: emphasize founder reliability + long-term
  relationship signals, NOT short-term ROI.
- "Trust is built before terms are discussed" — vision + team stability +
  long-term partnership signals.
</korean_honorific_rule>
```

7. `<provenance_discipline>` — preserve provenance tags from source artifacts.

8. `<process>` block — 6-step per PATTERNS.md lines 455-461:
   - Step 1: Read business_context, OBJECTIVES, workstream artifacts.
   - Step 2: Determine watermark_text from `audience.confidentiality` (B-D02 mapping — embed the WATERMARKS_EN/WATERMARKS_KO map from Plan 04 export.cjs in agent prompt for Type B Marp deck path).
   - Step 3: Compose Marp frontmatter + literal Cover slide watermark (DECK artifacts only — exec-summary/decision-memo skip).
   - Step 4: Generate slides per D-D01 7-9 sequence (DECK) OR narrative section per D-D02 (EXEC/DECISION).
   - Step 5: SELF-CHECK against `<banned_vocabulary>` + `<concreteness_rule>` + `<korean_honorific_rule>` (when applicable).
   - Step 6: Write to `{{OUT_PATH}}` via Write tool.

**Sub-task B — 4 Type B templates:**

`brief/templates/deliver/type-b/internal-deck.md` — per RESEARCH.md Code Example 3 lines 1383-1465 verbatim:
```markdown
---
audience.type: internal
audience.confidentiality: confidential
voice.tone: formal
voice.perspective: first-person-plural
business_context.model: {{business_model}}
business_context.region: {{region}}
voice.languages: {{languages}}
deliverable: internal-deck
generated_by: brief-deliver-type-b
generated_at: {{ISO-timestamp}}
marp: true
theme: default
paginate: true
footer: '{{watermark_text}}'
---

<!-- Slide 1: Cover -->
# {{project_title}}

> **{{watermark_text}}**
>
> Generated {{date}} by BRIEF

---

<!-- Slide 2: Problem (BMC Customer Segments + Customer Jobs) -->
## Problem

<!-- INSERT: ## Customer Segments + ## Customer Jobs -->

---

<!-- Slide 3: Solution (BMC Value Proposition + Type A PRODUCT-BRIEF Core Value) -->
## Solution

<!-- INSERT: ## Value Proposition -->

---

<!-- Slide 4: Market (DISCOVER market-sizing — provenance tags MUST be preserved) -->
## Market

<!-- INSERT: market-sizing.md (provenance tags preserved) -->

---

<!-- Slide 5: Strategy (BMC Key Activities + GTM Channels) — INTERNAL only -->
## Strategy

<!-- INSERT: ## Key Activities + ## Channels -->

---

<!-- Slide 6: Roadmap (ROADMAP workstream phases) -->
## Roadmap

<!-- INSERT: ## Phased Roadmap -->

---

<!-- Slide 7: Ask (Internal — executive decision request) -->
## Ask

<!-- INSERT: executive_decision_ask -->

---

<!-- Optional Slide 8: Team (OPERATIONS Team — INTERNAL only) -->
## Team

<!-- INSERT: ## Team -->

---

<!-- Optional Slide 9: Appendix (FINANCIAL summary — INTERNAL only) -->
## Appendix

<!-- INSERT: ## Financial Summary -->
```

`brief/templates/deliver/type-b/proposal-deck.md` — Same shape as internal-deck.md but:
- frontmatter: `audience.confidentiality: partner`, `theme: partner`, `footer: '{{watermark_text}}'` (which fills to "Partner-only — Do not redistribute" per Plan 04)
- Slide 5: `## Traction` (NOT Strategy) — content from BMC + GTM but EXCLUDES Risk-related content (partner-safe per CONTEXT.md D-D01). Insert: `<!-- INSERT: ## Channels + ## Acquisition Strategy -->` (no Key Activities — that's competitive intel).
- Slide 7: `## Ask` adapted for "partnership proposal / next steps" instead of "executive decision request".
- Slide 8 (Team): redacted note — `> Team details available upon request post-NDA.` instead of literal team listing.
- NO Slide 9 Appendix (FINANCIAL excluded for partner audience per CONTEXT.md D-D01).

`brief/templates/deliver/type-b/exec-summary.md` — NO Marp directives:
```markdown
---
audience.type: internal
audience.confidentiality: internal
voice.tone: formal
voice.perspective: first-person-plural
business_context.model: {{business_model}}
business_context.region: {{region}}
voice.languages: {{languages}}
deliverable: exec-summary
generated_by: brief-deliver-type-b
generated_at: {{ISO-timestamp}}
---

# EXEC-SUMMARY — {{project_title}}

> 1-2 page narrative summary. NO Marp render — markdown only.

## Context

<!-- INSERT: context (1 paragraph from OBJECTIVES + business_context) -->

## Problem

<!-- INSERT: problem (1-2 paragraphs from BMC ## Customer Jobs + ## Customer Segments) -->

## Recommendation

<!-- INSERT: recommendation (3-5 bullets from PRODUCT-BRIEF + ROADMAP) -->

## Risks

<!-- INSERT: ## Critical Risks -->

## Ask

<!-- INSERT: ask (1 paragraph + clear decision request) -->

---

> Generated by BRIEF Type B exec-summary agent. Hand to executive readers as
> a 1-2 page decision-support document. Does NOT render to PPTX (markdown only).
```

`brief/templates/deliver/type-b/decision-memo.md` — NO Marp directives:
```markdown
---
audience.type: internal
audience.confidentiality: internal
voice.tone: formal
voice.perspective: first-person-plural
business_context.model: {{business_model}}
business_context.region: {{region}}
voice.languages: {{languages}}
deliverable: decision-memo
generated_by: brief-deliver-type-b
generated_at: {{ISO-timestamp}}
---

# DECISION-MEMO — {{project_title}}

> ADR variant. 1-2 page decision record. NO Marp render — markdown only.

## Context

<!-- INSERT: context (1 paragraph) -->

## Decision

<!-- INSERT: decision (1-2 sentences in form: "We will [verb] [object] because [reason]") -->

## Alternatives Considered

<!-- INSERT: alternatives (3+ rejected options + rationale) -->

## Consequences

<!-- INSERT: consequences (positive + negative + neutral) -->

---

> Generated by BRIEF Type B decision-memo agent. ADR (Architecture Decision Record)
> variant adapted for business decisions. Records the decision + path-not-taken for
> future audit and onboarding context.
```

**Sub-task C — 3 Marp CSS themes:**

`brief/templates/deliver/marp-themes/default.css` (~80-100 LOC):
```css
/* @theme brief-default
 * Marp theme for BRIEF INTERNAL deck — neutral professional palette.
 * Brand colors variabilized at top.
 */

:root {
  --brief-primary: #1f2937;       /* slate-800 — neutral professional */
  --brief-accent: #3b82f6;        /* blue-500 — links + emphasis */
  --brief-text: #111827;          /* slate-900 */
  --brief-muted: #6b7280;         /* gray-500 */
  --brief-bg: #ffffff;
  --brief-warning: #dc2626;       /* red-600 — confidentiality banner */
}

@import 'default';

section {
  background: var(--brief-bg);
  color: var(--brief-text);
  font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
  font-size: 28px;
  padding: 60px;
}

h1, h2 { color: var(--brief-primary); font-weight: 700; }
h2 { font-size: 36px; border-bottom: 3px solid var(--brief-accent); padding-bottom: 12px; }
blockquote { color: var(--brief-warning); font-weight: 600; }
footer { color: var(--brief-muted); font-size: 14px; }
```

`brief/templates/deliver/marp-themes/partner.css` (~80-100 LOC):
```css
/* @theme brief-partner
 * Marp theme for BRIEF PROPOSAL deck — outward-facing partner deck palette.
 * More polished, brand-forward; suitable for external partner audiences.
 */

:root {
  --brief-primary: #0f766e;       /* teal-700 — outward brand */
  --brief-accent: #14b8a6;        /* teal-500 */
  --brief-text: #042f2e;          /* deep teal text */
  --brief-muted: #64748b;
  --brief-bg: #f8fafc;            /* slate-50 — subtle warmth */
  --brief-warning: #ea580c;       /* orange-600 — partner banner */
}

@import 'default';

section {
  background: var(--brief-bg);
  color: var(--brief-text);
  font-family: 'Inter', sans-serif;
  font-size: 28px;
  padding: 60px;
}

h1, h2 { color: var(--brief-primary); font-weight: 700; }
h2 { font-size: 36px; border-bottom: 3px solid var(--brief-accent); padding-bottom: 12px; }
blockquote { color: var(--brief-warning); font-weight: 600; font-style: normal; }
footer { color: var(--brief-muted); font-size: 14px; text-align: center; }
```

`brief/templates/deliver/marp-themes/confidential.css` (~50-70 LOC) — overlay/extension theme:
```css
/* @theme brief-confidential
 * CONFIDENTIAL banner overlay — applied IN ADDITION to default.css for confidential decks.
 * Highlights confidentiality marking on every slide (footer + cover).
 */

:root {
  --brief-warning: #dc2626;
  --brief-warning-bg: #fef2f2;
}

@import 'default';

footer {
  background: var(--brief-warning-bg);
  color: var(--brief-warning);
  font-weight: 700;
  border-top: 3px solid var(--brief-warning);
  padding: 8px;
}

section.cover blockquote {
  background: var(--brief-warning);
  color: white;
  padding: 16px 24px;
  border-radius: 4px;
  display: inline-block;
}
```

Run `node --test tests/brief-deliver-type-b-templates.test.cjs tests/brief-deliver-ko-en-branching.test.cjs` and assert all 19+ tests GREEN.

Plan 01 deliver.cjs tests must STILL pass (deliver.cjs frontmatter composition logic for voice.languages was implemented in Plan 01 and is exercised by the ko/en tests in Plan 06).

Commit message: `feat(08-06): implement Type B agent + 4 templates (Marp + non-Marp) + 3 CSS themes (DLV-05..07,09)`.
  </action>
  <verify>
    <automated>node --test tests/brief-deliver-type-b-templates.test.cjs tests/brief-deliver-ko-en-branching.test.cjs tests/brief-deliver-type-a.test.cjs 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - File `agents/brief-deliver-type-b.md` exists with `name: brief-deliver-type-b` + `tools: Read, Grep, Glob, Write` + `color: green`
    - Agent body contains `<banned_vocabulary>` block with all 16 EN words: `grep -cE "leverage|synergize|transform|holistic|delve|groundbreaking|best-in-class|seamless|cutting-edge|revolutionary|game-changing|landscape|unlock|empower|robust|innovative" agents/brief-deliver-type-b.md` returns ≥ 16 (count of word matches in regex enumeration)
    - Agent body contains all 8 KO banned words: `grep -cE "혁신적인|차별화된|게임체인저|패러다임 시프트|시너지|활용|최적화|글로벌 스탠더드" agents/brief-deliver-type-b.md` returns ≥ 8
    - Agent body contains `<concreteness_rule>` block: `grep -c "concreteness_rule" agents/brief-deliver-type-b.md` returns ≥ 2
    - Agent body contains `<korean_honorific_rule>` block: `grep -c "korean_honorific_rule" agents/brief-deliver-type-b.md` returns ≥ 2
    - Agent body contains all 4 hand-written exemplar headings: `grep -cE "^### (INVESTOR-IR|EXEC-SUMMARY|DECISION-MEMO|Korean)" agents/brief-deliver-type-b.md` returns 4
    - File `brief/templates/deliver/type-b/internal-deck.md` exists with `marp: true` + `paginate: true` + `footer:` + Cover slide literal watermark `> **{{watermark_text}}**`
    - internal-deck.md has 7-9 slides (count `^---$` separators between Cover and end): `grep -c "^---$" brief/templates/deliver/type-b/internal-deck.md` returns ≥ 7
    - File `brief/templates/deliver/type-b/proposal-deck.md` exists with `marp: true` + `theme: partner` + slide labeled `Traction` (NOT Strategy): `grep -c "## Traction" brief/templates/deliver/type-b/proposal-deck.md` returns ≥ 1; `grep -c "## Strategy" brief/templates/deliver/type-b/proposal-deck.md` returns 0
    - File `brief/templates/deliver/type-b/exec-summary.md` exists with NO `marp: true` + 5 sections: `grep -cE "^## (Context|Problem|Recommendation|Risks|Ask)$" brief/templates/deliver/type-b/exec-summary.md` returns 5
    - exec-summary.md does NOT have `marp: true`: `grep -c "marp: true" brief/templates/deliver/type-b/exec-summary.md` returns 0
    - File `brief/templates/deliver/type-b/decision-memo.md` exists with NO `marp: true` + 4 ADR sections: `grep -cE "^## (Context|Decision|Alternatives Considered|Consequences)$" brief/templates/deliver/type-b/decision-memo.md` returns 4
    - 3 Marp CSS theme files exist with `section {` declarations: `grep -l "section {" brief/templates/deliver/marp-themes/*.css | wc -l` returns 3
    - 19+ Wave 0 tests pass: `node --test tests/brief-deliver-type-b-templates.test.cjs tests/brief-deliver-ko-en-branching.test.cjs` exits 0
    - Plan 01 deliver.cjs tests still pass: `node --test tests/brief-deliver-type-a.test.cjs` exits 0
    - A1 zero-runtime-deps preserved
    - `git log -1 --format=%s` matches `^feat\\(08-06\\): implement Type B`
  </acceptance_criteria>
  <done>1 agent + 4 templates + 3 CSS themes committed; 19 Wave 0 RED→GREEN; ko/en branching works; no Marp dep added</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Workstream artifact body → Type B agent prompt | User-controlled markdown read for synthesis |
| Type B template `{{watermark_text}}` placeholder → Plan 08 workflow fill | Plan 08 calls watermarkFor(confidentiality, language) — closed-vocabulary mapping |
| Marp deck source markdown → npx render | Plan 04 export.cjs guards with detectBrowser + no --allow-local-files |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-06-01 | Information Disclosure | proposal-deck.md template includes confidential workstream content (FINANCIAL Appendix, Strategy Key Activities) | mitigate | Template explicitly EXCLUDES Slide 9 Appendix (no FINANCIAL); Slide 5 is "Traction" (NOT Strategy → Key Activities); Slide 8 Team is redacted note instead of literal team listing. Plan 03 leakage-diff.cjs catches if user edits proposal-deck.md to copy from internal-deck.md |
| T-08-06-02 | Repudiation (reputational) | Type B agent generates AI slop ("leverage", "synergize") in deck text | mitigate | Inline `<banned_vocabulary>` ban-list in agent prompt; Plan 04 export.cjs voice-fit.cjs post-check at template-fill time; 1-shot regenerate if density > 2/page |
| T-08-06-03 | Repudiation (reputational) | Korean honorific violation in proposal-deck.md (반말 in external Korean artifact) | mitigate | Inline `<korean_honorific_rule>` block; Plan 02 voice-fit.cjs HONORIFIC_VIOLATION_KO regex check; conditional firing only when audience.confidentiality external + region: kr |
| T-08-06-04 | Tampering | User edits Marp deck source to remove Cover slide watermark | accept | Layer 2 watermark relies on agent + template generating literal first-slide content. CC-03 hook (Plan 07) checks frontmatter (NOT body) — body removal is documented user-responsibility breach. Phase 9 HRD-04 pilot data may inform stricter v1.x check |
| T-08-06-05 | Information Disclosure | Marp `--allow-local-files` would let deck embed `/etc/passwd` via image markdown | mitigate | Plan 04 export.cjs DOES NOT pass --allow-local-files. Type B template SHOULD NOT include `file://` or local image embeds; agent prompt anti-pattern explicit |
| T-08-06-06 | Tampering | Marp CSS theme `@import 'default'` resolves to malicious CSS in user environment | accept | `@import 'default'` is Marp's internal theme reference (built-in). User would have to override Marp's internal theme path, which is outside BRIEF's threat model. Pinned `npx --yes @marp-team/marp-cli@4.3.1` reduces version-drift attack surface |
| T-08-06-07 | Information Disclosure | EXEC-SUMMARY / DECISION-MEMO `audience.confidentiality: internal` shipped to external by user | accept | EXEC-SUMMARY and DECISION-MEMO are markdown-only (no Marp render = no `/brief-export` mandatory checkpoint). User is responsible for not emailing internal markdown to external. Documentation in CLAUDE.md / `/brief-help` advises Type A/EXEC-SUMMARY/DECISION-MEMO are NOT externally safe by default |
</threat_model>

<verification>
- 1 Type B agent + 4 templates + 3 CSS themes committed
- Type B agent embeds banned-words ban-list (16 EN + 8 KO) verbatim
- Type B agent embeds 4 hand-written exemplars (NOT LLM-generated)
- Type B agent embeds Korean honorific rule with 5 반말 endings + conditional firing
- internal-deck.md has 7-9 slides + Cover literal watermark
- proposal-deck.md has Traction (NOT Strategy) + redacted Team + no Appendix
- exec-summary.md has 5 narrative sections (no Marp)
- decision-memo.md has 4 ADR sections (no Marp)
- 3 Marp CSS themes with brand color variables
- ko/en branching works per D-D03
- 19 Wave 0 RED→GREEN
- A1 zero-runtime-deps preserved
- No `--allow-local-files` in Plan 04 (cross-plan invariant)
</verification>

<success_criteria>
- 1 parameterized Type B agent (Phase 5 D-01 byte-identity) — NOT 4 separate agents
- 4 production templates: 2 Marp (deck) + 2 markdown-only (exec-summary, decision-memo)
- 3 Marp CSS themes (default for INTERNAL / partner for PROPOSAL / confidential overlay)
- Inline ban-list + concreteness rule + Korean honorific rule (NOT new gates per CONTEXT.md C-D03/C-D04/D-D04 reject)
- Layer 2 watermark = Marp footer directive + literal Cover slide content (survives copy-paste)
- ko/en branching: region: kr → ko 단일; --en opt-in adds en sibling
- Partner-safe content: Strategy → Traction, Team redacted, no FINANCIAL Appendix
- 19 Wave 0 RED→GREEN
</success_criteria>

<output>
After completion, create `.planning/phases/08-deliver-type-a-type-b-audience-enforcement-marp/08-06-SUMMARY.md` documenting:
1. Type B agent role + 6-step process + inline ban-list/concreteness/honorific contract
2. 4 templates inventory: 2 Marp (deck) + 2 markdown-only
3. internal-deck vs proposal-deck differences (Strategy/Traction, Team redaction, Appendix exclusion)
4. 3 Marp CSS themes brand color palette
5. ko/en branching test count + actual pass count
6. Files Plan 08 (workflows wire watermarkFor + askUser harness) consume from this plan
</output>
</content>
</invoke>