---
phase: 08-deliver-type-a-type-b-audience-enforcement-marp
plan: 05
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - agents/brief-deliver-type-a.md
  - brief/templates/deliver/type-a/product-brief.md
  - brief/templates/deliver/type-a/service-policy.md
  - brief/templates/deliver/type-a/high-level-spec.md
  - brief/templates/deliver/type-a/feature-map.md
  - tests/brief-deliver-type-a-templates.test.cjs
autonomous: true
requirements: [DLV-01, DLV-02, DLV-03, DLV-04]

must_haves:
  truths:
    - "agents/brief-deliver-type-a.md is parameterized via {{ARTIFACT}} (∈ product-brief / service-policy / high-level-spec / feature-map) — single agent file, NOT 4 separate agents per Phase 5 D-01 byte-identity"
    - "Type A agent inherits SAME provenance discipline as brief-domain-researcher (every quantitative claim carries [VERIFIED:url|date] / [ASSUMED:reasoning] / [FOUNDER-INPUT])"
    - "Type A agent NEVER hallucinates workstream content — missing source workstream → emit `> ⚠️ Placeholder — {workstream} not yet completed.` + warning to stderr"
    - "Type A agent NEVER uses pass/fail or compliance-theater vocabulary in synthesized content (Phase 4·5·7 ban-list inheritance)"
    - "Type A templates contain `<!-- INSERT: ## {section_name} -->` placeholders that match deliver.cjs SYNTHESIS_MAP source.sections values byte-identically"
    - "service-policy.md template has `<!--BEGIN business_model: b2b-->...<!--END business_model: b2b-->` AND `<!--BEGIN business_model: b2c-->...<!--END business_model: b2c-->` conditional prose blocks (Phase 7 D-14 byte-identity) populated with B2B SLA tiers + B2C refund/support templates"
    - "feature-map.md template defaults to Mermaid `mindmap` block (preferred); ASCII tree as fallback comment in template"
    - "Each template includes 5 mandatory frontmatter fields ({{voice.tone}}, {{voice.perspective}}, {{business_model}}, {{region}}) + voice.languages placeholder + deliverable: {{artifact-key}} + generated_by: brief-deliver-type-a + generated_at: {{ISO-timestamp}}"
  artifacts:
    - path: "agents/brief-deliver-type-a.md"
      provides: "Parameterized Type A synthesis agent (single file, {{ARTIFACT}} parameter)"
      min_lines: 200
      contains: "{{ARTIFACT}}"
    - path: "brief/templates/deliver/type-a/product-brief.md"
      provides: "PRODUCT-BRIEF template — frontmatter + section markers for Customer Segments + Value Proposition + Personas + Immutable Intent"
      min_lines: 60
      contains: "<!-- INSERT: ## Immutable Intent -->"
    - path: "brief/templates/deliver/type-a/service-policy.md"
      provides: "SERVICE-POLICY template with B2B/B2C conditional prose blocks (Phase 7 D-14 byte-identity)"
      min_lines: 100
      contains: "<!--BEGIN business_model: b2b-->"
    - path: "brief/templates/deliver/type-a/high-level-spec.md"
      provides: "HIGH-LEVEL-SPEC template — Functional Scope + Priority + Dependencies + Critical Risks sections"
      min_lines: 80
      contains: "<!-- INSERT: ## Component Map -->"
    - path: "brief/templates/deliver/type-a/feature-map.md"
      provides: "FEATURE-MAP template with Mermaid mindmap block + ASCII tree fallback"
      min_lines: 50
      contains: "mindmap"
    - path: "tests/brief-deliver-type-a-templates.test.cjs"
      provides: "Wave 0 fixture asserting templates exist + contain frontmatter + section markers + conditional prose markers + Mermaid block"
      contains: "templates/deliver/type-a"
  key_links:
    - from: "brief/templates/deliver/type-a/*.md"
      to: "brief/bin/lib/deliver.cjs SYNTHESIS_MAP"
      via: "<!-- INSERT: ## {section_name} --> placeholders MUST match SYNTHESIS_MAP entries byte-identically — fully spec-locked Plan 01 contract"
      pattern: "<!-- INSERT:"
    - from: "agents/brief-deliver-type-a.md"
      to: "brief/bin/lib/context-inject.cjs buildBusinessContext"
      via: "Agent prompt receives <business_context> block at spawn-time (Phase 5 D-13/D-14 byte-identity)"
      pattern: "business_context"
---

<objective>
Ship `agents/brief-deliver-type-a.md` (parameterized agent for Type A synthesis) + 4 Type A markdown templates under `brief/templates/deliver/type-a/`. Plus 1 Wave 0 test asserting templates exist + match the deliver.cjs SYNTHESIS_MAP contract from Plan 01.

Purpose: With Plan 01's deliver.cjs lib in place, Plan 05 layers the LLM agent + the actual templates that deliver.cjs reads + fills. Plan 01 used minimal stub templates inside the test setup; Plan 05 ships the real production templates that downstream `/brief-deliver --type-a` (Plan 08) will use.

The Type A agent is parameterized via `{{ARTIFACT}}` — ONE agent file handles all 4 artifacts (Phase 5 D-01 brief-domain-researcher pattern byte-identity). This stays under the surface cap (1 NET agent for Type A; Plan 06 ships 1 NET agent for Type B = 2 total NET agents for Phase 8).

Output: 1 agent file + 4 production templates + 1 Wave 0 test. After Plan 05, deliver.cjs is fully wired with real templates and the Type A agent is ready for Plan 08 to invoke from `commands/brief/deliver.md`.
</objective>

<execution_context>
@~/.claude/brief/workflows/execute-plan.md
@~/.claude/brief/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-deliver-type-a-type-b-audience-enforcement-marp/08-CONTEXT.md
@.planning/phases/08-deliver-type-a-type-b-audience-enforcement-marp/08-RESEARCH.md
@.planning/phases/08-deliver-type-a-type-b-audience-enforcement-marp/08-PATTERNS.md
@.planning/phases/08-deliver-type-a-type-b-audience-enforcement-marp/08-01-PLAN.md
@agents/brief-domain-researcher.md
@brief/workstreams/business-model-canvas/spec.yaml

<interfaces>
<!-- Plan 01 deliver.cjs SYNTHESIS_MAP contract — templates MUST match these section markers verbatim. -->

From Plan 01 brief/bin/lib/deliver.cjs SYNTHESIS_MAP (final spec):
```javascript
const SYNTHESIS_MAP = {
  'product-brief': {
    sources: [
      { artifact: 'workstreams/business-model-canvas/canvas.md',
        sections: ['## Customer Segments', '## Value Proposition'] },
      { artifact: 'workstreams/go-to-market/go-to-market.md',
        sections: ['## Personas'] },
    ],
    objectivesSections: ['## Immutable Intent'],
    template: 'brief/templates/deliver/type-a/product-brief.md',
    conditionalProse: false,
  },
  'service-policy': {
    sources: [
      { artifact: 'workstreams/operations/operations.md',
        sections: ['## Process', '## Tools'] },
      { artifact: 'workstreams/compliance/compliance.md',
        sections: ['## Documented obligations addressed:'] },
    ],
    objectivesSections: [],
    template: 'brief/templates/deliver/type-a/service-policy.md',
    conditionalProse: true,
  },
  'high-level-spec': {
    sources: [
      { artifact: 'workstreams/tech-arch/tech-arch.md',
        sections: ['## Component Map'] },
      { artifact: 'workstreams/roadmap/roadmap.md',
        sections: ['## Phased Roadmap'] },
      { artifact: 'workstreams/risk/risk.md',
        sections: ['## Critical Risks'] },
    ],
    objectivesSections: ['## Immutable Intent'],
    template: 'brief/templates/deliver/type-a/high-level-spec.md',
    conditionalProse: false,
  },
  'feature-map': {
    sources: [
      { artifact: 'workstreams/tech-arch/tech-arch.md',
        sections: ['## Component Map'] },
      { artifact: 'workstreams/business-model-canvas/canvas.md',
        sections: ['## Value Proposition'] },
    ],
    objectivesSections: [],
    template: 'brief/templates/deliver/type-a/feature-map.md',
    conditionalProse: false,
  },
};
```

Each template MUST contain `<!-- INSERT: {section_name} -->` placeholders matching the `sections` arrays byte-identically. deliver.cjs `body.replace(placeholder, content)` does literal substring replace.

From agents/brief-domain-researcher.md (Phase 5 D-01 — parameterized agent template to mirror):
- Frontmatter shape: name + description + tools + color
- {{CATEGORY}} parameterization at Task-spawn time
- Required-reading section
- business_context_contract section
- Anti-patterns + provenance discipline + process

The Type A agent file replaces `{{CATEGORY}}` with `{{ARTIFACT}}` and adapts role+process for synthesis (NOT research).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — Write RED test for Type A templates structural assertions</name>
  <files>tests/brief-deliver-type-a-templates.test.cjs</files>
  <read_first>
    - .planning/phases/08-deliver-type-a-type-b-audience-enforcement-marp/08-PATTERNS.md (lines 826-872 — type-a templates analog with frontmatter pattern + section markers + B2B/B2C conditional prose pattern)
    - .planning/phases/08-deliver-type-a-type-b-audience-enforcement-marp/08-01-PLAN.md (Task 2 SYNTHESIS_MAP — section-marker contract)
    - tests/brief-deliver-type-a.test.cjs (Plan 01 Task 1 — DO NOT touch, but understand contract)
    - tests/brief-audience-roundtrip.test.cjs (existing node:test pattern reference)
  </read_first>
  <behavior>
    - Test 1 (RED): `fs.existsSync('brief/templates/deliver/type-a/product-brief.md')` returns true.
    - Test 2 (RED): product-brief.md contains the literal strings `<!-- INSERT: ## Customer Segments -->`, `<!-- INSERT: ## Value Proposition -->`, `<!-- INSERT: ## Personas -->`, `<!-- INSERT: ## Immutable Intent -->` (all 4 placeholders matching SYNTHESIS_MAP['product-brief'].sources + objectivesSections).
    - Test 3 (RED): product-brief.md frontmatter contains all 5 mandatory keys + voice.languages + deliverable + generated_by + generated_at placeholders.
    - Test 4 (RED): service-policy.md contains `<!-- INSERT: ## Process -->`, `<!-- INSERT: ## Tools -->`, `<!-- INSERT: ## Documented obligations addressed: -->`.
    - Test 5 (RED): service-policy.md contains conditional prose blocks: `<!--BEGIN business_model: b2b-->`...`<!--END business_model: b2b-->` AND `<!--BEGIN business_model: b2c-->`...`<!--END business_model: b2c-->`.
    - Test 6 (RED): service-policy.md B2B block contains keywords ['SLA tiers', 'enterprise support', 'data processing', 'contract terms']; B2C block contains keywords ['refund policy', 'customer support hours', 'channel coverage', 'community guidelines'].
    - Test 7 (RED): high-level-spec.md contains `<!-- INSERT: ## Component Map -->`, `<!-- INSERT: ## Phased Roadmap -->`, `<!-- INSERT: ## Critical Risks -->`, `<!-- INSERT: ## Immutable Intent -->`.
    - Test 8 (RED): feature-map.md contains a Mermaid `mindmap` code block (verifiable via `\`\`\`mermaid\\nmindmap`); ASCII tree alternative is mentioned in template comment.
    - Test 9 (RED): All 4 templates have a YAML frontmatter block bounded by `---` lines AND contain `deliverable: {{artifact-key}}` placeholder + `voice.languages: {{languages}}` placeholder + `generated_by: brief-deliver-type-a`.
    - Test 10 (RED): `fs.existsSync('agents/brief-deliver-type-a.md')` returns true; agent file frontmatter contains `name: brief-deliver-type-a` + `tools: Read, Grep, Glob, Write` + `color: green`.
    - Test 11 (RED): agent file body contains `{{ARTIFACT}}` placeholder ≥ 5 times (parameter is referenced multiple times in role + process).
    - Test 12 (RED): agent file contains anti-pattern: NEVER hallucinate / NEVER use pass/fail / NEVER call as PostToolUse hook.
    - All 12 tests fail (RED) — files do not exist yet.
  </behavior>
  <action>
Create `tests/brief-deliver-type-a-templates.test.cjs` using node:test. 12+ tests covering all behaviors. Use `fs.readFileSync(...)` directly on the file paths (NO require — these are markdown files, not modules). Use `try { fs.statSync(...); } catch { /* RED */ }` pattern for the existsSync checks.

Commit message: `test(08-05): add failing Wave 0 tests for Type A templates + agent (DLV-01..04 templates)`. Run `node --test tests/brief-deliver-type-a-templates.test.cjs` and assert non-zero exit (RED — files missing).
  </action>
  <verify>
    <automated>node --test tests/brief-deliver-type-a-templates.test.cjs 2>&1 | grep -E "fail|ENOENT|no such file" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/brief-deliver-type-a-templates.test.cjs` exists with ≥ 12 `test(` invocations
    - `node --test tests/brief-deliver-type-a-templates.test.cjs` exits non-zero (Wave 0 RED — file-not-found errors)
    - `git log -1 --format=%s` matches `^test\\(08-05\\): add failing Wave 0 tests for Type A templates`
  </acceptance_criteria>
  <done>1 RED test file committed; node --test exits non-zero with file-not-found errors</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — Implement agents/brief-deliver-type-a.md + 4 production Type A templates</name>
  <files>agents/brief-deliver-type-a.md, brief/templates/deliver/type-a/product-brief.md, brief/templates/deliver/type-a/service-policy.md, brief/templates/deliver/type-a/high-level-spec.md, brief/templates/deliver/type-a/feature-map.md</files>
  <read_first>
    - .planning/phases/08-deliver-type-a-type-b-audience-enforcement-marp/08-PATTERNS.md (lines 360-398 — Type A agent analog from brief-domain-researcher; lines 826-872 — type-a template patterns + B2B/B2C example block)
    - agents/brief-domain-researcher.md (read FULL file lines 1-end — this is the byte-identity template; replace {{CATEGORY}} with {{ARTIFACT}}, adapt role for synthesis NOT research)
    - .planning/phases/08-deliver-type-a-type-b-audience-enforcement-marp/08-RESEARCH.md (Pattern 3 lines 463-478 — Type A synthesis mapping; lines 113 Summary — agent does narrative LLM polish over deterministic deliver.cjs synthesis)
    - .planning/phases/08-deliver-type-a-type-b-audience-enforcement-marp/08-01-PLAN.md (SYNTHESIS_MAP contract — section markers MUST match byte-identically)
    - brief/workstreams/business-model-canvas/spec.yaml (workstream artifact pattern; mirror Phase 7 D-12 frontmatter shape for deliverables)
    - tests/brief-deliver-type-a-templates.test.cjs (Plan 05 Task 1 — implementation must satisfy)
  </read_first>
  <action>
**Sub-task A — agents/brief-deliver-type-a.md:**

Create the agent file mirroring `agents/brief-domain-researcher.md` byte-identity with `{{CATEGORY}}` → `{{ARTIFACT}}` substitution. Per PATTERNS.md lines 366-372:

```yaml
---
name: brief-deliver-type-a
description: Type A deliverable synthesis agent for BRIEF DELIVER phase. Auto-synthesizes 4 PRD-input markdown artifacts (PRODUCT-BRIEF / SERVICE-POLICY / HIGH-LEVEL-SPEC / FEATURE-MAP) from 9 workstream outputs + OBJECTIVES.md immutable intent. Parameterized at Task-spawn time by {{ARTIFACT}} + <business_context> block (from context-inject.cjs). Spawned by brief/workflows/deliver.md. Read-only — never mutates OBJECTIVES.md or workstream artifacts; Write tool used ONLY to emit the deliverable file at {{OUT_PATH}}.
tools: Read, Grep, Glob, Write
color: green
---
```

Body sections (mirror brief-domain-researcher.md verbatim with synthesis-specific adaptation):

1. `<role>` block per PATTERNS.md lines 378-389:
```markdown
<role>
You are the BRIEF TYPE A DELIVERABLE SYNTHESIZER. You answer one question:
"What should the {{ARTIFACT}} contain for this project, drawn from existing workstream outputs?"

You produce a single markdown file at {{OUT_PATH}} matching the {{ARTIFACT}} schema.
You NEVER hallucinate workstream content — if a source workstream is missing, emit
a placeholder section with `> ⚠️ Placeholder — {workstream} not yet completed.` and warn.
You NEVER use pass/fail or compliance-theater vocabulary in synthesized content.

Spawned by: brief/workflows/deliver.md (orchestrator-side --type-a path).
You are NOT auto-attached via PostToolUse / SubagentStop hooks (Phase 4 Anti-pattern #2).
</role>
```

2. `<required_reading>` block — list:
   - The relevant workstream artifacts per `{{ARTIFACT}}` (CITE the SYNTHESIS_MAP from deliver.cjs)
   - .planning/OBJECTIVES.md (for ## Immutable Intent)
   - brief/templates/deliver/type-a/{{artifact-key}}.md (template skeleton)
   - .planning/PROJECT.md (key decisions)

3. `<business_context_contract>` block — from brief-domain-researcher.md verbatim. The agent MUST receive `<business_context>` block injected by deliver.md workflow (Plan 08); this block contains `business_model`, `region`, `language`, `audienceDefaults`. Synthesis adapts content to match.

4. `<anti_patterns>` block:
   - DO NOT use words from the AUDIENCE/COMPLIANCE banlist (`compliant`, `passed`, `violation`, `failed`, `준수`, `통과`, `위반`, `실패`).
   - DO NOT use AI slop banlist (`leverage`, `synergize`, `transform`, `holistic`, `delve`, ...).
   - DO NOT fabricate workstream content — if source is missing, placeholder + warn.
   - DO NOT mutate workstream artifacts or OBJECTIVES.md (Read-only).
   - DO NOT use Write tool for any path other than `{{OUT_PATH}}`.

5. `<provenance_discipline>` block:
   - Every quantitative claim CARRIED OVER from a source artifact retains its provenance tag verbatim (`[VERIFIED:url|date]` / `[ASSUMED:reasoning]` / `[FOUNDER-INPUT]`).
   - DO NOT strip provenance tags during synthesis.
   - DO NOT add new quantitative claims that lack provenance.

6. `<process>` block — 5-step:
   - Step 1: Read `<business_context>` injected at spawn time. Note `business_model`, `region`, `language`.
   - Step 2: Read template at `brief/templates/deliver/type-a/{{artifact-key}}.md`.
   - Step 3: Read source workstream artifacts per SYNTHESIS_MAP[{{ARTIFACT}}].sources. For each, extract requested sections via heading match (`## {section_name}` to next same-level heading).
   - Step 4: Read OBJECTIVES.md sections per SYNTHESIS_MAP[{{ARTIFACT}}].objectivesSections. Read `## Immutable Intent` if listed.
   - Step 5: Fill template `<!-- INSERT: ## {section} -->` placeholders with extracted content. Apply applyConditionalProse(business_model) if SYNTHESIS_MAP[{{ARTIFACT}}].conditionalProse is true. Compose 5-mandatory-fields frontmatter + voice.languages + deliverable + generated_by + generated_at. Write atomically to {{OUT_PATH}}.

**Sub-task B — 4 Type A templates:**

`brief/templates/deliver/type-a/product-brief.md`:
```markdown
---
audience.type: internal
audience.confidentiality: internal
voice.tone: {{voice.tone}}
voice.perspective: {{voice.perspective}}
business_context.model: {{business_model}}
business_context.region: {{region}}
voice.languages: {{languages}}
deliverable: product-brief
generated_by: brief-deliver-type-a
generated_at: {{ISO-timestamp}}
---

# PRODUCT-BRIEF — {{project_title}}

## Product Vision

<!-- INSERT: ## Immutable Intent -->

## Core Value

<!-- INSERT: ## Value Proposition -->

## Target User

<!-- INSERT: ## Customer Segments -->

## User Personas

<!-- INSERT: ## Personas -->

---

> Generated by BRIEF Type A synthesis agent. This artifact is intended as the
> first input to PM PRD authoring. Review the synthesis for completeness;
> regenerate via `/brief-deliver --type-a` if source workstreams change.
```

`brief/templates/deliver/type-a/service-policy.md`:
```markdown
---
audience.type: internal
audience.confidentiality: internal
voice.tone: {{voice.tone}}
voice.perspective: {{voice.perspective}}
business_context.model: {{business_model}}
business_context.region: {{region}}
voice.languages: {{languages}}
deliverable: service-policy
generated_by: brief-deliver-type-a
generated_at: {{ISO-timestamp}}
---

# SERVICE-POLICY — {{project_title}}

## Operational Process

<!-- INSERT: ## Process -->

## Tools & Infrastructure

<!-- INSERT: ## Tools -->

## Compliance Posture

<!-- INSERT: ## Documented obligations addressed: -->

## Service Tiers

<!--BEGIN business_model: b2b-->
- **SLA tiers** (Standard / Premium / Enterprise): 99.9% / 99.95% / 99.99% uptime
- **Enterprise support**: dedicated CSM + 4-hour P1 response time
- **Data processing terms**: standard DPA + customer-specific addendums available
- **Contract terms**: Net-30 / Net-60 / Net-90 payment options; multi-year discount
<!--END business_model: b2b-->

<!--BEGIN business_model: b2c-->
- **Refund policy**: 30-day no-questions refund + auto-renewal cancellation any time
- **Customer support hours**: 09:00-18:00 KST (kr) / 24x7 chat support (others)
- **Channel coverage**: app stores (iOS + Android) + web + community Discord
- **Community guidelines**: see /community/guidelines (zero-tolerance harassment policy)
<!--END business_model: b2c-->

---

> Generated by BRIEF Type A synthesis agent. Service tier content is automatically
> selected by `business_model` in your OBJECTIVES.md. Edit conditional prose blocks
> in this artifact as needed; preserve `<!--BEGIN/END business_model: X-->` markers.
```

`brief/templates/deliver/type-a/high-level-spec.md`:
```markdown
---
audience.type: internal
audience.confidentiality: internal
voice.tone: {{voice.tone}}
voice.perspective: {{voice.perspective}}
business_context.model: {{business_model}}
business_context.region: {{region}}
voice.languages: {{languages}}
deliverable: high-level-spec
generated_by: brief-deliver-type-a
generated_at: {{ISO-timestamp}}
---

# HIGH-LEVEL-SPEC — {{project_title}}

> PRD table-of-contents level. Component-tier detail; NOT detailed design.

## Immutable Intent

<!-- INSERT: ## Immutable Intent -->

## Functional Scope

<!-- INSERT: ## Component Map -->

## Priority & Phasing

<!-- INSERT: ## Phased Roadmap -->

## Dependencies

> Inferred from Component Map + Phased Roadmap above. Cross-reference workstream artifacts.

## Critical Risks

<!-- INSERT: ## Critical Risks -->

---

> Generated by BRIEF Type A synthesis agent. Hand off to PM/Engineering as the
> "what" + "when" + "risks" trio for PRD authoring. Detailed design (data models,
> API contracts, UI specs) is the PRD author's responsibility — explicitly NOT
> in this artifact.
```

`brief/templates/deliver/type-a/feature-map.md`:
```markdown
---
audience.type: internal
audience.confidentiality: internal
voice.tone: {{voice.tone}}
voice.perspective: {{voice.perspective}}
business_context.model: {{business_model}}
business_context.region: {{region}}
voice.languages: {{languages}}
deliverable: feature-map
generated_by: brief-deliver-type-a
generated_at: {{ISO-timestamp}}
---

# FEATURE-MAP — {{project_title}}

> Visual feature tree. Mermaid `mindmap` is preferred; ASCII tree fallback comment
> below if Mermaid renderer is unavailable.

## Feature Tree (Mermaid)

```mermaid
mindmap
  root(({{project_title}}))
    Components
      <!-- INSERT: ## Component Map -->
    Value
      <!-- INSERT: ## Value Proposition -->
```

## ASCII Tree (Fallback)

> If your renderer does not support Mermaid, the synthesis agent populates this
> ASCII tree from the same Component Map + Value Proposition sections. Mermaid
> is strongly preferred for visual quality.

```
{{project_title}}
├── Components/
│   <!-- INSERT-ASCII: ## Component Map -->
└── Value/
    <!-- INSERT-ASCII: ## Value Proposition -->
```

---

> Generated by BRIEF Type A synthesis agent. Edit Mermaid block to refine
> hierarchy or grouping. ASCII tree is auto-generated; regenerate via
> `/brief-deliver --type-a` if Component Map changes.
```

NOTE: feature-map.md has TWO categories of placeholder: `<!-- INSERT: -->` (deliver.cjs fills this with extracted section content) and `<!-- INSERT-ASCII: -->` (deliver.cjs ALSO fills this; for v1, both placeholders receive identical content — Plan 05 ships both forms, agent handles ASCII conversion if it's running narrative polish). For Wave 0 GREEN test, both placeholders MUST be present.

Run `node --test tests/brief-deliver-type-a-templates.test.cjs` and assert all 12+ tests pass GREEN.

ALSO re-run Plan 01 deliver.cjs tests — they should still pass because deliver.cjs uses placeholders that match the new templates. If Plan 01 used minimal stub templates inside `before()` setup, those continue to work for Plan 01 tests; Plan 05 templates are production for `/brief-deliver` runtime.

Commit message: `feat(08-05): implement Type A agent + 4 templates with B2B/B2C conditional prose (DLV-01..04 narrative)`.
  </action>
  <verify>
    <automated>node --test tests/brief-deliver-type-a-templates.test.cjs tests/brief-deliver-type-a.test.cjs 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - File `agents/brief-deliver-type-a.md` exists with frontmatter `name: brief-deliver-type-a` AND `tools: Read, Grep, Glob, Write` AND `color: green`
    - Agent body contains `{{ARTIFACT}}` placeholder ≥ 5 times: `grep -c "{{ARTIFACT}}" agents/brief-deliver-type-a.md` returns ≥ 5
    - Agent body contains anti-pattern markers: `grep -cE "NEVER (hallucinate|use pass/fail|auto-attached)" agents/brief-deliver-type-a.md` returns ≥ 3
    - File `brief/templates/deliver/type-a/product-brief.md` exists with all 4 INSERT placeholders: `grep -cE "<!-- INSERT: ## (Customer Segments|Value Proposition|Personas|Immutable Intent) -->" brief/templates/deliver/type-a/product-brief.md` returns 4
    - File `brief/templates/deliver/type-a/service-policy.md` exists with B2B/B2C conditional prose markers: `grep -c "<!--BEGIN business_model: b2[bc]-->" brief/templates/deliver/type-a/service-policy.md` returns 2
    - service-policy.md B2B block contains all 4 keywords (case-insensitive — template uses natural capitalization like "Enterprise support" / "Data processing terms"): `grep -ciE "(SLA tiers|enterprise support|data processing|contract terms)" brief/templates/deliver/type-a/service-policy.md` returns ≥ 4
    - service-policy.md B2C block contains all 4 keywords (case-insensitive — template uses natural capitalization like "Refund policy" / "Customer support hours"): `grep -ciE "(refund policy|customer support hours|channel coverage|community guidelines)" brief/templates/deliver/type-a/service-policy.md` returns ≥ 4
    - File `brief/templates/deliver/type-a/high-level-spec.md` exists with all 4 INSERT placeholders: `grep -cE "<!-- INSERT: ## (Component Map|Phased Roadmap|Critical Risks|Immutable Intent) -->" brief/templates/deliver/type-a/high-level-spec.md` returns 4
    - File `brief/templates/deliver/type-a/feature-map.md` exists with Mermaid mindmap block: `grep -c "mindmap" brief/templates/deliver/type-a/feature-map.md` returns ≥ 1
    - All 4 templates have frontmatter with `deliverable:` field: `grep -c "^deliverable:" brief/templates/deliver/type-a/*.md | grep -c ":1$"` returns 4 (1 each)
    - All 4 templates have `voice.languages: {{languages}}` placeholder
    - Plan 05 Wave 0 tests pass: `node --test tests/brief-deliver-type-a-templates.test.cjs` exits 0
    - Plan 01 deliver.cjs tests still pass: `node --test tests/brief-deliver-type-a.test.cjs` exits 0
    - `git log -1 --format=%s` matches `^feat\\(08-05\\): implement Type A agent`
  </acceptance_criteria>
  <done>1 agent + 4 templates committed; 12 Wave 0 tests RED→GREEN; Plan 01 deliver.cjs tests still pass</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Workstream artifact body → Type A agent prompt | User-controlled markdown read by agent for synthesis; treated as own user input |
| Type A template `<!-- INSERT -->` placeholders → deliver.cjs body.replace | Literal substring replace — placeholders are constants in template + SYNTHESIS_MAP, no injection vector |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-05-01 | Tampering | Type A agent fabricates workstream content not present in source | mitigate | Agent prompt explicit anti-pattern: "NEVER hallucinate workstream content — if source is missing, emit placeholder + warn". checkDependencies in deliver.cjs (Plan 01) gates this at the lib layer; agent narrative polish only fills NARRATIVE around extracted section content |
| T-08-05-02 | Repudiation (reputational) | Type A artifact uses pass/fail / compliance-theater vocabulary, leaks to PM PRD | mitigate | Agent anti-pattern explicit: "NEVER use pass/fail or compliance-theater vocabulary". Phase 4·5·7 vocabulary-lock test extends to Type A artifacts (Plan 08 canary E2E asserts no banned vocabulary in synthesized output) |
| T-08-05-03 | Information Disclosure | Type A agent given Write tool may write to paths outside {{OUT_PATH}} | mitigate | Agent frontmatter `tools: Read, Grep, Glob, Write` — Write is needed for {{OUT_PATH}} emit only. Anti-pattern explicit: "DO NOT use Write tool for any path other than {{OUT_PATH}}". Workflow (Plan 08) sets {{OUT_PATH}} to `.planning/deliverables/type-a/{artifact-key}.md` constant — no user-supplied path interpolation |
| T-08-05-04 | Information Disclosure | service-policy.md B2B block content visible in B2C output (or vice versa) | mitigate | applyConditionalProse (Plan 01) drops non-matching blocks via regex. Plan 05 templates use `<!--BEGIN/END business_model: X-->` markers; Plan 01 SYNTHESIS_MAP `conditionalProse: true` triggers the strip. Wave 0 test asserts both blocks present in TEMPLATE; canary E2E (Plan 08) asserts only matching block in OUTPUT |
</threat_model>

<verification>
- 1 agent file + 4 templates committed
- agents/brief-deliver-type-a.md inherits brief-domain-researcher.md shape verbatim with synthesis adaptation
- All 4 templates contain section markers matching Plan 01 SYNTHESIS_MAP byte-identically
- service-policy.md has both B2B and B2C conditional prose blocks with mandated keywords
- feature-map.md has Mermaid mindmap block (preferred) + ASCII tree fallback comment
- All templates have 5 mandatory frontmatter fields + voice.languages + deliverable + generated_by + generated_at placeholders
- 12 Wave 0 RED→GREEN
- Plan 01 deliver.cjs tests still pass (no regression)
- A1 zero-runtime-deps preserved (no package.json mods)
</verification>

<success_criteria>
- 1 parameterized Type A agent (Phase 5 D-01 byte-identity) — NOT 4 separate agents
- 4 production templates with section markers matching SYNTHESIS_MAP
- service-policy.md B2B/B2C conditional prose Phase 7 D-14 byte-identity
- feature-map.md ships Mermaid + ASCII fallback
- Anti-patterns: NEVER hallucinate / NEVER pass/fail / NEVER auto-attached
- Provenance discipline: tags carried over from source verbatim
- 12 Wave 0 RED→GREEN
</success_criteria>

<output>
After completion, create `.planning/phases/08-deliver-type-a-type-b-audience-enforcement-marp/08-05-SUMMARY.md` documenting:
1. Type A agent role + 5-step process
2. 4 templates inventory with placeholder count per template
3. service-policy.md B2B/B2C keyword inventory (4 each)
4. feature-map.md Mermaid + ASCII strategy
5. Test count + verify command + actual pass count
6. Files Plan 08 (commands/brief/deliver.md + workflows/deliver.md) consume from this plan (the agent name + template paths)
</output>
</content>
</invoke>