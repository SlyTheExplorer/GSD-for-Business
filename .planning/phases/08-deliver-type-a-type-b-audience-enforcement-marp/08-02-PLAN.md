---
phase: 08-deliver-type-a-type-b-audience-enforcement-marp
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - brief/bin/lib/voice-fit.cjs
  - brief/references/voice-fit-vocabulary.md
  - tests/brief-voice-fit-banned-words.test.cjs
  - tests/brief-voice-fit-concreteness.test.cjs
  - tests/brief-voice-fit-honorific-ko.test.cjs
autonomous: true
requirements: [DLV-05, DLV-06, DLV-07]

must_haves:
  truths:
    - "voice-fit.cjs checkBannedWords detects 16 EN seed banned words case-insensitively + 8 KO seed banned words exactly + computes density per ~250-words/page"
    - "voice-fit.cjs checkBannedWords HONORIFIC_VIOLATION_KO detection ONLY fires when isKorean: true AND isExternal: true (audience.confidentiality in {partner, public})"
    - "voice-fit.cjs checkConcreteness counts specific-numbers + dates + proper-nouns and computes concretenessRatio (per 100 words); needsImprovement: true when ratio < 3"
    - "voice-fit.cjs is a SEPARATE lib that runs PARALLEL to AUDIENCE — does NOT extend audience.cjs runDeterministicScreen (Phase 4·5·7 vocabulary-lock test continues to pass)"
    - "voice-fit.cjs returns { density, threshold: 2, exceedsThreshold, hits, pages } shape for banned-words check; test fixtures cover threshold = 2/page boundary"
    - "brief/references/voice-fit-vocabulary.md mirrors brief/references/audience-vocabulary.md shape with Banned EN/KO + Honorific KO + 4 concreteness exemplars (1 EN INVESTOR-IR + 1 EN EXEC-SUMMARY + 1 EN DECISION-MEMO + 1 KO 격식체) hand-written"
  artifacts:
    - path: "brief/bin/lib/voice-fit.cjs"
      provides: "Banned-words density check + concreteness heuristic + honorific-violation detection (ko)"
      min_lines: 130
      contains: "BANNED_EN"
    - path: "brief/references/voice-fit-vocabulary.md"
      provides: "Source-of-truth ban-list reference + 4 hand-written exemplars (NOT LLM-generated per C-D04)"
      min_lines: 100
      contains: "Banned vocabulary"
    - path: "tests/brief-voice-fit-banned-words.test.cjs"
      provides: "Wave 0 fixture for banned-words density"
      contains: "checkBannedWords"
    - path: "tests/brief-voice-fit-concreteness.test.cjs"
      provides: "Wave 0 fixture for concreteness heuristic"
      contains: "checkConcreteness"
    - path: "tests/brief-voice-fit-honorific-ko.test.cjs"
      provides: "Wave 0 fixture for KO 반말 detection only when isExternal"
      contains: "HONORIFIC_VIOLATION_KO"
  key_links:
    - from: "brief/bin/lib/voice-fit.cjs"
      to: "brief/references/voice-fit-vocabulary.md"
      via: "ban-list constants documented in reference; tests/brief-voice-fit-vocabulary-lock.test.cjs (future) grep both for sync"
      pattern: "voice-fit-vocabulary"
    - from: "brief/bin/lib/voice-fit.cjs"
      to: "brief/bin/lib/audience.cjs"
      via: "MUST NOT import or extend runDeterministicScreen — runs PARALLEL not as 5th gate (anti-pattern flag in PATTERNS.md line 288)"
      pattern: "(?!.*runDeterministicScreen)"
---

<objective>
Ship `brief/bin/lib/voice-fit.cjs` — the banned-words density + concreteness + Korean honorific-violation post-check lib for Type B agent output. Plus `brief/references/voice-fit-vocabulary.md` reference doc with hand-written concreteness exemplars (per C-D04 — must NOT be LLM-generated). Plus 3 Wave 0 RED→GREEN test files.

Purpose: Pitfall #10 (AI Slop) + Pitfall #11 (Korean honorific) mitigation. After every Type B agent generates artifact text, voice-fit.cjs scans for banned-word density (>2/page → 1-shot regenerate signal). When `audience.confidentiality in {partner, public}` AND region: kr, also scans for 반말 endings (`-야`, `-지`, `-라구요`, `-거든요`, `-는데요`). All checks run as a PARALLEL lib — NOT a 5th canonical gate (CONTEXT.md C-D03/C-D04/D-D04 explicit reject; Phase 4·5·7 vocabulary-lock test stays green).

Output: `brief/bin/lib/voice-fit.cjs` (~150 LOC) + reference doc (~100 LOC w/ exemplars) + 3 Wave 0 test fixtures. Plan 04 (export.cjs) wires this into the export workflow; Plan 06 (Type B agent) embeds the same regex constants verbatim in the agent prompt for self-check.
</objective>

<execution_context>
@~/.claude/brief/workflows/execute-plan.md
@~/.claude/brief/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-deliver-type-a-type-b-audience-enforcement-marp/08-CONTEXT.md
@.planning/phases/08-deliver-type-a-type-b-audience-enforcement-marp/08-RESEARCH.md
@.planning/phases/08-deliver-type-a-type-b-audience-enforcement-marp/08-PATTERNS.md
@brief/bin/lib/audience.cjs
@brief/bin/lib/align.cjs
@brief/references/audience-vocabulary.md

<interfaces>
<!-- Existing exports the executor will REUSE (NOT extend). -->

From brief/bin/lib/align.cjs (Korea-signal helper Phase 4 D-09):
```javascript
function detectKoreaSignalFromConfig(cwd: string): boolean;  // returns true if .planning/config.json has brief.region === 'kr'
```

From brief/bin/lib/audience.cjs (Phase 5 ban-list shape — voice-fit.cjs MIRRORS but does NOT extend):
```javascript
const BAN_EN = /\b(compliant|passed|violation|failed)\b/gi;     // AUDIENCE banned-words (Phase 4 vocab lock)
const BAN_KO = /(준수|통과|위반|실패)/g;                          // AUDIENCE Korean ban (vocab lock)
const HEDGING_EN = /\b(TBD|we believe|we think|...)\b/gi;       // AUDIENCE hedging (vocab lock)
const HEDGING_KO = /(아직 확정 전|우려|미해결|...)/g;            // AUDIENCE hedging KO (vocab lock)
function grepBanList(artifactPath): { hits, density, ... };     // AUDIENCE-internal — DO NOT call from voice-fit.cjs
```

VOICE-FIT lib MUST define DISTINCT regex constants (BANNED_EN / BANNED_KO / HONORIFIC_VIOLATION_KO) that do NOT collide with AUDIENCE vocabulary. PATTERNS.md "ANTI-PATTERN FLAG" lines 287-288: voice-fit.cjs is NEW lib, NOT extension of audience.cjs.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — Write 3 RED test fixtures (banned-words + concreteness + honorific-ko)</name>
  <files>tests/brief-voice-fit-banned-words.test.cjs, tests/brief-voice-fit-concreteness.test.cjs, tests/brief-voice-fit-honorific-ko.test.cjs</files>
  <read_first>
    - .planning/phases/08-deliver-type-a-type-b-audience-enforcement-marp/08-RESEARCH.md (Pattern 6 lines 739-822 — voice-fit.cjs API contract; Wave 0 Gaps lines 1593-1595)
    - .planning/phases/08-deliver-type-a-type-b-audience-enforcement-marp/08-PATTERNS.md (lines 247-296 — voice-fit.cjs analog + ANTI-PATTERN FLAG)
    - tests/brief-audience-roundtrip.test.cjs (existing node:test pattern — first 100 lines for setup/teardown convention)
    - brief/bin/lib/audience.cjs (lines 35-94 — REFERENCE for the BAN/HEDGING constants shape voice-fit will MIRROR — DO NOT import from audience.cjs)
  </read_first>
  <behavior>
    - banned-words test 1: `checkBannedWords('We leverage synergies and unlock value with our innovative platform.', { isKorean: false, isExternal: true })` returns object where `hits.length === 4` (leverage, synergize→synergies/no-match (synergize is the regex root, "synergies" doesn't match \b(synergize)\b), unlock, innovative — actually `leverage`, `unlock`, `innovative` = 3) — confirm the regex word-boundary expects exact stem match and write the test asserting the exact count. Validate: write a fixture sentence containing exactly `leverage` `synergize` `transform` (3 banned tokens) and assert `hits.length === 3`.
    - banned-words test 2: density boundary — sentence with exactly 250 words containing 3 banned tokens → `density === 3`, `exceedsThreshold === true`. Sentence with 250 words containing 1 banned token → `density === 1`, `exceedsThreshold === false`.
    - banned-words test 3: KO ban detection when `isKorean: true` — text containing `혁신적인` and `시너지` → 2 hits. With `isKorean: false` (English-only project) → 0 hits even if Korean text present.
    - banned-words test 4: returns object shape `{ density, threshold, exceedsThreshold, hits, pages }` with threshold === 2.
    - concreteness test 1: `checkConcreteness('We will reduce 15-person legal review cycles from 3 weeks to 4 days, achieving 47% efficiency gain by 2026-Q3.')` returns `concretenessRatio` ≥ 3 → `needsImprovement: false`. (Specific-numbers: "15", "3 weeks", "4 days", "47%". Dates: "2026". Concrete signals = 5+; wordCount ≈ 23; ratio ≈ 5/0.23 = 21.7. Note formula is concrete / (wordCount/100), so per 100 words.)
    - concreteness test 2: `checkConcreteness('We deliver innovative solutions that transform business outcomes.')` returns `needsImprovement: true` (no specific numbers, no dates, no 3-word proper noun chains).
    - honorific-ko test 1: `checkBannedWords('우리는 좋은 솔루션을 제공해야지.', { isKorean: true, isExternal: true })` returns `hits.length >= 1` (matches `-지` 반말 ending). The lookbehind for Hangul before the suffix is required.
    - honorific-ko test 2: `checkBannedWords('우리는 좋은 솔루션을 제공해야지.', { isKorean: true, isExternal: false })` returns 0 honorific hits (only fires when isExternal: true).
    - honorific-ko test 3: `checkBannedWords('서비스를 제공해야지.', { isKorean: false, isExternal: true })` returns 0 honorific hits (only fires when isKorean: true).
    - All tests fail (RED) — voice-fit.cjs does not exist yet.
  </behavior>
  <action>
Create 3 test files using `node:test`:

`tests/brief-voice-fit-banned-words.test.cjs` — 5+ test cases per behavior block. Use `require('../brief/bin/lib/voice-fit.cjs')` knowing it throws MODULE_NOT_FOUND in RED state. Wrap each test in `try { ... } catch (e) { assert.match(e.message, /Cannot find module/); }` so RED state passes the predictable-failure assertion. After Plan 02 Task 2 implements voice-fit.cjs, the catch block won't fire and the actual assertions inside `try` will run.

`tests/brief-voice-fit-concreteness.test.cjs` — 4+ tests covering specific-numbers, dates, proper-nouns, and the threshold boundary (concretenessRatio < 3 → needsImprovement: true).

`tests/brief-voice-fit-honorific-ko.test.cjs` — 4+ tests covering the conditional-firing rules (only when isKorean AND isExternal). Include 1 false-positive guard test: `checkBannedWords('아버지', { isKorean: true, isExternal: true })` returns 0 honorific hits because `아버지` is the noun "father", not the `-지` ending pattern. The regex `(?:[가-힣])(야|지|라구요|거든요|는데요)\b` uses `\b` word-boundary which fails to match in the middle of a word — assert this guard.

Commit message: `test(08-02): add failing Wave 0 tests for voice-fit.cjs banned-words + concreteness + honorific-ko`. Run `node --test tests/brief-voice-fit-*.test.cjs` and assert exit non-zero (RED).
  </action>
  <verify>
    <automated>node --test tests/brief-voice-fit-banned-words.test.cjs tests/brief-voice-fit-concreteness.test.cjs tests/brief-voice-fit-honorific-ko.test.cjs 2>&1 | grep -E "fail|MODULE_NOT_FOUND" | head -3</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/brief-voice-fit-banned-words.test.cjs` exists with ≥ 5 `test(` invocations
    - File `tests/brief-voice-fit-concreteness.test.cjs` exists with ≥ 4 `test(` invocations
    - File `tests/brief-voice-fit-honorific-ko.test.cjs` exists with ≥ 4 `test(` invocations including the `아버지` false-positive guard
    - `node --test tests/brief-voice-fit-*.test.cjs` exits non-zero (Wave 0 RED) — failure mode is predictable (module not found OR explicit fail in catch block)
    - `git log -1 --format=%s` matches `^test\\(08-02\\): add failing Wave 0 tests for voice-fit.cjs`
  </acceptance_criteria>
  <done>3 RED test files committed; node --test exits non-zero with predictable failure</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — Implement voice-fit.cjs + voice-fit-vocabulary.md reference</name>
  <files>brief/bin/lib/voice-fit.cjs, brief/references/voice-fit-vocabulary.md</files>
  <read_first>
    - .planning/phases/08-deliver-type-a-type-b-audience-enforcement-marp/08-RESEARCH.md (Pattern 6 lines 739-822 — copy implementation verbatim; Anti-Patterns lines 1051 — 5th canonical gate prohibition)
    - .planning/phases/08-deliver-type-a-type-b-audience-enforcement-marp/08-PATTERNS.md (lines 247-296 — voice-fit.cjs analog with regex constants; lines 287-288 — ANTI-PATTERN FLAG: MUST NOT extend audience.cjs runDeterministicScreen; lines 952-1000 — voice-fit-vocabulary.md reference shape)
    - brief/references/audience-vocabulary.md (read full file — voice-fit-vocabulary.md MIRRORS this shape: Preferred KO/EN + Ban-list KO/EN + extension procedure section)
    - tests/brief-voice-fit-banned-words.test.cjs (the test contract Plan 02 Task 1 wrote — implementation must satisfy these tests)
    - tests/brief-voice-fit-concreteness.test.cjs
    - tests/brief-voice-fit-honorific-ko.test.cjs
  </read_first>
  <action>
Create `brief/bin/lib/voice-fit.cjs` per RESEARCH.md Pattern 6 lines 743-813 verbatim. Required exports: `checkBannedWords`, `checkConcreteness`, `BANNED_EN`, `BANNED_KO`, `HONORIFIC_VIOLATION_KO`.

Imports (MUST NOT import audience.cjs runDeterministicScreen per anti-pattern flag — but MAY import detectKoreaSignalFromConfig from align.cjs for future helper use; for Plan 02 specifically, no align.cjs import is required since `isKorean` is passed via options):
```javascript
// no imports needed for the regex + arithmetic functions; pure utilities
```

Constants (verbatim from RESEARCH.md lines 753-761):
```javascript
const BANNED_EN = /\b(leverage|synergize|transform|holistic|delve|groundbreaking|best-in-class|seamless|cutting-edge|revolutionary|game-changing|landscape|unlock|empower|robust|innovative)\b/gi;
const BANNED_KO = /(혁신적인|차별화된|게임체인저|패러다임 시프트|시너지|활용|최적화|글로벌 스탠더드)/g;
const HONORIFIC_VIOLATION_KO = /(?:[가-힣])(야|지|라구요|거든요|는데요)\b/g;
```

Implement `checkBannedWords(text, options)` per RESEARCH.md lines 763-786 verbatim. Returns `{ density, threshold: 2, exceedsThreshold, hits, pages }`.

Implement `checkConcreteness(text)` per RESEARCH.md lines 791-810 verbatim. Returns `{ concrete, wordCount, concretenessRatio, needsImprovement }`. Threshold: concretenessRatio < 3 → needsImprovement: true.

Module exports: `module.exports = { checkBannedWords, checkConcreteness, BANNED_EN, BANNED_KO, HONORIFIC_VIOLATION_KO };`

Then create `brief/references/voice-fit-vocabulary.md` per PATTERNS.md lines 952-1000. Include:
- Title + 1-paragraph description (Phase 8 voice-fit.cjs reference)
- `## Banned vocabulary (EN — 16 seed)` listing all 16 EN words bullet-style
- `## Banned vocabulary (KO — 8 seed; CONTEXT.md C-D03)` listing all 8 KO words
- `## Honorific violations (KO — only when audience external + region:kr)` listing the 5 endings (`-야`, `-지`, `-라구요`, `-거든요`, `-는데요`)
- `## Concreteness exemplars (hand-written per C-D04)` section with EXACTLY 4 hand-written exemplars rendered as H3 (`### `) headings (byte-identical to Plan 06 Type B agent prompt embed at lines 259-269 — both files MUST emit `### INVESTOR-IR style (1)` / `### EXEC-SUMMARY style (1)` / `### DECISION-MEMO style (1)` / `### Korean exemplar (격식체 + 구체 수치 + 출처)` so the acceptance grep `^### (INVESTOR-IR|EXEC-SUMMARY|DECISION-MEMO|Korean)` returns exactly 4):

  ### INVESTOR-IR style (1)
  [페이앱 v2 reduces user onboarding from 12 minutes (industry baseline per Stripe Atlas 2025 benchmark) to 90 seconds — 87% reduction. Cohort 2026-Q2 (n=4,300 users) showed 91-day retention of 47% vs control 23% (p < 0.01). [VERIFIED:internal-pilot-cohort-2026-Q2-analysis-2026-04-25|2026-04-25]]

  ### EXEC-SUMMARY style (1)
  [We are recommending the Vendor X migration based on 3 data points: (a) Q1-2026 pilot showed 23% reduction in MTTR vs incumbent (n=14 incidents); (b) annual cost differential is $147K savings at current scale [SOURCE: 2026 vendor quote 2026-03-15]; (c) 9 of 11 engineering team members preferred Vendor X interface in blind 2-week trial [VERIFIED:internal-trial-2026-Q1].]

  ### DECISION-MEMO style (1)
  [We will adopt Vendor X over Vendor Y because (1) total cost of ownership over 3 years is $441K vs $580K (24% lower); (2) Vendor X SLA covers Korea region without latency surcharge per 2026-Q1 RFP response; (3) Korean PIPA compliance attested via ISO/IEC 27018 + Korea ISMS-P certification (Vendor Y has PIPA pending until Q4-2026).]

  ### Korean exemplar (격식체 + 구체 수치 + 출처)
  [페이앱은 2026년 2분기에 91일 유지율을 47%까지 개선했습니다 (대조군 23%, p < 0.01, n=4,300명). 한국 핀테크 시장 평균 91일 유지율은 27% 수준이며 [출처: 한국핀테크산업협회 2025 연간보고서, p. 47], 페이앱은 1.7배의 차이를 보였습니다.]
- `## Ban-list extension procedure (per Phase 4 D-09 inheritance)` 4-step section per PATTERNS.md lines 996-999.

These 4 exemplars MUST be hand-written by Claude in this task, NOT LLM-generated boilerplate. Per C-D04 / Pitfall #10: "LLM-generated exemplar는 그 자체로 slop 위험". The exemplars must contain actual specific numbers (47%, 4,300, 91-day, $15K-style figures), real or plausible source citations (with VERIFIED/CITED markers per Phase 5 D-04 provenance schema), and concrete time/date references. Avoid every word in BANNED_EN and BANNED_KO inside the exemplars.

Run all 3 voice-fit test files; ALL tests must turn GREEN.

Commit message: `feat(08-02): implement voice-fit.cjs banned-words + concreteness + honorific-ko + reference doc with hand-written exemplars`.
  </action>
  <verify>
    <automated>node --test tests/brief-voice-fit-banned-words.test.cjs tests/brief-voice-fit-concreteness.test.cjs tests/brief-voice-fit-honorific-ko.test.cjs 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - File `brief/bin/lib/voice-fit.cjs` exists with `module.exports` containing `checkBannedWords`, `checkConcreteness`, `BANNED_EN`, `BANNED_KO`, `HONORIFIC_VIOLATION_KO`
    - `grep -c "BANNED_EN\\b" brief/bin/lib/voice-fit.cjs` returns ≥ 2 (declaration + use in checkBannedWords)
    - `grep -cE "leverage\\|synergize\\|transform\\|holistic\\|delve\\|groundbreaking\\|best-in-class\\|seamless\\|cutting-edge\\|revolutionary\\|game-changing\\|landscape\\|unlock\\|empower\\|robust\\|innovative" brief/bin/lib/voice-fit.cjs` returns ≥ 1 (the 16-word EN regex)
    - `grep -cE "혁신적인\\|차별화된\\|게임체인저\\|패러다임 시프트\\|시너지\\|활용\\|최적화\\|글로벌 스탠더드" brief/bin/lib/voice-fit.cjs` returns ≥ 1 (the 8-word KO regex)
    - voice-fit.cjs MUST NOT import audience.cjs runDeterministicScreen — `grep -c "runDeterministicScreen" brief/bin/lib/voice-fit.cjs` returns 0
    - File `brief/references/voice-fit-vocabulary.md` exists with `## Banned vocabulary (EN`, `## Banned vocabulary (KO`, `## Honorific violations (KO`, `## Concreteness exemplars`, `## Ban-list extension procedure` headings (5 mandatory section headers)
    - Reference doc has 4 hand-written exemplars (count `## ###` or `### ` subheaders for INVESTOR-IR / EXEC-SUMMARY / DECISION-MEMO / Korean): `grep -cE "^### (INVESTOR-IR|EXEC-SUMMARY|DECISION-MEMO|Korean)" brief/references/voice-fit-vocabulary.md` returns 4
    - All 3 voice-fit test files pass: `node --test tests/brief-voice-fit-banned-words.test.cjs tests/brief-voice-fit-concreteness.test.cjs tests/brief-voice-fit-honorific-ko.test.cjs` exits 0
    - Phase 4·5·7 vocabulary-lock tests still pass (anti-pattern guard): `node --test tests/brief-audience-vocabulary-lock.test.cjs 2>/dev/null` exits 0 if file exists, else N/A
    - A1 zero-deps preserved: `node -e "console.log(Object.keys(require('./package.json').dependencies||{}).length)"` returns 0
    - `git log -1 --format=%s` matches `^feat\\(08-02\\): implement voice-fit.cjs`
  </acceptance_criteria>
  <done>voice-fit.cjs lib + reference doc shipped; 3 Wave 0 RED→GREEN; A1 + anti-pattern (no 5th gate, no runDeterministicScreen extension) preserved</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Type B agent output → voice-fit.cjs | LLM-generated text fed to regex post-check; treated as untrusted input |
| voice-fit-vocabulary.md → Type B agent prompt | Hand-written exemplars distributed to Type B agent prompt context (Plan 06) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-02-01 | Repudiation (reputational) | AI slop ("leverage", "synergize") shipped to investor → credibility damage | mitigate | voice-fit.cjs banned-words regex post-check + 1-shot regenerate dispatch (wired in Plan 04 export.cjs); FINDINGS-MATERIAL emit if persists |
| T-08-02-02 | Repudiation (reputational) | Korean honorific violation (반말 in external Korean artifact) → cultural offense | mitigate | HONORIFIC_VIOLATION_KO regex when audience.confidentiality in {partner, public} AND region: kr; conditionally fires only when isExternal AND isKorean per checkBannedWords options |
| T-08-02-03 | Tampering | False-positive flood from regex catching legitimate words ("아버지" → `-지`?) | mitigate | Regex uses `\b` word-boundary which fails inside Korean words; `아버지` is one Hangul-only token, no word-break, no match. Test suite includes false-positive guard for `아버지`. |
| T-08-02-04 | Information Disclosure | voice-fit-vocabulary.md exemplars contain real/plausible internal numbers | accept | Exemplars are hand-written + intentionally illustrative; any project numbers used are non-confidential canary data (페이앱 fictional B2C fintech). Hand-written discipline (NOT LLM-generated) preserved per C-D04. |
| T-08-02-05 | Denial of Service | Regex catastrophic backtracking on adversarial text | accept | All 3 regexes are linear-time (no nested quantifiers, no overlapping alternations); `BANNED_EN` is character-class alternation, `BANNED_KO` is exact-string alternation, `HONORIFIC_VIOLATION_KO` is single capture with bounded suffix. Worst-case is O(n) where n = text length. |
</threat_model>

<verification>
- 3 Wave 0 test files all pass: `node --test tests/brief-voice-fit-banned-words.test.cjs tests/brief-voice-fit-concreteness.test.cjs tests/brief-voice-fit-honorific-ko.test.cjs` exits 0
- voice-fit.cjs has zero imports from audience.cjs (anti-pattern guard): `grep -c "require.*audience" brief/bin/lib/voice-fit.cjs` returns 0
- voice-fit-vocabulary.md exists with all 5 mandatory section headers
- All 4 hand-written exemplars present (INVESTOR-IR / EXEC-SUMMARY / DECISION-MEMO / Korean)
- A1 zero-runtime-deps preserved (no `package.json` mods)
- AUDIENCE vocabulary-lock test continues to pass (no 5th gate added; no audience.cjs vocabulary changes)
</verification>

<success_criteria>
- Banned-words regex covers all 16 EN seed + 8 KO seed words exactly
- Honorific-violation regex matches 5 KO 반말 endings ONLY when isKorean AND isExternal
- Concreteness heuristic detects specific numbers + dates + proper nouns; threshold = 3 signals/100 words
- voice-fit.cjs is a SEPARATE lib (NOT a 5th canonical gate per CONTEXT.md C-D03 reject)
- voice-fit-vocabulary.md ships 4 hand-written exemplars per C-D04 (NOT LLM-generated)
- Reference doc includes ban-list extension procedure (Phase 4 D-09 inheritance)
- All 3 Wave 0 RED→GREEN
</success_criteria>

<output>
After completion, create `.planning/phases/08-deliver-type-a-type-b-audience-enforcement-marp/08-02-SUMMARY.md` documenting:
1. voice-fit.cjs API surface (3 functions + 3 regex constants exported)
2. voice-fit-vocabulary.md exemplar inventory (4 hand-written) — Plan 06 Type B agent prompt embeds these
3. Anti-pattern guard: no audience.cjs imports + AUDIENCE vocabulary-lock still passes
4. Test count + verify command + actual pass count
5. Files Plan 04 (export.cjs voice-fit dispatch) and Plan 06 (Type B agent prompt embed) consume from this plan
</output>
</content>
</invoke>