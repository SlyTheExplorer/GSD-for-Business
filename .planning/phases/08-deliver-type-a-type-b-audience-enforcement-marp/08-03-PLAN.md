---
phase: 08-deliver-type-a-type-b-audience-enforcement-marp
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - brief/bin/lib/leakage-diff.cjs
  - tests/brief-export-leakage-diff.test.cjs
  - tests/fixtures/deliver/intentional-leak-pair/internal-deck.md
  - tests/fixtures/deliver/intentional-leak-pair/proposal-deck.md
  - tests/fixtures/deliver/incidental-overlap-pair/internal-deck.md
  - tests/fixtures/deliver/incidental-overlap-pair/proposal-deck.md
autonomous: true
requirements: [DLV-06]

must_haves:
  truths:
    - "leakage-diff.cjs leakageDiff(currentArtifactPath) reads frontmatter audience.confidentiality, enumerates same-folder siblings with stricter confidentiality, extracts top-20 TF-IDF distinctive keywords from each stricter sibling, and emits FINDINGS-MATERIAL severity finding when ≥3 keywords match in current artifact body"
    - "Confidentiality strictness ordering: public(0) < partner(1) < internal(2) < confidential(3) per Phase 5 D-10 enum + Phase 8 B-D01"
    - "Hangul-aware tokenizer matches align.cjs computeTermOverlap pattern verbatim (`/[\uac00-\ud7af]{2,}/u` for Hangul + `/^[a-z][a-z0-9_-]+$/` for EN ≥4-letter)"
    - "STOP_WORDS_EN excludes ≥30 generic biz terms (about, above, customer, market, company, business, team, value, ...) — prevents false positives on common words"
    - "STOP_WORDS_KO excludes ≥15 generic Korean terms (있습니다, 입니다, 고객, 시장, 회사, 사업, 팀, 가치, 서비스, 제품, ...) — prevents false positives on Korean common words"
    - "Pair sibling files (.audience.md, .compliance.md) are SKIPPED in sibling enumeration — only same-extension primary markdown counts"
    - "intentional-leak fixture (5+ keywords copied PROPOSAL ← INTERNAL) → ≥1 finding emitted; incidental-overlap fixture (only generic terms shared) → 0 findings"
    - "Pure-CJS implementation — NO `natural` / `tfidf-search` / `node-tfidf` runtime dep added (A1 lock)"
  artifacts:
    - path: "brief/bin/lib/leakage-diff.cjs"
      provides: "TF-IDF cross-artifact keyword diff with Hangul-aware tokenizer + stop-word filter + 3-match threshold"
      min_lines: 130
      contains: "leakageDiff"
    - path: "tests/brief-export-leakage-diff.test.cjs"
      provides: "Wave 0 fixture for DLV-06 — TF-IDF accuracy intentional-leak vs incidental-overlap"
      min_lines: 100
      contains: "leakageDiff"
    - path: "tests/fixtures/deliver/intentional-leak-pair/internal-deck.md"
      provides: "Stricter (confidentiality: confidential) sibling with distinctive keywords; PROPOSAL leaks ≥5 of these into partner audience artifact"
      contains: "audience.confidentiality: confidential"
    - path: "tests/fixtures/deliver/intentional-leak-pair/proposal-deck.md"
      provides: "Less-strict (confidentiality: partner) artifact whose body intentionally copies 5+ distinctive keywords from internal-deck.md"
      contains: "audience.confidentiality: partner"
    - path: "tests/fixtures/deliver/incidental-overlap-pair/internal-deck.md"
      provides: "Stricter sibling with NO copied keywords (project-name + generic biz words only)"
      contains: "audience.confidentiality: confidential"
    - path: "tests/fixtures/deliver/incidental-overlap-pair/proposal-deck.md"
      provides: "Less-strict artifact sharing only generic terms (`고객`, `market`) — should produce 0 findings"
      contains: "audience.confidentiality: partner"
  key_links:
    - from: "brief/bin/lib/leakage-diff.cjs"
      to: "brief/bin/lib/frontmatter.cjs"
      via: "extractFrontmatter / stripFrontmatter for source artifact reads"
      pattern: "extractFrontmatter"
    - from: "brief/bin/lib/leakage-diff.cjs"
      to: "brief/bin/lib/align.cjs"
      via: "Hangul-aware tokenizer regex `/[\uac00-\ud7af]{2,}/u` IS the same regex from align.cjs computeTermOverlap (line 110-127) — REPLICATE verbatim, do NOT import (keeps lib boundaries clean)"
      pattern: "uac00-ud7af"
---

<objective>
Ship `brief/bin/lib/leakage-diff.cjs` — the TF-IDF cross-artifact keyword diff lib. After every `/brief-export` invocation (Plan 04), this lib enumerates same-folder siblings with stricter confidentiality, extracts top-20 distinctive keywords from each stricter sibling via Salton-1988 TF-IDF, and emits FINDINGS-MATERIAL severity finding when ≥3 distinctive keywords appear in the current (less-strict) artifact body.

Purpose: Pitfall #5 audience leakage's most-deferred mitigation (Phase 5 → Phase 8). Without this lib, copy-paste from INTERNAL-DECK to PROPOSAL-DECK is undetected by AUDIENCE/COMPLIANCE gates (which evaluate one artifact in isolation). Cross-artifact diff is the LAST programmatic line of defense before the user's eyeballs at the `/brief-export` 1-step confirm.

Output: `brief/bin/lib/leakage-diff.cjs` (~150 LOC pure CJS) + 1 Wave 0 test file + 4 fixture files (2 fixture-pairs: intentional-leak + incidental-overlap). Plan 04 (export.cjs) wires this as Step 1 of the export workflow (BEFORE AUDIENCE re-run).
</objective>

<execution_context>
@~/.claude/brief/workflows/execute-plan.md
@~/.claude/brief/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-deliver-type-a-type-b-audience-enforcement-marp/08-CONTEXT.md
@.planning/phases/08-deliver-type-a-type-b-audience-enforcement-marp/08-RESEARCH.md
@.planning/phases/08-deliver-type-a-type-b-audience-enforcement-marp/08-PATTERNS.md
@brief/bin/lib/align.cjs
@brief/bin/lib/frontmatter.cjs

<interfaces>
<!-- Existing exports the executor will REUSE. -->

From brief/bin/lib/frontmatter.cjs:
```javascript
function extractFrontmatter(content: string): object | null;
function stripFrontmatter(content: string): string;
```

From brief/bin/lib/align.cjs (Hangul tokenizer pattern — REPLICATE verbatim, do NOT import):
```javascript
// computeTermOverlap lines 97-114 — extract this regex shape:
//   English ≥4-letter lowercase: /^[a-z][a-z0-9_-]+$/
//   Hangul ≥2-character: /[\uac00-\ud7af]{2,}/u
// Tokenizer splits text on /[\s\p{P}]+/u then iterates words
```

Phase 5 D-10 confidentiality enum (CONTEXT.md B-D01 lock):
- public (0) < partner (1) < internal (2) < confidential (3)
- Higher number = stricter
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — Write RED test fixture + 2 fixture-pairs (intentional-leak + incidental-overlap)</name>
  <files>tests/brief-export-leakage-diff.test.cjs, tests/fixtures/deliver/intentional-leak-pair/internal-deck.md, tests/fixtures/deliver/intentional-leak-pair/proposal-deck.md, tests/fixtures/deliver/incidental-overlap-pair/internal-deck.md, tests/fixtures/deliver/incidental-overlap-pair/proposal-deck.md</files>
  <read_first>
    - .planning/phases/08-deliver-type-a-type-b-audience-enforcement-marp/08-RESEARCH.md (Pattern 5 lines 584-737 — full leakage-diff.cjs algorithm + false-positive control table; Wave 0 Gaps lines 1602-1604 — 2 fixture pairs required)
    - .planning/phases/08-deliver-type-a-type-b-audience-enforcement-marp/08-PATTERNS.md (lines 299-356 — leakage-diff.cjs analog + STOP_WORDS lists + STRICTNESS map; ANTI-PATTERN FLAG line 356 — A1 zero-deps lock)
    - brief/bin/lib/align.cjs (lines 97-114 — computeTermOverlap reference for Hangul-aware tokenizer regex)
    - tests/brief-audience-roundtrip.test.cjs (existing node:test pattern — first 100 lines for setup/teardown convention)
  </read_first>
  <behavior>
    - Test 1 (RED, intentional-leak): When `leakageDiff('tests/fixtures/deliver/intentional-leak-pair/proposal-deck.md')` runs, the result is `{ findings: [...], rationale: '...' }` where `findings.length >= 1` and `findings[0].severity === 'material'` and `findings[0].description` contains the word 'Cross-artifact leakage suspected' and references the stricter sibling 'internal-deck.md'.
    - Test 2 (RED, incidental-overlap): When `leakageDiff('tests/fixtures/deliver/incidental-overlap-pair/proposal-deck.md')` runs, the result is `{ findings: [], rationale: ... }` (zero findings — only generic terms shared).
    - Test 3 (RED, edge case — no siblings): When `leakageDiff(...)` is called with a folder containing only 1 markdown file (no siblings), returns `{ findings: [], rationale: 'scanned 0 sibling(s)' }`.
    - Test 4 (RED, edge case — no confidentiality field): When current artifact frontmatter lacks `audience.confidentiality`, returns `{ findings: [], rationale: 'current artifact missing confidentiality field — skipped' }`.
    - Test 5 (RED, strictness ordering): When current artifact is `confidentiality: confidential` (the strictest), siblings with `partner` / `internal` / `public` are NOT scanned (they are LESS strict — leakage from them INTO the current artifact is not a leakage concern). Returns 0 findings.
    - Test 6 (RED, paired-sibling skip): When folder contains `proposal-deck.md` + `proposal-deck.audience.md` + `proposal-deck.compliance.md` + `internal-deck.md`, the audience.md and compliance.md MUST be skipped from sibling enumeration (only `internal-deck.md` is scanned).
    - Test 7 (RED, tokenizer Hangul-aware): The internal `tokenize()` exposed function returns Hangul tokens for `'페이앱은 한국 핀테크 플랫폼입니다'` (returns `['페이앱은', '한국', '핀테크', '플랫폼입니다']` or similar — at least 3 tokens of length ≥ 2 Hangul chars). Note: actual tokenization splits on punctuation; verify ≥ 3 Hangul tokens emitted.
    - Test 8 (RED, tf-idf score): Internal `tfIdf(text, corpus)` returns at most 20 tokens, sorted by score descending; stop-words filtered out (e.g., 'about' or '고객' would never appear in output even if present in input).
    - All 8 tests fail (RED) — leakage-diff.cjs does not exist.
  </behavior>
  <action>
Create the 2 fixture-pairs first:

**Intentional-leak pair** — `tests/fixtures/deliver/intentional-leak-pair/`:

`internal-deck.md`:
```markdown
---
audience.type: internal
audience.confidentiality: confidential
voice.tone: formal
voice.perspective: first-person-plural
business_context.model: b2c
business_context.region: kr
---

# 페이앱 내부 전략 데크

우리의 핵심 차별화 요소는 **카카오뱅크-제휴-API**(beta) 활용 + **3단계-셀프-온보딩-플로우** 입니다.
주요 metric은 **91-day-retention-cohort-2026Q2** 47% (대조군 23%, p<0.01, n=4300).
경쟁사 대비 **toss-pay-comparison-benchmark** 1.7배 우수.

내부 GTM 전략:
- **regional-rollout-seoul-busan-2026H2** (서울+부산 동시 진입)
- **acquisition-channel-instagram-reels-priority** (Instagram Reels 우선)
- **financial-burn-runway-18-months** ($2.3M, 18개월 runway)
- **legal-pipa-isms-p-cert-q3-deadline** (Q3 ISMS-P 인증 마감)
```

`proposal-deck.md` — DELIBERATELY copies 5+ distinctive keywords from internal-deck.md:
```markdown
---
audience.type: external
audience.confidentiality: partner
voice.tone: formal
voice.perspective: first-person-plural
business_context.model: b2c
business_context.region: kr
---

# 페이앱 파트너 제안 데크

페이앱은 **카카오뱅크-제휴-API** 를 통해 빠른 통합이 가능합니다.
**3단계-셀프-온보딩-플로우** 로 사용자 마찰을 최소화합니다.
**91-day-retention-cohort-2026Q2** 데이터에 따르면 retention이 우수합니다.
**toss-pay-comparison-benchmark** 결과 시장 평균 이상의 성과를 보입니다.
파트너십 모델은 **regional-rollout-seoul-busan-2026H2** 를 기반으로 설계되었습니다.
```

(5 distinctive multi-word keywords intentionally leaked: `카카오뱅크-제휴-API`, `3단계-셀프-온보딩-플로우`, `91-day-retention-cohort-2026Q2`, `toss-pay-comparison-benchmark`, `regional-rollout-seoul-busan-2026H2`. These are hyphen-joined to count as single ≥4-letter EN tokens AND/OR Hangul-rich strings.)

**Incidental-overlap pair** — `tests/fixtures/deliver/incidental-overlap-pair/`:

`internal-deck.md`:
```markdown
---
audience.type: internal
audience.confidentiality: confidential
voice.tone: formal
voice.perspective: first-person-plural
business_context.model: b2c
business_context.region: kr
---

# 페이앱 내부 데크

우리는 새로운 사업을 만들고 있습니다. 고객의 요구사항을 분석합니다.
시장 조사 결과 회사 가치가 증가하고 있습니다. 팀 멤버는 함께 일합니다.
서비스 품질을 개선하기 위해 최선을 다하고 있습니다.
```

`proposal-deck.md` — Shares ONLY generic terms (고객 / 시장 / 회사 / 사업 / 팀 / 가치 / 서비스 / 제품 — all in STOP_WORDS_KO):
```markdown
---
audience.type: external
audience.confidentiality: partner
voice.tone: formal
voice.perspective: first-person-plural
business_context.model: b2c
business_context.region: kr
---

# 페이앱 파트너 제안

페이앱의 사업 모델은 핀테크 시장에서 차별화됩니다. 우리 회사는 고객 만족을 우선합니다.
서비스 가격은 합리적입니다. 팀 멤버 23명이 가치 창출에 매진하고 있습니다.
```

Then write `tests/brief-export-leakage-diff.test.cjs` per behavior block — 8+ tests covering all behaviors. Use try/catch wrapping require for RED state predictability per Plan 02 pattern.

Commit message: `test(08-03): add failing Wave 0 tests for leakage-diff.cjs + 2 fixture pairs (DLV-06)`. Run `node --test tests/brief-export-leakage-diff.test.cjs` and assert exit non-zero (RED).
  </action>
  <verify>
    <automated>node --test tests/brief-export-leakage-diff.test.cjs 2>&1 | grep -E "fail|MODULE_NOT_FOUND" | head -3</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/brief-export-leakage-diff.test.cjs` exists with ≥ 8 `test(` invocations
    - 4 fixture files exist: 2 intentional-leak + 2 incidental-overlap
    - intentional-leak/internal-deck.md contains `audience.confidentiality: confidential` + ≥5 distinctive multi-word keywords
    - intentional-leak/proposal-deck.md contains `audience.confidentiality: partner` + the SAME ≥5 distinctive keywords (verifiable via `comm <(grep -oE "[a-z0-9-]{8,}" tests/fixtures/deliver/intentional-leak-pair/internal-deck.md | sort -u) <(grep -oE "[a-z0-9-]{8,}" tests/fixtures/deliver/intentional-leak-pair/proposal-deck.md | sort -u) -12 | wc -l` returns ≥ 5)
    - incidental-overlap/proposal-deck.md and internal-deck.md share NO multi-word distinctive keywords (only common stop-words)
    - `node --test tests/brief-export-leakage-diff.test.cjs` exits non-zero (Wave 0 RED)
    - `git log -1 --format=%s` matches `^test\\(08-03\\): add failing Wave 0 tests for leakage-diff.cjs`
  </acceptance_criteria>
  <done>1 RED test file + 4 fixture files committed; node --test exits non-zero with predictable failure</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — Implement leakage-diff.cjs with Hangul-aware tokenizer + stop-words + TF-IDF + finding emit</name>
  <files>brief/bin/lib/leakage-diff.cjs</files>
  <read_first>
    - .planning/phases/08-deliver-type-a-type-b-audience-enforcement-marp/08-RESEARCH.md (Pattern 5 lines 584-737 — copy implementation verbatim including STOP_WORDS_EN, STOP_WORDS_KO, tokenize, tfIdf, leakageDiff functions; Don't Hand-Roll lines 1067 — TF-IDF lib prohibition)
    - .planning/phases/08-deliver-type-a-type-b-audience-enforcement-marp/08-PATTERNS.md (lines 299-356 — leakage-diff.cjs analog + STRICTNESS map + ANTI-PATTERN FLAG no-runtime-dep)
    - brief/bin/lib/align.cjs (lines 97-127 — computeTermOverlap reference; replicate the Hangul + EN-≥4-letter regex VERBATIM)
    - brief/bin/lib/frontmatter.cjs (extractFrontmatter, stripFrontmatter signatures)
    - tests/brief-export-leakage-diff.test.cjs (the test contract — implementation must satisfy these tests)
  </read_first>
  <action>
Create `brief/bin/lib/leakage-diff.cjs` per RESEARCH.md Pattern 5 lines 593-725 verbatim. Required exports: `leakageDiff`, `tokenize`, `tfIdf`, `STRICTNESS`.

Imports:
```javascript
const fs = require('fs');
const path = require('path');
const { extractFrontmatter, stripFrontmatter } = require('./frontmatter.cjs');
```

Constants (verbatim from RESEARCH.md lines 612-642):
```javascript
const STRICTNESS = { public: 0, partner: 1, internal: 2, confidential: 3 };

const STOP_WORDS_EN = new Set([
  'about', 'above', 'after', 'again', 'against', 'because', 'before', 'between',
  'during', 'every', 'further', 'should', 'their', 'there', 'these', 'those',
  'through', 'while', 'which', 'with', 'would', 'product', 'service',
  'customer', 'market', 'company', 'business', 'team', 'value',
]);

const STOP_WORDS_KO = new Set([
  '있습니다', '입니다', '됩니다', '합니다', '우리는', '있는', '있고',
  '하는', '하고', '및', '또는', '이것', '저것',
  '고객', '시장', '회사', '사업', '팀', '가치', '서비스', '제품',
]);
```

Implement `tokenize(text)` per RESEARCH.md lines 617-627 verbatim — Hangul-aware. Returns array of lowercase EN ≥4-letter tokens + Hangul ≥2-char sequences.

Implement `tfIdf(text, corpus)` per RESEARCH.md lines 644-673 verbatim — classical Salton TF-IDF, returns top-20 tokens by score.

Implement `leakageDiff(currentArtifactPath, options)` per RESEARCH.md lines 675-723 verbatim:
1. Read current artifact + extract frontmatter `audience.confidentiality` (handle both flat-dotted and nested forms via `currentFm['audience.confidentiality'] || (currentFm.audience && currentFm.audience.confidentiality)`).
2. If no confidentiality field OR not in STRICTNESS enum → return `{ findings: [], rationale: 'current artifact missing confidentiality field — skipped' }`.
3. Enumerate same-folder `.md` siblings (filter out `.audience.md` and `.compliance.md` paired-siblings, filter out current artifact itself).
4. For each sibling: extract frontmatter; if sibling confidentiality is NOT stricter than current, skip.
5. For stricter sibling: extract top-20 distinctive keywords via tfIdf with corpus = [sibling-body, current-body].
6. Count keywords appearing in current body via `currentBody.toLowerCase().includes(kw) || currentBody.includes(kw)` (case-insensitive for EN, exact for Hangul which has no case).
7. If matched.length >= 3 → emit finding with `{ severity: 'material', location: '{basename}:body', description: 'Cross-artifact leakage suspected: stricter sibling \\'{sib}\\' (confidentiality: {sibConf}) shares distinctive keywords [{kw1, kw2, ..., kw8}] with this artifact (confidentiality: {currentConf}). Verify intent.' }`.
8. Return `{ findings: [...], rationale: 'scanned N sibling(s)' }`.

Module exports: `module.exports = { leakageDiff, tokenize, tfIdf, STRICTNESS };`

NO runtime dep added. Verify via `node -e "console.log(Object.keys(require('./package.json').dependencies||{}).length)"` returns 0.

Run `node --test tests/brief-export-leakage-diff.test.cjs` and assert all 8 tests pass GREEN.

Commit message: `feat(08-03): implement leakage-diff.cjs TF-IDF cross-artifact diff (DLV-06)`.
  </action>
  <verify>
    <automated>node --test tests/brief-export-leakage-diff.test.cjs 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - File `brief/bin/lib/leakage-diff.cjs` exists with `module.exports = { leakageDiff, tokenize, tfIdf, STRICTNESS };` (or equivalent named exports)
    - `grep -c "STRICTNESS" brief/bin/lib/leakage-diff.cjs` returns ≥ 2 (declaration + reference)
    - `grep -c "STOP_WORDS_EN\\|STOP_WORDS_KO" brief/bin/lib/leakage-diff.cjs` returns ≥ 2 (one each, declaration)
    - `grep -c "uac00-ud7af" brief/bin/lib/leakage-diff.cjs` returns ≥ 1 (Hangul tokenizer regex inherited from align.cjs)
    - `grep -c "audience.compliance.md\\|audience.md\\|.compliance.md\\|.audience.md" brief/bin/lib/leakage-diff.cjs` returns ≥ 2 (paired-sibling skip filter present — must reject these from sibling enumeration)
    - leakage-diff.cjs MUST NOT import any runtime dep: `grep -cE "require.*'(natural|tfidf|node-tfidf)'" brief/bin/lib/leakage-diff.cjs` returns 0
    - All 8 leakage-diff tests pass: `node --test tests/brief-export-leakage-diff.test.cjs` exits 0
    - A1 zero-runtime-deps preserved: `node -e "console.log(Object.keys(require('./package.json').dependencies||{}).length)"` returns 0
    - `git log -1 --format=%s` matches `^feat\\(08-03\\): implement leakage-diff.cjs`
  </acceptance_criteria>
  <done>leakage-diff.cjs ships with pure-CJS Hangul-aware TF-IDF; 8 Wave 0 RED→GREEN; A1 preserved</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User-controlled .planning/deliverables/**/*.md → leakage-diff.cjs | Sibling artifact bodies read for TF-IDF analysis; paths derived from fs.readdirSync of single folder |
| leakage-diff.cjs → finding emission | Findings written to STDOUT/return value; consumed by export.cjs Step 1 (Plan 04) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03-01 | Information Disclosure | Confidential strategy leakage from INTERNAL deck → PROPOSAL deck via copy-paste | mitigate | TF-IDF cross-artifact diff flags 3+ keyword matches against stricter sibling. FINDINGS-MATERIAL severity per C-D02 lock |
| T-08-03-02 | Denial of Service | TF-IDF false positive flood — every export blocked by spurious matches | mitigate | STOP_WORDS_EN + STOP_WORDS_KO filter ≥ 30 generic terms before TF-IDF scoring; idf factor naturally suppresses tokens present in ALL corpus docs (project name shared by every artifact → idf = log(N/N) = 0); 3-keyword threshold prevents single-token false positives. Canary fixture validates intentional-leak DETECTED + incidental-overlap NOT FLAGGED |
| T-08-03-03 | Tampering | Symlink in .planning/deliverables/type-b/ pointing to /etc/passwd | accept | leakage-diff.cjs reads only `.md` files via readdirSync filter; symlinks resolved by fs.readFileSync would surface as empty/binary content (TF-IDF still safe). User-responsibility for not symlinking sensitive paths into .planning/. Phase 9 HRD-04 pilot data may inform stricter scoping |
| T-08-03-04 | Information Disclosure | leakage-diff.cjs writes finding description containing distinctive keywords (which ARE the leaked secrets) to stdout/STATE.md | mitigate | findings.description includes only the FIRST 8 matched keywords (`matched.slice(0, 8)`) — bounded; written to .planning/ which is the user's own workspace. No external sink. CC-03 hook (Plan 07) prevents the finding file itself from being committed without proper frontmatter (audit-trail integrity) |
</threat_model>

<verification>
- 8 Wave 0 tests pass: `node --test tests/brief-export-leakage-diff.test.cjs` exits 0
- Hangul tokenizer regex matches align.cjs precedent verbatim: `grep -c "uac00-ud7af" brief/bin/lib/leakage-diff.cjs` returns ≥ 1
- Stop-words filter present (≥ 30 EN + ≥ 15 KO terms): word counts via `node -e "const m = require('./brief/bin/lib/leakage-diff.cjs'); /* internal stop-words via test harness */"` (or grep heuristic)
- Paired-sibling skip filter present (test passes — folder with .audience.md does not include it in scan)
- A1 zero-runtime-deps preserved
- No new gate added (leakage-diff.cjs is a parallel lib, NOT a 5th canonical gate)
</verification>

<success_criteria>
- TF-IDF detects intentional-leak fixture with ≥ 1 finding
- TF-IDF does NOT flag incidental-overlap fixture (0 findings)
- Hangul-aware tokenizer matches align.cjs computeTermOverlap precedent
- STOP_WORDS lists prevent generic-word false positives
- Paired-sibling .audience.md / .compliance.md correctly skipped from enumeration
- 8 Wave 0 RED→GREEN
- A1 zero-runtime-deps preserved
- Pure-CJS implementation (no `natural` / `tfidf` / `node-tfidf` runtime dep)
</success_criteria>

<output>
After completion, create `.planning/phases/08-deliver-type-a-type-b-audience-enforcement-marp/08-03-SUMMARY.md` documenting:
1. leakage-diff.cjs API surface (4 exports: leakageDiff, tokenize, tfIdf, STRICTNESS)
2. STRICTNESS ordering enum (public < partner < internal < confidential)
3. STOP_WORDS_EN + STOP_WORDS_KO final word counts
4. 2 fixture pairs documented (intentional-leak vs incidental-overlap) with the 5+ distinctive keywords listed
5. Test count + verify command + actual pass count
6. Files Plan 04 (export.cjs Step 1 wiring) consumes from this plan
</output>
</content>
</invoke>