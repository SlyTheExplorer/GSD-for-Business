---
phase: 07-design-workstream-orchestration-compliance-checker
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - agents/brief-compliance-checker.md
  - brief/workflows/compliance.md
  - brief/bin/lib/compliance.cjs
  - brief/bin/lib/compliance-report.cjs
  - brief/references/compliance-vocabulary.md
  - brief/bin/brief-tools.cjs
  - tests/brief-compliance-vocabulary-lock.test.cjs
  - tests/brief-compliance-verdict.test.cjs
  - tests/brief-compliance-sibling-path.test.cjs
  - tests/brief-compliance-merge-rule.test.cjs
  - tests/brief-compliance-canonical-shape.test.cjs
  - tests/brief-compliance-no-hooks.test.cjs
autonomous: true
requirements:
  - CC-01
must_haves:
  truths:
    - "agents/brief-compliance-checker.md exists with COMPLIANCE-OK/FINDINGS-MATERIAL/FINDINGS-BLOCKING verdict enum"
    - "brief/bin/lib/compliance.cjs exports same canonical names as audience.cjs (validateVerdict, grepBanList, runDeterministicScreen, mergeVerdicts, runCompliance, commitComplianceVerdict, writeVerdict, siblingReportPath re-export)"
    - "brief/bin/lib/compliance.cjs mergeVerdicts returns FINDINGS-MATERIAL for material-only findings (NOT collapsed to COMPLIANCE-OK like Phase 4/5)"
    - "brief/references/compliance-vocabulary.md ban-list contains 'compliant', 'passed', '✅', 'compliance verified', '준수', '통과', '위반' as forbidden tokens"
    - "brief/bin/brief-tools.cjs case 'compliance' dispatcher with run + commit subcommands present"
    - "Wave 0 test scaffolds exist (vocabulary-lock, verdict, sibling-path, merge-rule, canonical-shape, no-hooks)"
  artifacts:
    - path: "agents/brief-compliance-checker.md"
      provides: "COMPLIANCE checker subagent template (verdict enum, vocabulary discipline, decision mechanism, output contract, examples)"
      min_lines: 200
    - path: "brief/workflows/compliance.md"
      provides: "COMPLIANCE gate orchestrator workflow (Step 0/1/2/3/4/5/6 mirroring audience-guard.md)"
      min_lines: 250
    - path: "brief/bin/lib/compliance.cjs"
      provides: "COMPLIANCE primitives library — validators, deterministic screen, merge rule, runCompliance, commitComplianceVerdict"
      max_lines: 400
      exports: ["VALID_DECISIONS", "VALID_SEVERITIES", "BAN_EN", "BAN_KO", "BAN_SYMBOL", "validateVerdict", "grepBanList", "runDeterministicScreen", "writeVerdict", "mergeVerdicts", "runCompliance", "commitComplianceVerdict", "siblingReportPath", "detectKoreaSignalFromConfig"]
    - path: "brief/bin/lib/compliance-report.cjs"
      provides: "COMPLIANCE verdict report renderer with mandatory PIPA disclaimer footer"
      max_lines: 100
    - path: "brief/references/compliance-vocabulary.md"
      provides: "COMPLIANCE preferred-vocabulary + ban-list (Phase 7 extends Phase 5 audience-vocabulary)"
      min_lines: 80
  key_links:
    - from: "brief/bin/lib/compliance.cjs"
      to: "brief/bin/lib/audience.cjs"
      via: "siblingReportPath re-export import"
      pattern: "require\\('\\./audience\\.cjs'\\)"
    - from: "brief/bin/lib/compliance.cjs"
      to: "brief/bin/lib/context-inject.cjs"
      via: "buildBusinessContext for compliance_packs+region"
      pattern: "require\\('\\./context-inject\\.cjs'\\)"
    - from: "brief/bin/lib/compliance.cjs"
      to: "brief/bin/lib/align.cjs"
      via: "detectKoreaSignalFromConfig re-use"
      pattern: "detectKoreaSignalFromConfig"
    - from: "brief/bin/brief-tools.cjs"
      to: "brief/bin/lib/compliance.cjs"
      via: "case 'compliance' dispatcher"
      pattern: "case 'compliance':"
---

<objective>
Ship the COMPLIANCE checker triad — agent + workflow + lib + report renderer + vocabulary file + dispatcher case + Wave 0 test scaffolds. This is the THIRD instance of the canonical evaluator-optimizer gate pattern (after Phase 4 ALIGN and Phase 5 AUDIENCE; gap-detect is the fourth in spirit), implemented as a literal copy-rename of the Phase 5 AUDIENCE files with one load-bearing deviation: `mergeVerdicts` preserves a distinct `FINDINGS-MATERIAL` verdict (not collapsed to OK like Phase 4/5) because CC-01 mandates findings on every artifact.

Purpose: Phase 7 cannot ship `/brief-design` (Plan 03) until the COMPLIANCE workflow it threads exists. Plan 01 delivers the foundational gate primitives Wave 2+ depends on.

Output: `agents/brief-compliance-checker.md`, `brief/workflows/compliance.md`, `brief/bin/lib/compliance.cjs`, `brief/bin/lib/compliance-report.cjs`, `brief/references/compliance-vocabulary.md`, dispatcher case in `brief-tools.cjs`, 6 Wave 0 test scaffolds.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-design-workstream-orchestration-compliance-checker/07-CONTEXT.md
@.planning/phases/07-design-workstream-orchestration-compliance-checker/07-RESEARCH.md
@.planning/phases/07-design-workstream-orchestration-compliance-checker/07-PATTERNS.md
@.planning/phases/07-design-workstream-orchestration-compliance-checker/07-VALIDATION.md

<interfaces>
<!-- Canonical gate primitives that Plan 01 mirrors. Extracted from Phase 4/5 codebase. -->
<!-- Executor MUST read audience.cjs + audience-guard.md + brief-audience-guard.md as the literal copy-rename source. -->

From brief/bin/lib/audience.cjs (440 lines — copy-rename target):
```javascript
const VALID_DECISIONS = new Set(['AUDIENCE-OK', 'DRIFTED-frontmatter', 'DRIFTED-content']);
const VALID_SEVERITIES = new Set(['blocking', 'material', 'nice-to-have']);
const BAN_EN = /\b(compliant|passed|violation|failed)\b/gi;
const BAN_KO = /(준수|통과|위반|실패)/g;
const BAN_SYMBOL = /[✅✓✗]/g;

function _siblingReportPath(artifactAbsPath, suffix) { ... }
function validateVerdict(v) { ... }
function grepBanList(text) { ... }
function runDeterministicScreen({artifact, baseline, businessContext}) { ... } // 3 sub-screens
function writeVerdict(verdictPath, verdictObj) { ... }
function mergeVerdicts(detFindings, llmVerdict) { ... } // ← Phase 7 DEVIATES here
async function runAudience(opts) { ... }
async function commitAudienceVerdict(opts) { ... }

module.exports = { VALID_DECISIONS, VALID_SEVERITIES, BAN_EN, BAN_KO, BAN_SYMBOL,
  validateVerdict, grepBanList, runDeterministicScreen, writeVerdict, mergeVerdicts,
  runAudience, commitAudienceVerdict, siblingReportPath: _siblingReportPath,
  detectKoreaSignalFromConfig };
```

From brief/bin/lib/audience-report.cjs (67 lines — copy-rename target):
```javascript
function renderAudienceReport(verdict, opts) { ... } // returns frontmatter + findings + rationale + optional override
module.exports = { renderAudienceReport };
```

From brief/bin/brief-tools.cjs (lines 558-635 — case 'audience' dispatcher pattern):
```javascript
case 'audience': {
  const audience = require('./lib/audience.cjs');
  const audSubcommand = args[1];
  // run / commit subcommands
}
```

From brief/references/compliance/korea/pipa-2026.md (Phase 5 — verbatim disclaimer source):
```
> 본 분석은 법적 자문이 아닙니다. ... 2026년 개정 개인정보 보호법(PIPA, 2026-09-11 시행)에 따라
> 위반 시 대표이사 개인 책임이 발생할 수 있으며 과징금 상한은 총매출의 10%입니다. ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Ship COMPLIANCE vocabulary + Wave 0 test scaffolds (vocabulary-lock + canonical-shape + no-hooks)</name>
  <files>
    brief/references/compliance-vocabulary.md (NEW — copy-rename of audience-vocabulary.md);
    tests/brief-compliance-vocabulary-lock.test.cjs (NEW — Wave 0);
    tests/brief-compliance-canonical-shape.test.cjs (NEW — Wave 0);
    tests/brief-compliance-no-hooks.test.cjs (NEW — Wave 0)
  </files>
  <read_first>
    brief/references/audience-vocabulary.md (literal copy-rename source — 99 lines);
    tests/brief-audience-vocabulary-lock.test.cjs (test pattern source);
    .planning/phases/07-design-workstream-orchestration-compliance-checker/07-RESEARCH.md (lines 152-191 — vocabulary ban-list spec; lines 282-309 — structural identity test spec);
    .planning/phases/07-design-workstream-orchestration-compliance-checker/07-PATTERNS.md (lines 437-477 — compliance-vocabulary deltas)
  </read_first>
  <behavior>
    - Test 1 (vocabulary-lock): grep audit on agents/brief-compliance-checker.md + brief/workflows/compliance.md + brief/bin/lib/compliance.cjs + brief/bin/lib/compliance-report.cjs + brief/references/compliance-vocabulary.md (outside the explicit `## Ban-list` heading section) returns ZERO matches for: `\bcompliant\b`, `\bpassed\b`, `compliance verified`, `audit complete`, `compliance OK`, `\b준수\b`, `\b통과\b`, `\b위반\b`, `감사 완료`, `컴플라이언스 확인 완료`, and `[✅✓✗]`
    - Test 2 (canonical-shape): assert agents/brief-compliance-checker.md contains the H2/XML sections `<role>`, `<required_reading>`, `<vocabulary_discipline>`, `<decision_mechanism>`, `<output_contract>`, `<process>`, `<examples>` — same shape as agents/brief-audience-guard.md
    - Test 3 (no-hooks): `! grep -r "compliance-checker\|brief-compliance-checker\|compliance_checker" hooks/` AND `! grep -r "brief/workflows/compliance\b" hooks/` (zero matches expected)
  </behavior>
  <action>
    1. Copy `brief/references/audience-vocabulary.md` → `brief/references/compliance-vocabulary.md`. Apply mechanical swaps:
       - Title: `# AUDIENCE Vocabulary` → `# COMPLIANCE Vocabulary`
       - "Phase 4 ALIGN" → "Phase 5 AUDIENCE" reference text becomes "Phase 5 AUDIENCE + Phase 4 ALIGN"
       - Loaded-by reference: `agents/brief-audience-guard.md` → `agents/brief-compliance-checker.md`
       - Test reference: `tests/brief-audience-vocabulary-lock.test.cjs` → `tests/brief-compliance-vocabulary-lock.test.cjs`
    2. Rewrite `## Preferred vocabulary (KO)` block to verbatim from 07-RESEARCH.md lines 176-181:
       ```
       - 문서화된 의무 사항 중 반영된 것: ...
       - 추가 작업이 필요한 의무 사항: ...
       - BRIEF로 확인할 수 없는 의무 사항 (자격 있는 한국 변호사 검토 필요): ...
       - 규정 조항: ... | 필요 증거: ... | artifact 내 위치: ... | 공백: ...
       - 2026년 개정 개인정보 보호법(PIPA)에 따라 위반 시 대표이사 개인 책임이 발생할 수 있으며, 과징금 상한은 총매출의 10%입니다.
       ```
    3. Rewrite `## Preferred vocabulary (EN)` from 07-RESEARCH.md lines 184-188.
    4. Extend `## Ban-list (EN)` adding: `compliant`, `passed`, `compliance verified`, `audit complete`, `compliance OK` (with inline note "gate-internal `COMPLIANCE-OK` enum string is fine"), `all clear`, `no issues`.
    5. Extend `## Ban-list (KO)` adding: `준수` (verdict-language only), `통과`, `위반` (prefer "추가 작업이 필요한 항목" or "법적 자문 필요 항목"), `감사 완료`, `컴플라이언스 확인 완료`.
    6. Inherit `## Ban-list (symbols)` (✅ ✓ ✗) verbatim.
    7. DELETE the `## Hedging vocabulary (EN)` and `## Hedging vocabulary (KO)` sections — audience-specific, not relevant to compliance.
    8. ADD section `## Clause-level findings format (CC-01 contract)` documenting the optional findings extension fields: `regulation_clause | required_evidence | found_in_artifact | gap` (per CC-01).
    9. Create `tests/brief-compliance-vocabulary-lock.test.cjs` — copy-rename of tests/brief-audience-vocabulary-lock.test.cjs. Update grep patterns to the Phase 7 ban-list. Audit globs: `agents/brief-compliance-checker.md`, `brief/workflows/compliance.md`, `brief/bin/lib/compliance.cjs`, `brief/bin/lib/compliance-report.cjs`, `brief/references/compliance-vocabulary.md` (excluding lines under `## Ban-list` headings).
    10. Create `tests/brief-compliance-canonical-shape.test.cjs` — assert the 7 XML/H2 sections exist in `agents/brief-compliance-checker.md` (test will initially fail/pending until Task 3 lands the file; mark as Wave 0 scaffold per VALIDATION.md row 07-01-01).
    11. Create `tests/brief-compliance-no-hooks.test.cjs` — `t.test('hooks/ directory contains no compliance references', ...)` running 2 grep audits via `child_process.execSync` and asserting zero matches.
  </action>
  <verify>
    <automated>node --test tests/brief-compliance-vocabulary-lock.test.cjs tests/brief-compliance-no-hooks.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    - File `brief/references/compliance-vocabulary.md` exists, contains `# COMPLIANCE Vocabulary` H1, `## Ban-list (EN)`, `## Ban-list (KO)`, `## Ban-list (symbols)`, `## Clause-level findings format (CC-01 contract)` headings
    - `grep -c '^- ' brief/references/compliance-vocabulary.md` returns >= 30 (preferred + ban-list entries)
    - `grep -c 'Hedging vocabulary' brief/references/compliance-vocabulary.md` returns 0 (sections deleted)
    - File `tests/brief-compliance-vocabulary-lock.test.cjs` exists, contains `BAN_LIST_EN = [` array with at least: compliant, passed, "compliance verified", "audit complete", "compliance OK"
    - File `tests/brief-compliance-canonical-shape.test.cjs` exists with assertions for 7 sections
    - File `tests/brief-compliance-no-hooks.test.cjs` exists with 2 grep audits
    - `node --test tests/brief-compliance-no-hooks.test.cjs` exits 0 (no hooks references — current state)
    - `node --test tests/brief-compliance-vocabulary-lock.test.cjs` exits 0 (vocabulary file passes its own audit)
  </acceptance_criteria>
  <done>Vocabulary file + 3 Wave 0 test scaffolds (vocabulary-lock + canonical-shape pending + no-hooks) committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Ship compliance.cjs + compliance-report.cjs lib + Wave 0 verdict/sibling-path/merge-rule tests</name>
  <files>
    brief/bin/lib/compliance.cjs (NEW — copy-rename of audience.cjs);
    brief/bin/lib/compliance-report.cjs (NEW — copy-rename of audience-report.cjs);
    tests/brief-compliance-verdict.test.cjs (NEW — Wave 0);
    tests/brief-compliance-sibling-path.test.cjs (NEW — Wave 0);
    tests/brief-compliance-merge-rule.test.cjs (NEW — Wave 0)
  </files>
  <read_first>
    brief/bin/lib/audience.cjs (literal copy-rename source — 441 lines);
    brief/bin/lib/audience-report.cjs (literal copy-rename source — 67 lines);
    brief/bin/lib/align.cjs (read detectKoreaSignalFromConfig export);
    brief/bin/lib/context-inject.cjs (read buildBusinessContext signature);
    brief/bin/lib/gap-detect.cjs (read line 25: `const { siblingReportPath } = require('./audience.cjs');` — same import pattern compliance.cjs uses);
    brief/references/compliance/korea/pipa-2026.md (verbatim disclaimer source — Phase 5 inheritance);
    .planning/phases/07-design-workstream-orchestration-compliance-checker/07-RESEARCH.md (lines 193-235 — deterministic screen + mergeVerdicts deviation);
    .planning/phases/07-design-workstream-orchestration-compliance-checker/07-PATTERNS.md (lines 237-433 — compliance.cjs + compliance-report.cjs deltas verbatim)
  </read_first>
  <behavior>
    - Test 1 (verdict): assert validateVerdict accepts COMPLIANCE-OK/FINDINGS-MATERIAL/FINDINGS-BLOCKING and rejects AUDIENCE-OK/DRIFTED-* (verdict enum literally swapped); accepts findings with optional regulation_clause/required_evidence/found_in_artifact/gap fields
    - Test 2 (sibling-path): assert `siblingReportPath('/x/canvas.md', 'compliance')` returns `/x/canvas.compliance.md`; assert it is the SAME function exported from audience.cjs (no duplication — gap-detect.cjs precedent)
    - Test 3 (merge-rule LOAD-BEARING DEVIATION): given findings=[{severity:'material', ...}], LLM verdict null → mergeVerdicts returns `{decision: 'FINDINGS-MATERIAL', severity: 'material', ...}` — NOT 'COMPLIANCE-OK'. Given findings=[{severity:'nice-to-have', ...}] only → returns 'COMPLIANCE-OK'. Given any blocking → 'FINDINGS-BLOCKING'.
  </behavior>
  <action>
    1. Copy `brief/bin/lib/audience.cjs` → `brief/bin/lib/compliance.cjs`. Apply these mechanical swaps:
       - File header comment: `Audience — AUDIENCE gate primitives (Plan 05-04...)` → `Compliance — COMPLIANCE gate primitives (Plan 07-01, third canonical instance after Phase 4 ALIGN + Phase 5 AUDIENCE). Duplicate-renamed from audience.cjs per 07-PATTERNS.md. Phase 7 D-01 lock: 3-output verdict COMPLIANCE-OK / FINDINGS-MATERIAL / FINDINGS-BLOCKING. LOAD-BEARING DEVIATION in mergeVerdicts: material findings yield FINDINGS-MATERIAL (NOT collapsed to OK).`
       - Replace `const { renderAudienceReport } = require('./audience-report.cjs');` → `const { renderComplianceReport } = require('./compliance-report.cjs');`
       - REPLACE `function _siblingReportPath(...)` definition with `const { siblingReportPath: _siblingReportPath } = require('./audience.cjs');` (re-use the canonical helper — gap-detect.cjs precedent)
       - ADD import after security.cjs: `const { buildBusinessContext } = require('./context-inject.cjs');` (load-bearing — deterministic screen reads compliance_packs from business_context)
       - `VALID_DECISIONS`: `new Set(['AUDIENCE-OK', 'DRIFTED-frontmatter', 'DRIFTED-content'])` → `new Set(['COMPLIANCE-OK', 'FINDINGS-MATERIAL', 'FINDINGS-BLOCKING'])`
       - `BAN_EN`: extend regex to `/\b(compliant|passed|violation|failed|compliance verified|audit complete)\b/gi` (case-insensitive `i` flag preserved)
       - `BAN_KO`: extend to `/(준수|통과|위반|실패|감사 완료|컴플라이언스 확인 완료)/g`
       - REMOVE constants `MANDATORY_FRONTMATTER_FIELDS`, `AUDIENCE_TYPE_ENUM`, `CONFIDENTIALITY_ENUM`, `BUSINESS_MODEL_ENUM`, `HEDGING_EN`, `HEDGING_KO` (audience-specific; remove the related branches in runDeterministicScreen too)
       - `validateVerdict`: extend findings shape to ACCEPT optional `regulation_clause`/`required_evidence`/`found_in_artifact`/`gap` string fields when present (do NOT require them; they're CC-01 extensions). Validate type-string when present.
       - `runDeterministicScreen`: REPLACE the audience-specific 3 sub-screens with the COMPLIANCE 3 sub-screens per 07-RESEARCH.md lines 193-201:
         (a) Pack-applicability: read `compliance_packs` from `businessContext` arg; if empty AND no Korea signal AND no Korean prose → return `{verdict: {decision: 'COMPLIANCE-OK', severity: 'nice-to-have', findings_count: 1, findings: [{severity: 'nice-to-have', location: 'baseline', description: 'no applicable compliance packs declared; gate ran in pass-through mode'}], rationale: 'Pack-applicability pass-through (Screen a)'}, short_circuited: true}`
         (b) PIPA hard-required-evidence: when packs include 'PIPA' AND artifact body matches `/(PII|personal information|customer data|biometric|location data|sensitive data|개인정보|위치정보|민감정보)/i` AND artifact body lacks `/(CPO|개인정보보호책임자|breach notification|침해 통지|consent|동의)/i` → return blocking finding with `regulation_clause: 'PIPA Art. 28-8'`, `required_evidence: 'CPO appointment + consent management evidence'`, short_circuit `FINDINGS-BLOCKING`
         (c) Ban-list grep: same pattern as audience.cjs `grepBanList`; additive material findings (do NOT short-circuit)
       - `mergeVerdicts` LOAD-BEARING DEVIATION (per 07-RESEARCH.md lines 224-235; 07-PATTERNS.md lines 347-354):
         ```javascript
         // Phase 7 DEVIATION from Phase 4/5 collapse rule. CC-01 mandates findings on
         // every artifact regardless of severity; we preserve a distinct FINDINGS-MATERIAL
         // verdict so the legal-counsel disclaimer renders even on non-blocking gaps.
         let decision;
         if (maxSev === 'blocking') decision = 'FINDINGS-BLOCKING';
         else if (maxSev === 'material') decision = 'FINDINGS-MATERIAL';
         else decision = 'COMPLIANCE-OK';
         ```
       - Rename `runAudience` → `runCompliance`. Pass `businessContext` through opts to `runDeterministicScreen`.
       - Rename `commitAudienceVerdict` → `commitComplianceVerdict`:
         - Sibling path: `_siblingReportPath(artifactPath, 'compliance')`
         - State write: `fm.brief.last_gate_results.compliance = { decision, severity, findings_count, at, ...override }`
         - Render call: `renderComplianceReport(verdict, { korea, override, overrideReason, packs })`
         - Override-decision mapping: `override ? 'COMPLIANCE-OK' : verdict.decision`
       - `module.exports`: re-export the same 13 names from analog, swapped: `{ VALID_DECISIONS, VALID_SEVERITIES, BAN_EN, BAN_KO, BAN_SYMBOL, validateVerdict, grepBanList, runDeterministicScreen, writeVerdict, mergeVerdicts, runCompliance, commitComplianceVerdict, siblingReportPath: _siblingReportPath, detectKoreaSignalFromConfig }`
    2. Copy `brief/bin/lib/audience-report.cjs` → `brief/bin/lib/compliance-report.cjs`. Apply:
       - Function rename `renderAudienceReport` → `renderComplianceReport`
       - `opts` accepts new `packs: string[]` parameter
       - Frontmatter `decision` override: `'AUDIENCE-OK'` → `'COMPLIANCE-OK'`
       - ADD body sections (Korean when `korea: true`):
         `## Documented obligations addressed` / `## 문서화된 의무 사항 중 반영된 것` (group findings.severity === 'nice-to-have' here)
         `## Obligations needing further work` / `## 추가 작업이 필요한 의무 사항` (group findings.severity === 'material' here)
         `## Obligations BRIEF cannot verify (requires qualified Korean counsel)` / `## BRIEF로 확인할 수 없는 의무 사항 (자격 있는 한국 변호사 검토 필요)` (group findings.severity === 'blocking' here)
       - Findings render — when `regulation_clause` / `required_evidence` / `found_in_artifact` / `gap` are present, render 4 indented sub-bullets (Korean labels: `규정 조항:` / `필요 증거:` / `artifact 내 위치:` / `공백:`; English labels: `Regulation clause:` / `Required evidence:` / `Found in artifact:` / `Gap:`)
       - LOAD-BEARING ADDITION: ALWAYS render mandatory disclaimer footer at end. Korean (when `korea: true`):
         ```
         > 본 분석은 법적 자문이 아닙니다. Findings는 자격 있는 한국 법률 자문가와 검토하기 위한 출발점입니다.
         > 
         > 2026년 개정 개인정보 보호법(PIPA, 2026-09-11 시행)에 따라, 위반 시 대표이사 개인 책임이 발생할 수 있으며 과징금 상한은 총매출의 10%입니다. 본 findings는 자격 있는 한국 법률 자문가와 검토하기 위한 출발점이며, 법적 자문을 대체하지 않습니다.
         ```
         English (otherwise):
         ```
         > Not legal advice. Refer to qualified Korean counsel before acting on findings.
         > 
         > Under 2026 PIPA amendments (effective 2026-09-11), breaches may result in personal liability for the CEO and administrative fines up to 10% of total turnover. Findings here are starting points for review with qualified Korean counsel — they are not legal advice.
         ```
       - Module export: `module.exports = { renderComplianceReport };`
    3. Create `tests/brief-compliance-verdict.test.cjs` with assertions:
       - `validateVerdict({decision:'COMPLIANCE-OK',severity:'nice-to-have',findings_count:0,findings:[],rationale:'ok'})` returns null
       - `validateVerdict({decision:'AUDIENCE-OK',...})` returns non-null error string (verdict swap)
       - `validateVerdict({decision:'FINDINGS-MATERIAL',severity:'material',findings_count:1,findings:[{severity:'material',location:'L:1',description:'gap',regulation_clause:'PIPA Art. 28-8'}],rationale:'r'})` returns null (clause-level extension accepted)
    4. Create `tests/brief-compliance-sibling-path.test.cjs`:
       - `siblingReportPath('/tmp/canvas.md','compliance')` === `'/tmp/canvas.compliance.md'`
       - `require('./brief/bin/lib/compliance.cjs').siblingReportPath` === `require('./brief/bin/lib/audience.cjs').siblingReportPath` (function-identity check via Function.toString or at minimum reference equality)
    5. Create `tests/brief-compliance-merge-rule.test.cjs` (LOAD-BEARING DEVIATION test):
       - `mergeVerdicts([{severity:'material',location:'l',description:'d'}], null)` → `{decision:'FINDINGS-MATERIAL',severity:'material',...}` (NOT 'COMPLIANCE-OK')
       - `mergeVerdicts([{severity:'nice-to-have',location:'l',description:'d'}], null)` → `{decision:'COMPLIANCE-OK','severity':'nice-to-have',...}`
       - `mergeVerdicts([{severity:'blocking',location:'l',description:'d'}], null)` → `{decision:'FINDINGS-BLOCKING','severity':'blocking',...}`
       - Comment block at top of test cites 07-RESEARCH.md lines 224-235 as the deviation rationale.
  </action>
  <verify>
    <automated>node --test tests/brief-compliance-verdict.test.cjs tests/brief-compliance-sibling-path.test.cjs tests/brief-compliance-merge-rule.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    - File `brief/bin/lib/compliance.cjs` exists, line count <= 400 (Phase 2 D-18); `wc -l brief/bin/lib/compliance.cjs` outputs ≤ 400
    - File `brief/bin/lib/compliance-report.cjs` exists, line count <= 100
    - `node -e "console.log(Object.keys(require('./brief/bin/lib/compliance.cjs')).sort().join(','))"` includes literal substrings: `BAN_EN,BAN_KO,BAN_SYMBOL,VALID_DECISIONS,VALID_SEVERITIES,commitComplianceVerdict,detectKoreaSignalFromConfig,grepBanList,mergeVerdicts,runCompliance,runDeterministicScreen,siblingReportPath,validateVerdict,writeVerdict`
    - `grep -c "FINDINGS-MATERIAL" brief/bin/lib/compliance.cjs` returns >= 2 (mergeVerdicts + VALID_DECISIONS)
    - `grep -c "AUDIENCE-OK\|DRIFTED-frontmatter\|DRIFTED-content" brief/bin/lib/compliance.cjs` returns 0 (no leftover audience enums)
    - `grep "총매출의 10%" brief/bin/lib/compliance-report.cjs` returns 1 match (Korean disclaimer)
    - `grep "10% of total turnover" brief/bin/lib/compliance-report.cjs` returns 1 match (English disclaimer)
    - All 3 new tests pass: `node --test tests/brief-compliance-verdict.test.cjs tests/brief-compliance-sibling-path.test.cjs tests/brief-compliance-merge-rule.test.cjs` exits 0
  </acceptance_criteria>
  <done>compliance.cjs + compliance-report.cjs lib pair shipped; 3 Wave 0 unit tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Ship brief-compliance-checker.md agent + brief/workflows/compliance.md workflow + brief-tools.cjs case 'compliance' dispatcher</name>
  <files>
    agents/brief-compliance-checker.md (NEW — copy-rename of agents/brief-audience-guard.md);
    brief/workflows/compliance.md (NEW — copy-rename of brief/workflows/audience-guard.md);
    brief/bin/brief-tools.cjs (MODIFIED — register case 'compliance')
  </files>
  <read_first>
    agents/brief-audience-guard.md (literal copy-rename source — 286 lines);
    brief/workflows/audience-guard.md (literal copy-rename source — 395 lines);
    brief/bin/brief-tools.cjs (read existing case 'audience' at lines ~558-635 — copy-rename source);
    brief/references/compliance-vocabulary.md (Task 1 output — required_reading reference);
    .planning/phases/07-design-workstream-orchestration-compliance-checker/07-PATTERNS.md (lines 68-234 — agent + workflow deltas verbatim)
  </read_first>
  <behavior>
    - Agent file: assert all 7 H2/XML sections from canonical-shape test pass; assert verdict enum is COMPLIANCE-OK/FINDINGS-MATERIAL/FINDINGS-BLOCKING (not AUDIENCE-OK/DRIFTED-*); assert required_reading lists `brief/references/compliance-vocabulary.md` and the 3 Korea primers
    - Workflow file: assert Step 0 TEXT_MODE detection, Step 1 deterministic screen invoking `compliance run`, Step 2 LLM Task spawn, Step 3 verdict routing (COMPLIANCE-OK → Step 4; FINDINGS-MATERIAL → Step 4 NO interrupt; FINDINGS-BLOCKING → Step 5), Step 4 atomic commit invoking `compliance commit`, Step 5 3-path interrupt, Step 6 force-accept; presence of `<no_hooks_assertion>` and `<command_surface_assertion>` blocks
    - Dispatcher: `node brief/bin/brief-tools.cjs compliance run --artifact X --baseline Y --verdict-out Z` parses without "unknown command" error
  </behavior>
  <action>
    1. Copy `agents/brief-audience-guard.md` → `agents/brief-compliance-checker.md`. Apply:
       - Frontmatter `name: brief-audience-guard` → `brief-compliance-checker`
       - Frontmatter `description`: rewrite to "Evaluates region+industry-aware regulatory compliance of a candidate artifact against loaded compliance packs (PIPA / ISMS-P / MyData) and OBJECTIVES.md compliance_packs. Emits a structured verdict JSON with a three-output decision (COMPLIANCE-OK / FINDINGS-MATERIAL / FINDINGS-BLOCKING) and clause-level findings (regulation_clause + required_evidence + found_in_artifact + gap). Mandatory legal-counsel disclaimer rendered in every emitted report. Read-only — never mutates the artifact or baseline. Spawned by brief/workflows/compliance.md via Task."
       - Frontmatter `color: purple` → `color: red` (distinct from align green/audience purple/gap-detect red — pick a non-collision color; verify via `grep "^color:" agents/*.md`. If `red` collides, use `orange`)
       - `<required_reading>` block: ADD lines:
         - `brief/references/compliance/korea/pipa-2026.md` (CONDITIONAL — load when state.brief.compliance_packs includes 'PIPA')
         - `brief/references/compliance/korea/isms-p.md` (CONDITIONAL — load when packs includes 'ISMS-P')
         - `brief/references/compliance/korea/mydata-2026.md` (CONDITIONAL — load when packs includes 'MyData')
         - SWAP `brief/references/audience-vocabulary.md` → `brief/references/compliance-vocabulary.md`
       - `<vocabulary_discipline>` block: replace ban-list with the Phase 7 ban-list from compliance-vocabulary.md; preserve "ban tokens permitted ONLY inside this block" disclaimer
       - `<decision_mechanism>` block: replace verdict enum with COMPLIANCE-OK/FINDINGS-MATERIAL/FINDINGS-BLOCKING + decision derivation per 07-RESEARCH.md lines 224-232 (the deviation: material → FINDINGS-MATERIAL, NOT OK)
       - `<output_contract>` block: extend findings JSON schema with optional `regulation_clause`, `required_evidence`, `found_in_artifact`, `gap` string fields per CC-01
       - `<examples>` block: rewrite — provide 3 examples:
         (1) Korean B2C fintech artifact mentioning 개인정보 + 결제 → emit FINDINGS-BLOCKING with PIPA Art. 28-8 clause-level finding
         (2) Korean enterprise B2B SaaS infrastructure (no consumer data) → emit COMPLIANCE-OK pack-applicability pass-through (Screen a)
         (3) Generic non-Korean artifact + empty compliance_packs → COMPLIANCE-OK pass-through with single nice-to-have advisory finding
       - VERIFY zero ban-list tokens leak outside `<vocabulary_discipline>` block
    2. Copy `brief/workflows/audience-guard.md` → `brief/workflows/compliance.md`. Apply:
       - All `audience` → `compliance` (case-sensitive in code; case-insensitive in prose)
       - `audience-verdict.tmp.json` → `compliance-verdict.tmp.json`
       - `audience.md` paired-sibling → `compliance.md`
       - `AUDIENCE-OK / DRIFTED-frontmatter / DRIFTED-content` → `COMPLIANCE-OK / FINDINGS-MATERIAL / FINDINGS-BLOCKING`
       - Step 3 routing CHANGE: `COMPLIANCE-OK → Step 4 commit`, `FINDINGS-MATERIAL → Step 4 commit (NO interrupt — proceed with material findings recorded)`, `FINDINGS-BLOCKING → Step 5 (3-path interrupt)`. CRITICAL: do NOT interrupt on FINDINGS-MATERIAL.
       - Step 5 collapse: COMPLIANCE has only ONE BLOCKING-routable verdict, so previous Step 5A/5B (frontmatter/content variants) collapse into a single Step 5. 3-path options (Korean text per 07-RESEARCH.md lines 240-252):
         `1. artifact 다시 쓰기 (re-spawn worker / 수동 편집)` / `2. OBJECTIVES 수정하기 (compliance_packs 또는 region 변경 필요)` / `3. 현재 상태 승인, 계속 진행 (force-accept) — 자격 있는 한국 변호사와 검토를 권장합니다`
       - Step 6 force-accept: append the verbatim PIPA disclaimer text to the override-acceptance message; Korean version when `region: kr` (verbatim from 07-RESEARCH.md lines 1217-1223)
       - Step 1 deterministic screen sub-screens (a/b/c) per 07-RESEARCH.md lines 193-201
       - Step 2 LLM pass: ADD `<business_context>...</business_context>` injection from `context-inject.cjs buildBusinessContext()` (load-bearing — PIPA scoping)
       - `<no_hooks_assertion>` block: copy-rename — Phase 7 ships `tests/brief-compliance-no-hooks.test.cjs`
       - `<command_surface_assertion>` block: extend FORBIDDEN list with `commands/brief/recompliance.md`, `commands/brief/realign-workstream.md`, `commands/brief/design-all.md`, `commands/brief/refinancial.md`, `commands/brief/recompliance-pack.md`, `commands/brief/redesign.md`
    3. Modify `brief/bin/brief-tools.cjs` to register `case 'compliance':` (after `case 'audience':`, before `case 'gap-detect':`). Verbatim copy-rename of the existing `case 'audience':` block (lines ~558-635) with these swaps:
       - `audience` → `compliance` throughout
       - `require('./lib/audience.cjs')` → `require('./lib/compliance.cjs')`
       - Subcommands: `run` and `commit` (same surface as audience)
       - Error string: `error('compliance: unknown subcommand …. Valid: run, commit')`
    4. Update `tests/brief-compliance-canonical-shape.test.cjs` (Wave 0 scaffold from Task 1) to PASSING state — agent file now exists with the 7 sections; tests should be green.
    5. Update `tests/brief-compliance-vocabulary-lock.test.cjs` audit globs — should still pass (agent + workflow + lib + report + vocabulary all clean of ban tokens outside their explicit ban-list sections).
  </action>
  <verify>
    <automated>node --test tests/brief-compliance-canonical-shape.test.cjs tests/brief-compliance-vocabulary-lock.test.cjs tests/brief-compliance-no-hooks.test.cjs && node brief/bin/brief-tools.cjs compliance 2>&1 | grep -q "unknown subcommand"</automated>
  </verify>
  <acceptance_criteria>
    - File `agents/brief-compliance-checker.md` exists; `grep -E '^---$' agents/brief-compliance-checker.md | wc -l` returns >= 2 (frontmatter delimiters)
    - `grep -c "name: brief-compliance-checker" agents/brief-compliance-checker.md` returns 1
    - `grep -c "COMPLIANCE-OK\|FINDINGS-MATERIAL\|FINDINGS-BLOCKING" agents/brief-compliance-checker.md` returns >= 6 (decision mechanism + output contract + examples)
    - `grep -c "AUDIENCE-OK\|DRIFTED-frontmatter\|DRIFTED-content" agents/brief-compliance-checker.md` returns 0 (no audience enum leakage)
    - `grep -c "regulation_clause" agents/brief-compliance-checker.md` returns >= 2 (output contract + example)
    - File `brief/workflows/compliance.md` exists; `grep -c "compliance run\|compliance commit" brief/workflows/compliance.md` returns >= 2
    - `grep -c "FINDINGS-MATERIAL.*Step 4\|Step 4.*FINDINGS-MATERIAL" brief/workflows/compliance.md` returns >= 1 (NO-interrupt routing)
    - `grep -c "<no_hooks_assertion>\|<command_surface_assertion>" brief/workflows/compliance.md` returns 2
    - `grep -c "commands/brief/recompliance.md" brief/workflows/compliance.md` returns >= 1 (FORBIDDEN list extension)
    - `grep -c "case 'compliance':" brief/bin/brief-tools.cjs` returns 1
    - `node brief/bin/brief-tools.cjs compliance 2>&1 | grep -c "unknown subcommand"` returns 1 (dispatcher routes; needs subcommand)
    - `node brief/bin/brief-tools.cjs compliance run --help 2>&1 | head -1` does NOT contain "unrecognized command"
    - All Wave 0 tests pass: `node --test tests/brief-compliance-{canonical-shape,vocabulary-lock,no-hooks,verdict,sibling-path,merge-rule}.test.cjs` exits 0
  </acceptance_criteria>
  <done>Agent + workflow + dispatcher case shipped; canonical shape + no-hooks + vocabulary-lock + verdict + sibling-path + merge-rule tests all green. COMPLIANCE checker triad complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → /brief-design CLI | User-supplied workstream slug + arguments cross into BRIEF orchestration |
| LLM Task spawn → workflow | LLM-generated verdict JSON crosses into compliance.cjs validateVerdict |
| compliance.cjs → STATE.md | Verdict object writes through readModifyWriteStateMd; sanitize override_reason |
| compliance-report.cjs → emitted .compliance.md | Report rendering inserts user-facing prose; ban-list discipline applies |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | brief/references/compliance-vocabulary.md | mitigate | Vocabulary-lock test (Task 1) audits the file itself + every consumer (compliance.md / agent / lib / report). `tests/brief-compliance-vocabulary-lock.test.cjs` enforces zero "compliant"/"passed"/"✅" outside `## Ban-list` headings. |
| T-07-02 | Tampering | compliance.cjs VALID_DECISIONS enum | mitigate | `tests/brief-compliance-verdict.test.cjs` asserts AUDIENCE-OK is REJECTED; only COMPLIANCE-OK/FINDINGS-MATERIAL/FINDINGS-BLOCKING accepted. validateVerdict pure function; no I/O. |
| T-07-03 | Tampering | compliance.cjs siblingReportPath | mitigate | `tests/brief-compliance-sibling-path.test.cjs` asserts function-identity with audience.cjs export (no fork; literal re-export per gap-detect.cjs precedent). |
| T-07-26 | Tampering | hooks/* | mitigate | `tests/brief-compliance-no-hooks.test.cjs` runs 2 grep audits in CI on every commit. Anti-pattern #2 enforced structurally — COMPLIANCE is orchestrator step only. |
</threat_model>

<verification>
- All 3 lib + agent + workflow files committed buildable (each task independently passes its tests)
- `node --test tests/brief-compliance-{vocabulary-lock,canonical-shape,no-hooks,verdict,sibling-path,merge-rule}.test.cjs` exits 0
- `node brief/bin/brief-tools.cjs compliance run --help` works (dispatcher recognizes the case)
- Phase 7 D-01 verdict enum locked: zero AUDIENCE-OK/DRIFTED-* tokens leak into compliance.cjs / agent / workflow
- LOAD-BEARING DEVIATION verified: mergeVerdicts material-only → FINDINGS-MATERIAL (NOT COMPLIANCE-OK)
</verification>

<success_criteria>
- 6 NEW files shipped: agent, workflow, compliance.cjs, compliance-report.cjs, vocabulary, dispatcher case
- 6 Wave 0 test scaffolds shipped + green
- compliance.cjs ≤ 400 lines (Phase 2 D-18)
- compliance-report.cjs ≤ 100 lines
- Zero ban-list token leakage (vocabulary-lock test green)
- Zero hooks references (anti-pattern #2 structural test green)
- Canonical shape mirrors agents/brief-audience-guard.md exactly
- LOAD-BEARING DEVIATION on mergeVerdicts documented + tested
</success_criteria>

<output>
After completion, create `.planning/phases/07-design-workstream-orchestration-compliance-checker/07-01-SUMMARY.md`
</output>
