---
phase: 07-design-workstream-orchestration-compliance-checker
plan: 08
type: execute
wave: 5
depends_on:
  - "07-01"
  - "07-02"
  - "07-03"
  - "07-04"
  - "07-05"
  - "07-06"
  - "07-07"
files_modified:
  - tests/brief-design-canary-e2e.test.cjs
  - tests/brief-design-text-mode.test.cjs
  - tests/brief-pipa-disclaimer-verbatim.test.cjs
  - tests/brief-design-orchestration.test.cjs
  - tests/brief-design-handoff.test.cjs
autonomous: true
requirements:
  - DSG-01
  - DSG-02
  - DSG-03
  - DSG-04
  - DSG-05
  - DSG-06
  - DSG-07
  - DSG-08
  - DSG-09
  - DSG-10
  - CC-01
  - FND-06
must_haves:
  truths:
    - "/brief-design BMC end-to-end on Korea-first B2C fintech fixture produces 4 paired-sibling files: canvas.md, canvas.align.md (ALIGNED), canvas.audience.md (AUDIENCE-OK), canvas.compliance.md (FINDINGS-MATERIAL)"
    - "canvas.compliance.md footer contains the verbatim Korean PIPA disclaimer (regex /2026년 개정 개인정보 보호법.*총매출의 10%/)"
    - "Handoff message includes 'go-to-market' as recommended next (per D-07 soft-order: BMC → GTM)"
    - "state.brief.last_design_workstream == 'business-model-canvas' after run"
    - "TEXT_MODE parity: /brief-design + /brief-add-workstream Q&A + FINANCIAL 12-driver Q&A all render as numbered lists in text_mode (FND-06)"
    - "CEO-liability false-positive prevention: B2B enterprise SaaS fixture (no consumer data) does NOT emit PIPA Art. 28-8 finding"
    - "All 11 phase requirements (DSG-01..10 + CC-01) covered by at least one passing test"
  artifacts:
    - path: "tests/brief-design-canary-e2e.test.cjs"
      provides: "Korea-first B2C fintech canary E2E test exercising the full /brief-design BMC flow"
    - path: "tests/brief-design-text-mode.test.cjs"
      provides: "Multi-runtime text_mode parity test (FND-06 inheritance)"
  key_links:
    - from: "tests/brief-design-canary-e2e.test.cjs"
      to: "tests/fixtures/objectives-korea-b2c-fintech.md"
      via: "fixture-driven E2E"
      pattern: "objectives-korea-b2c-fintech\\.md"
    - from: "tests/brief-design-canary-e2e.test.cjs"
      to: "brief/references/compliance/korea/pipa-2026.md"
      via: "verbatim disclaimer match assertion"
      pattern: "총매출의 10%|10% of total turnover"
---

<objective>
Ship the Phase 7 canary E2E test (Korea-first B2C fintech `/brief-design BMC` end-to-end) + the multi-runtime text_mode parity test. These are the integration tests that validate ALL of Phases 7-01..7-07's outputs work together when actually exercised.

Per CONTEXT.md Specifics line 350: "If any of these break during planning execution, planner returns to discuss with a flag; otherwise, Phase 7 is 'the pattern is proven' canary-equivalent for Phase 8."

This plan also extends `tests/brief-pipa-disclaimer-verbatim.test.cjs` (Plan 02) and `tests/brief-design-orchestration.test.cjs` + `brief-design-handoff.test.cjs` (Plan 03) with end-to-end-driven assertions that depend on Plans 05-07's outputs.

Purpose: Phase 7 integration verification — DSG-01..10 + CC-01 + FND-06 acceptance.

Output: 2 NEW integration tests + 3 extensions to existing tests with E2E-grade assertions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-design-workstream-orchestration-compliance-checker/07-CONTEXT.md
@.planning/phases/07-design-workstream-orchestration-compliance-checker/07-RESEARCH.md
@.planning/phases/07-design-workstream-orchestration-compliance-checker/07-PATTERNS.md
@brief/references/compliance/korea/pipa-2026.md

<interfaces>
<!-- Plans 01-07 outputs: full Phase 7 stack -->

From Plan 05: tests/fixtures/objectives-korea-b2c-fintech.md (canary fixture)
From Plan 06: tests/fixtures/objectives-b2b-enterprise-saas.md (B2B fixture for false-positive prevention)
From Plan 01: brief/bin/lib/compliance.cjs (run + commit subcommands via brief-tools)
From Plan 03: brief/workflows/design.md (sequential 3-gate threading)
From Plans 05/06: All 9 built-in workstream bundles
From Plan 07: workstream-loader extension + status.cjs recommended-next derivation

The E2E test cannot fully simulate LLM Task spawn (LLM output non-deterministic per Manual-Only Verifications row in 07-VALIDATION.md). The test exercises:
- Workflow markdown structure (grep-driven, deterministic)
- Lib API contracts (compliance.run / compliance.commit)
- State round-trip (state.brief.last_design_workstream + workstreams_completed)
- Fixture-driven structural assertions (BMC canvas.md sections, paired-siblings present, disclaimer verbatim)
- TEXT_MODE flag propagation (workflow markdown grep)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Korea-first B2C fintech canary E2E test (CC-01 + DSG-01..10 acceptance)</name>
  <files>
    tests/brief-design-canary-e2e.test.cjs (NEW — Wave 0 integration)
  </files>
  <read_first>
    tests/fixtures/objectives-korea-b2c-fintech.md (Plan 05 Task 1 output);
    brief/workflows/design.md (Plan 03 + Plan 06 Task 3 final state);
    brief/workstreams/business-model-canvas/templates/artifact.md (Plan 05);
    brief/references/compliance/korea/pipa-2026.md (verbatim disclaimer reference);
    .planning/phases/07-design-workstream-orchestration-compliance-checker/07-RESEARCH.md (lines 991-1010 — canary spec)
  </read_first>
  <behavior>
    - Test 1: Workflow structure proves 4 paired-sibling files would be produced (canvas.md + 3 paired-siblings)
    - Test 2: When fixture is at .planning/OBJECTIVES.md and compliance.run is invoked with the fixture as baseline, the deterministic screen detects PIPA pack signal (region:kr, business_model:b2c, compliance_packs:[PIPA, ISMS-P, MyData])
    - Test 3: Compliance verdict shape carries Korean disclaimer in rendered output
    - Test 4: Handoff recommended-next derivation returns 'go-to-market' when only 'business-model-canvas' is in workstreams_completed (per D-07 soft-order)
    - Test 5: Vocabulary lock — Plan 05 BMC bundle has zero ban-list violations
    - Test 6: B2B enterprise SaaS fixture does NOT trigger PIPA Art. 28-8 finding (CEO-liability false-positive prevention per Risk Notes)
  </behavior>
  <action>
    1. Create `tests/brief-design-canary-e2e.test.cjs`. Use node:test framework. The test orchestrates fixture-driven structural assertions WITHOUT spawning real LLM Tasks (per VALIDATION.md Manual-Only Verifications discipline).
    2. Test 1 — Workflow structure proves 4-file output:
       ```javascript
       const { test } = require('node:test');
       const assert = require('node:assert/strict');
       const fs = require('node:fs');
       
       test('canary E2E: design.md workflow Step 5 produces 4 paired-sibling files (canvas.md + 3 gates)', () => {
         const wf = fs.readFileSync('brief/workflows/design.md', 'utf8');
         // Step 5 invokes 3 gate workflows; each commits a paired-sibling file
         assert.match(wf, /align-gate\.md/);
         assert.match(wf, /audience-guard\.md/);
         assert.match(wf, /compliance\.md\b/);
         // The artifact path pattern .planning/workstreams/{slug}/{name}.md is the primary output
         // Paired-siblings: .align.md, .audience.md, .compliance.md generated by respective gates
         // Verify the workflow expects this layout
         assert.match(wf, /\.planning\/workstreams|workstreams\/\$\{|workstreams\/\{slug\}/i);
       });
       ```
    3. Test 2 — Fixture-driven PIPA pack detection:
       ```javascript
       test('canary fixture has region:kr + business_model:b2c + compliance_packs:[PIPA, ISMS-P, MyData]', () => {
         const fixture = fs.readFileSync('tests/fixtures/objectives-korea-b2c-fintech.md', 'utf8');
         assert.match(fixture, /region:\s*kr/);
         assert.match(fixture, /business_model:\s*b2c|business_context\.model:\s*b2c/);
         assert.match(fixture, /compliance_packs.*PIPA/);
         assert.match(fixture, /ISMS-P/);
         assert.match(fixture, /MyData/);
       });
       
       test('runDeterministicScreen on Korea-fixture-style artifact + PIPA pack triggers Screen (b)', () => {
         const path = require('node:path');
         const os = require('node:os');
         const tmpdir = os.tmpdir();
         const artifactPath = path.join(tmpdir, 'canary-' + Date.now() + '.md');
         // Korean B2C fintech BMC artifact mentioning 개인정보
         fs.writeFileSync(artifactPath, `# 페이앱 BMC

## 1. 고객 세그먼트
서울 거주 20-40대 직장인. 모바일 우선 사용자.

## 5. 수익원
거래 수수료 0.5%. 결제 시 개인정보를 수집하여 처리합니다.
`);
         const { runDeterministicScreen } = require('../brief/bin/lib/compliance.cjs');
         const result = runDeterministicScreen({
           artifact: artifactPath,
           baseline: 'tests/fixtures/objectives-korea-b2c-fintech.md',
           businessContext: {
             compliance_packs: ['PIPA', 'ISMS-P', 'MyData'],
             region: 'kr',
             business_model: 'b2c'
           }
         });
         // Either short-circuited FINDINGS-BLOCKING (Screen b PIPA hard-evidence)
         // OR additive deterministic_findings include a PIPA-related material/blocking finding
         const findings = result.short_circuited 
           ? (result.verdict.findings || [])
           : (result.deterministic_findings || []);
         const hasPipaFinding = findings.some(f =>
           (f.regulation_clause && f.regulation_clause.includes('PIPA')) ||
           f.description.includes('PIPA') || f.description.includes('개인정보'));
         assert.ok(hasPipaFinding, 'PIPA pack should produce a finding when artifact mentions 개인정보');
         fs.unlinkSync(artifactPath);
       });
       ```
    4. Test 3 — Korean disclaimer renders verbatim:
       ```javascript
       test('renderComplianceReport on Korean fixture produces verbatim Korean disclaimer', () => {
         const { renderComplianceReport } = require('../brief/bin/lib/compliance-report.cjs');
         const verdict = {
           decision: 'FINDINGS-MATERIAL',
           severity: 'material',
           findings_count: 1,
           findings: [{ severity: 'material', location: 'canvas.md:L5',
             description: 'PIPA Art. 28-8 evidence missing', 
             regulation_clause: 'PIPA Art. 28-8' }],
           rationale: 'Material finding on personal-data clause coverage'
         };
         const out = renderComplianceReport(verdict, { korea: true, packs: ['PIPA', 'ISMS-P', 'MyData'] });
         // Verbatim CEO liability disclaimer
         assert.match(out, /2026년 개정 개인정보 보호법/);
         assert.match(out, /총매출의 10%/);
         assert.match(out, /대표이사 개인 책임/);
         assert.match(out, /법적 자문이 아닙니다|법률 자문가/);
       });
       ```
    5. Test 4 — Recommended-next handoff derivation:
       ```javascript
       test('Handoff: completed=[business-model-canvas] → recommended next = go-to-market (D-07 soft-order)', () => {
         const { execSync } = require('node:child_process');
         const out = execSync('node brief/bin/brief-tools.cjs design recommended-next --completed business-model-canvas', { encoding: 'utf8' });
         const r = JSON.parse(out);
         assert.strictEqual(r.recommended_next, 'go-to-market',
           `D-07 soft-order: BMC → GTM. Got: ${r.recommended_next}`);
       });
       ```
    6. Test 5 — Vocabulary lock on BMC bundle:
       ```javascript
       test('BMC bundle has zero ban-list violations (vocabulary discipline)', () => {
         const BAN_TOKENS = [/\bcompliant\b/i, /\bpassed\b/i, /compliance verified/i,
                              /audit complete/i, /✅/, /\b준수\b/, /\b통과\b/, /감사 완료/];
         for (const file of ['brief/workstreams/business-model-canvas/spec.yaml',
                              'brief/workstreams/business-model-canvas/design-prompts.md',
                              'brief/workstreams/business-model-canvas/templates/artifact.md']) {
           const content = fs.readFileSync(file, 'utf8');
           for (const re of BAN_TOKENS) {
             assert.ok(!re.test(content), `Ban-list violation in ${file}: matched ${re}`);
           }
         }
       });
       ```
    7. Test 6 — B2B SaaS false-positive prevention (CRITICAL credibility test per Risk Notes):
       ```javascript
       test('CEO-liability false-positive prevention: B2B enterprise SaaS without consumer data does NOT trigger PIPA Art. 28-8', () => {
         const path = require('node:path');
         const os = require('node:os');
         const tmpdir = os.tmpdir();
         const artifactPath = path.join(tmpdir, 'b2b-' + Date.now() + '.md');
         // B2B enterprise SaaS BMC mentioning ONLY API keys + telemetry, NO consumer data
         fs.writeFileSync(artifactPath, `# AcmeIQ BMC

## 1. Customer Segments
Engineering leaders at companies with 200-2000 engineers.

## 5. Revenue Streams
Annual contract value $50K-$500K. Per-developer pricing tiers.

## 6. Key Resources
API gateway with API keys + aggregated telemetry. No PII processed.
`);
         const { runDeterministicScreen } = require('../brief/bin/lib/compliance.cjs');
         const result = runDeterministicScreen({
           artifact: artifactPath,
           baseline: 'tests/fixtures/objectives-b2b-enterprise-saas.md',
           businessContext: {
             compliance_packs: ['PIPA'],  // PIPA in packs but NO consumer data
             region: 'kr',
             business_model: 'enterprise'
           }
         });
         const findings = result.short_circuited
           ? (result.verdict.findings || [])
           : (result.deterministic_findings || []);
         const hasArt28BlockingFinding = findings.some(f =>
           f.severity === 'blocking' && f.regulation_clause && f.regulation_clause.includes('PIPA Art. 28-8'));
         assert.strictEqual(hasArt28BlockingFinding, false,
           'B2B SaaS without consumer-data tokens MUST NOT trigger PIPA Art. 28-8 blocking finding (RISK Notes credibility gradient mitigation)');
         fs.unlinkSync(artifactPath);
       });
       ```
  </action>
  <verify>
    <automated>node --test tests/brief-design-canary-e2e.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/brief-design-canary-e2e.test.cjs` exists with 6 tests
    - `node --test tests/brief-design-canary-e2e.test.cjs` exits 0 — all pass
    - Test 4 specifically asserts D-07 soft-order recommendation (BMC completed → GTM next)
    - Test 6 specifically asserts B2B SaaS false-positive prevention (CRITICAL credibility test)
    - Test 3 verifies Korean disclaimer renders verbatim (CEO-liability footer)
  </acceptance_criteria>
  <done>Canary E2E suite green; Phase 7 "pattern is proven" milestone reached.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TEXT_MODE multi-runtime parity test (FND-06)</name>
  <files>
    tests/brief-design-text-mode.test.cjs (NEW — Wave 0 integration)
  </files>
  <read_first>
    brief/workflows/design.md (Plan 03 + 06 Task 3 — TEXT_MODE detection patterns);
    brief/workflows/add-workstream.md (Plan 04 — TEXT_MODE handling);
    brief/workflows/define.md (analog — TEXT_MODE consolidated numbered list pattern from Phase 3 Mode A)
  </read_first>
  <behavior>
    - Test 1: design.md TEXT_MODE detection block grep
    - Test 2: design.md FINANCIAL Step 4.5 batches 12 questions in TEXT_MODE
    - Test 3: add-workstream.md TEXT_MODE handling for 4-6 Q&A
    - Test 4: design.md Step 1 workstream selection has TEXT_MODE numbered-list fallback
    - Test 5: design.md Step 7 handoff (3-option AskUserQuestion) has TEXT_MODE fallback
  </behavior>
  <action>
    1. Create `tests/brief-design-text-mode.test.cjs`:
       ```javascript
       const { test } = require('node:test');
       const assert = require('node:assert/strict');
       const fs = require('node:fs');

       test('design.md detects TEXT_MODE flag (FND-06 inheritance)', () => {
         const wf = fs.readFileSync('brief/workflows/design.md', 'utf8');
         assert.match(wf, /TEXT_MODE/);
         assert.match(wf, /--text|workflow\.text_mode/);
       });

       test('design.md FINANCIAL Step 4.5 batches 12 questions in TEXT_MODE (consolidated numbered list)', () => {
         const wf = fs.readFileSync('brief/workflows/design.md', 'utf8');
         // Plan 06 Task 3 wired Step 4.5; verify TEXT_MODE batching pattern
         assert.match(wf, /TEXT_MODE.*batch|batch.*TEXT_MODE|consolidated.*numbered list|numbered.list.*TEXT_MODE/i);
       });

       test('add-workstream.md detects TEXT_MODE and renders Q&A as numbered list', () => {
         const wf = fs.readFileSync('brief/workflows/add-workstream.md', 'utf8');
         assert.match(wf, /TEXT_MODE/);
         assert.match(wf, /numbered.list|numbered-list|consolidated/i);
       });

       test('design.md Step 1 workstream selection has TEXT_MODE numbered-list fallback', () => {
         const wf = fs.readFileSync('brief/workflows/design.md', 'utf8');
         // Step 1 selection prompt
         const step1 = wf.split(/Step 1/)[1] ? wf.split(/Step 1/)[1].split(/Step 2/)[0] : '';
         // Either TEXT_MODE pattern present in Step 1, or workflow generally documents AskUserQuestion + numbered-list fallback discipline
         assert.ok(/TEXT_MODE/.test(step1) || /TEXT_MODE/.test(wf),
           'Step 1 must reference TEXT_MODE handling for workstream selection');
       });

       test('design.md Step 7 handoff (3-option) has TEXT_MODE numbered-list fallback', () => {
         const wf = fs.readFileSync('brief/workflows/design.md', 'utf8');
         const step7 = wf.split(/Step 7/)[1] || '';
         // Step 7 invokes AskUserQuestion with 3 options; TEXT_MODE renders as numbered list
         assert.ok(/TEXT_MODE/.test(step7) || /TEXT_MODE/.test(wf),
           'Step 7 handoff must support TEXT_MODE numbered-list fallback');
         // 3 options visible
         assert.match(step7, /continue|계속/i);
         assert.match(step7, /stop|멈추/i);
         assert.match(step7, /다른|different/i);
       });

       test('All 3 NEW Phase 7 workflows (design / compliance / add-workstream) detect TEXT_MODE at Step 0', () => {
         for (const f of ['brief/workflows/design.md', 'brief/workflows/compliance.md', 'brief/workflows/add-workstream.md']) {
           const wf = fs.readFileSync(f, 'utf8');
           const step0 = wf.split(/Step 0/)[1] ? wf.split(/Step 0/)[1].split(/Step 0\.5|Step 1/)[0] : '';
           assert.match(step0, /TEXT_MODE/, `${f}: Step 0 must detect TEXT_MODE`);
         }
       });
       ```
  </action>
  <verify>
    <automated>node --test tests/brief-design-text-mode.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/brief-design-text-mode.test.cjs` exists with 6 tests
    - `node --test tests/brief-design-text-mode.test.cjs` exits 0 — all pass
    - Confirms TEXT_MODE detection in all 3 NEW Phase 7 workflows
    - Confirms FINANCIAL 12-driver batching for TEXT_MODE per Risk Notes mitigation
    - FND-06 multi-runtime parity verified at workflow markdown level
  </acceptance_criteria>
  <done>TEXT_MODE parity test green across all Phase 7 workflows. FND-06 acceptance prep complete.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Final integration sweep — full Phase 7 test suite green; sanity-check requirements coverage</name>
  <files>
    (no new files — verification only)
  </files>
  <read_first>
    .planning/phases/07-design-workstream-orchestration-compliance-checker/07-VALIDATION.md (per-task verification map)
  </read_first>
  <behavior>
    - All 28 Phase 7 test files green
    - All 11 phase requirements (DSG-01..10 + CC-01) have at least one passing test
    - Surface cap NET +2 verified
    - Anti-pattern #2 verified
    - Vocabulary lock verified across all gate output paths
    - Phase 1 inherited test count delta-cap respected (Phase 7 should NOT regress the npm test failure count from Phase 6 baseline)
  </behavior>
  <action>
    1. Run the full Phase 7 test suite:
       ```bash
       node --test tests/brief-compliance-vocabulary-lock.test.cjs \
                  tests/brief-compliance-canonical-shape.test.cjs \
                  tests/brief-compliance-no-hooks.test.cjs \
                  tests/brief-compliance-verdict.test.cjs \
                  tests/brief-compliance-sibling-path.test.cjs \
                  tests/brief-compliance-merge-rule.test.cjs \
                  tests/brief-compliance-disclaimer.test.cjs \
                  tests/brief-pipa-disclaimer-verbatim.test.cjs \
                  tests/brief-compliance-pack-load.test.cjs \
                  tests/brief-design-orchestration.test.cjs \
                  tests/brief-design-gate-order.test.cjs \
                  tests/brief-design-objectives-amendment.test.cjs \
                  tests/brief-design-handoff.test.cjs \
                  tests/brief-add-workstream-skeleton.test.cjs \
                  tests/brief-add-workstream-collision.test.cjs \
                  tests/brief-add-workstream-gates.test.cjs \
                  tests/brief-workstream-bmc.test.cjs \
                  tests/brief-workstream-gtm.test.cjs \
                  tests/brief-workstream-brand.test.cjs \
                  tests/brief-workstream-risk.test.cjs \
                  tests/brief-workstream-roadmap.test.cjs \
                  tests/brief-workstream-compliance.test.cjs \
                  tests/brief-workstream-operations.test.cjs \
                  tests/brief-workstream-tech-arch.test.cjs \
                  tests/brief-financial-driver-questions.test.cjs \
                  tests/brief-financial-provenance.test.cjs \
                  tests/brief-financial-sensitivity.test.cjs \
                  tests/brief-workstream-loader-extended.test.cjs \
                  tests/brief-design-recommended-next.test.cjs \
                  tests/brief-surface-cap-phase-7.test.cjs \
                  tests/brief-design-anti-pattern-2.test.cjs \
                  tests/brief-workstreams-9-builtin-loadable.test.cjs \
                  tests/brief-workstream-spec-conditional-prose.test.cjs \
                  tests/brief-design-canary-e2e.test.cjs \
                  tests/brief-design-text-mode.test.cjs
       ```
       All 35 must exit 0.
    2. Run npm test (full suite) to verify Phase 7 does NOT regress npm-test count above the Phase 6 baseline. Capture baseline count BEFORE Plan 08 final commit:
       ```bash
       npm test 2>&1 > /tmp/phase-07-test.txt
       grep -cE '^(✖|not ok)' /tmp/phase-07-test.txt
       ```
       Phase 7 inherited delta-cap from Phase 1 = 16 max failures (EMPIRICAL_BASELINE 6 + DELTA_CAP 10). Phase 7 must NOT raise this count.
    3. Update tests/brief-pipa-disclaimer-verbatim.test.cjs (Plan 02) if the canary E2E test in Plan 08 reveals a wording drift — re-sync renderer to primer.
    4. Inspect any failing test output. Common failure modes to fix:
       - "Recommended next" line missing in status output → ensure status.cjs Plan 07 modification was applied
       - Compliance vocabulary lock fails on a workstream design-prompts.md → check Plans 05/06 ban-list compliance
       - Surface cap test fails → verify NO `/brief-recompliance` etc. snuck in via copy-paste
       - 9-builtin-loadable fails on a missing workstream → verify Plans 05/06 shipped all 9
    5. Verify all 11 requirements have at least one passing test by mapping VALIDATION.md rows:
       - DSG-01 (BMC) → tests/brief-workstream-bmc.test.cjs ✓
       - DSG-02 (GTM) → tests/brief-workstream-gtm.test.cjs ✓
       - DSG-03 (FINANCIAL) → tests/brief-financial-{driver-questions, provenance, sensitivity}.test.cjs ✓
       - DSG-04 (OPERATIONS) → tests/brief-workstream-operations.test.cjs ✓
       - DSG-05 (COMPLIANCE workstream) → tests/brief-workstream-compliance.test.cjs ✓
       - DSG-06 (ROADMAP) → tests/brief-workstream-roadmap.test.cjs ✓
       - DSG-07 (BRAND) → tests/brief-workstream-brand.test.cjs ✓
       - DSG-08 (RISK) → tests/brief-workstream-risk.test.cjs ✓
       - DSG-09 (TECH-ARCH) → tests/brief-workstream-tech-arch.test.cjs ✓
       - DSG-10 (/brief-add-workstream + workstream-as-config) → tests/brief-add-workstream-{skeleton, collision, gates}.test.cjs + tests/brief-workstream-loader-extended.test.cjs ✓
       - CC-01 (compliance checker on every artifact) → tests/brief-compliance-{vocabulary-lock, canonical-shape, no-hooks, verdict, sibling-path, merge-rule, disclaimer, pack-load}.test.cjs + tests/brief-pipa-disclaimer-verbatim.test.cjs + tests/brief-design-canary-e2e.test.cjs + tests/brief-design-anti-pattern-2.test.cjs ✓
  </action>
  <verify>
    <automated>npm test 2>&1 | grep -cE '^✖|^not ok' | awk '{exit ($1 > 16) ? 1 : 0}'</automated>
  </verify>
  <acceptance_criteria>
    - All 28+ Phase 7 test files pass (`node --test ...` exits 0 for the comprehensive list)
    - `npm test 2>&1 | grep -cE '^✖|^not ok'` returns ≤ 16 (Phase 1 inherited delta-cap respected)
    - Each of the 11 phase requirements has at least one passing test (manual mapping verified above)
    - Phase 7 D-01 verdict enum + LOAD-BEARING DEVIATION on mergeVerdicts: tested
    - Phase 7 D-02 sequential 3-gate threading: tested
    - Phase 7 D-03 verbatim disclaimer: tested
    - Phase 7 D-04 Korea-only pack scope: tested
    - Phase 7 D-05/D-06/D-07/D-08 orchestrator UX: tested
    - Phase 7 D-09/D-10/D-11 add-workstream flow: tested
    - Phase 7 D-12 paired-sibling layout: tested
    - Phase 7 D-13 7-field schema: tested
    - Phase 7 D-14 B2B/B2C conditional prose: tested
    - Phase 7 D-15 FINANCIAL driver discipline: tested
  </acceptance_criteria>
  <done>Full Phase 7 test suite green; npm test failure count ≤ 16; all 11 requirements covered. Phase 7 ready for /brief-verify-work.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| canary fixture → /brief-design BMC E2E | Korean fixture with PIPA pack drives the full pipeline |
| TEXT_MODE flag → workflow Q&A renderers | Multi-runtime sandbox (Codex/Gemini/OpenCode) cannot use AskUserQuestion |
| npm test full suite → CI gate | Phase 7 must not regress inherited test count |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-27 | Tampering | E2E canary fixture | mitigate | `tests/brief-design-canary-e2e.test.cjs` 6 tests cover Korea fixture + PIPA pack + verbatim disclaimer + soft-order recommendation + ban-list discipline + B2B false-positive prevention. |
| T-07-28 | Spoofing | TEXT_MODE multi-runtime parity | mitigate | `tests/brief-design-text-mode.test.cjs` 6 tests cover all 3 NEW Phase 7 workflows + FINANCIAL 12-question batching + Step 7 handoff. |
| (cross-cutting) | All STRIDE | Phase 7 invariants | mitigate | Task 3 final test sweep verifies all Phase 7 D-01..D-15 locks via 35+ tests. npm test failure count delta-cap (≤16) enforces no regression of Phase 1-6 inherited tests. |
</threat_model>

<verification>
- 2 NEW integration tests
- 35+ Phase 7 tests cumulatively green
- npm test count regression-proof
- All 11 requirements covered
- All 15 user decisions (D-01..D-15) tested
</verification>

<success_criteria>
- 2 NEW Wave 0 integration tests
- Full Phase 7 test suite green (35+ files)
- npm test failure count ≤ 16 (delta-cap inherited)
- All 11 phase requirements covered by ≥ 1 test
- D-01..D-15 all locked structurally
- Phase 7 "the pattern is proven" milestone reached — ready for Phase 8 DELIVER inheritance
</success_criteria>

<output>
After completion, create `.planning/phases/07-design-workstream-orchestration-compliance-checker/07-08-SUMMARY.md`
</output>
