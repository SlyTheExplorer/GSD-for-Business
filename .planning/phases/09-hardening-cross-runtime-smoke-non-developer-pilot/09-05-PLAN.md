---
phase: 09-hardening-cross-runtime-smoke-non-developer-pilot
plan: 05
type: execute
wave: 3
depends_on: [00, 01, 02, 03, 04]
files_modified:
  - commands/brief/add-backlog.md
  - commands/brief/add-phase.md
  - commands/brief/add-todo.md
  - commands/brief/analyze-dependencies.md
  - commands/brief/audit-fix.md
  - commands/brief/audit-milestone.md
  - commands/brief/audit-uat.md
  - commands/brief/autonomous.md
  - commands/brief/check-todos.md
  - commands/brief/cleanup.md
  - commands/brief/complete-milestone.md
  - commands/brief/discuss-phase.md
  - commands/brief/do.md
  - commands/brief/docs-update.md
  - commands/brief/execute-phase.md
  - commands/brief/explore.md
  - commands/brief/extract_learnings.md
  - commands/brief/fast.md
  - commands/brief/from-gsd2.md
  - commands/brief/health.md
  - commands/brief/import.md
  - commands/brief/insert-phase.md
  - commands/brief/intel.md
  - commands/brief/join-discord.md
  - commands/brief/list-phase-assumptions.md
  - commands/brief/list-workspaces.md
  - commands/brief/manager.md
  - commands/brief/map-codebase.md
  - commands/brief/milestone-summary.md
  - commands/brief/new-milestone.md
  - commands/brief/new-project.md
  - commands/brief/new-workspace.md
  - commands/brief/next.md
  - commands/brief/note.md
  - commands/brief/plan-milestone-gaps.md
  - commands/brief/plan-phase.md
  - commands/brief/plant-seed.md
  - commands/brief/profile-user.md
  - commands/brief/quick.md
  - commands/brief/reapply-patches.md
  - commands/brief/remove-phase.md
  - commands/brief/remove-workspace.md
  - commands/brief/research-phase.md
  - commands/brief/resume-work.md
  - commands/brief/review-backlog.md
  - commands/brief/review.md
  - commands/brief/scan.md
  - commands/brief/session-report.md
  - commands/brief/set-profile.md
  - commands/brief/settings.md
  - commands/brief/spec-phase.md
  - commands/brief/stats.md
  - commands/brief/thread.md
  - commands/brief/update.md
  - commands/brief/validate-phase.md
  - commands/brief/verify-work.md
  - commands/brief/workstreams.md
  - commands/brief/init.md
  - bin/install.js
  - .planning/SURFACE-AUDIT.md
  - CLAUDE.md
  - commands/brief/define.md
  - commands/brief/discover.md
  - commands/brief/design.md
  - commands/brief/add-workstream.md
  - commands/brief/deliver.md
  - commands/brief/export.md
  - commands/brief/status.md
  - commands/brief/help.md
  - commands/brief/progress.md
  - commands/brief/undo.md
  - commands/brief/pause-work.md
autonomous: true
requirements: [HRD-02, HRD-05]

must_haves:
  truths:
    - "After this plan, `ls commands/brief/*.md | wc -l` returns 12 (down from 68; 56 files deleted in atomic commit)"
    - "The 12 remaining files match LOCKED_12 slug list exactly: define, discover, design, add-workstream, deliver, export, status, help, init, progress, undo, pause-work"
    - "`commands/brief/new-project.md` is renamed to `commands/brief/init.md` (per Open Question 1 / PATTERNS.md recommendation); the file's `name:` frontmatter is also updated to `brief:init`; bin/install.js SRC tuples updated accordingly"
    - "`bin/install.js` has zero references to any of the 56 deleted slugs (per Pitfall 1 mitigation)"
    - "`backup/original-gsd` branch still resolves and contains the deleted command files (verified by `git checkout backup/original-gsd -- commands/brief/{sample}.md` recovering a sample)"
    - "`.planning/SURFACE-AUDIT.md` exists with 12-row commands table + 0-row skills note + 56-entry Removed-in-v1 appendix (grouped by phase-introduced per RESEARCH.md Discretion-4)"
    - "`CLAUDE.md` Surface Caps section is updated to reference SURFACE-AUDIT.md and reflect the 12/12 cmd cap + 0/8 skill cap as v1 final state"
    - ".claude/skills/ remains empty (0/8 cap met by reservation per A-D03)"
    - "All deletions + the rename + bin/install.js cleanup + SURFACE-AUDIT.md + CLAUDE.md update land in a SINGLE atomic git commit (per Pitfall 1 — partial state breaks installer)"
    - "Wave 0 fixtures brief-surface-audit-count.test.cjs + brief-surface-audit-install-cleanup.test.cjs + brief-surface-audit-doc.test.cjs all pass GREEN (no skips)"
    - "Zero new runtime npm dependencies (A1 preserved)"
  artifacts:
    - path: ".planning/SURFACE-AUDIT.md"
      provides: "v1 launch surface audit doc — 12-row commands table + 0-row skills + 56-entry Removed-in-v1 appendix"
      min_lines: 80
      contains: "User-Facing Commands"
    - path: "commands/brief/init.md"
      provides: "Renamed from new-project.md per A-D01 lineup using `init` slug; frontmatter `name: brief:init`"
      contains: "brief:init"
    - path: "bin/install.js"
      provides: "Cleaned up — zero refs to any deleted slug (Pitfall 1)"
      pattern_absent: "(add-backlog|audit-uat|cleanup|discuss-phase|execute-phase|plan-phase|verify-work)\\.md"
    - path: "CLAUDE.md"
      provides: "Surface Caps section updated to v1 final state (12/12 cmds, 0/8 skills, references SURFACE-AUDIT.md)"
      contains: "SURFACE-AUDIT.md"
  key_links:
    - from: ".planning/SURFACE-AUDIT.md"
      to: "commands/brief/*.md"
      via: "12-row table lists every locked command + Removed-in-v1 appendix lists every deleted slug"
      pattern: "/brief-(define|discover|design|add-workstream|deliver|export|status|help|init|progress|undo|pause-work)"
    - from: "CLAUDE.md"
      to: ".planning/SURFACE-AUDIT.md"
      via: "Surface Caps section references the audit doc by name"
      pattern: "SURFACE-AUDIT\\.md"
    - from: "bin/install.js"
      to: "commands/brief/*.md (12 locked)"
      via: "SRC tuples list only the 12 locked slugs; zero refs to deleted 56"
      pattern: "init|define|discover|design|add-workstream|deliver|export|status|help|progress|undo|pause-work"
---

<objective>
Execute HRD-02 surface pruning per A-D01..A-D04 — the architectural keystone of Phase 9. Atomically delete 56 inherited GSD command files from `commands/brief/`, rename `new-project.md` → `init.md` (Open Question 1 resolution per PATTERNS.md recommendation), clean up all `bin/install.js` references to deleted slugs (Pitfall 1 mitigation), generate `.planning/SURFACE-AUDIT.md` audit doc, and update `CLAUDE.md` Surface Caps section. Single atomic commit. Single worktree-isolated agent (Phase 8 cwd-bug lesson).

Purpose: Closes HRD-02 (success criterion #2 in ROADMAP Phase 9). Mitigates Pitfall #1 (Skill bloat) and Pitfall #12 (slash-command memorability) by reducing the user-facing surface from 68 inherited commands to 12 locked commands matching the 4D phase model + minimum life-cycle helpers. Per A-D02, this is hard delete (no soft-hide via frontmatter `hidden: true`); the `backup/original-gsd` branch preserves the deleted files for rollback.

Output: 56 deleted command files, 1 renamed file (new-project.md → init.md), 1 cleaned-up bin/install.js, 1 new SURFACE-AUDIT.md, 1 updated CLAUDE.md. All in a SINGLE atomic git commit. Wave 0 fixtures brief-surface-audit-count + brief-surface-audit-install-cleanup + brief-surface-audit-doc flip from skip→active and pass.
</objective>

<execution_context>
@~/.claude/brief/workflows/execute-plan.md
@~/.claude/brief/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-hardening-cross-runtime-smoke-non-developer-pilot/09-CONTEXT.md
@.planning/phases/09-hardening-cross-runtime-smoke-non-developer-pilot/09-RESEARCH.md
@.planning/phases/09-hardening-cross-runtime-smoke-non-developer-pilot/09-PATTERNS.md
@bin/install.js
@CLAUDE.md
@tests/brief-surface-audit-count.test.cjs
@tests/brief-surface-audit-install-cleanup.test.cjs
@tests/brief-surface-audit-doc.test.cjs

<interfaces>
<!-- Constants the executor MUST honor verbatim. Same as Plan 00 fixture interface. -->

LOCKED_12 (per A-D01) — slugs to KEEP:
```
define, discover, design, add-workstream,
deliver, export, status, help,
init, progress, undo, pause-work
```

DELETED_56 (per A-D02) — slugs to REMOVE from `commands/brief/`:
```
add-backlog, add-phase, add-todo, analyze-dependencies, audit-fix,
audit-milestone, audit-uat, autonomous, check-todos, cleanup,
complete-milestone, discuss-phase, do, docs-update, execute-phase,
explore, extract_learnings, fast, from-gsd2, health, import,
insert-phase, intel, join-discord, list-phase-assumptions,
list-workspaces, manager, map-codebase, milestone-summary,
new-milestone, new-project, new-workspace, next, note,
plan-milestone-gaps, plan-phase, plant-seed, profile-user, quick,
reapply-patches, remove-phase, remove-workspace, research-phase,
resume-work, review, review-backlog, scan, session-report,
set-profile, settings, spec-phase, stats, thread, update,
validate-phase, verify-work, workstreams
```
NOTE: `new-project.md` appears in DELETED_56 because its content is NOT deleted — it is RENAMED to `init.md`. The atomic commit performs `git mv commands/brief/new-project.md commands/brief/init.md` instead of `git rm` for that one entry. The other 55 entries are pure `git rm`.

From `bin/install.js` (existing — Phase 1 D-08 byte-identity SRC tuple cleanup pattern at lines ~3576-3581):
```javascript
// Existing pattern: array of "old gsd-*.md" entries to remove on install before copying new ones.
// HRD-02 extends this pattern to remove all 56 deleted command refs across the file.
```

From PATTERNS.md (lines 695-736) — `.planning/SURFACE-AUDIT.md` schema (verbatim template).

From CLAUDE.md "Surface Caps" section (existing — Phase 2 D-09 source) — text near lines mentioning ≤12 commands + ≤8 skills; this plan updates it to reference the new SURFACE-AUDIT.md and to reflect 12/12 + 0/8 as v1 final.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: [WORKTREE-ISOLATED AGENT — LARGE SCOPE: 60+ files in single atomic commit] Atomic 56-file deletion + new-project.md→init.md rename + 11-file sed bulk-rewrite (name: gsd:* → name: brief:*) + bin/install.js cleanup + SURFACE-AUDIT.md + CLAUDE.md update — single commit</name>
  <files>
    commands/brief/add-backlog.md, commands/brief/add-phase.md, commands/brief/add-todo.md, commands/brief/analyze-dependencies.md, commands/brief/audit-fix.md, commands/brief/audit-milestone.md, commands/brief/audit-uat.md, commands/brief/autonomous.md, commands/brief/check-todos.md, commands/brief/cleanup.md, commands/brief/complete-milestone.md, commands/brief/discuss-phase.md, commands/brief/do.md, commands/brief/docs-update.md, commands/brief/execute-phase.md, commands/brief/explore.md, commands/brief/extract_learnings.md, commands/brief/fast.md, commands/brief/from-gsd2.md, commands/brief/health.md, commands/brief/import.md, commands/brief/insert-phase.md, commands/brief/intel.md, commands/brief/join-discord.md, commands/brief/list-phase-assumptions.md, commands/brief/list-workspaces.md, commands/brief/manager.md, commands/brief/map-codebase.md, commands/brief/milestone-summary.md, commands/brief/new-milestone.md, commands/brief/new-project.md, commands/brief/new-workspace.md, commands/brief/next.md, commands/brief/note.md, commands/brief/plan-milestone-gaps.md, commands/brief/plan-phase.md, commands/brief/plant-seed.md, commands/brief/profile-user.md, commands/brief/quick.md, commands/brief/reapply-patches.md, commands/brief/remove-phase.md, commands/brief/remove-workspace.md, commands/brief/research-phase.md, commands/brief/resume-work.md, commands/brief/review.md, commands/brief/review-backlog.md, commands/brief/scan.md, commands/brief/session-report.md, commands/brief/set-profile.md, commands/brief/settings.md, commands/brief/spec-phase.md, commands/brief/stats.md, commands/brief/thread.md, commands/brief/update.md, commands/brief/validate-phase.md, commands/brief/verify-work.md, commands/brief/workstreams.md, commands/brief/init.md, bin/install.js, .planning/SURFACE-AUDIT.md, CLAUDE.md
  </files>
  <read_first>
    - bin/install.js (full file — locate every SRC tuple, PATCHES_DIR_NAME, command-list, and removal-loop reference; specifically lines 3576-3581 per RESEARCH.md Pitfall 1 + lines 1676-1689 per RESEARCH.md "bin/install.js SRC tuple cleanup")
    - commands/brief/new-project.md (full file — confirm current `name:` frontmatter; the rename to init.md must update `name:` from whatever it currently is to `brief:init`)
    - .planning/phases/09-hardening-cross-runtime-smoke-non-developer-pilot/09-PATTERNS.md (lines 695-736 — SURFACE-AUDIT.md schema verbatim; lines 1002-1011 — backup-branch preservation discipline)
    - .planning/phases/09-hardening-cross-runtime-smoke-non-developer-pilot/09-RESEARCH.md (lines 478-489 — Pitfall 1 dangling-refs detail; lines 642-681 — SURFACE-AUDIT.md schema; lines 444-446 — surface deletion atomic-commit discipline)
    - CLAUDE.md (full file — locate "## Surface Caps" section; this plan updates it to reference SURFACE-AUDIT.md and the 12/12 + 0/8 v1 final numbers)
    - tests/brief-surface-audit-count.test.cjs (Wave 0 fixture — confirm LOCKED_12 list matches; this plan's deletion makes the fixture pass)
    - tests/brief-surface-audit-install-cleanup.test.cjs (Wave 0 fixture — confirm DELETED_56 list matches)
    - tests/brief-surface-audit-doc.test.cjs (Wave 0 fixture — confirm SURFACE-AUDIT.md schema this plan generates)
  </read_first>
  <critical_cwd_warning>
You are running in a worktree at `/tmp/gsd-worktree-09-05-1/`. NEVER use `cd ..` or absolute paths outside this worktree. All file operations are relative to this worktree's project root. The main repository at `/Users/agent/GSD-for-Business` is INVISIBLE to your changes — your commits land in the worktree branch only. After your atomic commit lands, the orchestrator merges your worktree branch back into main.

PRE-FLIGHT CHECK (run BEFORE any file modification):
1. `git rev-parse --show-toplevel` — confirm output is `/tmp/gsd-worktree-09-05-1/` (or whatever the worktree root is); if it shows `/Users/agent/GSD-for-Business`, STOP and report cwd-bug.
2. `git rev-parse backup/original-gsd` — confirm backup branch exists; if it errors, STOP and report missing backup.
3. `git status` — confirm worktree is clean (no untracked changes from a prior failed agent).
  </critical_cwd_warning>
  <action>
    This task runs as a SINGLE worktree-isolated agent (Phase 8 cwd-bug lesson per planning_context recommendation). Steps must execute in order; the final commit is atomic.

    **Scope justification (W4 revision iteration 1 — large but justified):** This single task modifies 60+ files: 55 deletions (`git rm`) + 1 rename (`git mv`) + 1 frontmatter edit on the renamed file + 11-file sed bulk-rewrite (name: gsd:* → name: brief:*) + bin/install.js cleanup + SURFACE-AUDIT.md generation + CLAUDE.md surface-caps section update. The 60+ scope is mechanical (regex match + delete or substitute; no per-file design judgment) and atomicity is load-bearing — Pitfall 1 (skill bloat / dangling refs) requires the deletion + install.js cleanup + frontmatter rewrite to land in ONE commit so partial state never reaches main. The 11-file sed bulk-rewrite is added in revision iteration 1 (W9) to close the visible `name: gsd:*` drift in the LOCKED_12 commands; it is a single regex substitution, mechanical, and belongs in this same commit because half-renamed surface frontmatter is exactly the dual-vocabulary state Phase 1 was designed to eliminate. bin/install.js cleanup may proceed in this same task because it's mechanical (regex match deleted command names + remove from list), no per-file judgment.

    **Step 1 — Verify backup branch + Pre-flight check (Pitfall 2 per PATTERNS.md):**

    ```bash
    git rev-parse backup/original-gsd  # MUST succeed; abort if not
    # Spot-check recovery (read-only, in-worktree, does not affect main):
    git show backup/original-gsd:commands/brief/audit-fix.md | head -5  # MUST print frontmatter
    ```
    If either fails, STOP and report. Do NOT proceed with deletions if backup is missing.

    **Step 2 — Atomic file operations (use git mv + git rm; do NOT use plain `rm`):**

    a. Rename `commands/brief/new-project.md` → `commands/brief/init.md` (per Open Question 1):
       ```bash
       git mv commands/brief/new-project.md commands/brief/init.md
       ```
       Then read the renamed file's frontmatter and update `name:` field to `brief:init` (whatever it was before — likely `gsd:new-project` or `brief:new-project`). Use Edit tool for surgical frontmatter line edit; do NOT rewrite body.

    b. `git rm` the other 55 files (DELETED_56 minus `new-project`):
       ```bash
       git rm commands/brief/add-backlog.md commands/brief/add-phase.md commands/brief/add-todo.md commands/brief/analyze-dependencies.md commands/brief/audit-fix.md commands/brief/audit-milestone.md commands/brief/audit-uat.md commands/brief/autonomous.md commands/brief/check-todos.md commands/brief/cleanup.md commands/brief/complete-milestone.md commands/brief/discuss-phase.md commands/brief/do.md commands/brief/docs-update.md commands/brief/execute-phase.md commands/brief/explore.md commands/brief/extract_learnings.md commands/brief/fast.md commands/brief/from-gsd2.md commands/brief/health.md commands/brief/import.md commands/brief/insert-phase.md commands/brief/intel.md commands/brief/join-discord.md commands/brief/list-phase-assumptions.md commands/brief/list-workspaces.md commands/brief/manager.md commands/brief/map-codebase.md commands/brief/milestone-summary.md commands/brief/new-milestone.md commands/brief/new-workspace.md commands/brief/next.md commands/brief/note.md commands/brief/plan-milestone-gaps.md commands/brief/plan-phase.md commands/brief/plant-seed.md commands/brief/profile-user.md commands/brief/quick.md commands/brief/reapply-patches.md commands/brief/remove-phase.md commands/brief/remove-workspace.md commands/brief/research-phase.md commands/brief/resume-work.md commands/brief/review.md commands/brief/review-backlog.md commands/brief/scan.md commands/brief/session-report.md commands/brief/set-profile.md commands/brief/settings.md commands/brief/spec-phase.md commands/brief/stats.md commands/brief/thread.md commands/brief/update.md commands/brief/validate-phase.md commands/brief/verify-work.md commands/brief/workstreams.md
       ```
       (55 files; verified count: 56 entries in DELETED_56 minus `new-project` = 55.)

    c. Verify `ls commands/brief/*.md | wc -l` returns 12.

    d. Verify the 12 remaining files are exactly LOCKED_12:
       ```bash
       ls commands/brief/*.md | sed 's|commands/brief/||;s|\.md$||' | sort > /tmp/actual.txt
       printf "%s\n" add-workstream define deliver design discover export help init pause-work progress status undo | sort > /tmp/expected.txt
       diff /tmp/actual.txt /tmp/expected.txt  # must be empty
       ```

    **Step 2.5 — W9 sed bulk-rewrite: `name: gsd:*` → `name: brief:*` across the 11 retained LOCKED_12 commands (excluding init.md which Step 2a already updated):**

    Per W9 revision (iteration 1): 11 of 12 LOCKED_12 commands may retain `name: gsd:*` frontmatter drift inherited from Phase 1's incomplete rename. Close that visible drift now in the same atomic commit:

    ```bash
    # Cross-platform sed: BSD sed (macOS) requires `-i ''`; GNU sed (Linux) requires `-i` only.
    # Use perl -i -pe instead for universal portability:
    for f in commands/brief/define.md commands/brief/discover.md commands/brief/design.md commands/brief/add-workstream.md commands/brief/deliver.md commands/brief/export.md commands/brief/status.md commands/brief/help.md commands/brief/progress.md commands/brief/undo.md commands/brief/pause-work.md; do
      [ -f "$f" ] && perl -i -pe 's/^name: gsd:/name: brief:/' "$f"
    done
    ```

    NOTE: `commands/brief/help.md` is fully rewritten by Plan 02 (HRD-03), which lands in Wave 2 BEFORE this Wave 2 plan if the orchestrator runs Plan 02 first; if Plan 02 has already landed, `help.md` already has `name: brief:help` and the sed line above is a no-op (idempotent). If Plan 02 has NOT landed yet, this step still cleans up gsd: drift in help.md frontmatter; Plan 02's full rewrite supersedes anything written here. Either ordering is correct.

    Verify post-sed: `grep -l "^name: gsd:" commands/brief/*.md 2>/dev/null` MUST return nothing (zero remaining gsd: drift in LOCKED_12 frontmatter).

    **Step 3 — Clean up `bin/install.js`:**

    a. For each slug in DELETED_56 (including `new-project`), search bin/install.js for direct references:
       ```bash
       for slug in add-backlog add-phase add-todo analyze-dependencies audit-fix audit-milestone audit-uat autonomous check-todos cleanup complete-milestone discuss-phase do docs-update execute-phase explore extract_learnings fast from-gsd2 health import insert-phase intel join-discord list-phase-assumptions list-workspaces manager map-codebase milestone-summary new-milestone new-project new-workspace next note plan-milestone-gaps plan-phase plant-seed profile-user quick reapply-patches remove-phase remove-workspace research-phase resume-work review review-backlog scan session-report set-profile settings spec-phase stats thread update validate-phase verify-work workstreams; do
         grep -n "['\"\\/]\\b${slug}\\.md\\b" bin/install.js || true
       done
       ```

    b. For each match, remove the line. Patterns observed in install.js (per RESEARCH.md):
       - SRC tuples: `[ '<slug>.md', ... ]` arrays — remove the matching `<slug>.md` array entry, preserving comma + array shape.
       - removal-loop entries (lines 3576-3581 area): `'commands/brief/<slug>.md'` strings — remove the whole array element.
       - PATCHES_DIR_NAME / MANIFEST_NAME path concatenations: similar string match — remove.
       - Verify-block lookups: `if (file === '<slug>.md')` style — remove the conditional branch entirely if it only fires for a deleted slug.

    c. After every deletion, verify the install.js still parses: `node -c bin/install.js`. If a syntax error appears, the line removal broke a comma or bracket — fix surgically.

    d. ALSO: if any reference to `new-project.md` remains (now-renamed), update those to `init.md` instead of removing — these are the 1 entry that survives as the renamed file.

    e. Verify zero remaining refs to DELETED_56 slugs (per Pitfall 1):
       ```bash
       for slug in add-backlog add-phase add-todo analyze-dependencies audit-fix audit-milestone audit-uat autonomous check-todos cleanup complete-milestone discuss-phase do docs-update execute-phase explore extract_learnings fast from-gsd2 health import insert-phase intel join-discord list-phase-assumptions list-workspaces manager map-codebase milestone-summary new-milestone new-workspace next note plan-milestone-gaps plan-phase plant-seed profile-user quick reapply-patches remove-phase remove-workspace research-phase resume-work review review-backlog scan session-report set-profile settings spec-phase stats thread update validate-phase verify-work workstreams; do
         if grep -E "['\"\\/]${slug}\\.md['\"\\)]" bin/install.js; then echo "STILL PRESENT: $slug"; fi
       done
       # Note: 'new-project' is excluded from this final check — it remains as init.md.
       ```
       The final loop must print nothing.

    **Step 4 — Generate `.planning/SURFACE-AUDIT.md`:**

    Write the audit doc per PATTERNS.md lines 695-736 schema verbatim. Section breakdown:

    a. Front-matter heading + metadata block:
       ```
       # BRIEF Surface Audit — v1 Launch

       **Audited:** 2026-04-27
       **Cap source:** CLAUDE.md "Surface Caps" section (Phase 2 D-09); HRD-02 closure (Phase 9)
       **Enforcement:** Surface cap is documentation-enforced per Phase 2 D-07. No pre-commit hook blocks new command additions; reviewer discipline maintains the cap. (W10 revision iteration 1 — explicit note to prevent ambiguity: this audit doc is the v1 enforcement mechanism, not a runtime gate.)
       **Status:** PASS — 12 commands ≤ 12 cap; 0 skills ≤ 8 cap
       ```
       Avoid the word `compliance` / `compliant` (vocabulary lock).

    b. `## User-Facing Commands (12 / 12 cap)` table — exact 12 rows per RESEARCH.md lines 651-664. Use the same one-line rationale text per command (do not invent novel descriptions).

    c. `## Skills (0 / 8 cap)` paragraph — empty by reservation per A-D03.

    d. `## Removed in v1 (56 commands deleted)` appendix — list ALL 56 deleted slugs grouped by phase-introduced (per RESEARCH.md Discretion-4 recommendation: by phase-introduced for traceability). Use these two groups as a starting heuristic (cite RESEARCH.md):
       - **Inherited GSD developer surfaces (28):** audit-fix, audit-uat, audit-milestone, autonomous, check-todos, cleanup, complete-milestone, do, docs-update, explore, fast, health, import, intel, manager, milestone-summary, new-milestone, new-workspace, profile-user, quick, reapply-patches, scan, session-report, set-profile, settings, stats, thread, update.
       - **Inherited GSD orchestrator surfaces, not domain-mapped (28):** add-backlog, add-phase, add-todo, analyze-dependencies, discuss-phase, execute-phase, extract_learnings, from-gsd2, insert-phase, join-discord, list-phase-assumptions, list-workspaces, map-codebase, new-project, next, note, plan-milestone-gaps, plan-phase, plant-seed, remove-phase, remove-workspace, research-phase, resume-work, review, review-backlog, spec-phase, validate-phase, verify-work, workstreams.

       NOTE: `new-project` is in the second group as a deleted slug, but its FILE was renamed to `init.md` rather than deleted; document this in a footnote: "`new-project.md` was renamed to `init.md` per A-D01 lineup (slug `init`); the file content is preserved and re-shaped to /brief-init in the locked 12-cmd lineup, NOT deleted."

       28 + 28 = 56 — verify the total.

    e. Recovery instructions:
       ```
       > All deleted commands preserved on `backup/original-gsd` branch.
       > Recovery: `git checkout backup/original-gsd -- commands/brief/{name}.md`
       ```

    Use `core.atomicWriteFileSync` semantics if invoked through brief-tools; otherwise plain `fs.writeFileSync` is acceptable for `.planning/` artifacts created during plan execution (per Phase 1 norms — atomic commit covers the whole-plan transaction).

    **Step 5 — Update `CLAUDE.md` Surface Caps section:**

    a. Locate the `## Surface Caps` section in CLAUDE.md (existing per Phase 2 D-09).

    b. Update the text:
       - Replace the "Current state:" sentence (which currently reads "Total user-facing commands = 68; total agents = 23. Both counts still exceed the cap.") with: `**Current state (v1 launch):** 12 user-facing commands, 0 skills. Cap met for both. Audit: see `.planning/SURFACE-AUDIT.md`.`
       - Preserve the rationale, scope clarification, and definition of "user-facing" sections.
       - Reference the new `SURFACE-AUDIT.md` audit doc by relative path so readers can navigate.

    c. Do NOT touch other CLAUDE.md sections (Project, Constraints, Tech stack, etc. are unchanged by this plan).

    **Step 6 — Atomic commit:**

    All changes from Steps 2-5 land in ONE commit. Stage explicitly (do NOT use `git add -A` — too broad, risks staging unrelated dev cruft):
    ```bash
    git add commands/brief/init.md  # the renamed file's frontmatter update
    git add bin/install.js
    git add .planning/SURFACE-AUDIT.md
    git add CLAUDE.md
    # The 55 git rm + 1 git mv from Step 2 are already staged.
    git add commands/brief/define.md commands/brief/discover.md commands/brief/design.md commands/brief/add-workstream.md commands/brief/deliver.md commands/brief/export.md commands/brief/status.md commands/brief/help.md commands/brief/progress.md commands/brief/undo.md commands/brief/pause-work.md  # the 11 sed-bulk-rewrite frontmatter edits (W9)
    git status  # verify staged set: 12 modified (11 W9 sed + init.md frontmatter), 1 modified (install.js), 1 new (SURFACE-AUDIT.md), 1 modified (CLAUDE.md), 55 deleted, 1 renamed (new-project.md → init.md). Total ~70 staged entries.
    ```

    Then commit via the gsd-sdk:
    ```bash
    gsd-sdk query commit "feat(09): HRD-02 surface pruning to locked 12 cmds + SURFACE-AUDIT.md + CLAUDE.md sync (Phase 9 Plan 05)"
    ```
    (If the sdk wrapper is not available in worktree, use `git commit -m "..."` with a HEREDOC matching CLAUDE.md commit conventions; never `--no-verify`.)

    **Step 7 — Post-commit verification:**

    a. `ls commands/brief/*.md | wc -l` returns 12.
    b. `node --test tests/brief-surface-audit-count.test.cjs tests/brief-surface-audit-install-cleanup.test.cjs tests/brief-surface-audit-doc.test.cjs` exits 0.
    c. `git log -1 --name-status` shows the expected 55 D + 1 R + 4 M (modified) entries.
    d. `git rev-parse backup/original-gsd` still resolves; spot-check `git show backup/original-gsd:commands/brief/audit-fix.md | head -5` still prints frontmatter (backup intact).
    e. `node -e "console.log(Object.keys(require('./package.json').dependencies||{}).length)"` returns 0 (A1 preserved).

    **Phase 8 cwd-bug discipline:**
    - Worktree-isolated agent ONLY. Never operate against `/Users/agent/GSD-for-Business` directly.
    - All file paths are relative to the worktree root.
    - Final commit lands in the worktree branch; orchestrator merges to main after this plan completes.
  </action>
  <verify>
    <automated>node --test tests/brief-surface-audit-count.test.cjs tests/brief-surface-audit-install-cleanup.test.cjs tests/brief-surface-audit-doc.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `ls commands/brief/*.md | wc -l` returns exactly 12.
    - `ls commands/brief/*.md | sed 's|.*/||;s|\\.md$||' | sort | tr '\\n' ' '` returns exactly: `add-workstream define deliver design discover export help init pause-work progress status undo ` (in alphabetical order).
    - `ls commands/brief/new-project.md 2>/dev/null` returns nothing (file renamed).
    - `ls commands/brief/init.md` returns 1 line (renamed file present).
    - `grep -c "name: brief:init" commands/brief/init.md` returns 1 (frontmatter updated).
    - `grep -E "['\"\\\\/]\\b(add-backlog|audit-fix|audit-uat|cleanup|discuss-phase|execute-phase|plan-phase|verify-work|review|cleanup|workstreams)\\.md\\b" bin/install.js` returns nothing (Pitfall 1 fixed for spot-check sample of deleted slugs).
    - `node -c bin/install.js` exits 0 (no syntax errors after removals).
    - `ls .planning/SURFACE-AUDIT.md` returns 1 line.
    - `wc -l .planning/SURFACE-AUDIT.md` shows ≥80 lines.
    - `head -1 .planning/SURFACE-AUDIT.md` returns `# BRIEF Surface Audit — v1 Launch`.
    - `grep -cE "^\\| [0-9]+\\s+\\|\\s+\\\`/brief-(define|discover|design|deliver|export|add-workstream|status|help|init|progress|undo|pause-work)\\\`" .planning/SURFACE-AUDIT.md` returns 12 (all 12 LOCKED_12 rows present in commands table).
    - `grep -c "Removed in v1.*56\\|56 commands deleted\\|56 commands" .planning/SURFACE-AUDIT.md` returns ≥1 (Removed in v1 section header includes count).
    - `grep -c "0 / 8 cap\\|0/8 cap\\|0 skills" .planning/SURFACE-AUDIT.md` returns ≥1 (skills cap section).
    - `grep -c "backup/original-gsd" .planning/SURFACE-AUDIT.md` returns ≥1 (recovery instructions).
    - `grep -c "SURFACE-AUDIT\\.md" CLAUDE.md` returns ≥1 (CLAUDE.md references the new audit doc).
    - `grep -c "12 user-facing commands\\|12/12\\|12 cmds" CLAUDE.md` returns ≥1 (Surface Caps section reflects v1 final state).
    - `git log -1 --pretty=%s` returns a single commit message starting with `feat(09):` or matching CLAUDE.md commit-style; same commit MUST contain the 55 D + 1 R + 4 M entries (`git log -1 --name-status | wc -l` shows ≥60 lines including header/footer).
    - `git rev-parse backup/original-gsd` exits 0 (backup branch intact).
    - `git show backup/original-gsd:commands/brief/audit-fix.md 2>&1 | head -2 | grep -c "^---"` returns ≥1 (a deleted file is recoverable from backup).
    - `node --test tests/brief-surface-audit-count.test.cjs tests/brief-surface-audit-install-cleanup.test.cjs tests/brief-surface-audit-doc.test.cjs` exits 0 with all assertions passing (no skips related to disk state, no failures on deletion-derived assertions).
    - **W9 sed bulk-rewrite verification:** `grep -l "^name: gsd:" commands/brief/*.md 2>/dev/null` returns nothing (zero remaining gsd: drift in LOCKED_12 frontmatter post-rewrite).
    - **W9 — all 12 LOCKED_12 commands have `name: brief:` frontmatter:** `grep -lE "^name: brief:" commands/brief/*.md | wc -l` returns 12 (every retained command has been migrated to brief: namespace).
    - **W10 — SURFACE-AUDIT.md documents the documentation-enforcement model:** `grep -c "documentation-enforced\|Phase 2 D-07\|reviewer discipline" .planning/SURFACE-AUDIT.md` returns ≥1.
    - `node -e "console.log(Object.keys(require('./package.json').dependencies||{}).length)"` returns `0`.
  </acceptance_criteria>
  <done>commands/brief/ contains exactly the 12 LOCKED_12 files; new-project.md renamed to init.md with corrected frontmatter; bin/install.js has zero refs to any of 56 deleted slugs; SURFACE-AUDIT.md generated with 12-row table + 56-entry appendix + backup-branch recovery instructions; CLAUDE.md Surface Caps section references SURFACE-AUDIT.md; all in single atomic commit; backup/original-gsd intact; 3 Wave 0 surface-audit fixtures GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mass file deletion | 56 files removed from main branch; backup branch is the rollback path |
| bin/install.js modifications | Cleanup of dangling SRC tuples; syntax-fragile (must `node -c` validate after each removal) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-9-03 | T (Tampering) / D (Denial of Service) | Wrong file deleted (e.g., one of LOCKED_12 slugs accidentally rm'd) | mitigate | Pre-flight verifies LOCKED_12 list against disk via `diff /tmp/actual.txt /tmp/expected.txt`; backup branch (A-D02) preserves every deleted file; rollback path documented in SURFACE-AUDIT.md. Worktree-isolated agent (Phase 8 cwd-bug lesson) means a misfire only affects the worktree branch, not main, until orchestrator merges. |
| T-9-14 | T (Tampering) | bin/install.js syntax error after line removal breaks `npm install` for users | mitigate | After every removal section, run `node -c bin/install.js` to validate parsing. The atomic commit means partial state (some refs removed, some left) never reaches main — either all 56 cleanups land or none do. |
| T-9-15 | I (Information Disclosure) | SURFACE-AUDIT.md contains the full deleted-command list (potentially internal-only metadata) | accept | All deleted commands are inherited GSD developer surfaces and were public in upstream GSD. The list is committed to a public repo; no secrets. |
| T-9-16 | E (Elevation of Privilege) | Worktree-isolated agent could reach the main repo if cwd-bug fires | mitigate | `<critical_cwd_warning>` block embedded; pre-flight `git rev-parse --show-toplevel` check is the first action of the agent; fails closed (STOP and report) if cwd is wrong. |
</threat_model>

<verification>
After this single-task plan completes:
- `ls commands/brief/*.md | wc -l` = 12.
- `node --test tests/brief-surface-audit-count.test.cjs tests/brief-surface-audit-install-cleanup.test.cjs tests/brief-surface-audit-doc.test.cjs` exits 0.
- `node -c bin/install.js` exits 0 (syntax intact).
- `git rev-parse backup/original-gsd` exits 0 (backup intact).
- A1 preserved.
- Single atomic commit landed in worktree branch.
</verification>

<success_criteria>
- 12 commands remain (LOCKED_12 exact match)
- 56 deletions atomic — single commit
- new-project.md → init.md rename completes (Open Question 1 resolved)
- bin/install.js zero dangling refs (Pitfall 1)
- SURFACE-AUDIT.md generated with 12-row + 56-entry appendix (A-D04) + W10 documentation-enforcement note
- CLAUDE.md Surface Caps updated to reference audit doc
- backup/original-gsd intact (A-D02 rollback path verified)
- 3 Wave 0 surface-audit fixtures GREEN
- **W4 scope explicit:** 60+ file changes documented in task name + body (mechanical bulk operations only; atomicity load-bearing per Pitfall 1)
- **W9 closure:** 11 LOCKED_12 commands rewritten via sed bulk-rewrite (`name: gsd:*` → `name: brief:*`) in same atomic commit; zero remaining gsd: frontmatter drift in retained surface
- **W10 closure:** SURFACE-AUDIT.md documents the surface cap as documentation-enforced per Phase 2 D-07 (no pre-commit hook in v1; reviewer discipline maintains the cap)
</success_criteria>

<output>
After completion, create `.planning/phases/09-hardening-cross-runtime-smoke-non-developer-pilot/09-05-SUMMARY.md` documenting:
- Final disk state: `commands/brief/*.md` count, list of 12 remaining slugs.
- Total bin/install.js cleanup count: number of references removed (RESEARCH.md anticipates many SRC tuple anchors; record actual count for v1.1 reference).
- Whether the 28+28 phase-introduced grouping in SURFACE-AUDIT.md "Removed in v1" appendix matches RESEARCH.md categorization or required adjustment (record any deviations).
- Verification that all tests in tests/brief-surface-audit-*.test.cjs are GREEN.
- Confirmation: `node -c bin/install.js` passes; `git rev-parse backup/original-gsd` resolves.
</output>
