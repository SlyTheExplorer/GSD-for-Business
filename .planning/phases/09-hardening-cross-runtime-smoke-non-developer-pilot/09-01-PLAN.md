---
phase: 09-hardening-cross-runtime-smoke-non-developer-pilot
plan: 01
type: execute
wave: 1
depends_on: [00]
files_modified:
  - brief/bin/lib/smoke-test.cjs
  - brief/bin/brief-tools.cjs
  - .planning/SMOKE-TEST.md
autonomous: true
requirements: [HRD-01]

must_haves:
  truths:
    - "User runs `brief-tools.cjs smoke-test run` and receives a 4×5 PASS/FAIL/SKIP matrix at `.planning/SMOKE-TEST.md`"
    - "smoke-test.cjs exports `RUNTIMES` (4 entries: claude/codex/gemini/opencode), `COMMANDS` (5 entries: init/define/discover/design/deliver), `smokeOneCell(runtime, cmd, briefRoot)`, `buildMatrix(briefRoot)`, `renderMatrixMarkdown(matrix)`"
    - "smokeOneCell uses `execFileSync(process.execPath, [path.join(briefRoot, 'brief/bin/brief-tools.cjs'), cmd, '--smoke', '--text'], { env, encoding: 'utf-8', timeout: 5000 })` — Pitfall 4 mitigation"
    - "Each cell returns { status: 'PASS'|'FAIL'|'SKIP', reason: string }; FAIL when stdout includes 'AskUserQuestion' AND runtime.env.text_mode_default===true (text_mode fallback failed); FAIL when subprocess throws (catches err)"
    - "case 'smoke-test' in brief-tools.cjs follows the case 'voice-fit' (lines 864-907) try/catch + core.error + core.output byte-identity pattern; subcommand 'run' invokes buildMatrix→renderMatrixMarkdown→atomicWriteFileSync"
    - "SMOKE-TEST.md has a markdown header, a 4×5 matrix table (5 columns init/define/discover/design/deliver), a 4-row body (claude/codex/gemini/opencode), and a FAIL/SKIP detail section"
    - "Zero new runtime npm dependencies (A1 preserved); built-in node:child_process + node:fs + node:path only"
  artifacts:
    - path: "brief/bin/lib/smoke-test.cjs"
      provides: "Cross-runtime smoke test orchestration: subprocess fan-out + matrix render"
      min_lines: 130
      contains: "execFileSync"
    - path: "brief/bin/brief-tools.cjs"
      provides: "Adds case 'smoke-test' dispatcher (mirroring case 'voice-fit' byte-identity)"
      contains: "case 'smoke-test'"
    - path: ".planning/SMOKE-TEST.md"
      provides: "4×5 PASS/FAIL/SKIP audit doc per B-D04"
      min_lines: 20
      contains: "Runtime"
  key_links:
    - from: "brief/bin/brief-tools.cjs"
      to: "brief/bin/lib/smoke-test.cjs"
      via: "require('./lib/smoke-test.cjs') in case 'smoke-test'"
      pattern: "require.*smoke-test\\.cjs"
    - from: "brief/bin/lib/smoke-test.cjs"
      to: "brief/bin/brief-tools.cjs (mock subprocess child)"
      via: "execFileSync(process.execPath, [path.join(briefRoot, 'brief/bin/brief-tools.cjs'), cmd, '--smoke', '--text'])"
      pattern: "brief-tools\\.cjs.*--smoke"
    - from: "brief/bin/lib/smoke-test.cjs"
      to: ".planning/SMOKE-TEST.md"
      via: "core.atomicWriteFileSync(outPath, renderMatrixMarkdown(matrix))"
      pattern: "atomicWriteFileSync"
---

<objective>
Implement HRD-01 cross-runtime smoke test as a stub-driven (B-D01) lib + dispatcher case + audit doc. After this plan lands, `node brief/bin/brief-tools.cjs smoke-test run` produces `.planning/SMOKE-TEST.md` with a 4×5 PASS/FAIL/SKIP matrix verifying that BRIEF's runtime detection (FND-06 substrate) works for Claude Code, Codex, Gemini, and OpenCode without invoking real CLIs.

Purpose: Closes HRD-01 (success criterion #1 in ROADMAP Phase 9). Verifies that text_mode fallback activates correctly for runtimes without AskUserQuestion (codex/gemini/opencode) without requiring those CLIs to be installed or paid API calls. Stub-driven by design per B-D01 — never invokes real Codex/Gemini/OpenCode CLIs (deferred to v1.1 `--live` opt-in).

Output: New `brief/bin/lib/smoke-test.cjs` (~150 LOC mirroring `voice-fit.cjs` lib structure), new `case 'smoke-test'` dispatcher in `brief-tools.cjs` (mirroring `case 'voice-fit'` lines 864-907 byte-identity), and `.planning/SMOKE-TEST.md` generated artifact. Wave 0 fixtures (Plan 00) flip from skip→active and pass.
</objective>

<execution_context>
@~/.claude/brief/workflows/execute-plan.md
@~/.claude/brief/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-hardening-cross-runtime-smoke-non-developer-pilot/09-CONTEXT.md
@.planning/phases/09-hardening-cross-runtime-smoke-non-developer-pilot/09-RESEARCH.md
@.planning/phases/09-hardening-cross-runtime-smoke-non-developer-pilot/09-PATTERNS.md
@.planning/phases/09-hardening-cross-runtime-smoke-non-developer-pilot/09-VALIDATION.md
@brief/bin/lib/voice-fit.cjs
@brief/bin/lib/leakage-diff.cjs
@brief/bin/brief-tools.cjs
@tests/brief-define-atomic-commit.test.cjs
@tests/ask-user-questions-fallback.test.cjs
@tests/brief-smoke-test-stub.test.cjs
@tests/brief-smoke-test-text-mode.test.cjs
@tests/brief-smoke-test-output-format.test.cjs

<interfaces>
<!-- Existing exports the executor will REUSE. -->

From brief/bin/lib/core.cjs (existing — DO NOT modify):
```javascript
function atomicWriteFileSync(filePath: string, content: string): void;
function planningPaths(cwd: string): { planning: string, ... };  // returns absolute paths
function output(jsonResult: object, raw: boolean, fallbackText: string): void;
function error(message: string): void;
```

From brief/bin/brief-tools.cjs (existing — case dispatcher pattern, lines 864-907 are case 'voice-fit'):
```javascript
case 'voice-fit': {
  const voiceFit = require('./lib/voice-fit.cjs');
  // ... try/catch + core.output + core.error pattern
  break;
}
// Plan 01 adds `case 'smoke-test'` immediately after, mirroring this exact shape.
```

NEW exports from brief/bin/lib/smoke-test.cjs (this plan creates):
```javascript
module.exports = {
  RUNTIMES,            // 4-entry array; matches Plan 00 fixture interface
  COMMANDS,            // 5-entry array
  smokeOneCell,        // (runtime, cmd, briefRoot) → { status, reason }
  buildMatrix,         // (briefRoot) → 4×5 array of cells
  renderMatrixMarkdown, // (matrix) → SMOKE-TEST.md body string
};
```

Wave 0 test fixtures (already exist after Plan 00; this plan turns them green):
- tests/brief-smoke-test-stub.test.cjs — asserts buildMatrix returns 4×5; each cell shape {status, reason}; RUNTIMES has 4 entries.
- tests/brief-smoke-test-text-mode.test.cjs — asserts text_mode_default for each runtime; smokeOneCell on codex doesn't fail with text_mode reason.
- tests/brief-smoke-test-output-format.test.cjs — asserts SMOKE-TEST.md schema (5-column header, 4 runtime rows).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create brief/bin/lib/smoke-test.cjs (RUNTIMES + COMMANDS + smokeOneCell + buildMatrix + renderMatrixMarkdown)</name>
  <files>brief/bin/lib/smoke-test.cjs</files>
  <read_first>
    - brief/bin/lib/voice-fit.cjs (analog A — lib structure: header doc-comment, 'use strict', requires, constants, functions, module.exports at bottom)
    - brief/bin/lib/leakage-diff.cjs (analog A.2 — confirms lib pattern; uses extractFrontmatter)
    - tests/brief-define-atomic-commit.test.cjs (analog B — `execFileSync(process.execPath, [...], { env, encoding, timeout })` pattern at line 30)
    - .planning/phases/09-hardening-cross-runtime-smoke-non-developer-pilot/09-PATTERNS.md (lines 256-332 — verbatim byte-identity template for smoke-test.cjs including imports, constants, smokeOneCell, module.exports)
    - .planning/phases/09-hardening-cross-runtime-smoke-non-developer-pilot/09-RESEARCH.md (lines 288-331 — Pattern 1 stub-driven smoke test; lines 525-536 — Pitfall 4 timeout 5s discipline)
    - tests/brief-smoke-test-stub.test.cjs (the Wave 0 fixture — see what assertions to satisfy)
    - tests/brief-smoke-test-text-mode.test.cjs (the Wave 0 fixture — see what text_mode assertions to satisfy)
    - tests/brief-smoke-test-output-format.test.cjs (the Wave 0 fixture — see the SMOKE-TEST.md schema)
  </read_first>
  <behavior>
    - smokeOneCell with claude runtime + 'init' cmd → returns {status: 'PASS'|'FAIL', reason: string}; never returns SKIP for claude (claude has full runtime support).
    - smokeOneCell with codex runtime + 'init' cmd → returns FAIL with reason ≠ 'text_mode...' (text_mode plumbing intact); subprocess sees INSTRUCTION_FILE=AGENTS.md env.
    - smokeOneCell catches subprocess errors and returns {status: 'FAIL', reason: <first line of err.message>}; never throws to caller.
    - smokeOneCell sets timeout: 5000 — any cell exceeding 5s returns FAIL with timeout reason (Pitfall 4).
    - buildMatrix(briefRoot) returns a 4-row × 5-column 2D array; rows in RUNTIMES order, columns in COMMANDS order.
    - renderMatrixMarkdown(matrix) returns markdown with: header (`# BRIEF Cross-Runtime Smoke Test — v1 Launch`), Run date metadata, the 4×5 table with PASS/FAIL/SKIP cell values, and a FAIL/SKIP Detail section listing each non-PASS cell's reason.
    - RUNTIMES export contains exactly: claude (text_mode_default: false), codex (INSTRUCTION_FILE: AGENTS.md, text_mode_default: true), gemini (text_mode_default: true), opencode (text_mode_default: true).
    - COMMANDS export contains exactly: ['init', 'define', 'discover', 'design', 'deliver'].
  </behavior>
  <action>
    Create `brief/bin/lib/smoke-test.cjs` byte-identical to PATTERNS.md lines 264-332 with these specifics:

    1. Header doc-comment block matching voice-fit.cjs lines 1-21 shape, citing 09-RESEARCH.md §Pattern 1 lines 288-331 + B-D01 stub-driven discipline + Pitfall 4 timeout mitigation.

    2. `'use strict';` then requires:
       ```javascript
       const { execFileSync } = require('node:child_process');
       const fs = require('node:fs');
       const path = require('node:path');
       ```
       NEVER `const cross_spawn = require('cross-spawn')`, NEVER `const execa = require('execa')`. A1 invariant.

    3. RUNTIMES constant (per B-D01 + B-D03):
       ```javascript
       const RUNTIMES = [
         { name: 'claude',   env: {                                   text_mode_default: false } },
         { name: 'codex',    env: { INSTRUCTION_FILE: 'AGENTS.md',    text_mode_default: true  } },
         { name: 'gemini',   env: {                                   text_mode_default: true  } },
         { name: 'opencode', env: {                                   text_mode_default: true  } },
       ];
       ```

    4. COMMANDS constant (per B-D02):
       ```javascript
       const COMMANDS = ['init', 'define', 'discover', 'design', 'deliver'];
       ```

    5. `function smokeOneCell(runtime, cmd, briefRoot)`:
       - Build `const env = { ...process.env, ...runtime.env, BRIEF_RUNTIME_MOCK: runtime.name };` (NOTE: spread `runtime.env` last so it OVERRIDES inherited INSTRUCTION_FILE if any).
       - **IMPORTANT:** the `text_mode_default` key in runtime.env is a stub-test marker (NOT a real env var consumed by brief-tools.cjs). Strip it before spreading into env to avoid polluting child env: `const { text_mode_default, ...envOverrides } = runtime.env; const env = { ...process.env, ...envOverrides, BRIEF_RUNTIME_MOCK: runtime.name };`.
       - try/catch around `execFileSync(process.execPath, [path.join(briefRoot, 'brief', 'bin', 'brief-tools.cjs'), cmd, '--smoke', '--text'], { env, encoding: 'utf-8', timeout: 5000 });`
       - If output includes 'AskUserQuestion' AND runtime.env.text_mode_default===true → return `{ status: 'FAIL', reason: 'AskUserQuestion present despite text_mode' }`.
       - Otherwise return `{ status: 'PASS', reason: '' }`.
       - On catch (subprocess threw — e.g., unknown subcommand, ETIMEDOUT, exit !== 0): if `err.code === 'ETIMEDOUT'` return `{ status: 'FAIL', reason: 'timeout 5s' }`; else return `{ status: 'FAIL', reason: (err.message || '').split('\n')[0] }`.
       - If the cmd is one that brief-tools.cjs doesn't recognize as a valid `--smoke` target (e.g., `define` may not have a smoke entry yet), the subprocess will exit non-zero — that's a legitimate FAIL with reason being the error message. Per B-D02, this is acceptable: the smoke test reveals which cells need brief-tools.cjs `--smoke` flag support; cells failing this way are flagged in SMOKE-TEST.md "FAIL/SKIP Detail" section for v1.1 follow-up. Do NOT silently mark these as SKIP — FAIL is the load-bearing signal.

    6. `function buildMatrix(briefRoot)`:
       - Return `RUNTIMES.map(rt => COMMANDS.map(cmd => smokeOneCell(rt, cmd, briefRoot)));`

    7. `function renderMatrixMarkdown(matrix)`:
       - Header: `'# BRIEF Cross-Runtime Smoke Test — v1 Launch\n\n'`
       - Metadata: `'**Run:** ' + new Date().toISOString().slice(0, 10) + '\n'` + `'**Approach:** Stub-driven (B-D01). NEVER invokes real Codex/Gemini/OpenCode CLIs.\n'` + `'**Result format:** PASS / FAIL / SKIP per cell + one-line reason.\n\n'`
       - Table header: `'| Runtime    | init | define | discover | design | deliver |\n|------------|------|--------|----------|--------|---------|\n'`
       - Table body: for each i, ``'| ' + RUNTIMES[i].name.padEnd(10) + ' | ' + matrix[i].map(c => c.status.padEnd(4)).join(' | ') + ' |\n'`` (column widths to keep table readable but tolerant of FAIL).
       - "FAIL/SKIP Detail" section: collect every non-PASS cell with `(runtime, cmd) → reason` and emit one bullet line per item. If all PASS, emit `(All cells PASS — text_mode fallback verified across all 4 runtimes × 5 commands.)`.

    8. `module.exports = { RUNTIMES, COMMANDS, smokeOneCell, buildMatrix, renderMatrixMarkdown };` at the bottom (mirror voice-fit.cjs export shape).

    Per A1 invariant: ZERO new runtime deps. Verify post-creation: `node -e "console.log(Object.keys(require('./package.json').dependencies||{}).length)"` MUST still return 0.
  </action>
  <verify>
    <automated>node --test tests/brief-smoke-test-stub.test.cjs tests/brief-smoke-test-text-mode.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `ls brief/bin/lib/smoke-test.cjs` returns 1 line.
    - `wc -l brief/bin/lib/smoke-test.cjs` shows ≥130 lines.
    - `grep -c "execFileSync\\|node:child_process" brief/bin/lib/smoke-test.cjs` returns ≥2.
    - `grep -c "timeout: 5000\\|timeout:5000" brief/bin/lib/smoke-test.cjs` returns ≥1 (Pitfall 4 mitigation explicit).
    - `grep -c "AskUserQuestion" brief/bin/lib/smoke-test.cjs` returns ≥1 (text_mode fallback assertion present).
    - `grep -c "RUNTIMES\\s*=" brief/bin/lib/smoke-test.cjs` returns ≥1; `grep -E "name:\\s*'(claude|codex|gemini|opencode)'" brief/bin/lib/smoke-test.cjs | wc -l` returns 4 (all 4 runtimes).
    - `grep -c "COMMANDS\\s*=" brief/bin/lib/smoke-test.cjs` returns ≥1; `grep -E "'(init|define|discover|design|deliver)'" brief/bin/lib/smoke-test.cjs` returns ≥5 (all 5 commands).
    - `grep -E "require\\('(commander|cross-spawn|execa|fast-levenshtein)'\\)" brief/bin/lib/smoke-test.cjs` returns nothing.
    - `node -e "console.log(Object.keys(require('./package.json').dependencies||{}).length)"` returns `0` (A1 preserved).
    - `node -e "const s=require('./brief/bin/lib/smoke-test.cjs'); console.log(s.RUNTIMES.length, s.COMMANDS.length, typeof s.smokeOneCell, typeof s.buildMatrix, typeof s.renderMatrixMarkdown)"` outputs `4 5 function function function`.
    - `node --test tests/brief-smoke-test-stub.test.cjs tests/brief-smoke-test-text-mode.test.cjs` exits 0 with all skips removed (fixture tests now active and passing).
  </acceptance_criteria>
  <done>smoke-test.cjs lib exists with all 5 exports; Wave 0 fixtures brief-smoke-test-stub + brief-smoke-test-text-mode are now GREEN (no longer skipped); A1 invariant preserved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add `case 'smoke-test'` dispatcher to brief-tools.cjs + generate .planning/SMOKE-TEST.md</name>
  <files>brief/bin/brief-tools.cjs, .planning/SMOKE-TEST.md</files>
  <read_first>
    - brief/bin/brief-tools.cjs (lines 864-907 — case 'voice-fit' byte-identity template; copy try/catch + core.error + core.output shape)
    - .planning/phases/09-hardening-cross-runtime-smoke-non-developer-pilot/09-PATTERNS.md (lines 385-420 — verbatim case 'smoke-test' template)
    - brief/bin/lib/core.cjs (atomicWriteFileSync, planningPaths, output, error — the 4 helpers the dispatcher uses)
    - tests/brief-smoke-test-output-format.test.cjs (Wave 0 fixture — schema this task must satisfy)
  </read_first>
  <behavior>
    - `node brief/bin/brief-tools.cjs smoke-test run` invocation: builds matrix, writes `.planning/SMOKE-TEST.md` via atomicWriteFileSync, prints success line including outPath, exits 0.
    - `node brief/bin/brief-tools.cjs smoke-test run --out /tmp/smoke.md` writes to /tmp/smoke.md instead of default location.
    - `node brief/bin/brief-tools.cjs smoke-test bogus` calls core.error with `smoke-test: unknown subcommand 'bogus'. Valid: run` and exits non-zero.
    - dispatcher try/catch wraps all lib calls; runtime errors invoke core.error(err.message) — no absolute-path stack leakage to user.
    - The generated SMOKE-TEST.md has exactly 4 runtime rows + 5 command columns header, matching the schema in the Wave 0 output-format fixture.
  </behavior>
  <action>
    1. Locate `case 'voice-fit'` in `brief/bin/brief-tools.cjs` (lines 864-907 per PATTERNS.md). Immediately AFTER the `break;` of `case 'voice-fit'`, insert a new `case 'smoke-test':` block with byte-identity to PATTERNS.md lines 392-420:
       ```javascript
       case 'smoke-test': {
         // Plan 01 — SMOKE-TEST dispatcher. Mirrors `case 'voice-fit'` byte-identity
         // pattern. Stub-driven (B-D01) — never invokes real Codex/Gemini/OpenCode CLIs.
         //
         // Subcommands:
         //   smoke-test run [--out <path>]
         //     → buildMatrix() over RUNTIMES × COMMANDS, renders to SMOKE-TEST.md
         const smoke = require('./lib/smoke-test.cjs');
         const stSubcommand = args[1];
         const stOutIdx = args.indexOf('--out');
         const stOutPath = stOutIdx !== -1
           ? args[stOutIdx + 1]
           : path.join(core.planningPaths(cwd).planning, 'SMOKE-TEST.md');
         try {
           if (stSubcommand === 'run') {
             const matrix = smoke.buildMatrix(cwd);
             const md = smoke.renderMatrixMarkdown(matrix);
             core.atomicWriteFileSync(stOutPath, md);
             core.output({ matrix, outPath: stOutPath }, raw, `SMOKE-TEST.md written: ${stOutPath}`);
             break;
           }
           core.error(`smoke-test: unknown subcommand '${stSubcommand}'. Valid: run`);
         } catch (err) {
           core.error(err.message);
         }
         break;
       }
       ```
       (Use the exact identifier names — `stSubcommand`, `stOutIdx`, `stOutPath` — to avoid collision with surrounding voice-fit / leakage-diff dispatcher local vars per PATTERNS.md byte-identity discipline; if the file uses a different prefix convention, match the surrounding case dispatcher's prefix style. Verify by reading lines around 864-907 first.)

    2. Run the new dispatcher once to generate `.planning/SMOKE-TEST.md`:
       ```bash
       cd /Users/agent/GSD-for-Business
       node brief/bin/brief-tools.cjs smoke-test run
       ```
       The output should be `SMOKE-TEST.md written: /Users/agent/GSD-for-Business/.planning/SMOKE-TEST.md`.

    3. Verify the generated SMOKE-TEST.md schema:
       - First line: `# BRIEF Cross-Runtime Smoke Test — v1 Launch`
       - Contains `**Run:**`, `**Approach:** Stub-driven (B-D01)`.
       - Has the 4×5 table with header row containing `Runtime | init | define | discover | design | deliver`.
       - Has 4 body rows: claude, codex, gemini, opencode.
       - Has a `## FAIL/SKIP Detail` section.

    4. The generated cells are EXPECTED to mostly be FAIL initially because brief-tools.cjs may not yet recognize `--smoke` flag for `init`/`define`/etc. cmds. That's OK and load-bearing — SMOKE-TEST.md becomes a v1.1 follow-up checklist showing which dispatcher cmds need `--smoke` handler additions. Document this expected partial-FAIL state in the FAIL/SKIP Detail section by virtue of the renderMatrixMarkdown output.

    5. Per Phase 8 cwd-bug lesson: This task does NOT spawn a worktree-isolated agent. It edits `brief-tools.cjs` directly in the main worktree.

    6. Verify A1 invariant unchanged after edit: `node -e "console.log(Object.keys(require('./package.json').dependencies||{}).length)"` returns `0`.
  </action>
  <verify>
    <automated>node brief/bin/brief-tools.cjs smoke-test run && node --test tests/brief-smoke-test-output-format.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "case 'smoke-test'" brief/bin/brief-tools.cjs` returns 1.
    - `grep -c "require.*smoke-test\\.cjs" brief/bin/brief-tools.cjs` returns 1.
    - `grep -c "smoke-test: unknown subcommand" brief/bin/brief-tools.cjs` returns 1 (subcommand validation present).
    - `node brief/bin/brief-tools.cjs smoke-test run` exits 0 and creates `.planning/SMOKE-TEST.md`.
    - `node brief/bin/brief-tools.cjs smoke-test bogus` exits non-zero (subcommand validation works) — verify via `node brief/bin/brief-tools.cjs smoke-test bogus 2>&1; echo "EXIT: $?"` and confirm EXIT is non-zero.
    - `ls .planning/SMOKE-TEST.md` returns 1 line.
    - `head -1 .planning/SMOKE-TEST.md` returns `# BRIEF Cross-Runtime Smoke Test — v1 Launch`.
    - `grep -c "Runtime.*init.*define.*discover.*design.*deliver" .planning/SMOKE-TEST.md` returns ≥1 (header row present with 5 cmd columns).
    - `grep -cE "^\\| (claude|codex|gemini|opencode)" .planning/SMOKE-TEST.md` returns 4 (all 4 runtime rows).
    - `grep -c "FAIL/SKIP Detail\\|## FAIL" .planning/SMOKE-TEST.md` returns ≥1.
    - `node --test tests/brief-smoke-test-output-format.test.cjs` exits 0 with no skips.
    - `node -e "console.log(Object.keys(require('./package.json').dependencies||{}).length)"` returns `0`.
  </acceptance_criteria>
  <done>case 'smoke-test' dispatcher lands in brief-tools.cjs (byte-identity to case 'voice-fit'); SMOKE-TEST.md generated with 4×5 schema; Wave 0 brief-smoke-test-output-format fixture is GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| smokeOneCell subprocess invocation | Spawns `process.execPath` (Node binary) with controlled arg array; env extends process.env with BRIEF_RUNTIME_MOCK + INSTRUCTION_FILE |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-9-02 | E (Elevation of Privilege) / I (Information Disclosure) | smoke-test.cjs subprocess invocation — shell injection if any command name comes from untrusted input | mitigate | Command list is hardcoded in `COMMANDS` constant (5 entries), runtime list is hardcoded in `RUNTIMES` constant (4 entries). NEVER reads `args` from argv for cell construction. Subprocess is invoked via `execFileSync(process.execPath, [array of args], options)` — no shell, no string concat, no quoting. Per RESEARCH.md line 134: built-in node:child_process avoids cross-platform shell-quoting concerns by design. |
| T-9-07 | D (Denial of Service) | Subprocess hang if text_mode plumbing breaks (Pitfall 4) | mitigate | `execFileSync` invoked with `timeout: 5000` (5s); on ETIMEDOUT cell is marked FAIL with reason 'timeout 5s'. Total worst-case test duration: 4 runtimes × 5 commands × 5s = 100s; acceptable for one-shot smoke test invocation, NOT for unit-test-loop. |
| T-9-08 | I (Information Disclosure) | err.message in cell.reason may leak absolute paths | accept | The reason field is rendered into `.planning/SMOKE-TEST.md` which is committed via atomicWriteFileSync — the audit doc is internal pre-launch material; absolute paths in error reasons are acceptable for diagnostic value (mirrors PATTERNS.md "Dispatcher byte-identity" — only the *user-facing dispatcher* output is path-stripped via core.error). The matrix audit doc is diagnostic, not user-facing slash-command output. |
</threat_model>

<verification>
After both tasks complete:
- `node brief/bin/brief-tools.cjs smoke-test run` exits 0 and produces SMOKE-TEST.md.
- `node --test tests/brief-smoke-test-stub.test.cjs tests/brief-smoke-test-text-mode.test.cjs tests/brief-smoke-test-output-format.test.cjs` exits 0; all 3 fixtures GREEN (no skips related to Plan 01).
- `node -e "console.log(Object.keys(require('./package.json').dependencies||{}).length)"` still returns `0`.
- `grep -E "require\\('(commander|fast-levenshtein|cross-spawn|execa)'\\)" brief/bin/lib/smoke-test.cjs` returns nothing.
</verification>

<success_criteria>
- HRD-01 substrate operational: `brief-tools.cjs smoke-test run` produces 4×5 matrix
- 3 Wave 0 fixtures (brief-smoke-test-stub/text-mode/output-format) GREEN
- SMOKE-TEST.md committed at `.planning/SMOKE-TEST.md`
- Zero new npm runtime dependencies (A1 preserved)
- Stub-driven approach honored (B-D01) — no real Codex/Gemini/OpenCode CLI invocation
- timeout: 5000 explicit per cell (Pitfall 4 mitigation)
</success_criteria>

<output>
After completion, create `.planning/phases/09-hardening-cross-runtime-smoke-non-developer-pilot/09-01-SUMMARY.md` documenting which cells PASS vs FAIL in the initial SMOKE-TEST.md run (the FAIL list becomes a v1.1 follow-up backlog item). Note explicitly that initial-run FAILs are EXPECTED for cells where brief-tools.cjs doesn't yet implement `--smoke` flag handling — these are diagnostic signals for v1.1 cell-by-cell repair, NOT a Phase 9 blocker.
</output>
