---
phase: 09-hardening-cross-runtime-smoke-non-developer-pilot
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/brief-help-categorization.test.cjs
  - tests/brief-help-partial-match.test.cjs
  - tests/brief-help-levenshtein.test.cjs
  - tests/brief-smoke-test-stub.test.cjs
  - tests/brief-smoke-test-text-mode.test.cjs
  - tests/brief-smoke-test-output-format.test.cjs
  - tests/brief-surface-audit-count.test.cjs
  - tests/brief-surface-audit-install-cleanup.test.cjs
  - tests/brief-surface-audit-doc.test.cjs
  - tests/brief-pilot-journal-structure.test.cjs
  - tests/brief-v1-launch-gate.test.cjs
autonomous: true
requirements: [HRD-01, HRD-02, HRD-03, HRD-04, HRD-05]

must_haves:
  truths:
    - "All 11 Wave 0 test fixtures exist as `.test.cjs` files registered with `node:test` (describe + at least one `test('TODO ...')` stub each)"
    - "Each fixture is RED (skipped or failing with TODO message) until Wave 1/2 plans implement the production code that makes it green"
    - "Each fixture imports the analog pattern (architecture-counts.test.cjs filesystem-driven OR ask-user-questions-fallback.test.cjs OR brief-define-atomic-commit.test.cjs subprocess pattern) and lays the assertion skeleton with verifiable acceptance criteria"
    - "All 11 fixture files run via `node scripts/run-tests.cjs tests/brief-help-*.test.cjs tests/brief-smoke-*.test.cjs tests/brief-surface-*.test.cjs tests/brief-pilot-*.test.cjs tests/brief-v1-launch-*.test.cjs` without crashing the runner (skipped tests are acceptable; runtime errors are NOT)"
    - "No production code (no `brief/bin/lib/help.cjs`, no `brief/bin/lib/smoke-test.cjs`, no `.planning/SMOKE-TEST.md`, no `.planning/SURFACE-AUDIT.md`) is created in this plan — fixtures only"
  artifacts:
    - path: "tests/brief-help-categorization.test.cjs"
      provides: "Wave 0 RED fixture for HRD-03 4D phase categorization (DEFINE/DISCOVER/DESIGN/DELIVER/HELPERS) — `describe('/brief-help 4D categorization')` block with `test.skip` or `test('TODO ...')` stubs that reference `help.buildCatalog` + `help.renderCategorized`"
      min_lines: 20
      contains: "renderCategorized"
    - path: "tests/brief-help-partial-match.test.cjs"
      provides: "Wave 0 RED fixture for HRD-03 substring/prefix match (e.g., `def` matches `define`)"
      min_lines: 15
      contains: "partial"
    - path: "tests/brief-help-levenshtein.test.cjs"
      provides: "Wave 0 RED fixture for HRD-03 inline two-row DP Levenshtein + suggestTopK (distance ≤ 3 threshold)"
      min_lines: 25
      contains: "levenshtein"
    - path: "tests/brief-smoke-test-stub.test.cjs"
      provides: "Wave 0 RED fixture for HRD-01 4×5 matrix shape validation (4 RUNTIMES × 5 COMMANDS = 20 cells)"
      min_lines: 20
      contains: "buildMatrix"
    - path: "tests/brief-smoke-test-text-mode.test.cjs"
      provides: "Wave 0 RED fixture for HRD-01 text_mode fallback per runtime (codex/gemini/opencode INSTRUCTION_FILE env)"
      min_lines: 20
      contains: "text_mode"
    - path: "tests/brief-smoke-test-output-format.test.cjs"
      provides: "Wave 0 RED fixture for HRD-01 SMOKE-TEST.md schema (4 runtime rows + 5 command columns header)"
      min_lines: 15
      contains: "SMOKE-TEST.md"
    - path: "tests/brief-surface-audit-count.test.cjs"
      provides: "Wave 0 RED fixture for HRD-02 12-cmd + 0-skill cap audit (currently 68 cmds → expected fail until Plan 05 prunes)"
      min_lines: 25
      contains: "LOCKED_12"
    - path: "tests/brief-surface-audit-install-cleanup.test.cjs"
      provides: "Wave 0 RED fixture for HRD-02 bin/install.js no-dangling-refs (per Pitfall 1)"
      min_lines: 25
      contains: "DELETED_56"
    - path: "tests/brief-surface-audit-doc.test.cjs"
      provides: "Wave 0 RED fixture for HRD-02 SURFACE-AUDIT.md schema check (12 rows in commands table)"
      min_lines: 15
      contains: "SURFACE-AUDIT.md"
    - path: "tests/brief-pilot-journal-structure.test.cjs"
      provides: "Wave 0 RED fixture for HRD-04 friction journal frontmatter (pilot_id, user_role, logged) + at least 1 Pitfall #9 row"
      min_lines: 20
      contains: "pilot_id"
    - path: "tests/brief-v1-launch-gate.test.cjs"
      provides: "Wave 0 RED fixture for V1-LAUNCH-GATE.md three-prong checklist schema"
      min_lines: 15
      contains: "V1-LAUNCH-GATE"
  key_links:
    - from: "tests/brief-help-*.test.cjs"
      to: "brief/bin/lib/help.cjs (created by Plan 02)"
      via: "require('../brief/bin/lib/help.cjs') — fixtures wrap require in try/catch OR use t.skip until file exists"
      pattern: "require.*help\\.cjs"
    - from: "tests/brief-smoke-test-*.test.cjs"
      to: "brief/bin/lib/smoke-test.cjs (created by Plan 01)"
      via: "require('../brief/bin/lib/smoke-test.cjs') — same skip-until-exists pattern"
      pattern: "require.*smoke-test\\.cjs"
    - from: "tests/brief-surface-audit-*.test.cjs"
      to: "commands/brief/*.md (state changed by Plan 05)"
      via: "fs.readdirSync('commands/brief') — assertion fails until Plan 05 prunes from 68 → 12"
      pattern: "readdirSync.*commands/brief"
    - from: "tests/brief-pilot-journal-structure.test.cjs"
      to: ".planning/pilot/01-*-friction-journal.md (created by Plan 03)"
      via: "fs.existsSync + glob match on pilot/01-*-friction-journal.md"
      pattern: "pilot/01-"
    - from: "tests/brief-v1-launch-gate.test.cjs"
      to: ".planning/V1-LAUNCH-GATE.md (created by Plan 06)"
      via: "fs.existsSync + regex on three-prong checklist"
      pattern: "V1-LAUNCH-GATE\\.md"
---

<objective>
Scaffold all 11 Wave 0 test fixtures as RED-state describe-only stubs that will turn GREEN as Wave 1/2 plans land production code. Per the Nyquist Rule and 09-VALIDATION.md, every Wave 1/2 task must have an automated `<verify>` command pointing at a fixture that EXISTS — Wave 0 creates that infrastructure upfront.

Purpose: Per Phase 8 lib-split discipline, tests precede implementation. Wave 0 produces eleven structurally-correct, registerable `node:test` files that the runner discovers but each test is currently `test.skip` or asserts `TODO`. As Plans 01-06 ship code, the executor flips skip→active and assertions go GREEN. This plan ships ZERO production code — only test infrastructure.

Output: Eleven `tests/brief-{help,smoke,surface,pilot,v1-launch}-*.test.cjs` files that:
1. Register with `node:test` via `describe(...) { test(...) }` blocks.
2. Reference the analog file from PATTERNS.md so the executor in Plans 01-06 can extend without re-discovery.
3. Encode the LOCKED_12 slug list, DELETED_56 list, RUNTIMES/COMMANDS arrays, and category names as constants — the source of truth that Plans 01-06 must match byte-identically.
4. Run cleanly under `node scripts/run-tests.cjs tests/brief-help-*.test.cjs tests/brief-smoke-*.test.cjs tests/brief-surface-*.test.cjs tests/brief-pilot-*.test.cjs tests/brief-v1-launch-*.test.cjs` (skips OK; runtime errors NOT OK).
</objective>

<execution_context>
@~/.claude/brief/workflows/execute-plan.md
@~/.claude/brief/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-hardening-cross-runtime-smoke-non-developer-pilot/09-CONTEXT.md
@.planning/phases/09-hardening-cross-runtime-smoke-non-developer-pilot/09-RESEARCH.md
@.planning/phases/09-hardening-cross-runtime-smoke-non-developer-pilot/09-PATTERNS.md
@.planning/phases/09-hardening-cross-runtime-smoke-non-developer-pilot/09-VALIDATION.md
@tests/architecture-counts.test.cjs
@tests/ask-user-questions-fallback.test.cjs
@tests/brief-define-atomic-commit.test.cjs

<interfaces>
<!-- Constants the fixtures MUST encode verbatim. Plans 01-06 implement against these. -->

LOCKED_12 (per A-D01) — slug list (commands/brief/<slug>.md):
```javascript
const LOCKED_12 = [
  'define', 'discover', 'design', 'add-workstream',
  'deliver', 'export', 'status', 'help',
  'init', 'progress', 'undo', 'pause-work',
];
```
NOTE: `init` slug requires `commands/brief/new-project.md` → `commands/brief/init.md` rename in Plan 05 per Open Question 1 / PATTERNS.md recommendation.

PHASE_CATEGORIES (per C-D01) — slug → 4D phase header:
```javascript
const PHASE_CATEGORIES = {
  define: 'DEFINE', discover: 'DISCOVER', design: 'DESIGN',
  deliver: 'DELIVER', export: 'DELIVER', 'add-workstream': 'DESIGN',
  status: 'HELPERS', help: 'HELPERS', 'init': 'HELPERS',
  'progress': 'HELPERS', 'undo': 'HELPERS', 'pause-work': 'HELPERS',
};
```

RUNTIMES (per B-D01 + B-D03) — 4 mock runtime stubs:
```javascript
const RUNTIMES = [
  { name: 'claude',   env: {                                   text_mode_default: false } },
  { name: 'codex',    env: { INSTRUCTION_FILE: 'AGENTS.md',    text_mode_default: true  } },
  { name: 'gemini',   env: {                                   text_mode_default: true  } },
  { name: 'opencode', env: {                                   text_mode_default: true  } },
];
```

COMMANDS (per B-D02) — 5 critical commands per runtime:
```javascript
const COMMANDS = ['init', 'define', 'discover', 'design', 'deliver'];
```

DELETED_56 (per A-D02) — slugs being pruned (Plan 05 deletes these from `commands/brief/`):
```javascript
const DELETED_56 = [
  'add-backlog', 'add-phase', 'add-todo', 'analyze-dependencies', 'audit-fix',
  'audit-milestone', 'audit-uat', 'autonomous', 'check-todos', 'cleanup',
  'complete-milestone', 'discuss-phase', 'do', 'docs-update', 'execute-phase',
  'explore', 'extract_learnings', 'fast', 'from-gsd2', 'health', 'import',
  'insert-phase', 'intel', 'join-discord', 'list-phase-assumptions',
  'list-workspaces', 'manager', 'map-codebase', 'milestone-summary',
  'new-milestone', 'new-project', 'new-workspace', 'next', 'note',
  'plan-milestone-gaps', 'plan-phase', 'plant-seed', 'profile-user', 'quick',
  'reapply-patches', 'remove-phase', 'remove-workspace', 'research-phase',
  'resume-work', 'review', 'review-backlog', 'scan', 'session-report',
  'set-profile', 'settings', 'spec-phase', 'stats', 'thread', 'update',
  'validate-phase', 'verify-work', 'workstreams',
];
// 56 entries (verified by `ls commands/brief/*.md | wc -l == 68; 68 - 12 locked = 56)
// Note: 'new-project' is in DELETED_56 because the file is renamed to 'init.md' in Plan 05.
```

From `node:test` (built-in, Node 22+):
```javascript
const { describe, test } = require('node:test');
const assert = require('node:assert/strict');
```

From PATTERNS.md analog files:
- `tests/architecture-counts.test.cjs:14-58` — filesystem-driven structural test pattern (countMdFiles, parseDocCount).
- `tests/ask-user-questions-fallback.test.cjs:34-44` — TEXT_MODE fallback structural test pattern.
- `tests/brief-define-atomic-commit.test.cjs:30` — execFileSync subprocess pattern.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold 3 HRD-03 /brief-help fixtures (categorization + partial-match + levenshtein)</name>
  <files>tests/brief-help-categorization.test.cjs, tests/brief-help-partial-match.test.cjs, tests/brief-help-levenshtein.test.cjs</files>
  <read_first>
    - tests/architecture-counts.test.cjs (analog pattern from PATTERNS.md — filesystem-driven describe/test/assert skeleton with `node:test` + `node:assert/strict`)
    - .planning/phases/09-hardening-cross-runtime-smoke-non-developer-pilot/09-PATTERNS.md (lines 424-505 — exact byte-identity template for these three fixtures, including the `describe('/brief-help 4D categorization (HRD-03 / C-D01)')` and Levenshtein test cases including the define↔design distance-2 known collision per Pitfall 3)
    - .planning/phases/09-hardening-cross-runtime-smoke-non-developer-pilot/09-RESEARCH.md (lines 333-378 — verbatim two-row DP Levenshtein + suggestTopK reference impl that the fixture asserts against)
  </read_first>
  <action>
    Create the three help fixture files at the listed paths. Each file MUST:
    1. Begin with `'use strict';` then require `node:test` ({ describe, test }) and `node:assert/strict`.
    2. Define `const ROOT = path.join(__dirname, '..');` and `const COMMANDS_DIR = path.join(ROOT, 'commands', 'brief');`.
    3. Wrap the `require('../brief/bin/lib/help.cjs')` in a try/catch with a top-level boolean `let helpAvailable = false;` so the fixture does NOT crash the runner before Plan 02 lands `help.cjs`. When unavailable, every test inside calls `t.skip('blocked: brief/bin/lib/help.cjs not yet created (Plan 02)')`.
    4. Encode the LOCKED_12 and PHASE_CATEGORIES constants verbatim from `<interfaces>` above.

    File-specific contents:

    **tests/brief-help-categorization.test.cjs** (HRD-03 / C-D01):
      - `describe('/brief-help 4D categorization (HRD-03 / C-D01)', () => { ... })`
      - Test 1: `renderCategorized emits 5 phase headers (DEFINE/DISCOVER/DESIGN/DELIVER/HELPERS)` — when helpAvailable=true, asserts each `^##\s+(DEFINE|DISCOVER|DESIGN|DELIVER|HELPERS)$` regex matches in output of `help.renderCategorized(help.buildCatalog(COMMANDS_DIR))`. When false, `t.skip(...)`.
      - Test 2: `every command in commands/brief/*.md appears in renderCategorized output` — for each catalog entry, assert output `.includes('/brief-' + e.slug)`.
      - Test 3 (TODO until Plan 05 prunes): `commands/brief/*.md count is 12` — when helpAvailable=true AND `fs.readdirSync(COMMANDS_DIR).filter(f => f.endsWith('.md')).length === 12`, assert; otherwise `t.skip('blocked: HRD-02 surface pruning not yet executed (Plan 05)')`. Include a `done` callback only if test() requires it; node:test prefers async functions.

    **tests/brief-help-partial-match.test.cjs** (HRD-03 / C-D02):
      - `describe('/brief-help <topic> partial keyword match (HRD-03 / C-D02)', () => { ... })`
      - Test 1: `topic 'def' matches define via slug substring` — calls a hypothetical `help.matchTopic(catalog, 'def')` (TODO: confirm exact export name during Plan 02; for now invoke via slug filter as in PATTERNS.md lines 362-365 byte-identity: `catalog.filter(e => e.slug.toLowerCase().includes('def') || e.description.toLowerCase().includes('def'))`).
      - Test 2: `case-insensitive — 'DEFINE' matches define` (uppercase input).
      - Test 3: `topic 'design' matches define + design + add-workstream (DESIGN category prefix)` — assert at least 2 matches contain expected slugs.
      - Until helpAvailable=true, all tests `t.skip('blocked: brief/bin/lib/help.cjs not yet created (Plan 02)')`.

    **tests/brief-help-levenshtein.test.cjs** (HRD-03 / C-D03 + Pitfall 3):
      - `describe('Levenshtein top-3 typo suggestion (HRD-03 / C-D03)', () => { ... })`
      - Test 1: `exact match → distance 0` — `assert.strictEqual(help.levenshtein('define', 'define'), 0)`.
      - Test 2: `one-edit → distance 1` — `assert.strictEqual(help.levenshtein('define', 'defin'), 1)` AND `assert.strictEqual(help.levenshtein('define', 'desine'), 1)`.
      - Test 3: `define ↔ design known collision (Pitfall 3 documented)` — `assert.strictEqual(help.levenshtein('define', 'design'), 2)`.
      - Test 4: `suggestTopK returns ≤3 results below distance threshold` — assert returned array length ≤ 3, all `r.distance ≤ 3`, sorted ascending by distance.
      - Test 5: `suggestTopK('xyz123', candidates, 3, 3) returns empty (no candidate within distance ≤3)` — `assert.deepStrictEqual(out, [])`.
      - Each test guarded by helpAvailable; otherwise `t.skip('blocked: brief/bin/lib/help.cjs not yet created (Plan 02)')`.

    Per A1 invariant: NO npm requires (no `commander`, no `fast-levenshtein`). Built-ins only.
    Per Phase 8 cwd-bug lesson: This task does NOT spawn a worktree-isolated agent (Wave 0 fixtures are scaffolding-only and run in the main worktree).
  </action>
  <verify>
    <automated>node --test tests/brief-help-categorization.test.cjs tests/brief-help-partial-match.test.cjs tests/brief-help-levenshtein.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `ls tests/brief-help-categorization.test.cjs tests/brief-help-partial-match.test.cjs tests/brief-help-levenshtein.test.cjs` returns 3 files.
    - `grep -l "describe" tests/brief-help-*.test.cjs | wc -l` returns 3.
    - `grep -c "node:test" tests/brief-help-categorization.test.cjs` returns ≥1 (require statement present).
    - `grep -c "node:assert/strict" tests/brief-help-levenshtein.test.cjs` returns ≥1.
    - `grep -c "LOCKED_12" tests/brief-help-categorization.test.cjs` returns ≥1 (constant encoded).
    - `grep -c "define.*design.*2\|levenshtein.*define.*design" tests/brief-help-levenshtein.test.cjs` returns ≥1 (Pitfall 3 collision encoded).
    - `grep -c "Plan 02\|help\\.cjs" tests/brief-help-categorization.test.cjs` returns ≥1 (skip rationale points at Plan 02).
    - `node --test tests/brief-help-categorization.test.cjs tests/brief-help-partial-match.test.cjs tests/brief-help-levenshtein.test.cjs` exits 0 (skips are OK; runtime errors are NOT OK). Inspect output for `# tests` and `# pass + # skipped` summary; total should equal sum of test() declarations.
    - No `require('commander')`, no `require('fast-levenshtein')`, no other npm package — `grep -E "require\\('(commander|fast-levenshtein|js-levenshtein|cross-spawn|execa)'\\)" tests/brief-help-*.test.cjs` returns nothing (`exit 1`).
  </acceptance_criteria>
  <done>3 RED help-fixture files exist, register with node:test, skip cleanly until Plan 02 ships help.cjs, and encode LOCKED_12/PHASE_CATEGORIES constants byte-identical to interfaces block above.</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold 3 HRD-01 smoke-test fixtures + 3 HRD-02 surface-audit fixtures</name>
  <files>tests/brief-smoke-test-stub.test.cjs, tests/brief-smoke-test-text-mode.test.cjs, tests/brief-smoke-test-output-format.test.cjs, tests/brief-surface-audit-count.test.cjs, tests/brief-surface-audit-install-cleanup.test.cjs, tests/brief-surface-audit-doc.test.cjs</files>
  <read_first>
    - tests/brief-define-atomic-commit.test.cjs (lines 25-50 — execFileSync subprocess analog for HRD-01 fixtures)
    - tests/ask-user-questions-fallback.test.cjs (lines 30-50 — TEXT_MODE fallback assertion analog for brief-smoke-test-text-mode.test.cjs)
    - tests/architecture-counts.test.cjs (lines 14-58 — countMdFiles + filesystem-driven structural pattern for surface-audit fixtures)
    - .planning/phases/09-hardening-cross-runtime-smoke-non-developer-pilot/09-PATTERNS.md (lines 508-691 — byte-identity templates for these 6 fixtures, including DELETED_56 list)
    - .planning/phases/09-hardening-cross-runtime-smoke-non-developer-pilot/09-RESEARCH.md (lines 478-489 — Pitfall 1 bin/install.js dangling-refs background)
  </read_first>
  <action>
    Create six fixture files. All follow the same RED-state pattern (try/catch require, skip when production code absent, encode constants verbatim).

    **tests/brief-smoke-test-stub.test.cjs** (HRD-01 / B-D01..B-D04):
      - Header `use strict` + node:test + node:assert/strict + node:child_process { execFileSync } + node:path.
      - `let smokeAvailable = false; let smoke = null; try { smoke = require('../brief/bin/lib/smoke-test.cjs'); smokeAvailable = true; } catch (e) {}`.
      - Encode RUNTIMES (4 entries) and COMMANDS (5 entries) constants verbatim from `<interfaces>`.
      - `describe('Cross-runtime smoke matrix (HRD-01 / B-D01..B-D04)', () => { ... })`:
        - Test 1: `buildMatrix produces 4 runtimes × 5 commands = 20 cells`. When smokeAvailable=true, call `smoke.buildMatrix(BRIEF_ROOT)`, assert `matrix.length === 4`, then for each row `assert.strictEqual(row.length, 5, '5 command cells per row')`. When false, `t.skip('blocked: brief/bin/lib/smoke-test.cjs not yet created (Plan 01)')`.
        - Test 2: `each cell has shape {status, reason}` — for each cell, assert `['PASS', 'FAIL', 'SKIP'].includes(cell.status)` and `typeof cell.reason === 'string'`.
        - Test 3: `RUNTIMES export contains exactly 4 names: claude, codex, gemini, opencode` — `assert.deepStrictEqual(smoke.RUNTIMES.map(r => r.name).sort(), ['claude','codex','gemini','opencode'])`.

    **tests/brief-smoke-test-text-mode.test.cjs** (HRD-01 / B-D03):
      - Same header pattern; require smoke-test.cjs in try/catch.
      - `describe('text_mode fallback per runtime (HRD-01 / B-D03)', () => { ... })`:
        - Test 1: `non-Claude runtime cell with INSTRUCTION_FILE env emits no AskUserQuestion` — when smokeAvailable=true, find codex runtime, call `smoke.smokeOneCell(codex, 'init', BRIEF_ROOT)`. Assert: if cell.status === 'FAIL', then `assert.ok(!cell.reason.includes('text_mode'), 'FAIL must not be due to text_mode plumbing breakage')`.
        - Test 2: `claude runtime has text_mode_default false (AskUserQuestion enabled)` — `assert.strictEqual(claude.env.text_mode_default, false)`.
        - Test 3: `codex/gemini/opencode runtimes have text_mode_default true` — for each non-claude runtime, `assert.strictEqual(rt.env.text_mode_default, true)`.

    **tests/brief-smoke-test-output-format.test.cjs** (HRD-01 / B-D04):
      - Encode `const SMOKE_TEST_MD = path.join(__dirname, '..', '.planning', 'SMOKE-TEST.md');`.
      - `describe('SMOKE-TEST.md schema (HRD-01 / B-D04)', () => { ... })`:
        - Test 1: `SMOKE-TEST.md exists` — `assert.ok(fs.existsSync(SMOKE_TEST_MD), 'SMOKE-TEST.md not yet generated (Plan 01)')`. Skip with rationale if absent.
        - Test 2: `header row contains 5 command columns: init | define | discover | design | deliver` — read file content, assert regex `/\|\s*Runtime\s*\|\s*init\s*\|\s*define\s*\|\s*discover\s*\|\s*design\s*\|\s*deliver\s*\|/.test(content)`.
        - Test 3: `4 runtime rows present (claude, codex, gemini, opencode)` — for each runtime name, `assert.match(content, new RegExp('^\\|\\s*' + name + '\\s*\\|', 'mi'))`.

    **tests/brief-surface-audit-count.test.cjs** (HRD-02 / A-D01..A-D03):
      - Encode LOCKED_12 verbatim from `<interfaces>`.
      - `const SKILLS_DIR_LOCAL = path.join(ROOT, '.claude', 'skills');`.
      - `describe('Surface cap audit (HRD-02 / A-D01..A-D03)', () => { ... })`:
        - Test 1: `commands/brief/*.md count == 12 (locked)` — when `commands/brief` exists, assert `countMdFiles(COMMANDS_DIR) === 12`. Currently fails (68); skip with `t.skip('blocked: HRD-02 surface pruning not yet executed (Plan 05); current count=' + countMdFiles(COMMANDS_DIR))` when count !== 12.
        - Test 2: `all 12 LOCKED_12 slugs exist as files` — for each slug, assert `present.includes(slug)`. Skip until count===12.
        - Test 3: `.claude/skills/ is empty (0/8 cap)` — when SKILLS_DIR_LOCAL doesn't exist, return (0 by absence is OK); else assert `skills.length === 0`.

    **tests/brief-surface-audit-install-cleanup.test.cjs** (HRD-02 / Pitfall 1):
      - `const INSTALL_JS = path.join(ROOT, 'bin', 'install.js');`.
      - Encode DELETED_56 verbatim from `<interfaces>` (56 entries).
      - `describe('bin/install.js cleanliness post-HRD-02 pruning (Pitfall 1)', () => { const installContent = fs.readFileSync(INSTALL_JS, 'utf-8'); for (const slug of DELETED_56) { test(\`bin/install.js has zero references to deleted command "\${slug}"\`, (t) => { ... }) } })`.
      - Per-test body: build regex `new RegExp('[\\'"\\/]' + slug.replace(/-/g, '\\\\-') + '\\\\.md[\\'"\\)]')` and assert `!re.test(installContent)`. Currently fails (install.js still has refs); when running `t.skip('blocked: HRD-02 install.js cleanup not yet executed (Plan 05); refs still present')` if regex matches.
      - Use a guard: read DELETED_56.length and assert exactly 56 entries.

    **tests/brief-surface-audit-doc.test.cjs** (HRD-02 / A-D04):
      - `const SURFACE_AUDIT_MD = path.join(ROOT, '.planning', 'SURFACE-AUDIT.md');`.
      - `describe('SURFACE-AUDIT.md schema (HRD-02 / A-D04)', () => { ... })`:
        - Test 1: `SURFACE-AUDIT.md exists` — when absent, `t.skip('blocked: SURFACE-AUDIT.md not yet generated (Plan 05)')`.
        - Test 2: `commands table has 12 rows` — read file content, count matches of `/^\|\s+\d+\s+\|\s+\`\/brief-/m`, assert ≥ 12.
        - Test 3: `Removed in v1 section exists with 56 entries` — assert `/##\s+Removed in v1.*56\s+commands/i` regex matches.

    Per A1 invariant: built-ins only (`node:fs`, `node:path`, `node:child_process`, `node:test`, `node:assert/strict`).
  </action>
  <verify>
    <automated>node --test tests/brief-smoke-test-stub.test.cjs tests/brief-smoke-test-text-mode.test.cjs tests/brief-smoke-test-output-format.test.cjs tests/brief-surface-audit-count.test.cjs tests/brief-surface-audit-install-cleanup.test.cjs tests/brief-surface-audit-doc.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    - All 6 files exist: `ls tests/brief-smoke-test-stub.test.cjs tests/brief-smoke-test-text-mode.test.cjs tests/brief-smoke-test-output-format.test.cjs tests/brief-surface-audit-count.test.cjs tests/brief-surface-audit-install-cleanup.test.cjs tests/brief-surface-audit-doc.test.cjs` returns 6 lines.
    - `grep -c "RUNTIMES\\|COMMANDS\\|claude\\|codex" tests/brief-smoke-test-stub.test.cjs` returns ≥1 (4 runtime constants encoded).
    - `grep -c "INSTRUCTION_FILE" tests/brief-smoke-test-text-mode.test.cjs` returns ≥1.
    - `grep -c "LOCKED_12" tests/brief-surface-audit-count.test.cjs` returns ≥1.
    - `grep -c "DELETED_56" tests/brief-surface-audit-install-cleanup.test.cjs` returns ≥1, AND DELETED_56 array contains exactly 56 string entries (verify by `node -e "const m=require('fs').readFileSync('tests/brief-surface-audit-install-cleanup.test.cjs','utf-8').match(/DELETED_56\\s*=\\s*\\[([\\s\\S]*?)\\]/); const items=m[1].split(',').filter(s=>s.trim().match(/^['\"]/)); console.log(items.length)" → must print 56`).
    - `grep -c "execFileSync" tests/brief-smoke-test-stub.test.cjs` returns ≥1 (subprocess pattern from analog).
    - `grep -c "Plan 01\\|Plan 05\\|smoke-test\\.cjs" tests/brief-smoke-test-stub.test.cjs` returns ≥1 (skip rationale points at production-code plans).
    - `node --test tests/brief-smoke-test-stub.test.cjs tests/brief-smoke-test-text-mode.test.cjs tests/brief-smoke-test-output-format.test.cjs tests/brief-surface-audit-count.test.cjs tests/brief-surface-audit-install-cleanup.test.cjs tests/brief-surface-audit-doc.test.cjs` exits 0 (skips OK).
  </acceptance_criteria>
  <done>6 RED smoke + surface-audit fixture files exist, register with node:test, skip cleanly until Plans 01/05 ship production code, encode RUNTIMES/COMMANDS/LOCKED_12/DELETED_56 constants byte-identical to interfaces block.</done>
</task>

<task type="auto">
  <name>Task 3: Scaffold HRD-04 pilot journal fixture + V1-LAUNCH-GATE fixture (last 2 of 11)</name>
  <files>tests/brief-pilot-journal-structure.test.cjs, tests/brief-v1-launch-gate.test.cjs</files>
  <read_first>
    - tests/architecture-counts.test.cjs (lines 14-58 — filesystem read + regex pattern for both fixtures)
    - .planning/phases/09-hardening-cross-runtime-smoke-non-developer-pilot/09-PATTERNS.md (lines 833-871 — pilot journal schema; lines 769-794 — V1-LAUNCH-GATE schema)
    - .planning/phases/09-hardening-cross-runtime-smoke-non-developer-pilot/09-RESEARCH.md (lines 538-549 — Pitfall 5 friction journal discipline; line 786 — V1-launch-gate test entry in validation map)
    - brief/bin/lib/frontmatter.cjs (extractFrontmatter helper for parsing pilot journal frontmatter — exact import statement: `const { extractFrontmatter } = require('../brief/bin/lib/frontmatter.cjs');`)
  </read_first>
  <action>
    Create the final 2 Wave 0 fixtures.

    **tests/brief-pilot-journal-structure.test.cjs** (HRD-04 / D-D03):
      - Header use strict + node:test + node:assert/strict + node:fs + node:path.
      - `const { extractFrontmatter } = require(path.join(ROOT, 'brief', 'bin', 'lib', 'frontmatter.cjs'));`
      - `const PILOT_DIR = path.join(ROOT, '.planning', 'pilot');`
      - `describe('Pilot friction journal structure (HRD-04 / D-D03)', () => { ... })`:
        - Test 1: `.planning/pilot/ directory exists with at least 1 friction journal file matching 01-*-friction-journal.md` — `if (!fs.existsSync(PILOT_DIR)) { t.skip('blocked: .planning/pilot/ not yet created (Plan 03)'); return; }`. Then `const files = fs.readdirSync(PILOT_DIR).filter(f => /^01-.*-friction-journal\.md$/.test(f)); assert.ok(files.length >= 1, 'expected at least 1 file matching 01-*-friction-journal.md')`.
        - Test 2: `frontmatter has required keys: pilot_id, user_role, logged` — read first matching file, parse with extractFrontmatter, assert `fm.pilot_id === 1 || fm.pilot_id === '01' || fm.pilot_id === '1'` (numeric or string accepted), assert `typeof fm.user_role === 'string' && fm.user_role.length > 0`, assert `typeof fm.logged === 'string'` and matches ISO date `/^\d{4}-\d{2}-\d{2}/`.
        - Test 3: `body contains Pitfall #9 vocabulary section header AND at least 1 friction row` — read body, assert `/##\s+Pitfall\s+#?9/i.test(body)` AND assert at least one markdown table row matching `/\|\s+\w[^|]+\|\s+(low|medium|high|blocker)\s+\|/i.test(body)` (at least one severity row present).
        - Test 4: `body uses banned-vocabulary-free language (CC-03 hook compat)` — extract body via robust awk-equivalent JS (skip frontmatter): `const lines = content.split(/\r?\n/); let n = 0; let body = ''; for (const ln of lines) { if (ln === '---') { n++; continue; } if (n >= 2) body += ln + '\n'; }`. Then check basic banned set: assert `!/\b(compliant|passed|green check|green checkmark)\b/i.test(body)` (4 explicitly-named drift words). AND import the canonical BANNED_EN regex from voice-fit.cjs to catch all 16 EN words (per B5/W6 revision iteration 1) — wrap in try/catch so the fixture stays RED-state-safe before voice-fit.cjs is reachable: `let voiceFitBanned = null; try { voiceFitBanned = require(path.join(ROOT, 'brief', 'bin', 'lib', 'voice-fit.cjs')).BANNED_EN; } catch (e) {}`. If `voiceFitBanned` is non-null, also assert `[...body.matchAll(voiceFitBanned)].length === 0` (zero matches against the canonical 16-word EN regex). Skip the canonical-list portion of the assertion only when voice-fit.cjs is unavailable. **DO NOT use `tail -n +12` line-offset extraction — that's fragile to frontmatter line drift.** **DO NOT hand-type the BANNED_EN alternation — it omitted 4 words in the previous draft (transform/landscape/unlock/empower).**

    **tests/brief-v1-launch-gate.test.cjs** (V1 launch gate / D-D04):
      - `const V1_GATE_MD = path.join(ROOT, '.planning', 'V1-LAUNCH-GATE.md');`
      - `describe('V1-LAUNCH-GATE.md three-prong checklist (D-D04)', () => { ... })`:
        - Test 1: `V1-LAUNCH-GATE.md exists` — when absent, `t.skip('blocked: V1-LAUNCH-GATE.md not yet generated (Plan 06)')`.
        - Test 2: `three prongs present in checklist` — read content, assert all three regexes match: `/\(i\).*pilot.*finding/i`, `/\(ii\).*smoke.*test/i`, `/\(iii\).*surface.*cap/i`.
        - Test 3: `verdict line present` — assert content matches `/Verdict:\s*(PASS|HOLD|FAIL)/i`.
        - Test 4: `references SMOKE-TEST.md and SURFACE-AUDIT.md by name` — `assert.match(content, /SMOKE-TEST\.md/)` AND `assert.match(content, /SURFACE-AUDIT\.md/)`.

    Per A1 invariant: built-ins only.
  </action>
  <verify>
    <automated>node --test tests/brief-pilot-journal-structure.test.cjs tests/brief-v1-launch-gate.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    - Both files exist: `ls tests/brief-pilot-journal-structure.test.cjs tests/brief-v1-launch-gate.test.cjs` returns 2 lines.
    - `grep -c "extractFrontmatter\\|frontmatter\\.cjs" tests/brief-pilot-journal-structure.test.cjs` returns ≥1 (uses existing helper).
    - `grep -c "Pitfall.*9\\|pilot_id\\|user_role\\|logged" tests/brief-pilot-journal-structure.test.cjs` returns ≥1 (frontmatter keys encoded).
    - `grep -c "compliant\\|passed\\|green check" tests/brief-pilot-journal-structure.test.cjs` returns ≥1 (basic vocabulary-lock check encoded).
    - `grep -c "BANNED_EN\\|voice-fit\\.cjs" tests/brief-pilot-journal-structure.test.cjs` returns ≥1 (canonical BANNED_EN imported per B5/W6 revision; fixture references voice-fit.cjs directly rather than re-typing the alternation).
    - `grep -c "tail -n" tests/brief-pilot-journal-structure.test.cjs` returns 0 (line-offset-fragile tail-based body extraction NOT used; awk-equivalent JS body extractor used instead per B5 revision).
    - `grep -c "(i)\\|(ii)\\|(iii)\\|three.prong\\|Verdict" tests/brief-v1-launch-gate.test.cjs` returns ≥1 (three-prong schema encoded).
    - `grep -c "Plan 03\\|Plan 06" tests/brief-pilot-journal-structure.test.cjs tests/brief-v1-launch-gate.test.cjs` returns ≥1 each (skip rationale).
    - `node --test tests/brief-pilot-journal-structure.test.cjs tests/brief-v1-launch-gate.test.cjs` exits 0 (skips OK).
    - Combined check: `node scripts/run-tests.cjs tests/brief-help-*.test.cjs tests/brief-smoke-*.test.cjs tests/brief-surface-*.test.cjs tests/brief-pilot-*.test.cjs tests/brief-v1-launch-*.test.cjs` runs without crashing the runner — all 11 fixtures discoverable and skippable cleanly.
  </acceptance_criteria>
  <done>All 11 Wave 0 fixtures exist, register with node:test, skip cleanly, and encode the constants/contracts that Plans 01-06 must match. The Wave 0 gap from 09-VALIDATION.md is closed; Wave 1 may begin.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test fixture files | Self-contained; read-only operations on filesystem; no external user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-9-04 | T (Tampering) / S (Spoofing) | Test fixtures writing to source files inadvertently | mitigate | All Wave 0 fixtures are READ-ONLY (`fs.readdirSync`, `fs.readFileSync`, `extractFrontmatter`). No `fs.writeFileSync` calls in any fixture. Pre-commit CC-03 hook (Phase 8) validates `.planning/` frontmatter on commit; tests are excluded by default. |
| T-9-05 | D (Denial of Service) | Long-running fixtures stalling CI | mitigate | All fixtures are filesystem-readonly (~100ms each); none invokes subprocess at test-time (HRD-01 subprocess pattern is in production lib `smoke-test.cjs` which has `timeout: 5000` per Pitfall 4). Wave 0 fixtures only `require` smoke-test.cjs; they don't call `buildMatrix` blocking. |
| T-9-06 | I (Information Disclosure) | Test fixtures emitting absolute paths in error messages | accept | Test runner output is local-developer-visible only; absolute paths in `assert` failure messages are acceptable per Phase 8 dispatcher discipline (test fixture is NOT user-facing dispatcher output). |
</threat_model>

<verification>
After all 3 tasks complete:
- `ls tests/brief-help-*.test.cjs tests/brief-smoke-*.test.cjs tests/brief-surface-*.test.cjs tests/brief-pilot-*.test.cjs tests/brief-v1-launch-*.test.cjs | wc -l` returns 11.
- `node scripts/run-tests.cjs tests/brief-help-*.test.cjs tests/brief-smoke-*.test.cjs tests/brief-surface-*.test.cjs tests/brief-pilot-*.test.cjs tests/brief-v1-launch-*.test.cjs` exits 0 (skips counted, no runtime errors).
- `node -e "console.log(Object.keys(require('./package.json').dependencies||{}).length)"` still returns `0` (A1 invariant preserved).
- `grep -E "require\\('(commander|fast-levenshtein|js-levenshtein|cross-spawn|execa)'\\)" tests/brief-help-*.test.cjs tests/brief-smoke-*.test.cjs tests/brief-surface-*.test.cjs tests/brief-pilot-*.test.cjs tests/brief-v1-launch-*.test.cjs` returns nothing.
</verification>

<success_criteria>
- 11/11 Wave 0 fixtures present in tests/
- All fixtures register with `node:test` and run via `node --test` and `node scripts/run-tests.cjs` without crash
- Skip rationales reference the specific Wave 1/2 plan that turns RED→GREEN
- LOCKED_12, DELETED_56 (=56 entries), RUNTIMES (=4), COMMANDS (=5) constants byte-identical to interfaces block
- Zero new npm dependencies added (A1 invariant)
- No `fs.writeFileSync` calls in any fixture
</success_criteria>

<output>
After completion, create `.planning/phases/09-hardening-cross-runtime-smoke-non-developer-pilot/09-00-SUMMARY.md` documenting which fixtures landed and any deviations from the constants encoded above.
</output>
