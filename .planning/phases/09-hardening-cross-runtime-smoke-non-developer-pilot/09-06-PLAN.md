---
phase: 09-hardening-cross-runtime-smoke-non-developer-pilot
plan: 06
type: execute
wave: 4
depends_on: [00, 01, 02, 03, 04, 05]
files_modified:
  - .planning/HRD-05-CLOSURE-RATIONALE.md
  - .planning/RESIDUAL-FAILS-V1.md
  - .planning/V1-LAUNCH-GATE.md
  - docs/ARCHITECTURE.md
  # B3 revision iteration 1: Task 1 also edits 19+ test files matched by the missing-file references.
  # The actual list is determined at runtime via grep enumeration. Wildcard convention:
  - tests/bug-2004-*.test.cjs
  - tests/bug-2075-*.test.cjs
  # Plus any other tests resolved at runtime via /tmp/affected-tests.txt enumeration
  # (the executor runs `grep -rln "pr-branch\.md\|diagnose-issues\.md\|ui-brand\.md" tests/` and edits each match).
  # Per RESEARCH.md A7 the count is approximately 19; record actual count in SUMMARY.
autonomous: true
requirements: [HRD-05, HRD-04]

must_haves:
  truths:
    - "User reads `.planning/HRD-05-CLOSURE-RATIONALE.md` and sees per-test rationale for each of the 19 missing-file test triage decisions (DELETE assertion vs CREATE file) per Open Question 2 / PATTERNS.md recommendation"
    - "User reads `.planning/V1-LAUNCH-GATE.md` and sees a single-page three-prong checklist (i) 0 blocking pilot findings (ii) smoke test PASS or documented SKIP (iii) surface cap compliance — with current PASS/HOLD verdict per prong"
    - "User reads `.planning/RESIDUAL-FAILS-V1.md` and sees the deferred (c) 30 source-behavior + (d) 13 source-content drift items per D-D02 with v1.1 remediation plan"
    - "`docs/ARCHITECTURE.md` Total commands/workflows/agents counts match disk reality post-Plan-05 (lines 116/127/137 synced; `tests/architecture-counts.test.cjs` GREEN)"
    - "`npm test 2>&1 | grep -cE '^✖'` returns ≤ 16 (EMPIRICAL_BASELINE 6 + DELTA_CAP 10) — Phase 9 closure target met"
    - "Wave 0 fixture `tests/brief-v1-launch-gate.test.cjs` GREEN"
    - "All HRD-05(a) per-test triage decisions for the 3 candidate files (pr-branch.md, diagnose-issues.md, ui-brand.md) follow the recommended ALL-DELETE rubric per RESEARCH.md Pitfall 2 / PATTERNS.md (none tied to LOCKED_12)"
    - "Zero new runtime npm dependencies (A1 preserved)"
  artifacts:
    - path: ".planning/HRD-05-CLOSURE-RATIONALE.md"
      provides: "Per-test triage audit doc — 19 missing-file tests, each with rationale + ALL-DELETE-assertion decision per locked-12 rubric"
      min_lines: 50
      contains: "pr-branch"
    - path: ".planning/RESIDUAL-FAILS-V1.md"
      provides: "v1 carry-forward fail catalog — (c) 30 + (d) 13 items deferred per D-D02 with v1.1 remediation notes"
      min_lines: 50
      contains: "v1.1"
    - path: ".planning/V1-LAUNCH-GATE.md"
      provides: "Single-page three-prong launch gate — verdict per prong + notes on HRD-04 partial 1/3 acceptance"
      min_lines: 40
      contains: "Verdict"
    - path: "docs/ARCHITECTURE.md"
      provides: "Total commands/workflows/agents counts synced to disk post-Plan-05 deletion"
      pattern: "Total commands.*12|Total agents.*26|Total workflows"
  key_links:
    - from: ".planning/V1-LAUNCH-GATE.md"
      to: ".planning/SMOKE-TEST.md"
      via: "prong (ii) cites SMOKE-TEST.md as evidence"
      pattern: "SMOKE-TEST\\.md"
    - from: ".planning/V1-LAUNCH-GATE.md"
      to: ".planning/SURFACE-AUDIT.md"
      via: "prong (iii) cites SURFACE-AUDIT.md as evidence"
      pattern: "SURFACE-AUDIT\\.md"
    - from: ".planning/V1-LAUNCH-GATE.md"
      to: ".planning/pilot/01-*-friction-journal.md"
      via: "prong (i) cites pilot journal as evidence (severity=blocker count = 0)"
      pattern: "pilot/01-"
    - from: ".planning/HRD-05-CLOSURE-RATIONALE.md"
      to: "tests/bug-2004-*.test.cjs, tests/bug-2075-*.test.cjs (and other affected tests)"
      via: "per-test rationale links the test file to the DELETE-vs-CREATE decision"
      pattern: "bug-2004\\|bug-2075\\|pr-branch\\|diagnose-issues\\|ui-brand"
---

<objective>
Close Phase 9 with HRD-05(a) per-test triage + HRD-05(b) ARCHITECTURE.md count sync + V1-LAUNCH-GATE.md + RESIDUAL-FAILS-V1.md. After this plan lands, the v1 launch gate doc reads PASS for all 3 prongs (i 0 blocking pilot findings, ii smoke test PASS or documented SKIP, iii surface cap compliance), and the residual deferred-to-v1.1 work catalog is recorded for transparency.

Purpose: Closes HRD-05(a) per-test triage (19 tests → ALL DELETE assertion per locked-12 rubric — none of pr-branch.md / diagnose-issues.md / ui-brand.md are tied to LOCKED_12; per RESEARCH.md Pitfall 2). Closes HRD-05(b) remaining work (ARCHITECTURE.md Total counts sync — runs AFTER Plan 05 HRD-02 deletion stabilizes disk counts). Generates the V1-LAUNCH-GATE.md verifier output and RESIDUAL-FAILS-V1.md catalog for v1.1 carry-forward. Final gate verifies `npm test 2>&1 | grep -cE '^✖'` ≤ 16 per Phase 1 EMPIRICAL_BASELINE 6 + DELTA_CAP 10.

Output: 4 new/modified docs (.planning/HRD-05-CLOSURE-RATIONALE.md, .planning/RESIDUAL-FAILS-V1.md, .planning/V1-LAUNCH-GATE.md, docs/ARCHITECTURE.md). Plus the 19 missing-file test assertion deletions (per-test sed edits or Edit-tool surgical line removals). Wave 0 fixture brief-v1-launch-gate flips from skip→active and passes.
</objective>

<execution_context>
@~/.claude/brief/workflows/execute-plan.md
@~/.claude/brief/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-hardening-cross-runtime-smoke-non-developer-pilot/09-CONTEXT.md
@.planning/phases/09-hardening-cross-runtime-smoke-non-developer-pilot/09-RESEARCH.md
@.planning/phases/09-hardening-cross-runtime-smoke-non-developer-pilot/09-PATTERNS.md
@.planning/phases/01-foundation-fork-hygiene-removal-rename/01-VERIFICATION.md
@.planning/SMOKE-TEST.md
@.planning/SURFACE-AUDIT.md
@.planning/pilot/01-korean-non-technical-product-owner-friction-journal.md
@docs/ARCHITECTURE.md
@tests/architecture-counts.test.cjs
@tests/command-count-sync.test.cjs
@tests/brief-v1-launch-gate.test.cjs

<interfaces>
<!-- Inputs the executor reads. -->

From .planning/phases/01-*/01-VERIFICATION.md (referenced from STATE.md per RESEARCH.md line 884):
- 19 missing-file tests breakdown by file referenced:
  * `brief/workflows/pr-branch.md` — referenced by tests/bug-2004-pr-branch-*.test.cjs and friends
  * `brief/workflows/diagnose-issues.md` — referenced by tests asserting `gsd-debugger` agent existence
  * `brief/references/ui-brand.md` — referenced by tests asserting `gsd-ui-researcher`/`gsd-ui-checker` agent existence
- The exact test file list (per RESEARCH.md Validation Architecture map): `tests/bug-2004-*.test.cjs`, `tests/bug-2075-*.test.cjs`, plus other tests that reference these 3 files.

Triage rubric per PATTERNS.md (lines 875-886) — ALL 3 candidate files DELETE assertion (none tied to LOCKED_12):

| Candidate File | Tied to locked-12? | Action | Rationale |
|----------------|---------------------|--------|-----------|
| pr-branch.md | NO — milestone PR flow inherited from GSD developer surface | DELETE assertion | Not in 12-cmd lineup |
| diagnose-issues.md | NO — gsd-debugger agent intentionally absent post-Phase-1 (FND-02 dev-surface removal) | DELETE assertion | Permanently gone in v1 |
| ui-brand.md | NO — gsd-ui-researcher/gsd-ui-checker intentionally absent post-Phase-1 | DELETE assertion | Permanently gone in v1 |

From docs/ARCHITECTURE.md (verified pre-Plan-05 via RESEARCH.md line 871):
```
Line 116: **Total commands:** 77   ← drift; sync to disk count post-Plan-05 (target: 12)
Line 127: **Total workflows:** 74  ← drift; sync to disk count
Line 137: **Total agents:** 31     ← drift; sync to disk count (currently 26 unchanged from Phase 8)
```
NOTE: Line numbers may shift slightly post-Plan-05; locate via `grep -n "Total (commands|workflows|agents)" docs/ARCHITECTURE.md`. Disk counts to use:
- `node -e "console.log(require('node:fs').readdirSync('commands/brief').filter(f=>f.endsWith('.md')).length)"` → 12 post-Plan-05.
- `node -e "console.log(require('node:fs').readdirSync('brief/workflows').filter(f=>f.endsWith('.md')).length)"` → some value (currently 70 pre-Plan-05; post-Plan-05 may differ if any workflow tied to a deleted command also got swept — record actual count).
- `node -e "console.log(require('node:fs').readdirSync('agents').filter(f=>f.endsWith('.md')).length)"` → 26 (Phase 8 introduced 2 new + Phase 5/6/7 introduced 3 new; unchanged in Phase 9).

V1-LAUNCH-GATE.md schema per PATTERNS.md (lines 770-794):
- Header + Verdict + 3-prong table + Notes section.
- Single-page (≤ 80 lines per RESEARCH.md Discretion-8).

RESIDUAL-FAILS-V1.md schema per PATTERNS.md (lines 798-829):
- Header + (c) 30 source-behavior table + (d) 13 source-content table + v1.1 estimated effort.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: HRD-05(a) per-test triage — DELETE 19 missing-file assertions + log rationale in HRD-05-CLOSURE-RATIONALE.md</name>
  <files>.planning/HRD-05-CLOSURE-RATIONALE.md, tests/bug-2004-pr-branch-milestone.test.cjs, tests/bug-2075-worktree-deletion-safeguards.test.cjs</files>
  <read_first>
    - .planning/phases/01-foundation-fork-hygiene-removal-rename/01-VERIFICATION.md (full file — exhaustive list of the 19 missing-file tests by name; this is the source of truth for which tests need editing)
    - .planning/phases/09-hardening-cross-runtime-smoke-non-developer-pilot/09-PATTERNS.md (lines 875-891 — triage rubric ALL-DELETE for all 3 candidate files)
    - .planning/phases/09-hardening-cross-runtime-smoke-non-developer-pilot/09-RESEARCH.md (lines 491-505 — Pitfall 2 detail; lines 700-707 — Assumption A7 grep-based enumeration of missing-file references)
    - tests/bug-2004-pr-branch-milestone.test.cjs (a representative test that asserts pr-branch.md exists; this plan removes that assertion)
    - tests/bug-2075-worktree-deletion-safeguards.test.cjs (representative)
  </read_first>
  <action>
    **Step A — Enumerate the actual 19 tests (per Assumption A7):**

    Run grep to find every test that references the 3 candidate missing files:
    ```bash
    cd /Users/agent/GSD-for-Business
    grep -rln "pr-branch\\.md\\|diagnose-issues\\.md\\|ui-brand\\.md" tests/ | sort > /tmp/affected-tests.txt
    cat /tmp/affected-tests.txt
    ```
    Expect ~19 entries. If the count differs significantly from 19, record the actual count in the SUMMARY and triage each one.

    **Step B — For each affected test, apply the DELETE-assertion rubric:**

    Per PATTERNS.md triage rubric (verbatim): ALL 3 candidate files → DELETE assertion. None are tied to LOCKED_12 (pr-branch is GSD milestone PR flow; diagnose-issues is `gsd-debugger` dev-surface; ui-brand is `gsd-ui-*` dev-surface — all 3 intentionally absent post-Phase-1 FND-02).

    For each test file in /tmp/affected-tests.txt:
    1. Open file, locate the assertion that references the missing file (e.g., `assert.ok(fs.existsSync(...pr-branch.md))` or `assert.match(content, /pr-branch/)` or similar).
    2. If the assertion is a STANDALONE test (the test only checks file existence): comment out or delete the WHOLE `test('...', () => { ... })` block; replace with `test.skip('removed: pr-branch.md is GSD-only and not in locked-12 lineup (HRD-05a closure)', () => {});` so the skip is visible in test runner output (NOT a silent removal).
    3. If the assertion is part of a larger test that does additional work: comment out only the missing-file assertion line(s), preserve the rest of the test body, and add a single-line comment `// HRD-05a closure: pr-branch.md not in locked-12 lineup; assertion removed per PATTERNS.md triage rubric`.
    4. Do NOT use `--no-verify` for the resulting commit. CC-03 hook checks `.planning/` frontmatter; tests/ files are not gated.

    **Step C — Write `.planning/HRD-05-CLOSURE-RATIONALE.md`:**

    Single audit doc per Open Question 2 recommendation. Schema:
    ```markdown
    # HRD-05(a) Per-Test Closure Rationale

    **Closed:** 2026-04-27
    **Phase 1 HALT-ACCEPTED baseline:** 19 missing-file tests
    **Decision rule:** ALL DELETE-assertion (per PATTERNS.md lines 875-891)
    **Locked 12-cmd lineup membership check:** None of pr-branch.md / diagnose-issues.md / ui-brand.md ties to a LOCKED_12 slug; all 3 are inherited GSD developer surfaces removed in Phase 1 FND-02.

    ## Per-Test Decisions

    | Test File | Missing File Referenced | Decision | Rationale |
    |-----------|-------------------------|----------|-----------|
    | tests/bug-2004-pr-branch-milestone.test.cjs | brief/workflows/pr-branch.md | DELETE assertion | pr-branch.md is GSD milestone PR flow inherited from developer surface; not in LOCKED_12 |
    | (... 18 more rows, one per affected test) ...|

    ## Aggregate

    - 19 tests triaged
    - 19 DELETE-assertion decisions
    - 0 CREATE-file decisions

    Per Pitfall 2: ALL CREATE-file decisions would re-introduce dual-vocabulary clutter that Phase 1 was designed to eliminate. The DELETE-assertion approach is correct because the workflows referenced (pr-branch / diagnose-issues / ui-brand) are intentionally absent in v1 per FND-02 and are not in the locked 12-cmd lineup per A-D01.
    ```

    Fill in the table with the actual 19 tests from /tmp/affected-tests.txt. If fewer than 19 tests are found, record the actual count and proceed (Assumption A7 may be off-by-one); if more, triage each per the same rubric.

    **Step D — Verify the 19 tests now skip or pass:**

    ```bash
    node scripts/run-tests.cjs $(cat /tmp/affected-tests.txt | tr '\n' ' ')
    ```
    Expect: ALL the formerly-failing tests now either pass (assertion removed; test does additional work that succeeds) or skip (test was wholly about file existence; replaced with test.skip). NO failures from these specific tests after the closure.

    Per Phase 8 cwd-bug lesson: This task does NOT spawn a worktree-isolated agent (each test edit is surgical and well-scoped to a single file at a time, all in main worktree).

    Per A1 invariant: no npm changes.
  </action>
  <verify>
    <automated>node scripts/run-tests.cjs $(grep -rln "pr-branch\\.md\\|diagnose-issues\\.md\\|ui-brand\\.md" tests/ | sort | tr '\\n' ' ')</automated>
  </verify>
  <acceptance_criteria>
    - `ls .planning/HRD-05-CLOSURE-RATIONALE.md` returns 1 line.
    - `wc -l .planning/HRD-05-CLOSURE-RATIONALE.md` shows ≥50 lines.
    - `head -1 .planning/HRD-05-CLOSURE-RATIONALE.md` returns `# HRD-05(a) Per-Test Closure Rationale`.
    - `grep -c "pr-branch\\|diagnose-issues\\|ui-brand" .planning/HRD-05-CLOSURE-RATIONALE.md` returns ≥3 (all 3 candidate files documented).
    - `grep -cE "^\\| tests/" .planning/HRD-05-CLOSURE-RATIONALE.md` returns close to 19 (record actual count if different — A7 may be off; record in SUMMARY).
    - `grep -c "DELETE assertion\\|DELETE-assertion" .planning/HRD-05-CLOSURE-RATIONALE.md` returns ≥1 in the aggregate row.
    - `grep -c "CREATE assertion\\|CREATE-file" .planning/HRD-05-CLOSURE-RATIONALE.md` returns 0 in decisions (rubric is ALL-DELETE; word may appear in rationale text but no row uses it as decision).
    - `grep -rln "pr-branch\\.md\\|diagnose-issues\\.md\\|ui-brand\\.md" tests/ | wc -l` returns the same count as the pre-closure enumeration (file references not all stripped — only the assertions, comments may still mention the file names with rationale).
    - For each affected test file, running `node --test <file>` exits 0 (test passes or skips with rationale; no failures from missing-file assertions).
    - `grep -c "HRD-05a closure\\|HRD-05.*not in locked-12" tests/bug-2004-*.test.cjs tests/bug-2075-*.test.cjs 2>/dev/null` returns ≥1 (rationale comments embedded in test files).
  </acceptance_criteria>
  <done>HRD-05-CLOSURE-RATIONALE.md exists with per-test decisions; 19 (or actual count) missing-file assertions are removed/skipped with rationale; affected tests now pass or skip cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: HRD-05(b) ARCHITECTURE.md count sync (post-Plan-05 disk counts)</name>
  <files>docs/ARCHITECTURE.md</files>
  <read_first>
    - docs/ARCHITECTURE.md (lines around 116/127/137 — locate `**Total commands:**`, `**Total workflows:**`, `**Total agents:**` lines via `grep -n "Total (commands|workflows|agents)" docs/ARCHITECTURE.md`)
    - tests/architecture-counts.test.cjs (filesystem-driven test — confirms no hardcoded counts; just reads doc)
    - tests/command-count-sync.test.cjs (Plan 04 fixed the regex; this plan's edit makes the count match)
    - .planning/phases/09-hardening-cross-runtime-smoke-non-developer-pilot/09-PATTERNS.md (lines 913-930 — count sync action)
  </read_first>
  <action>
    1. Compute disk counts (must run AFTER Plan 05 HRD-02 deletion lands):
       ```bash
       echo "commands: $(ls commands/brief/*.md 2>/dev/null | wc -l)"
       echo "workflows: $(ls brief/workflows/*.md 2>/dev/null | wc -l)"
       echo "agents: $(ls agents/*.md 2>/dev/null | wc -l)"
       ```
       Record the 3 numbers (expected: 12 / ~70 / 26 — the workflows count may have shifted slightly from 70 if Plan 05 swept any workflow tied to a deleted command, OR if Plan 02 left brief/workflows/help.md as orphan; record actual).

    2. Locate the 3 lines in docs/ARCHITECTURE.md:
       ```bash
       grep -nE "^\\*\\*Total (commands|workflows|agents):\\*\\*" docs/ARCHITECTURE.md
       ```
       Expected: 3 hits matching `Total commands:`, `Total workflows:`, `Total agents:`.

    3. Replace each line with the disk count using Edit:
       - `**Total commands:** 77` → `**Total commands:** 12`
       - `**Total workflows:** 74` → `**Total workflows:** <actual>` (record exact count)
       - `**Total agents:** 31` → `**Total agents:** 26`

    4. Verify by running both architecture tests:
       ```bash
       node --test tests/architecture-counts.test.cjs tests/command-count-sync.test.cjs
       ```
       Both must exit 0 (architecture-counts is filesystem-driven so will compare doc to disk; command-count-sync now matches `commands/brief/` regex per Plan 04 + the corrected count from this task).

    5. If `tests/command-count-sync.test.cjs` expects an inline annotation (e.g., `commands/brief/*.md  # 12 slash commands`) that does not yet exist in ARCHITECTURE.md (because Phase 1's drift left only the `Total commands:` line, not the inline annotation), search for the inline annotation pattern:
       ```bash
       grep -nE "commands/brief/\\*\\.md.*#\\s*\\d+\\s+slash commands" docs/ARCHITECTURE.md || echo "NOT FOUND"
       ```
       If `NOT FOUND`, the command-count-sync.test.cjs may legitimately fail until ARCHITECTURE.md gets a structural addition. Per HRD-05(b) scope (PATTERNS.md lines 913-930), the simple `Total <component>: N` lines are the source of truth — if command-count-sync.test.cjs requires more, treat the additional drift as part of the deferred (b) residual and document in RESIDUAL-FAILS-V1.md (next task) rather than expanding scope here.

    Per Phase 8 cwd-bug lesson: This task does NOT spawn a worktree-isolated agent (single-file 3-line edit).

    Per A1 invariant: no npm changes.
  </action>
  <verify>
    <automated>node --test tests/architecture-counts.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "^\\*\\*Total commands:\\*\\* 12$" docs/ARCHITECTURE.md` returns ≥1 line (count = 12 post-Plan-05).
    - `grep -E "^\\*\\*Total agents:\\*\\* 26$" docs/ARCHITECTURE.md` returns ≥1 line.
    - `grep -E "^\\*\\*Total workflows:\\*\\*" docs/ARCHITECTURE.md` returns 1 line; the number on that line matches `ls brief/workflows/*.md | wc -l`.
    - `node --test tests/architecture-counts.test.cjs` exits 0 (no count-mismatch failures for commands or agents; workflows passes if Plan 05's workflow sweep left counts in-sync).
    - `node -e "console.log(Object.keys(require('./package.json').dependencies||{}).length)"` returns `0`.
  </acceptance_criteria>
  <done>ARCHITECTURE.md Total counts synced to disk reality (12 / actual / 26); architecture-counts.test.cjs GREEN; HRD-05(b) primary scope closed.</done>
</task>

<task type="auto">
  <name>Task 3: Generate V1-LAUNCH-GATE.md + RESIDUAL-FAILS-V1.md + final empirical fail-count check (≤16)</name>
  <files>.planning/V1-LAUNCH-GATE.md, .planning/RESIDUAL-FAILS-V1.md</files>
  <read_first>
    - .planning/SMOKE-TEST.md (Plan 01 output — referenced by V1-LAUNCH-GATE prong (ii))
    - .planning/SURFACE-AUDIT.md (Plan 05 output — referenced by V1-LAUNCH-GATE prong (iii))
    - .planning/pilot/01-korean-non-technical-product-owner-friction-journal.md (Plan 03 output — referenced by V1-LAUNCH-GATE prong (i); inspect for severity=blocker count, expected 0)
    - .planning/phases/09-hardening-cross-runtime-smoke-non-developer-pilot/09-PATTERNS.md (lines 770-794 — V1-LAUNCH-GATE.md schema; lines 798-829 — RESIDUAL-FAILS-V1.md schema)
    - .planning/phases/01-foundation-fork-hygiene-removal-rename/01-VERIFICATION.md (full — list of (c) 30 source-behavior + (d) 13 source-content drift items per D-D02 deferral)
    - tests/brief-v1-launch-gate.test.cjs (Wave 0 fixture — schema this task must satisfy)
  </read_first>
  <action>
    **Subtask A — Run the empirical fail-count check (HRD-05 closure target ≤16):**

    ```bash
    cd /Users/agent/GSD-for-Business
    node scripts/run-tests.cjs > /tmp/full-test-output.txt 2>&1
    # W5 revision iteration 1: regex matches BOTH spec reporter (^✖) AND TAP reporter (^not ok).
    # Earlier draft used `^✖` only, which is reporter-dependent and may miss TAP-mode failures.
    FAIL_COUNT=$(grep -cE '^(✖|not ok)' /tmp/full-test-output.txt)
    echo "Fail count: $FAIL_COUNT (target ≤16)"
    ```
    Record the count. If > 16, halt and document the residual fails for re-triage; if ≤ 16, proceed.

    **Subtask B — Write `.planning/RESIDUAL-FAILS-V1.md`:**

    Schema per PATTERNS.md lines 798-829. Sections:
    1. Header with Phase 1 HALT-ACCEPTED baseline (63), Phase 9 HRD-05(a)+(b) closure count (whatever was closed; should be ~47), v1 ship count (the FAIL_COUNT measured above; ≤16).
    2. `## (c) 30 source-behavior drift — DEFERRED to v1.1` table:
       - hooks/brief-check-update-worker.js MANAGED_HOOKS array (gsd-* names not renamed) — v1.1 byte-replace per Phase 1 D-08.
       - bin/install.js CONV-07 function (gsd-* name detection) — v1.1.
       - hooks/brief-read-guard.js JSON output drift — v1.1.
       - Custom-file detection helpers — v1.1.
       - Document each test that fails because of these drift items (use grep to enumerate from /tmp/full-test-output.txt).
       - Aim for ~30 rows; if fewer fails are observed, document accurately.
    3. `## (d) 13 source-content drift — DEFERRED to v1.1` table:
       - agents/brief-*.md required_reading blocks (≥20 agents have stale gsd-* refs).
       - **NOTE per W9 revision iteration 1:** commands/brief/*.md frontmatter `name: gsd:*` drift across the 11 retained LOCKED_12 commands was CLOSED in Plan 05 (not deferred). The Plan 05 atomic commit includes a sed bulk-rewrite that migrates all 11 retained commands' frontmatter from `name: gsd:*` to `name: brief:*`. Only `init.md` (renamed from new-project.md) had its frontmatter updated separately. Verify post-Plan-05 that `grep -l "^name: gsd:" commands/brief/*.md 2>/dev/null` returns nothing; if it still returns matches, Plan 05's W9 sed step did not run — re-triage. Do NOT include the LOCKED_12 commands/brief/*.md frontmatter in the (d) deferred list — that drift is closed in Phase 9.
       - brief/workflows/verify-work.md (Phase 1 deferred per VERIFICATION.md).
       - Other source-content drift items (agents required_reading, workflow file refs to deleted commands) — aim for ~13 rows; if fewer because Plan 05 W9 closed some, document accurately and reduce the count.
    4. `## v1.1 Estimated Effort` paragraph — ~4-6h source diff per CONTEXT D-D02; HRD-05 (c)+(d) is launch-non-blocking.

    **Subtask C — Write `.planning/V1-LAUNCH-GATE.md`:**

    Schema per PATTERNS.md lines 770-794. Single page, ≤80 lines. Sections:
    1. Header + Verdict line (PASS / HOLD / FAIL based on prong checks below).
    2. `## Three-Prong Checklist (D-D04)` — table:
       - Prong (i) "0 blocking pilot findings": evidence = `.planning/pilot/01-korean-non-technical-product-owner-friction-journal.md`. Status: PASS if `grep -cE "\\|\\s*blocker\\s*\\|" .planning/pilot/01-*.md` returns 0; HOLD otherwise.
       - Prong (ii) "Smoke test PASS on all 4 runtimes × 5 commands (or documented SKIP)": evidence = `.planning/SMOKE-TEST.md` 20-cell matrix. Status: PASS if matrix exists with all-PASS or all-PASS-or-documented-SKIP; HOLD if any unexplained FAIL. (Note: Plan 01 likely produces some FAILs because brief-tools.cjs `--smoke` flag handlers may not exist for all 5 commands; per Plan 01 SUMMARY guidance these are diagnostic v1.1 follow-up signals — verdict is PASS WITH NOTES if all FAILs have documented reasons in the FAIL/SKIP Detail section.)
       - Prong (iii) "Surface cap compliance (12 cmds + 0 skills)": evidence = `.planning/SURFACE-AUDIT.md` + `tests/brief-surface-audit-count.test.cjs` GREEN. Status: PASS if `ls commands/brief/*.md | wc -l == 12` AND `tests/brief-surface-audit-count.test.cjs` exits 0; HOLD otherwise.
    3. `## Notes` section:
       - HRD-04 partial 1/3 is NOT a launch blocker (D-D01 explicit Out of Scope; v1.1 beta program completes 2/3).
       - HRD-05 (a)+(b) closure target ≤16 npm test fails (EMPIRICAL_BASELINE 6 + DELTA_CAP 10) — record actual fail count.
       - "Upgrading from earlier brief-cc" — re-run `npx brief-cc@latest` to refresh installed commands; stale `.claude/commands/brief/{deleted}.md` files in user config dirs may persist (Pitfall 1 mitigation; documented).

    Compute the verdict:
    - All 3 prongs PASS → top-level Verdict: PASS
    - Any prong HOLD or FAIL → top-level Verdict: HOLD with the prong noted
    - Per CONTEXT D-D04, HRD-04 partial is NOT a blocker — that's already encoded in prong (i)'s severity=blocker definition.

    **Subtask D — Verify the Wave 0 V1-launch-gate fixture passes:**

    ```bash
    node --test tests/brief-v1-launch-gate.test.cjs
    ```
    Expected: exits 0 (V1-LAUNCH-GATE.md exists with three prongs + Verdict + SMOKE-TEST.md/SURFACE-AUDIT.md cross-references).

    **Subtask E — Final guard: vocabulary lock on V1-LAUNCH-GATE.md:**

    Run vocabulary-lock check on V1-LAUNCH-GATE.md:
    ```bash
    grep -ciE "\\b(compliant|passed|green check|leverage|synergize|holistic|delve|groundbreaking|best-in-class|seamless|cutting-edge|revolutionary|game-changing|robust|innovative|transform)\\b" .planning/V1-LAUNCH-GATE.md
    ```
    Expected: 0 (no banned vocabulary). The word `passed` is banned per Phase 4·5·7 vocab lock — use `PASS` (capitalized verdict label) which is acceptable as a literal token, not the past-tense verb.

    Per Phase 8 cwd-bug lesson: This task does NOT spawn a worktree-isolated agent.

    Per A1 invariant: no npm changes.
  </action>
  <verify>
    <automated>node --test tests/brief-v1-launch-gate.test.cjs && [ "$(node scripts/run-tests.cjs 2>&1 | grep -cE '^(✖|not ok)')" -le "16" ]</automated>
  </verify>
  <acceptance_criteria>
    - `ls .planning/V1-LAUNCH-GATE.md` returns 1 line; `wc -l .planning/V1-LAUNCH-GATE.md` shows 30-80 lines (single-page constraint).
    - `head -1 .planning/V1-LAUNCH-GATE.md` returns `# BRIEF v1 Launch Gate`.
    - `grep -cE "Verdict:\\s*(PASS|HOLD|FAIL)" .planning/V1-LAUNCH-GATE.md` returns ≥1 (verdict line present).
    - `grep -cE "\\(i\\).*pilot.*finding|\\(ii\\).*smoke.*test|\\(iii\\).*surface.*cap" .planning/V1-LAUNCH-GATE.md` returns ≥3 (all 3 prongs labeled).
    - `grep -c "SMOKE-TEST\\.md" .planning/V1-LAUNCH-GATE.md` returns ≥1.
    - `grep -c "SURFACE-AUDIT\\.md" .planning/V1-LAUNCH-GATE.md` returns ≥1.
    - `grep -c "pilot/01-" .planning/V1-LAUNCH-GATE.md` returns ≥1.
    - `grep -c "HRD-04 partial 1/3.*NOT a launch blocker\\|HRD-04 partial.*not.*blocker" .planning/V1-LAUNCH-GATE.md` returns ≥1 (D-D04 acceptance).
    - Vocabulary lock: `grep -ciE "\\b(compliant|leverage|synergize|holistic|delve|groundbreaking|best-in-class|seamless|cutting-edge|revolutionary|game-changing|robust|innovative)\\b" .planning/V1-LAUNCH-GATE.md` returns 0.
    - `ls .planning/RESIDUAL-FAILS-V1.md` returns 1 line; `wc -l .planning/RESIDUAL-FAILS-V1.md` shows ≥50 lines.
    - `grep -c "v1\\.1\\|v1.1" .planning/RESIDUAL-FAILS-V1.md` returns ≥3 (deferred-to-v1.1 labels).
    - `grep -cE "^## \\(c\\)|^## \\(d\\)" .planning/RESIDUAL-FAILS-V1.md` returns 2 (both (c) and (d) sections present).
    - Final empirical: `node scripts/run-tests.cjs 2>&1 | grep -cE '^(✖|not ok)'` returns ≤ 16 — regex matches BOTH spec (`^✖`) and TAP (`^not ok`) reporter outputs per W5 revision. (Record exact count in SUMMARY; if > 16, this plan must be re-triaged — the actual blocker check is the comparison in the verify automated command which will exit 0 only when ≤16.)
    - `node --test tests/brief-v1-launch-gate.test.cjs` exits 0 with all skips removed and all assertions passing.
    - `node -e "console.log(Object.keys(require('./package.json').dependencies||{}).length)"` returns `0`.
  </acceptance_criteria>
  <done>V1-LAUNCH-GATE.md generated with single-page 3-prong checklist + Verdict; RESIDUAL-FAILS-V1.md catalogues (c)+(d) deferred items for v1.1; final empirical fail count ≤16; Wave 0 brief-v1-launch-gate fixture GREEN; vocabulary lock satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Per-test triage edits | Surgical removal/skip of assertions in 19 test files; each edit scoped to one file |
| docs/ARCHITECTURE.md count sync | 3-line edit reading disk counts; no schema change |
| V1-LAUNCH-GATE.md generation | Audit doc reading SMOKE-TEST.md / SURFACE-AUDIT.md / pilot journal; pure markdown write |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-9-17 | T (Tampering) | Per-test triage accidentally removes a load-bearing assertion (not just the missing-file one) | mitigate | Each test edit is surgical (single line OR single test() block); the assertion being removed is identified by `pr-branch.md`/`diagnose-issues.md`/`ui-brand.md` substring match. Pre-edit grep enumerates the exact line numbers; post-edit `node --test <file>` validates the test still parses and either passes or skips with rationale. Per-test rationale logged in `.planning/HRD-05-CLOSURE-RATIONALE.md` for audit. |
| T-9-18 | I (Information Disclosure) | RESIDUAL-FAILS-V1.md catalogues source-behavior bugs (could read as a vulnerability list) | accept | The deferred items are drift items (renames not propagated through every file, frontmatter strings still showing `gsd:`); they are not security vulnerabilities — they are documented technical debt. The list is committed to a public repo per existing project conventions. |
| T-9-19 | E (Elevation of Privilege) | Empirical fail-count check writes /tmp/full-test-output.txt — outside git | accept | /tmp/ is ephemeral by design; the file is read for the FAIL_COUNT computation and the value is recorded in RESIDUAL-FAILS-V1.md. No persistent state outside the repo. |
</threat_model>

<verification>
After all 3 tasks complete:
- 4 artifacts exist: HRD-05-CLOSURE-RATIONALE.md, RESIDUAL-FAILS-V1.md, V1-LAUNCH-GATE.md, docs/ARCHITECTURE.md (modified).
- `node --test tests/brief-v1-launch-gate.test.cjs tests/architecture-counts.test.cjs` exits 0.
- `node scripts/run-tests.cjs 2>&1 | grep -cE '^(✖|not ok)'` returns ≤ 16 (W5 — regex matches both spec and TAP reporter outputs).
- `node -e "console.log(Object.keys(require('./package.json').dependencies||{}).length)"` returns `0`.
- V1-LAUNCH-GATE.md vocabulary lock satisfied (no banned words).
</verification>

<success_criteria>
- HRD-05(a) per-test triage: 19 missing-file tests resolved (DELETE assertion or skip with rationale)
- HRD-05(b) ARCHITECTURE.md sync: Total commands/workflows/agents = disk counts post-Plan-05
- V1-LAUNCH-GATE.md three-prong checklist with PASS/HOLD verdict
- RESIDUAL-FAILS-V1.md catalogues (c) 30 + (d) 13 deferred items for v1.1
- Final empirical: `npm test` fail count ≤ 16
- Wave 0 brief-v1-launch-gate fixture GREEN
- All Phase 9 plans now complete; ready for `/brief-verify-work`
</success_criteria>

<output>
After completion, create `.planning/phases/09-hardening-cross-runtime-smoke-non-developer-pilot/09-06-SUMMARY.md` documenting:
- Final V1-LAUNCH-GATE.md verdict (PASS/HOLD/FAIL) per prong with the values measured.
- Final empirical fail count (exact number, ≤ 16 expected).
- The 19-test count (or actual) breakdown by candidate file (pr-branch / diagnose-issues / ui-brand).
- Any deviations from PATTERNS.md ALL-DELETE rubric (none expected; record any exceptions with rationale).
- Phase 9 closure status: ready for `/brief-verify-work` invocation.
</output>
