---
phase: 09-hardening-cross-runtime-smoke-non-developer-pilot
plan: 04
type: execute
wave: 1
depends_on: [00]
files_modified:
  - tests/command-count-sync.test.cjs
autonomous: true
requirements: [HRD-05]

must_haves:
  truths:
    - "tests/command-count-sync.test.cjs line 48 regex is updated from `commands\\/gsd\\/\\*\\.md` to `commands\\/brief\\/\\*\\.md` (closes one of the 14 (b) ARCH-drift items)"
    - "After Plan 04 lands, the regex-mismatch failure mode in tests/command-count-sync.test.cjs is closed. The count-mismatch failure mode (e.g., the `\\d+` capture group not matching the `Total commands:` value in `docs/ARCHITECTURE.md`) remains until Plan 06 syncs the ARCHITECTURE.md counts to disk reality. Plan 04 is therefore necessary but NOT sufficient for `node --test tests/command-count-sync.test.cjs` to exit 0; the full GREEN state requires Plan 04 (regex path fix) AND Plan 06 (count sync) AND Plan 05 (the deletion that makes the disk count = 12)."
    - "Plan 04 does NOT touch docs/ARCHITECTURE.md count lines — the actual count sync is done by Plan 06 AFTER Plan 05's HRD-02 deletion lands; Plan 04 fixes only the test-side drift item that's purely a string substitution"
    - "Zero new runtime npm dependencies (A1 preserved); pure regex string substitution"
  artifacts:
    - path: "tests/command-count-sync.test.cjs"
      provides: "Updated line 48 regex pointing at commands/brief/ instead of commands/gsd/"
      contains: "commands/brief/"
  key_links:
    - from: "tests/command-count-sync.test.cjs"
      to: "docs/ARCHITECTURE.md (count sync — Plan 06)"
      via: "Plan 06 syncs Total commands/workflows/agents AFTER HRD-02 deletion lands; this test then validates"
      pattern: "commands/brief"
---

<objective>
Surgical one-line fix for `tests/command-count-sync.test.cjs` line 48: regex `commands\/gsd\/\*\.md` → `commands\/brief\/\*\.md`. Closes one of the 14 (b) ARCH-drift items per HRD-05 / D-D02. The remaining (b) work — `docs/ARCHITECTURE.md` Total commands/workflows/agents count sync — happens in Plan 06 AFTER Plan 05 (HRD-02) deletes 56 commands, because the disk counts only stabilize post-pruning.

Purpose: Closes HRD-05(b) test-side drift item that's a pure string substitution. This is a Wave 1 plan (independent of HRD-02 deletion ordering) because the regex fix is correct REGARDLESS of which 12 commands the locked lineup contains — `commands/brief/` is the right path; `commands/gsd/` was always drift from Phase 1's `gsd-*` → `brief-*` rename.

**Per W8 revision (iteration 1):** must_haves.truth #2 reworded to make the necessary-but-not-sufficient relationship between Plan 04 (this plan), Plan 05 (HRD-02 deletion), and Plan 06 (ARCHITECTURE.md count sync) explicit. Running `node --test tests/command-count-sync.test.cjs` after Plan 04 alone may still report failure for count-mismatch reasons; that's expected and is closed by Plan 06.

Output: `tests/command-count-sync.test.cjs` line 48 regex updated. No code changes elsewhere. No new tests, no new lib, no new doc.
</objective>

<execution_context>
@~/.claude/brief/workflows/execute-plan.md
@~/.claude/brief/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-hardening-cross-runtime-smoke-non-developer-pilot/09-CONTEXT.md
@.planning/phases/09-hardening-cross-runtime-smoke-non-developer-pilot/09-RESEARCH.md
@.planning/phases/09-hardening-cross-runtime-smoke-non-developer-pilot/09-PATTERNS.md
@tests/command-count-sync.test.cjs
@docs/ARCHITECTURE.md

<interfaces>
<!-- Existing test file the executor edits surgically. -->

From tests/command-count-sync.test.cjs line 48 (verified per RESEARCH.md line 870 + PATTERNS.md lines 895-908):
```javascript
// CURRENT (drift from Phase 1):
const m = content.match(/commands\/gsd\/\*\.md[^\n]*#\s*(\d+)\s+slash commands/);
```

After this plan:
```javascript
// FIXED:
const m = content.match(/commands\/brief\/\*\.md[^\n]*#\s*(\d+)\s+slash commands/);
```

The test reads `docs/ARCHITECTURE.md`, looks for a comment-style annotation like `commands/brief/*.md  # 12 slash commands`, and asserts the regex-captured count matches the actual `fs.readdirSync` count. After Plan 06 syncs ARCHITECTURE.md counts, this test goes GREEN; until then it may still fail for the count-mismatch reason (NOT for the regex-path-mismatch reason this plan fixes).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update tests/command-count-sync.test.cjs line 48 regex from commands/gsd/ to commands/brief/</name>
  <files>tests/command-count-sync.test.cjs</files>
  <read_first>
    - tests/command-count-sync.test.cjs (full file — confirm the regex is actually on line 48; if line numbers have shifted, locate the regex via `grep -n "commands/gsd/" tests/command-count-sync.test.cjs`; the substitution is the same regardless of line number)
    - .planning/phases/09-hardening-cross-runtime-smoke-non-developer-pilot/09-PATTERNS.md (lines 895-908 — exact before/after snippet)
    - docs/ARCHITECTURE.md (lines 116/127/137 area — confirm the doc's annotation style; this plan does NOT edit ARCHITECTURE.md, only the test that reads it)
  </read_first>
  <action>
    1. Open `tests/command-count-sync.test.cjs`. Locate the regex literal `/commands\/gsd\/\*\.md[^\n]*#\s*(\d+)\s+slash commands/` (per RESEARCH.md line 870, PATTERNS.md lines 895-908). The line number is reported as 48 in PATTERNS.md but if file has shifted, find via `grep -n 'commands/gsd/' tests/command-count-sync.test.cjs` (single-quoted shell — NO double-escaping the slash).

    2. Replace the regex with `/commands\/brief\/\*\.md[^\n]*#\s*(\d+)\s+slash commands/` — pure string substitution `commands\/gsd\/` → `commands\/brief\/`. Preserve every other character (escapes, `[^\n]*`, capture group, etc.).

    3. Scan for any OTHER `commands/gsd/` references in the same file — there should be none after Phase 1 rename, but verify via `grep -c 'commands/gsd/' tests/command-count-sync.test.cjs`. Result must be 0 after this task.

    4. Do NOT edit `docs/ARCHITECTURE.md` — count sync is Plan 06's responsibility (after Plan 05 HRD-02 deletion lands).

    5. Do NOT edit any other file. This is a surgical 1-character substitution affecting one regex on one line.

    Per Phase 8 cwd-bug lesson: This task does NOT spawn a worktree-isolated agent (single-file edit in main worktree).

    Per A1 invariant: no npm changes. Verify `node -e "console.log(Object.keys(require('./package.json').dependencies||{}).length)"` returns 0.

    **Per B2 revision (iteration 1):** the verify command and acceptance criteria below use single-quoted shell strings with the literal regex source `commands\/brief\/` (just two backslashes — one for the JavaScript regex `/` escape, none added by the shell when single-quoted). Earlier draft used four backslashes which is over-escaped for `grep` because `grep -c` (and `grep -E` here) take a regex pattern, and a single-quoted shell string passes the bytes through unchanged. The literal pattern that must match the source code is `commands\/brief\/` (3 chars + backslash + 5 chars + backslash) = `commands\/brief\/`.
  </action>
  <verify>
    <automated>grep -c 'commands\/brief\/' tests/command-count-sync.test.cjs && [ "$(grep -c 'commands\/gsd\/' tests/command-count-sync.test.cjs)" = "0" ]</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'commands\/brief\/' tests/command-count-sync.test.cjs` returns ≥1 (the fix is present). Use single-quoted shell string; the pattern matches the literal source bytes `commands\/brief\/` (where `\/` is the regex-escaped forward slash inside the JS regex literal).
    - `grep -c 'commands\/gsd\/' tests/command-count-sync.test.cjs` returns 0 (no remaining drift in this file).
    - `grep -nE 'commands\\\\/brief\\\\/\\*\\.md' tests/command-count-sync.test.cjs` returns at least one line containing the corrected regex (capture group `\d+` and `slash commands` literal preserved). Note: this grep uses `-E` (extended regex) and the `\\\\` in shell expands to `\\` in the regex engine, which matches a literal `\\` (two characters) in the file — but the source contains `\/` (one backslash + slash), so this acceptance check uses 2 shell backslashes only: `grep -nE 'commands\\/brief\\/\\*\\.md' tests/command-count-sync.test.cjs`. The shell passes `commands\/brief\/\*\.md` to grep -E; grep -E treats `\/` as `/` and `\*` as literal `*`. This finds the literal source text.
    - `node -e "const fs=require('node:fs'); const c=fs.readFileSync('tests/command-count-sync.test.cjs','utf-8'); const re=/commands\\/brief\\/\\*\\.md\\[\\^\\\\n\\]\\*#\\\\s\\*\\(\\\\d\\+\\)\\\\s\\+slash commands/; console.log(re.test(c) ? 'fix-present' : 'fix-missing')"` outputs `fix-present` (regex literal preserved correctly post-edit). If this complex node-eval is unreliable, simpler check: `node -e "const fs=require('node:fs'); const c=fs.readFileSync('tests/command-count-sync.test.cjs','utf-8'); console.log(c.includes('commands\\\\/brief\\\\/') ? 'fix-present' : 'fix-missing')"` (uses string-includes — JS source `commands\/brief\/` is rendered in the file as the byte sequence `c-o-m-m-a-n-d-s-\-/-b-r-i-e-f-\-/` so the JS string literal needs `\\\\/` to produce one `\\/` in memory).
    - **Simplest verification (preferred — avoids over-escaping):** `node -e "const fs=require('node:fs'); const c=fs.readFileSync('tests/command-count-sync.test.cjs','utf-8'); console.log({brief: c.includes(String.raw\`commands\\/brief\\/\`), gsd: c.includes(String.raw\`commands\\/gsd\\/\`)})"` outputs `{ brief: true, gsd: false }`.
    - `node --test tests/command-count-sync.test.cjs` exits with NO regex-pattern-not-found error (the test may fail for count-mismatch reasons until Plan 06 syncs ARCHITECTURE.md, but the regex itself now compiles and matches the doc's annotation style).
    - `node -e "console.log(Object.keys(require('./package.json').dependencies||{}).length)"` returns `0`.
  </acceptance_criteria>
  <done>tests/command-count-sync.test.cjs line 48 regex points at `commands/brief/` (drift fixed); zero remaining `commands\/gsd\/` strings in the file; one of HRD-05's 14 (b) drift items closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test file regex | Pure string substitution; no execution, no I/O changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-9-13 | T (Tampering) | Wrong replacement scope (e.g., editing escapes incorrectly) | mitigate | Replacement is byte-identity except for the literal token `gsd` → `brief`. Acceptance criteria includes both presence (`commands/brief/`) and absence (`commands/gsd/`) checks via `grep` (single-quoted shell — NO over-escaping). Regex literal validated via `String.raw`-based node check (preferred, robust to shell quoting). |
</threat_model>

<verification>
After the task completes:
- `grep -c 'commands\/brief\/' tests/command-count-sync.test.cjs` ≥ 1.
- `grep -c 'commands\/gsd\/' tests/command-count-sync.test.cjs` = 0.
- `node -e "console.log(Object.keys(require('./package.json').dependencies||{}).length)"` = 0.
- The full ARCHITECTURE.md sync (Total commands/workflows/agents) is NOT done in this plan — that's Plan 06's responsibility after Plan 05 HRD-02 deletion stabilizes the disk counts.
</verification>

<success_criteria>
- One of HRD-05's 14 (b) ARCH-drift items closed (regex points at `commands/brief/` not `commands/gsd/`)
- Zero new dependencies (A1 preserved)
- Independent of Plan 05 (HRD-02 deletion) — this fix is correct regardless of which 12 commands are in the locked lineup
- Plan 06 will close the remaining (b) work (ARCHITECTURE.md Total counts) AFTER Plan 05 lands
- B2 over-escaping defect (4 backslashes) corrected to 2 backslashes via single-quoted shell strings
- W8 must_haves.truth #2 explicitly states necessary-but-not-sufficient relationship between Plans 04, 05, 06 for command-count-sync.test.cjs GREEN
</success_criteria>

<output>
After completion, create `.planning/phases/09-hardening-cross-runtime-smoke-non-developer-pilot/09-04-SUMMARY.md` recording the line number where the regex was found (PATTERNS.md said line 48; record actual line if shifted) and confirming no other `commands/gsd/` references remain in the test file.
</output>
