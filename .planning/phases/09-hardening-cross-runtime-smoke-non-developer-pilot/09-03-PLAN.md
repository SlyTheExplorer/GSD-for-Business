---
phase: 09-hardening-cross-runtime-smoke-non-developer-pilot
plan: 03
type: execute
wave: 1
depends_on: [00]
files_modified:
  - .planning/pilot/01-korean-non-technical-product-owner-friction-journal.md
autonomous: true
requirements: [HRD-04]

must_haves:
  truths:
    - "User can read `.planning/pilot/01-korean-non-technical-product-owner-friction-journal.md` and see the dogfooding session's friction items as a Pitfall #9 vocabulary table (frequency × severity)"
    - "Pilot journal frontmatter has 4 required keys: pilot_id (=1), user_role (string), logged (ISO date), audience.confidentiality (=internal — CC-03 hook compatibility per Open Question 3 recommendation)"
    - "Pilot journal body has the `## Pitfall #9 — Non-developer barriers` section header AND ≥1 friction row in markdown table form (severity column ∈ {low, medium, high, blocker})"
    - "Each friction row cites a specific phase where the friction occurred (Pitfall 5 discipline — concrete provenance prevents performative journaling)"
    - "Journal contains AT LEAST 1 severity=high item (Pitfall 5: 0 high-severity is suspicious; Phase 8 cwd-bug + agent quota are documented severity=high)"
    - "Journal explicitly logs '2 of 3 pilots deferred to v1.1 beta' as Out of Scope per D-D01 — this is NOT a v1 launch blocker per D-D04"
    - "**B4 transparency note (revision iteration 1):** Journal explicitly states 'pilot 1 of 3 is the BRIEF build-team vision-keeper — a non-technical Korean product owner who used BRIEF end-to-end on the BRIEF project itself (dogfooding); they are NOT a fully external pilot'. This transparency over silent ambiguity is required by the revision: HRD-04 spec demands 3 NOT-build-team pilots, but D-D01 accepts the vision-keeper as 1 of 3 because they are non-technical and used BRIEF on a real planning project. The other 2 pilots (fully external) are deferred to v1.1 beta per D-D01 + D-D04."
    - "Journal vocabulary is banned-word-free (CC-03 hook + Phase 4·5·7 vocab-lock compat — no `compliant`, `passed`, `green check`, no Pitfall #10 banned-words). The check uses an awk-based body extractor (NOT line-offset-fragile `tail -n +N`) and matches the canonical BANNED_EN list imported from `brief/bin/lib/voice-fit.cjs` (16 EN words: leverage|synergize|transform|holistic|delve|groundbreaking|best-in-class|seamless|cutting-edge|revolutionary|game-changing|landscape|unlock|empower|robust|innovative) per B5/W6 revision."
  artifacts:
    - path: ".planning/pilot/01-korean-non-technical-product-owner-friction-journal.md"
      provides: "HRD-04 partial 1/3 friction journal with Pitfall #9 vocabulary table + severity=high triage notes + Out of Scope log for remaining 2/3 + B4 transparency note (1/3 is build-team vision-keeper)"
      min_lines: 60
      contains: "Pitfall #9"
  key_links:
    - from: ".planning/pilot/01-korean-non-technical-product-owner-friction-journal.md"
      to: "tests/brief-pilot-journal-structure.test.cjs"
      via: "fixture asserts pilot_id + user_role + logged frontmatter + Pitfall #9 row + vocabulary lock (canonical BANNED_EN imported from voice-fit.cjs)"
      pattern: "pilot_id"
    - from: ".planning/pilot/01-korean-non-technical-product-owner-friction-journal.md"
      to: ".planning/V1-LAUNCH-GATE.md (Plan 06)"
      via: "Plan 06 verifier reads pilot journal severity=blocker count for prong (i)"
      pattern: "severity=blocker\\|severity:.*blocker"
    - from: ".planning/pilot/01-korean-non-technical-product-owner-friction-journal.md"
      to: ".planning/REQUIREMENTS.md (HRD-04 wording)"
      via: "REQUIREMENTS.md HRD-04 was reworded in revision iteration 1 to match D-D01 acceptance — vision-keeper counts as pilot 1/3"
      pattern: "vision-keeper|build-team"
---

<objective>
Ship the partial 1/3 non-developer pilot friction journal at `.planning/pilot/01-korean-non-technical-product-owner-friction-journal.md` per HRD-04 / D-D01..D-D04. The current dogfooding session (Korean non-technical product owner running BRIEF on BRIEF) is the 1-of-3 pilot per D-D01; remaining 2/3 are explicitly Out of Scope (deferred to v1.1 beta program).

**Per B4 revision (iteration 1):** the journal must explicitly state that pilot 1/3 is the BRIEF build-team vision-keeper (not a fully external pilot), and that they count as a non-developer pilot per D-D01 because they are non-technical and used BRIEF end-to-end on a real planning project. REQUIREMENTS.md HRD-04 was reworded in this revision to match this acceptance language. The other 2 pilots are deferred to v1.1 beta per D-D01 + D-D04 (NOT a launch blocker).

Purpose: Closes HRD-04 partial criterion (success criterion #4 in ROADMAP Phase 9, with D-D04 explicit Out of Scope acceptance). Encodes the Pitfall #9 (non-developer friction) vocabulary as a frequency × severity table, with phase citations per row (Pitfall 5 anti-rationalization discipline). Severity=blocker rows are the load-bearing input to V1-LAUNCH-GATE.md prong (i): 0 blocking findings = launch-ready.

Output: One markdown file at `.planning/pilot/01-korean-non-technical-product-owner-friction-journal.md` with frontmatter (pilot_id, user_role, logged, audience.confidentiality=internal) + structured Pitfall #9 friction table + Out of Scope log + B4 transparency note. No production code, no tests, no lib changes. Wave 0 fixture `tests/brief-pilot-journal-structure.test.cjs` flips from skip→active and passes.
</objective>

<execution_context>
@~/.claude/brief/workflows/execute-plan.md
@~/.claude/brief/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-hardening-cross-runtime-smoke-non-developer-pilot/09-CONTEXT.md
@.planning/phases/09-hardening-cross-runtime-smoke-non-developer-pilot/09-RESEARCH.md
@.planning/phases/09-hardening-cross-runtime-smoke-non-developer-pilot/09-PATTERNS.md
@.planning/research/PITFALLS.md
@brief/bin/lib/voice-fit.cjs
@brief/bin/lib/frontmatter.cjs
@tests/brief-pilot-journal-structure.test.cjs

<interfaces>
<!-- Existing artifact patterns the executor extends. -->

From .planning/research/PITFALLS.md (line 217+):
> Pitfall #9 — Non-Developer User Friction (Terminal Fear)
> Phase to address: Phase 0 establish "non-developer first" constraint; Phase 2/3 implement; Pre-launch usability test with 3 non-developer planners.
> Examples cited: agent telemetry intimidation, terminal fear, slash-command memorability, gauntlet load.

From CONTEXT.md (D-D01) AND REQUIREMENTS.md HRD-04 (revised iteration 1):
> HRD-04 = partial 1/3. The vision-keeper (Korean non-technical product owner) counts as pilot 1/3 under D-D01 acceptance because they are non-technical and used BRIEF end-to-end on a real planning project (dogfooding BRIEF on BRIEF), even though they are also the build-team vision-keeper. Findings logged with explicit transparency that pilot 1/3 is the build-team vision-keeper (not a fully external pilot). Remaining 2/3 deferred to v1.1 beta (Out of Scope explicit, NOT a launch blocker per D-D04).

From PATTERNS.md (lines 833-871) — schema:
- Frontmatter (CC-03 hook compatibility): pilot_id, user_role, logged, audience.confidentiality:internal, voice.languages:[ko,en].
- Body: `## Pitfall #9 — Non-developer barriers` section with markdown table (Friction Item / Severity / Frequency / Phase Citation / Mitigation).
- Free-form Appendix at bottom for narrative.

From MEMORY.md user_role.md (Korean, vision-keeper, dogfooding BRIEF on BRIEF; surfaces discomfort when in implementation seat):
- This pilot user IS the dogfooder; severity=high on Pitfall 8 cwd-bug AND Phase 8 Plan 06 agent quota fatigue is concrete provenance, NOT performative.

From brief/bin/lib/voice-fit.cjs (line 25 — canonical source of truth for BANNED_EN):
```javascript
const BANNED_EN = /\b(leverage|synergize|transform|holistic|delve|groundbreaking|best-in-class|seamless|cutting-edge|revolutionary|game-changing|landscape|unlock|empower|robust|innovative)\b/gi;
```
The 16 banned EN words are the canonical list; Phase 8 voice-fit gate exports this regex from `module.exports.BANNED_EN`. The vocabulary-lock check in this plan's acceptance criteria uses the SAME regex (or imports it directly via `node -e "console.log(require('./brief/bin/lib/voice-fit.cjs').BANNED_EN.source)"`) per B5/W6 revision.

From brief/bin/lib/frontmatter.cjs (line ~?): `extractFrontmatter(content)` returns parsed frontmatter object or null. Used for the W3 acceptance check.

Wave 0 fixture (tests/brief-pilot-journal-structure.test.cjs) asserts:
- File exists at `.planning/pilot/01-*-friction-journal.md` (regex match).
- Frontmatter has pilot_id, user_role, logged.
- Body has `## Pitfall #9` header AND ≥1 markdown table row with severity column.
- No banned vocabulary (uses awk-based body extractor + canonical BANNED_EN list per B5/W6 revision).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create .planning/pilot/01-korean-non-technical-product-owner-friction-journal.md (with B4 transparency note)</name>
  <files>.planning/pilot/01-korean-non-technical-product-owner-friction-journal.md</files>
  <read_first>
    - .planning/phases/09-hardening-cross-runtime-smoke-non-developer-pilot/09-PATTERNS.md (lines 833-871 — pilot journal schema with frontmatter + Pitfall #9 table + appendix)
    - .planning/phases/09-hardening-cross-runtime-smoke-non-developer-pilot/09-RESEARCH.md (lines 124-139 — pilot journal template; lines 538-549 — Pitfall 5 discipline: each row cites phase, severity=high MUST be triaged)
    - .planning/research/PITFALLS.md (lines 217-260 — Pitfall #9 vocabulary: terminal fear, agent telemetry, slash-command memorability, gauntlet load)
    - .planning/REQUIREMENTS.md (HRD-04 entry — revised in iteration 1 to match D-D01 vision-keeper acceptance; the journal MUST be consistent with the revised wording)
    - tests/brief-pilot-journal-structure.test.cjs (Wave 0 fixture — required frontmatter keys, Pitfall #9 header, severity column, banned-vocab check)
    - brief/bin/lib/voice-fit.cjs (line 25 — canonical BANNED_EN regex; the vocabulary-lock check uses THIS regex, not a hand-typed alternation that omits 4 words)
    - brief/bin/lib/frontmatter.cjs (extractFrontmatter — used in the W3 acceptance check below)
    - /Users/agent/.claude/projects/-Users-agent-GSD-for-Business/memory/MEMORY.md.user_role.md (if accessible — pilot user's actual context: Korean, non-technical product owner, dogfooding BRIEF on BRIEF; surfaces discomfort in implementation seat)
  </read_first>
  <action>
    Create directory `.planning/pilot/` if it doesn't exist (mkdir -p semantics). Then create the friction journal file with this exact structure:

    ```markdown
    ---
    pilot_id: 1
    user_role: korean-non-technical-product-owner
    logged: 2026-04-27
    audience:
      confidentiality: internal
    voice:
      languages: [ko, en]
    ---

    # Friction Journal — BRIEF dogfooding session (1 of 3 pilots)

    **Pilot scope:** Korean non-technical product owner running BRIEF on BRIEF (dogfooding the framework using itself as the example project from Phase 1 fork through Phase 8 DELIVER).

    **Transparency note (B4 revision iteration 1):** Pilot 1 of 3 is the BRIEF build-team vision-keeper — a non-technical Korean product owner who used BRIEF end-to-end on the BRIEF project itself. They are NOT a fully external pilot. They count as pilot 1/3 under D-D01 acceptance because they are non-technical (do not write production code in this project; act as product owner / vision-keeper) and used BRIEF on a real planning project. REQUIREMENTS.md HRD-04 was reworded in revision iteration 1 to match this acceptance. The other 2 pilots (fully external non-developer business planners) are deferred to v1.1 beta per D-D01 + D-D04 — explicitly NOT a v1 launch blocker.

    **Coverage:** 1 of 3 non-developer planners per HRD-04 success criterion. Remaining 2/3 deferred to v1.1 beta program per D-D01 (explicit Out of Scope).

    **Reading discipline (Pitfall 5):** Each friction row cites a concrete phase where it occurred. severity=high items are triaged below — fixed in Phase 9 OR explicitly logged as v1.1 with rationale.

    ## Pitfall #9 — Non-developer barriers

    | Friction Item | Severity | Frequency | Phase Citation | Mitigation Status |
    |---------------|----------|-----------|----------------|-------------------|
    | smart_discuss table clutter — gray-area gauntlet load | medium | every phase since Phase 3 (4/4) | Phase 3, 5, 7, 8 | mitigated — batch table with Recommended defaults; user accepts area as-locked-set without per-area discussion |
    | agent quota fatigue — Plan 06 agent-spawn limit hit | high | once | Phase 8 Plan 06 | mitigated — single worktree-isolated agent for large plans; Wave parallelism capped at Wave 1 in Phase 9 |
    | cwd bug exposure — 3 worktree agents leaked into main | high | once | Phase 8 Wave 1 | mitigated — `<critical_cwd_warning>` block embedded in every executor agent prompt (Plan 08-08 reinforced version) |
    | AskUserQuestion fallback gaps — runtime-detection assumed but never end-to-end verified before Phase 9 | medium | latent | Phase 1 FND-06 deferred → Phase 9 HRD-01 | mitigated — Phase 9 stub-driven smoke test verifies text_mode plumbing for all 4 runtimes |
    | command surface memorability — 68 inherited commands obscure the 4D phase mental model | high | every session | Phase 1 inherited surface; Phase 2 D-09 cap source; Phase 9 HRD-02 closes | mitigated — surface pruning to locked 12 commands + rich /brief-help with Levenshtein typo suggestion (HRD-03) |
    | implementation-seat discomfort — vision-keeper in product owner role surfaces fatigue when forced into code-review seat | medium | every plan-checker iteration | Phase 5/7/8 plan-checker review cycles | partial mitigation — delegated mode (skip 4-gray-area gauntlet on implementation-heavy phases per memory feedback_delegated_mode.md); pilot user can opt out of code-review in v1.1 |

    ## Severity=high Triage (Pitfall 5 discipline)

    All 3 severity=high items above are mitigated within Phase 9 closure or were already mitigated by Phase 8 prior:

    1. **agent quota fatigue** — Phase 8 lesson: Plan 06 fallback was inline implementation; Phase 9 plans cap Wave parallelism at Wave 1 (per planning_context recommendation in this plan's parent /brief-plan-phase invocation).
    2. **cwd bug exposure** — Plan 08-08 reinforced the `<critical_cwd_warning>` block; Phase 9 plans (this set) embed the same block in any worktree-isolated agent task. Plan 05 (HRD-02 atomic deletion) is the single-agent worktree task in Wave 2.
    3. **command surface memorability** — HRD-02 prunes from 68 → 12 (Plan 05); HRD-03 ships rich /brief-help (Plan 02 of this Wave). Both close in Phase 9.

    No severity=blocker items found. v1 launch gate prong (i) "0 blocking pilot findings" expected to read PASS at Phase 9 verifier (Plan 06).

    ## Out of Scope (per D-D01 — v1.1 beta program)

    - **2 of 3 remaining non-developer pilots:** Recruit + observe + journal during v1.1 beta. Selection target: one EN business planner (US/EU) + one non-Korean Asian region planner — for cross-region/cross-language friction signal beyond the Korean dogfooding baseline. The launch posture per D-D04 explicitly accepts the partial 1/3 as NOT a launch blocker.

    ## Appendix — narrative observations (free-form)

    The dogfooding session reinforced the Phase 8 lesson that BRIEF's 5-phase shape (DEFINE → DISCOVER → DESIGN → DELIVER + continuous ALIGN) actually works for the non-developer audience when each phase has a single canonical entry slash command. The 4D phase categorization in /brief-help (HRD-03) maps onto the user's existing mental model from running through 8 prior phases. Ahead of v1 launch the larger UX risk is the gap between "Korean product owner who has been running BRIEF for 8 phases" and "EN founder running BRIEF for the first time" — the v1.1 beta program is the right place to surface that signal, not a Phase 9 task.

    The Phase 8 cwd-bug remains the most painful single friction event in the journal: the user's discomfort when 3 worktree agents leaked their changes into main was visceral. The Plan 08-08 mitigation has held since Plan 09-00 onward, but the pilot user's note for v1.1 is to add a hard pre-flight check (e.g., `git rev-parse --show-toplevel` assertion) inside every worktree-isolated agent's first action, beyond the prompt-level `<critical_cwd_warning>` block.
    ```

    Per Pitfall 5 discipline:
    - Each row in the Pitfall #9 table cites a specific phase (column "Phase Citation").
    - 3 severity=high rows are present — NOT zero (zero would be suspicious per Pitfall 5).
    - Each severity=high row has triage notes in the dedicated section below the table.

    Per CC-03 hook compatibility:
    - audience.confidentiality: internal — minimal frontmatter per Open Question 3 recommendation.
    - voice.languages: [ko, en] — multi-language signal for the bilingual pilot.

    Per vocabulary lock (Phase 4 D-09 / Phase 5 D-09 / Phase 7 D-01 + B5/W6 canonical-list import):
    - NO `compliant`, NO `passed`, NO `green check`, NO `green checkmark` in body.
    - Per the canonical BANNED_EN imported from `brief/bin/lib/voice-fit.cjs`: NO `leverage`, `synergize`, `transform`, `holistic`, `delve`, `groundbreaking`, `best-in-class`, `seamless`, `cutting-edge`, `revolutionary`, `game-changing`, `landscape`, `unlock`, `empower`, `robust`, `innovative` (16 EN words). Use plain factual language.

    Per Phase 8 cwd-bug lesson: This task does NOT spawn a worktree-isolated agent. The friction journal is a static markdown file written directly in the main worktree.

    No new dependencies. No code changes. Markdown only.
  </action>
  <verify>
    <automated>node --test tests/brief-pilot-journal-structure.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `ls .planning/pilot/01-korean-non-technical-product-owner-friction-journal.md` returns 1 line.
    - `wc -l .planning/pilot/01-korean-non-technical-product-owner-friction-journal.md` shows ≥60 lines.
    - `head -1 .planning/pilot/01-korean-non-technical-product-owner-friction-journal.md` returns `---` (frontmatter delimiter).
    - **W3 frontmatter parses via extractFrontmatter:** `node -e "const fm=require('./brief/bin/lib/frontmatter.cjs').extractFrontmatter(require('node:fs').readFileSync('.planning/pilot/01-korean-non-technical-product-owner-friction-journal.md','utf-8')); console.log(fm.pilot_id, fm.user_role, fm.logged)"` outputs 3 non-empty values (e.g., `1 korean-non-technical-product-owner 2026-04-27`). All 3 must be defined and non-empty.
    - `grep -cE "^pilot_id:\\s*1$|^pilot_id:\\s*'?1'?|^pilot_id:\\s*'?01'?" .planning/pilot/01-korean-non-technical-product-owner-friction-journal.md` returns ≥1 (frontmatter has pilot_id).
    - `grep -c "user_role:" .planning/pilot/01-korean-non-technical-product-owner-friction-journal.md` returns ≥1.
    - `grep -cE "^logged:\\s*\\d{4}-\\d{2}-\\d{2}" .planning/pilot/01-korean-non-technical-product-owner-friction-journal.md` returns ≥1 (ISO date).
    - `grep -c "confidentiality: internal\\|confidentiality:internal" .planning/pilot/01-korean-non-technical-product-owner-friction-journal.md` returns ≥1.
    - `grep -cE "^## Pitfall #9" .planning/pilot/01-korean-non-technical-product-owner-friction-journal.md` returns ≥1.
    - `grep -cE "\\|\\s*(low|medium|high|blocker)\\s*\\|" .planning/pilot/01-korean-non-technical-product-owner-friction-journal.md` returns ≥3 (at least 3 severity rows).
    - `grep -cE "\\|\\s*high\\s*\\|" .planning/pilot/01-korean-non-technical-product-owner-friction-journal.md` returns ≥1 (at least 1 severity=high row, per Pitfall 5).
    - `grep -cE "\\|\\s*blocker\\s*\\|" .planning/pilot/01-korean-non-technical-product-owner-friction-journal.md` returns 0 (no severity=blocker — launch-ready signal).
    - **B4 transparency note present:** `grep -cE "(vision-keeper|build-team|build team).*not.*fully external|Pilot 1 of 3 is the BRIEF build-team" .planning/pilot/01-korean-non-technical-product-owner-friction-journal.md` returns ≥1 (the journal explicitly states 1/3 is the build-team vision-keeper, not a fully external pilot).
    - **B5/W6 vocabulary lock — robust body extraction (awk, NOT tail -n +N):** Run the body-only banned-word scan using awk to skip the frontmatter (everything between the first and second `---` line):
      ```bash
      awk '/^---$/{n++; next} n>=2' .planning/pilot/01-korean-non-technical-product-owner-friction-journal.md > /tmp/pilot-body.txt
      ```
      Then check both the basic banned set AND the canonical BANNED_EN imported from voice-fit.cjs:
      ```bash
      # Basic check (Phase 4·5·7 vocab lock):
      grep -ciE '\b(compliant|passed|green check|green checkmark)\b' /tmp/pilot-body.txt
      # Output MUST be 0.
      # Canonical BANNED_EN check (per B5/W6 — import from voice-fit.cjs source of truth):
      node -e "const re=require('./brief/bin/lib/voice-fit.cjs').BANNED_EN; const body=require('node:fs').readFileSync('/tmp/pilot-body.txt','utf-8'); const hits=[...body.matchAll(re)].map(m=>m[0]); console.log(hits.length, JSON.stringify(hits))"
      # Output MUST start with `0` (zero matches in body).
      ```
      The body MUST contain ZERO matches against either the basic regex OR the canonical BANNED_EN regex. (NOTE: the words `transform`, `landscape`, `unlock`, `empower` are intentionally absent — they ARE in BANNED_EN per voice-fit.cjs and earlier hand-typed checks omitted them. Importing the canonical regex closes that gap.)
    - `grep -c "v1\\.1\\|v1.1 beta\\|deferred" .planning/pilot/01-korean-non-technical-product-owner-friction-journal.md` returns ≥1 (Out of Scope log present).
    - `node --test tests/brief-pilot-journal-structure.test.cjs` exits 0 with all assertions passing (no skips related to file absence; no failures on frontmatter or vocabulary-lock assertions).
  </acceptance_criteria>
  <done>Pilot friction journal exists at `.planning/pilot/01-korean-non-technical-product-owner-friction-journal.md`; Wave 0 brief-pilot-journal-structure fixture is GREEN; vocabulary lock satisfied (using awk-based body extractor + canonical BANNED_EN imported from voice-fit.cjs per B5/W6); frontmatter parses cleanly via extractFrontmatter (W3); B4 transparency note present (1/3 is build-team vision-keeper); severity=high triage section present (Pitfall 5); Out of Scope log for remaining 2/3 pilots explicit (D-D01).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Friction journal markdown file | Read-only artifact; user-authored static markdown; CC-03 PreToolUse-on-Bash hook may scan frontmatter on commit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-9-01 | T (Tampering) | Malformed frontmatter could break CC-03 hook on commit | mitigate | Pilot journal generated from this plan's exact template (not user-typed); frontmatter keys verified by tests/brief-pilot-journal-structure.test.cjs AND by the W3 inline acceptance check (extractFrontmatter parses + 3 non-empty values); the file is committed via `gsd-sdk query commit` which respects CC-03 hook; on hook failure, fix frontmatter and re-commit (NEVER --no-verify per project rules). |
| T-9-12 | I (Information Disclosure) | Pilot journal contains internal observations about Phase 8 cwd-bug | accept | audience.confidentiality: internal frontmatter signals the journal is internal pre-launch material; not exported via /brief-export; not included in any Type B deck. The cwd-bug is already logged in Phase 8 SUMMARYs and STATE.md. |
| T-9-20 | T (Tampering) | Vocabulary-lock check could miss banned words if regex hand-typed (omits 4 of 16 BANNED_EN entries) | mitigate | Per B5/W6 revision: the acceptance criteria import the canonical BANNED_EN regex directly from `brief/bin/lib/voice-fit.cjs` rather than re-typing the alternation. This keeps the test in sync with the source of truth even if BANNED_EN expands later. |
| T-9-21 | T (Tampering) | `tail -n +N` body extraction is line-offset-fragile (frontmatter line count drift) | mitigate | Per B5 revision: the acceptance criteria use awk to extract everything after the second `---` line (`awk '/^---$/{n++;next}n>=2'`) — robust to frontmatter length changes. |
</threat_model>

<verification>
After the task completes:
- `ls .planning/pilot/01-korean-non-technical-product-owner-friction-journal.md` returns 1 line.
- `node --test tests/brief-pilot-journal-structure.test.cjs` exits 0 (Wave 0 fixture GREEN).
- W3 inline frontmatter parse: `node -e "const fm=require('./brief/bin/lib/frontmatter.cjs').extractFrontmatter(require('node:fs').readFileSync('.planning/pilot/01-korean-non-technical-product-owner-friction-journal.md','utf-8')); console.log(fm.pilot_id, fm.user_role, fm.logged)"` outputs 3 non-empty values.
- Vocabulary lock satisfied (awk body extractor + canonical BANNED_EN regex from voice-fit.cjs): zero matches in body.
- B4 transparency note present (build-team vision-keeper / not fully external explicitly stated).
- File commits cleanly under CC-03 hook (frontmatter validates).
</verification>

<success_criteria>
- HRD-04 partial 1/3 logged at canonical path
- B4 transparency note: pilot 1/3 explicitly identified as build-team vision-keeper (not fully external)
- REQUIREMENTS.md HRD-04 (revised iteration 1) and pilot journal are consistent (vision-keeper acceptance per D-D01)
- Pitfall #9 vocabulary table present with phase citations on every row
- ≥1 severity=high row + 0 severity=blocker rows (Pitfall 5 discipline + V1-LAUNCH-GATE prong (i) PASS path)
- Out of Scope log for remaining 2/3 pilots explicit (D-D01)
- audience.confidentiality: internal frontmatter (CC-03 hook compatibility per Open Question 3)
- Vocabulary lock satisfied using awk-based body extractor (NOT tail -n +N) and canonical BANNED_EN imported from voice-fit.cjs (per B5/W6 revision)
- W3 inline frontmatter-parse acceptance check (extractFrontmatter returns 3 non-empty values)
</success_criteria>

<output>
After completion, create `.planning/phases/09-hardening-cross-runtime-smoke-non-developer-pilot/09-03-SUMMARY.md` documenting: (a) the final friction-row count + severity distribution; (b) any vocabulary-lock workarounds (if a banned word was load-bearing in a citation, document the alternative phrasing chosen); (c) confirmation that severity=blocker count is 0 (V1-LAUNCH-GATE prong (i) ready to PASS); (d) confirmation that the W3 frontmatter-parse acceptance check returned 3 non-empty values; (e) confirmation that REQUIREMENTS.md HRD-04 wording is consistent with the journal's B4 transparency note.
</output>
